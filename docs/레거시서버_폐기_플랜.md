# 레거시 서버(27.102.203.143) 폐기 프로젝트 플랜

> **이 문서가 본 프로젝트의 단일 기준 문서(SoT)다. 작업 진행 시마다 상태·이력을 이 문서에 직접 갱신한다.**
> 최초 작성: 2026-07-03 (비토) / 최종 갱신: 2026-07-16 (레거시 143 디스크 100% 풀 사고 대응·서버 전수 점검·정리 — §7)

---

## 0. 목표

- **월 511,500원 다우클라우드 레거시 서버(27.102.203.143, SSH 포트 27153) 완전 폐기.**
- 폐기 조건: 이 서버에 남은 실사용 기능 3개(템플릿관리자 / PAY 사용량 조회 / 게이트웨이 결과 적재 수신)를 한줄로로 완전 흡수.
- 이미 확정된 흐름: 레거시웹은 한줄로 이관 2개월 경과. 카카오/RCS 메뉴는 레거시에서 차단 상태라 한줄로와 병행 비교·테스트 가능.
- 비밀번호·자격증명은 이 문서에 기재하지 않는다 (Harold님만 보유). 서버 SSH는 Harold님만 접속.

---

## 1. 서버 전수 인벤토리 — 폐기 전 영향표

> 원칙: "폐기 = 전 서비스 영향표" — 아래 전 항목이 [대체완료/폐기확정/백업완료] 중 하나가 되기 전에는 서버를 내리지 않는다.

| # | 서비스 | 위치/포트 | 상태(2026-07-03) | 폐기 전 처리 |
|---|--------|----------|------------------|--------------|
| 1 | 템플릿관리자 SPA (event-admin.invitocorp.com) | `/home/event/admin`, nginx 443 | 실사용 중 | 한줄로 카카오템플릿 관리로 대체 (Track B) |
| 2 | 템플릿관리자 API (event-admin-api.invitocorp.com) | Spring Boot 8081, `live2-event-admin-server.jar`, user `event` | 실사용 중 | SPA와 동시 폐기 |
| 3 | 템플릿관리자 DB | MySQL 3388 `event_admin` | 실사용 중 | 템플릿·업체 데이터 이관 + 백업 후 폐기 |
| 4 | PAY 사이트 (pay.invitobiz.com:8080) | tomcat6 (`/usr/local/tomcat6`), docBase `/home/pay` → 실체 `/www/pay` | 실사용 중 (조회 용도만. 카드결제 기능은 불필요 확정 — Harold 2026-07-03) | 한줄로 Agent 발송결과+정산으로 대체 (Track D) |
| 5 | PAY DB | MariaDB 3388 `sales` (user `sales`) | 실사용 중 — **외주 게이트웨이가 결과 통계를 직접 적재** | 적재 목적지 변경 협의 (Track D) + 백업 |
| 6 | QTmsg 알림 SMS Agent (kkotemp/mtdeliver 등 jar 6종) | `/home/invitoMsg/`, 포트 34483~34983, socat 3306→3388 | 템플릿관리자 검수 알림용 | 템플릿관리자 폐기와 함께 불필요 — 한줄로 알림톡 검수 알림(kakao_alarm_users)이 대체 (확인 필요 ①) |
| 7 | invitobiz.com 웹사이트 | nginx 443, acme.sh ZeroSSL | 이니시스 결제 차단됨(2026-06-15), 안내만 | **폐기/유지 결정 필요 ②** — 유지 시 이전처 필요 |
| 8 | tomcat6_b | `/usr/local/tomcat6_b` | 이니시스 결제용(JDK1.7 고정 이력) — 결제 차단됐으므로 사실상 사장 | 미확인 ③ — 프로세스 확인 후 폐기 확정 |
| 9 | SSL 인증서 | event-admin(프론트·API)·invitobiz 전부 ZeroSSL acme.sh 자동갱신 (2026-07-03 프론트 유료→무료 전환 완료) | 정상 | 서버 폐기 시 함께 소멸 — 도메인 이전 계획에 포함 |
| 10 | DNS | event-admin / event-admin-api / pay / invitobiz → 27.102.203.143 | 실사용 | 폐기 직전 정리 (Track F) |
| 11 | 기타 미확인 프로세스 | — | — | **폐기 전 전수 실사 필요 ④**: `netstat -tlnp`, `pm2/systemd/crontab 목록`, MySQL 3388 전체 DB 목록 |

미확인 항목(①~④)은 §5 열린 질문에서 추적.

---

## 2. 대체 대상 상세 (실측 분석 결과)

### 2-1. 템플릿관리자 (Track B에서 대체)

- 용도: QTmsg 에이전트 업체들의 카카오 알림톡 템플릿 등록·관리. 카카오/RCS 메뉴는 현재 차단 상태.
- 데이터 원천: MySQL 3388 `event_admin`.
- 이관 방식(Harold 확정): 남아있는 템플릿들을 한줄로에 계정 생성된 업체들과 **이름 매칭**으로 이관 → 한줄로/레거시 양쪽 비교 테스트.
- **미해결 쟁점(§5-③)**: 레거시 템플릿의 발신프로필키가 문자중계서버(54/58, DPK_HM) 축이라, 한줄로 IMC 축으로 옮길 때 단순 DB 복사인지 재등록+재검수인지 확인 필요.

### 2-2. 에이전트 전용 계정 체계 (Track A — 2026-07-03 코드 완료)

한줄로에 구현 완료된 기반:

- DB: `companies.usage_type`(web/agent/both, DEFAULT 'web', CHECK) + `company_agent_ids`(회사 1:N 발송ID, 전역 UNIQUE) — 운영 DDL 실행·실측 완료(web 76).
- 슈퍼관리자: 회사 생성/수정 모달에 사용구분 3버튼 + 에이전트 발송ID 매핑 관리 UI + 목록 배지.
- 게이팅(4경로): ①hanjul.ai 전 라우트 PrivateRoute 회수(에이전트 계정 = `/kakao-rcs`만) ②로그인 랜딩 = 카카오 템플릿 관리 ③app.hanjul.ai 로그인 403 ④KakaoRcsPage = 알림톡 탭만 + ENT 게이팅 면제 + 헤더 로그아웃.
- 에이전트 ID 다계정 업체: 한줄로 계정 1개 발급 → `company_agent_ids`에 발송ID N개 매핑 → 합산 조회 구조.
- 수정 파일: backend `companies.ts`/`admin.ts`/`auth.ts`, frontend `authStore.ts`/`App.tsx`/`LoginPage.tsx`/`KakaoRcsPage.tsx`/`AdminDashboard.tsx`. tsc 0 확인.

### 2-3. 게이트웨이 템플릿 연동 (Track C — ★2026-07-20 1안 API 개통·조회 실측 통과)

> **★2026-07-20 현황**: 강문희 **구현 완료 + 「카카오 템플릿관리 API정의서」 수령**. 62서버(우리) 화이트리스트 등록 완료, **조회(GET) 실측 정상**(`{"data":[],"req_result":"00"}`). PUT은 upsert라 테스트 전용 billid 수령 후 진행 예정. 계약 전문·미확정 2건·설계 반영 포인트 = **[Track B+C 설계문서 §4-0](2026-07-14-template-migration-track-bc-design.md)**(SoT).
> 핵심 확정 3가지: **54=P코드 / 58=R코드 계정** · **billid = 계정이 아닌 고객사(납입자ID) 단위**(1건 등록 시 하위 전 계정 사용 가능하도록 엔진 절차 변경) · **usemod = 서버별 상수**(54: HM1~3 / 58: HM1~6). 삭제 기능 없음, 1초 500회 레이트리밋.
> **★0720 PUT 왕복 점검 완료(R0001) — 서버 결함 1건 발견**: `tran_tmplcd`를 빈 문자열로 보내면 **tmplcd까지 빈 값으로 저장**(계약은 그 반대). 우리 회피책 = **tran_tmplcd 항상 채움(기본=tmplcd 복사)**. insert/update 분기·키 동작·usemod 저장은 정상. 상세=설계문서 §4-0-1.
> **★0720 매핑 시드 확보 — 서팀장 추가 요청 불요**: 0716 제공 「템플릿관리자 등록 목록 (1).xlsx」가 API payload와 1:1(등록서버·Bill_ID·회사명·SenderKey·외부모듈·인비토코드·외부템플릿코드). **Bill_ID 41개 / 4,681행 / 계정 218 / 프로필 211**. P=54·R=58 확정, 병기 `P0042;R0003` 실사례 존재. **usemod는 서버 상수가 아니라 행 단위 값**(54에 HM1~4 혼재) → 기존분은 조회로 유지. tran_tmplcd=tmplcd 불일치 0건. 상세=설계문서 §4-0-2.
> 잔여: 강문희 결함 확인·수정 회신 → **54 포팅** → P0001 동일 점검 → 최종 통지. 테스트 계정 = P0001(54)·R0001(58).

외주(강문희, 중계서버 운영) 제안 2가지 (메일 수신, 스크린샷 확보) — **1안 채택·개통 완료**:

| | 1안 (API 연동) | 2안 (통과, 외주 추천) |
|---|---|---|
| 방식 | 한줄로가 템플릿 등록 시 API로 중계서버 템플릿매핑 DB에 자동 전송. BILL_ID(bindid, 54/58 구분) 계정별 사전 등록 | 중계서버 무변경. 고객사 발송 프로그램이 SMSQ_SEND INSERT 시 `k_templ_code` + `k_etc_json='{"senderkey":"..."}'` 직접 기입 → DPK_HM 그대로 통과 |
| 발송 시 고객사 입력 | 템플릿코드만 | 템플릿코드 + 발신프로필키 둘 다 |
| 개발 부담 | 중계서버 API 데몬 1.5일 + 한줄로 발신 API + BILL_ID 관리 | 에이전트 업체들 각자 INSERT문 수정 |
| 판단 기준 | — | **에이전트 업체들이 자기 발송 프로그램을 고칠 수 있는가** |

**서팀장 2026-07-10 회신 (첨부 구성도 = 강문희 작성 예시 — 목표 발송 흐름: 템플릿관리 웹 → 템플릿코드·발신프로필키 DB → 발송관리프로그램(업체) → SMSQ_SEND → 문자발송Agent → 문자중계서버):**

0. **현행 = 완전 수동 3중 등록 (Harold 설명 2026-07-10)** — 고객사가 레거시 템플릿관리자에 요청 → 서팀장이 휴머스온(IMC=휴머스온의 API·Agent) 웹에서 수동 등록 → 승인 후 템플릿관리자에 수동 반영 → **게이트웨이(엔진)에도 수동 등록**(= 아래 billID→senderkey 매칭의 정체). 이관 목표 = 한줄로의 기존 자동 검수 흐름(IMC API 등록 요청·승인 템플릿코드 자동 수신·반려사유 자동 기록)으로 수동 단계를 전부 제거하고, 발송은 INSERT에 senderkey를 실어 **게이트웨이 무변경 통과**.
1. **신규 업체** = `k_templ_code` + `k_etc_json='{"senderkey":"..."}'` 함께 INSERT하면 추가 작업 없이 발송(서팀장 실무 확인) — 게이트웨이 수동 등록 단계 소멸 확인.
2. **기존 업체** = 게이트웨이에 수동 등록해 둔 매칭으로 senderkey 미기입 발송 중(등록 주체 = 서팀장/인비토 운영). 매칭이 게이트웨이 측 등록이라 **143 폐기와 무관하게 유지**(등록 위치·목록 추출만 최종 확인 — §5-⑬). **빅뱅 전환 불필요**: 기존 등록분은 그대로 동작, 그 업체가 새 템플릿을 만드는 시점부터 senderkey 포함 방식으로 업체별 점진 전환(발송 프로그램 수정 불가 업체는 과도기에 신규 승인분만 게이트웨이 등록 유지). 커스텀/타사 템플릿코드 유용/senderkey 복수 업체는 그 자산이 한줄로 IMC(휴머스온) 계정에서 조회되는지 업체별 분류 필요.
3. **계정 매칭·리셀러** = 템플릿관리자 계정(user 59, parent_no 상위계정 구조)과 한줄로 계정이 별개 생성이라 매칭 방법 확정 필요. 특히 **더화이트(리셀러, 템플릿 1,988건=44%)**는 대행 관리 + end-client에도 접속 계정을 파 준 상태 — 한줄로에서 ①더화이트 계정 아래로만 볼지 ②end-client 계정도 팔지 ③고객사가 다른데 템플릿 공유가 가능한지 규정 확정 후 업체 안내 필요(§5-⑭, 0705 핸드오프 §6-2와 동일 쟁점).

**2026-07-10 한줄로 카카오 템플릿 관리 소스 정독(비토) — 이관 방식 근거 5사실 + 베스트안:**

1. **IMC 연동 = 전사 단일 API 키** (`utils/alimtalk-api.ts` — `IMC_API_KEY` env 하나, 회사별 계정 아님). "휴머스온 계정 동일 여부"는 전사 1회 확정이면 끝.
2. **같은 휴머스온 계정 개연성의 실측 근거 존재** — `utils/kakao-template-sync.ts:122` 주석에 2026-05-26 Harold raw 정독 실측 "IMC 전체 total = 4,849건" 기록. 한줄로 카카오는 잠금(자체 등록 소수)인데 4,849건이 보임 = 레거시 4,519건이 같은 계정 아래일 규모와 부합. **확정 실측 = `getSender('6be1390acc6dceecd2441f93fd14324d6d82ed1d')`(아난티) 1콜**(함수 기존 존재).
3. **신규 등록·검수 자동화 완비** — 프로필 카톡인증 등록·템플릿 CRUD·검수요청/취소·반려사유 수신·templateCode 자동 백필 4중 안전망(웹훅+30분 워커+5분 job+단건 GET). 신규 흐름 추가 개발 0.
4. **없는 것 = import 2종뿐** — ①이미 IMC에 등록된 senderKey "연결" 경로(현행 POST /senders는 카톡인증 신규 등록 전용) ②그 senderKey의 기존 승인 템플릿을 IMC→`kakao_templates` 행으로 생성(현행 sync는 기존 행 코드 백필 전용·행 생성 안 함). → **Track B 개발 실체 = 슈퍼관리자 import 2개.**
5. **발송 축 = 한줄로 코드 변경 0** — 표준 QTmsg 라인은 의도적 senderkey 미주입(중계서버 매칭 의존, D234+ "표준 라인 payload 불변" 안전 전제 — `utils/alimtalk-emphasize.ts`) / 비토 라인만 withSenderKey 주입(2026-07-07). 에이전트 업체 발송은 업체 프로그램↔게이트웨이 규약(한줄로 코드 밖).

**베스트안(서팀장 의견 교환 후 확정)**: 이관 = "레거시 DB 복사"가 아니라 **회사별 senderKey 연결 + IMC 원본에서 템플릿 pull**. 검수 상태·템플릿코드가 IMC 원본 그대로 → 재검수 쟁점(§5-③) 소멸. 레거시 event_admin은 대조 검증용으로만(폐기 전 mysqldump 원칙 불변). 잔여 실측 2 = ①getSender(아난티) ②IMC 전체 목록 item에 senderKey 필드 포함 여부(sync 디버그 로그가 첫 item 키를 출력 — 1회 실행으로 확인, 포함 시 회사별 일괄 분류 가능).

### 2-4. PAY 사이트 (Track D에서 대체)

2026-07-03 폴더 전체 다운로드(`C:\Users\ceo\Downloads\pay-app.tgz`, 20MB) 분석 완료:

- 구조: Spring 3 + MyBatis + JSP 17장, 소스(.java) 포함. 기능 = ①발송량 조회(page1, 일별×업체별 SMS/LMS/MMS/카카오 성공·실패·대기 + 엑셀) ②선불 충전(이니시스 카드+계좌 수기 — **불필요 확정**) ③충전 이력(page3) ④계정·업체·단가 관리(page4).
- DB (`sales`@3388) 테이블 6개:
  - `RSRM_SalesStts` — **핵심.** 일별 발송 통계 (CustId, StoreId, DestDt, MsgType S/L/M/K, TotCnt/OkCnt/FailCnt/ReadyCnt, RemAmt). **이 앱에는 INSERT 코드가 없음 = 외주 게이트웨이가 직접 적재.**
  - `RSRM_SalesMst` — 업체 마스터 (CustId, CustNm, StoreId, StoreNm, PayTp 1후불/2선불, Email, MobNo)
  - `RSRM_FillAmtHist` — 충전 이력 (StoreId, FillAmt, FillDtTm, PayMethod 1카드/2계좌, PayFkey)
  - `RSRM_Mem` / `RSRM_MemStore` — 로그인 계정(MemGubun 1=총판/2=스토어) + 계정-업체 매핑
  - `RSRM_SalesPrice` — 업체별 단가 (SMS/LMS/MMS + 통신사별 MMS)
- 한줄로 대체 형태: `company_agent_ids` 매핑 기반 Agent 발송결과 조회 + 슈퍼관리자 정산 합산. 데이터 유입은 외주 적재 목적지 변경(§5-②) 확정 후 설계.
- QTmsg 에이전트 발송 자체는 한줄로 MySQL과 **별개** — 에이전트가 게이트웨이에 직접 발송, 결과 통계만 이 sales DB로 넘어오는 구조 (Harold 확인 2026-07-03).

---

## 3. 트랙별 실행 계획 + 상태

> 상태 값: `대기` / `진행중` / `코드완료` / `배포완료` / `검증완료` / `차단(사유)`

| 트랙 | 내용 | 상태 | 담당 | 비고 |
|------|------|------|------|------|
| A | 사용구분(웹/에이전트/둘다) + 에이전트 게이팅 + 발송ID 매핑 | **코드완료 (2026-07-03)** | 비토 → Harold 배포 | 배포 후 실측 4항목: 로그인 랜딩 / URL 직접입력 회수 / app 403 / 발송ID CRUD |
| A-2 | (부수 완료) event-admin 프론트 SSL 유료→무료 ZeroSSL 자동갱신 전환 | **검증완료 (2026-07-03)** | 비토+Harold | 연 99,000원 절감. acme.sh --list 등록 확인 |
| A-3 | 가비아 유료 인증서(99,000원) 환불 문의 — 발급 2일차·미사용·개인키 미수령 상태라 환불 가능성 있음 (가비아 1544-4370, 상품별 15/30일 환불 보증 확인) | 대기 | Harold(또는 서팀장) | 환불 진행 시 revoke 되어도 무료 cert로 대체됐으므로 영향 0 |
| B | 레거시 템플릿 이관 — import 2종(senderKey 연결+IMC pull) + 브랜드 계층 가시성 + 계정 생성(B_ 76%만 pull·bizp_/업체지정 24%는 기존 매핑 유지) | **설계완료(2026-07-14)** — 관문=아난티 getSender 실측 | 비토(구현) + Harold(승인·실측) | 설계 = [2026-07-14-template-migration-track-bc-design.md](2026-07-14-template-migration-track-bc-design.md). §5-③ 재검수 쟁점 = IMC 원본 pull로 소멸 |
| C | 게이트웨이 템플릿 연동 — **1안(API) 확정(Harold 2026-07-14)** + 신규·자체게이트웨이 이관 업체 senderkey 동봉 병행. 매핑 CT+아웃박스+효과검증+대조 워커 | **설계완료(2026-07-14)** — 강문희 제안서 발신 대기 | Harold(발신) → 강문희(API수신 1.5일) → 비토(구현) | 제안서 = [2026-07-14-legacy-template-api-proposal-kang.md](2026-07-14-legacy-template-api-proposal-kang.md) · 자체게이트웨이 = [자비스 지시서](2026-07-14-jarvis-gateway-template-spec.md) |
| D | PAY 대체 — 적재 목적지 변경 + Phase 2 일괄(agent 조회 / both 발송결과 Agent 구분 / 슈퍼관리자 정산 + 거래내역서 웹·Agent 이용구분) | **Phase 2 유형별 조회 코드완료·배포대기(2026-07-23)** · Phase 1 수신DB(0707) | 비토 → Harold 배포 | **런북·진행 = [docs/2026-07-07-pay-absorption-track-d-design.md](2026-07-07-pay-absorption-track-d-design.md) §7·§9.** invito pay-ingest-db(MariaDB 10.11)·3테이블 이관·SysId 백필·3-IP 계정·방화벽 완료. **★0723 에이전트 발송통계 유형별(SMS/LMS/MMS/카카오알림톡) 3화면(고객사 StatsTab·슈퍼 AdminDashboard·사용자 ResultsModal) 재개발 코드완료(tsc0·vitest900·Codex1R) + 웹/에이전트/테스트 탭. 전환갭 백필 완료(58=07-08~13·54=07-08~09 통째 누락 → 143→62 자체 복원·유형합 1,957만 일치).** 다음=배포→57갭 확인→슈퍼 정산합산(billing 별도). ★정산 일괄 원칙(분리 배포 금지) |
| E | 잔여 서비스 정리 — 알림 Agent 6종 / invitobiz.com / tomcat6_b / 미확인 프로세스 전수 실사 | **진행중 (2026-07-16 전수 실사)** | Harold+비토 | §1 ①③④ 진전(§7 0716): 웹발송 Agent 6개(Oracle)=정지·kkotemp(event-admin 검수알림·MySQL)=유지 / tomcat6_b=invitobiz 웹앱(8070·발송0·결제0·조회 42계정) / tomcat6(8080)=빈 유휴 / 8090 /python=死. 잔여=폐기 전 42계정 공지+발송내역 데이터 확보 |
| F | 최종 폐기 — 전체 백업(MySQL 3388 전 DB + /home + /www + nginx conf) → DNS 정리 → 2주 관찰 → 해지 | 대기 | Harold | §6 체크리스트 전 항목 완료가 선행 조건 |

권장 순서: A 배포·실측 → B(이관) ∥ D(서팀장 답변 오는 대로) → C(외주 협의) → E → F.

---

## 4. 서팀장 요청 5문항 (2026-07-03 발신 — 2026-07-07 회신 도착)

> 회신 첨부 3종: `2017-0928-인비토_매장_선불잔액_통계IF테이블설계.xlsx`, `PAY ID.xlsx`(=RSRM_Mem 덤프), `2017-0925-인비토_매장_선불잔액_통계IF업무절차설계.pptx`(강문희 2017.09.25). 원본 = Harold Downloads.

1. 중계서버가 통계를 sales DB로 넣는 게 맞는지 — 어느 서버·계정으로 → **답변: 맞음.** MariaDB, 테이블 3개(RSRM_SalesMst 운영자 원부 / RSRM_SalesStts 중계서버 통계 / RSRM_FillAmtHist 충전이력·잔액동기화). **54·57·58** 서버에서 `sales` 계정으로 DB 직접 INSERT. (기존 문서엔 54/58만 — **57 추가 확인**)
2. 적재 주기·방식 → **답변: 거의 실시간(과거 대표 요청으로 1분 간격 동작). DB 직접 INSERT(mysql driver port open).**
3. 적재 항목·RemAmt 여부 → **답변(테이블설계 엑셀): RSRM_SalesStts = DestDt(YYYYMMDD)·CustId·StoreId·RemAmt(Float, 잔액 게이트웨이가 넣음 O)·MsgType·TotCnt·OkCnt·FailCnt·ReadyCnt·UpdTm·InsTm. MsgType = S/L/M/K 외 X(팩스)·KS(카톡후SMS)·KL(카톡후LMS)까지.** PK 표기 = DestDt+CustId+StoreId (MsgType은 PK 미표기 — 실제 스키마 재확인 대상). "있으면 삭제 후 insert"(replace).
4. PAY 조회 업체 전체목록(CustId+실제업체명+StoreId) → **⚠️ 부분 답변.** 받은 `PAY ID.xlsx` = **RSRM_Mem(로그인 계정) 270건뿐** (고객사 92 + 매장 176[B0046 총판 174] + super 2, distinct CustId 92, prefix B44·C37·D11). **실제 업체명(CustNm)·발송ID(StoreId, S00x)·단가·PayTp가 담긴 RSRM_SalesMst와 RSRM_MemStore는 미전달** — 로그인 핸들(MemId)만으론 회사 매칭 불가. **RSRM_SalesMst 덤프 재요청 필요.**
5. 적재 목적지 변경 가능 여부(강문희) → **답변: 가능.** "DB 종류·접속정보 주면 변경 가능(54·57·58에서 접속 open만 되면). API도 가능하나 **delete·insert 둘 다** 되어야 함(실시간 replace)." ⚠️ **충전이력 수집(역방향)은 답변에 없음 — §5-⑧ 신규.**

### 4-2. 서팀장 2차 체크 메일 (2026-07-07 발신 — ★회신 도착·전부 반영)

1. **RSRM_SalesMst 덤프** → **답변: 수령(730행).** CustId·CustNm·StoreId·StoreNm·PayTp·RemAmt 포함, **단가 컬럼 없음**(별도 RSRM_SalesPrice). 분석 = 설계 §2-4. CustId 고유 499·1 CustId:N StoreId 4곳(B0046=200)·(CustId,StoreId) 중복 0. 후불 다수는 StoreId NULL → 매핑은 CustId+StoreId 이원.
2. **선불(PayTp=2)** → **답변: 385행(잔액>0 177행·합 3,927만원).** 서팀장 예시 5곳(런소프트·피케이포유·언니가간다·시큐엠·고운) 원부 대조 확인, 고운(B0225) 종료·잔액 508,002원 보존. "대표적으로"라 실제 선불은 더 많음 → 활성 업체는 유입 데이터로 확정. §4 READ-only 결론 유지.
3. **강문희 확정** → **답변: 수신 DB 5요소 우리가 정해 전달 방식(현행 예시 143:21772·DB sales). 방화벽 = 수신측 인바운드만 open(54·57·58→송신). replace = 일자+서버(CustId prefix LIKE) 전체 삭제 후 (DestDt,CustId,StoreId,MsgType) 재삽입 → 유형 소실 없음. ★개선 = RSRM_SalesStts에 SysId(54/57/58) 컬럼 추가·StoreId=CliBillId.**
4. **선불 5% 현재 충전 방식** → **답변: 게이트웨이 직접 + PAY 화면 병존. PAY 폐기 후 게이트웨이 직접만으로 충분**(잔액은 게이트웨이가 1분 push라 한줄로 READ-only로 최신 유지 — §4).
5. **RSRM_SalesPrice 단가표(신규 요청)** → **답변: 대부분 NULL.** 업체가 PAY에 노출 원하는 곳만 지정하는 오픈 시점 규정 → **단가 원장 = 한줄로로 확정**(직원이 슈퍼관리자에서 입력, Agent 단가는 웹 cost_per_*와 분리).
6. **billIdPrefix ↔ 서버 매핑** → **답변: B=54 / C=57 / D=58**(한줄로 62와 같은 대역·마지막 옥텟 관례). 이관 SalesStts SysId 백필 완료(54=801,720/57=7,468/58=125,044·NULL 0).

### 4-1. 세 첨부 정독 검토 (2026-07-07 비토)

**업무절차 PPTX가 드러낸 양방향 구조 (조회 앱 아님 — 잔액 파이프라인의 한 축):**
- **정방향 (중계서버 → sales DB)**: "잔액/통계 DB 자동 push 엔진" — 1분 간격 RSRM_SalesStts(replace) + RSRM_SalesMst.RemAmt 잔액 동기화.
- **역방향 (sales DB → 중계서버)**: "충전금액 자동 수집 엔진" — 중계서버가 **RSRM_FillAmtHist(충전이력)를 읽어가** 자체 엔진 잔액에 반영 후 RsApplyFlag N→Y update. 즉 PAY의 선불 충전 입력이 중계서버 잔액을 올리는 유일 경로일 수 있음.
- 강문희 답변은 **정방향(통계/잔액 적재)만** 다룸. 역방향(충전 공급)을 한줄로가 이어받아야 하는지 미해결 → **★2026-07-20 해소: 이어받아야 함이 확정**(⑧번 참조. PAY가 유일 창구·인비토 내부 입력 → 슈퍼관리자 충전 화면 필수).
- ⚠️ **2026-07-07 기재 "충전은 게이트웨이 직접"은 오기** — 2026-07-20 서팀장 확인으로 **게이트웨이 직접 입력 경로는 존재하지 않음**이 확정됐다. 이 오기가 Track D 설계 §4를 "READ-only로 충분"으로 잘못 이끌었다(0720 전면 개정).

**흡수 방식 권장 (정답 1개):** 한줄로 측(PG서버 또는 별도 인스턴스)에 RSRM_SalesStts/Mst/FillAmtHist **동일 스키마 MariaDB 수신 테이블**을 만들고, 그 접속정보를 강문희에게 전달 → 게이트웨이는 접속 대상 DB만 교체(코드 최소변경). API안(1.5일 데몬)보다 DB직결이 replace 시맨틱·개발부담 면에서 유리하고, 강문희도 DB직결을 1순위로 답함. 이후 한줄로가 이 테이블을 읽어 Track D 화면 2종 + 슈퍼관리자 정산 합산.

**충전(역방향) 처리 분기:** 현재 활성 에이전트 업체가 후불(PayTp=1)이면 FillAmtHist는 사실상 死데이터 → 통계/잔액 정방향만 이관하면 끝. 선불(PayTp=2)이 있으면 한줄로가 FillAmtHist 상당 데이터를 새 DB에 써주고 중계서버 수집엔진이 그걸 읽게 목적지 변경 필요. → **RSRM_SalesMst.PayTp 분포부터 확인**(RSRM_SalesMst 재요청 시 동봉).

**강문희에게 넘길 것 (목적지 변경 발주 시):** ①수신 DB 종류(MariaDB 동일)·호스트·포트·DB명·계정/비번 ②54·57·58 인바운드 방화벽 open 확정 ③replace 기준키 = (DestDt, CustId, StoreId) 단위 삭제 후 유형별 재삽입 여부 확정 ④역방향 충전 수집을 유지할지(선불 존재 시).

---

### 4-3. 템플릿관리자 이관 사전 체크 12문항 (2026-07-10 작성 → ★2026-07-14 서팀장 8문항 회신 도착·반영 완료)

> **2026-07-14 회신 요지**: ①휴머스온 웹 계정=관리자 대표계정(invito)·한줄로 API 계정과 "다르다" 답변 — 단 웹 계정≠API 테넌트일 수 있어 **아난티 getSender 실측으로만 확정**(관문 유지) ②게이트웨이 매핑=엔진 자체 저장·143 미참조 확정 + 수동 등록 절차 전문 확보 ③매핑 전체 목록 엑셀 수령(4,159건 — 대조 baseline) ④senderkey 직접/매칭 의존 구분 기록 없음("무수정" 전제 영업) ⑤템플릿코드 접두사 = B_(휴머스온)/bizp_(다우)/기타(업체지정) ⑥더화이트=직접 접속 계정 최근 미사용 → 관리계정 단일 귀속 ⑦폐업·테스트 자체 삭제 완료(휴면 표기) ⑧검수알림 Agent=템플릿관리자 전용(§5-① 해소). 엑셀 실측(비토): 통과 4,299 = B_ 3,290(76%)/bizp_ 791(18%)/기타 218(5%) · 회사 51·프로필 242·휴면 4(전부 더화이트)·@테스트 1 제외. **방향 확정(Harold 2026-07-14): 강문희 1안(API 연동) 채택 + 신규·자체게이트웨이 이관 업체는 senderkey 동봉 온보딩 병행.** QTmsg 매뉴얼 정독으로 1안=알림톡 공식 경로(매핑 최상위 우선) 확정, PPT 오타 2건(k_templ_code·senerkey→정본 k_template_code·senderkey) 식별. **산출 문서 3종**: [자비스 지시서](2026-07-14-jarvis-gateway-template-spec.md) · [강문희 제안서](2026-07-14-legacy-template-api-proposal-kang.md)(★0715 Word 변환 — Downloads/템플릿연동_1안_진행제안_인비토_20260715.docx, 서팀장 검토 후 발신) · [Track B+C 설계](2026-07-14-template-migration-track-bc-design.md). 브랜드 가시성 방침 = 고객사 관리자 전체 열람 / 각 브랜드는 자기 sender_key 템플릿만(§5-⑭ 대체 — ★0715 소스 확인: 현행 회사 전체 노출 정책이라 스코프 신설 필요 = 유일 신규 개발). ★0715 서팀장 잔여 실무 4건 = Bill_ID 확정본(최우선)·휴머스온 계정 교차 확인·수동 등록 컷오버·업체 연락처+안내(설계문서 §5-5). 원 12문항 중 Q10(친구톡)=카카오 공식 폐지로 불요, Q9(시세이도 귀속)=엑셀로 해소(4브랜드 1계정), Q12(공지)=실무 4건-④로 흡수.

> 발신 전 우리 방침(안) 3개를 전제로 공유: ①신규 업체 = 발송 INSERT에 k_etc_json senderkey 포함 스펙 확정(게이트웨이 수동 등록 소멸) ②기존 업체 = 기존 게이트웨이 매칭 유지 + 새 템플릿 시점부터 업체별 점진 전환(일괄 컨택 X) ③템플릿 이관 = 레거시 DB 복사 X, IMC 원본에서 senderKey 단위 pull(재검수 없음).

1. 서팀장이 휴머스온 웹에서 템플릿 등록에 쓰던 계정 = 한줄로 API 연동 휴머스온 계정 동일 여부 즉답. (한줄로 쪽 교차 실측 = 아난티 senderKey getSender 1콜)
2. 게이트웨이에 템플릿코드→senderkey를 등록하던 실제 절차(누가·어느 화면/DB) + 그 매칭이 143(event_admin) 참조가 아니라 게이트웨이 자체 저장인지 최종 확인.
3. 게이트웨이 기존 매칭 전체 목록(업체별 템플릿코드→senderkey) 추출 가능 여부 — 이관 후 대조 검증용.
4. 에이전트 업체 구분 목록: k_etc_json senderkey 직접 기입 업체 vs 엔진 매칭 의존 업체.
5. 커스텀/타사 템플릿코드 유용 업체 목록 — 우리 휴머스온 계정에서 안 보일 수 있는 자산 = 별도 처리 대상.
6. 더화이트 1,988건의 실 고객사 분포 + 직접 접속 계정을 파 준 업체 목록.
7. 리셀러 규정(안) 의견: 발신프로필 단위 회사 귀속 / 고객사 다른 템플릿 공유 불가 / 직접 접속 필요 업체만 별도 계정 + 더화이트 담당자 계정 병기.
8. 신규 생성 대상 34개 중 폐업/휴면/테스트 제외 선별.
9. 인비토 계정 중복(한줄로 2개) 정리 + 레거시 시세이도 13건의 시세이도/한국시세이도 귀속.
10. 레거시 친구톡 템플릿 실사용 업체 존재 여부(한줄로는 친구톡 폐지 — 브랜드메시지 전환).
11. 검수 알림 SMS Agent(kkotemp/mtdeliver)의 템플릿관리자 외 용도 여부(§5-①).
12. 이관 후 업체 접속 전환 공지(레거시 웹 차단 전 안내) 절차 초안.

> 관문 = 1번. "같다" 확인 시 Track B 개발 = 슈퍼관리자 import 2개(프로필 연결+템플릿 pull)로 좁혀지고 아난티 파일럿 개시 가능. 강문희 질의는 서팀장 회신 후 잔여분만.

## 5. 열린 질문 / 결정 대기

| # | 질문 | 결정자 | 상태 |
|---|------|--------|------|
| ① | 레거시 알림 SMS Agent(kkotemp/mtdeliver)가 템플릿관리자 외 다른 용도로도 쓰이는지 | Harold | **해소(2026-07-16)**: kkotemp-agent.service(/home/event/libs/invitoMsg·qtmsg.jar+mysql_jdbc=MySQL)=event-admin 검수알림 전용 → 템플릿관리자와 함께 유지. /home/invitoMsg/invitoAgent_*(6개·ojdbc=Oracle)=구 웹발송 senders(발송0)=0716 정지. 공유 클래스 qtmsg/main/QtMsg라 blanket pkill 금지·정확 PID만 |
| ② | invitobiz.com 웹사이트 폐기 vs 타 서버 이전 | Harold | **방침 확정(2026-07-16): 7/31 유지 후 폐기.** 실체=구 invito 웹발송 솔루션(tomcat6_b 8070·usom 앱·Oracle orcl). 발송 0(7월 SMSQ_SEND 실카운트 0)·결제 0(둘 다 차단) — 최근30일 42계정·7일 8계정=발송내역/정산 조회 전용(정본 Oracle SMSQ_SEND). 폐기 전=42계정 공지+데이터 확보(§6) |
| ③ | 레거시 템플릿(중계서버 54/58 축)의 한줄로 IMC 이관 시 재검수 필요 여부 — 발신프로필키 호환성 | 비토 조사 + 필요 시 카카오 검수팀 확인 | 미확인 |
| ④ | 서버 내 미확인 프로세스 전수 실사 (netstat/crontab/DB 목록) | Harold 실행(비토 명령어 제공) | 미실행 |
| ⑤ | 게이트웨이 연동 1안 vs 2안 | Harold+외주 | 미결 |
| ⑥ | '둘다(both)' 회사의 카카오&RCS ENTERPRISE 게이팅 유지 여부 (현재: 에이전트 전용만 면제) | Harold | 현행 유지 중 |
| ⑦ | MySQL 3388 백업 범위 — event_admin, sales 외 다른 DB 존재 여부 | ④와 함께 확인 | 미확인 |
| ⑧ | **충전이력 역방향(sales→중계서버 수집엔진 RSRM_FillAmtHist)** — Harold 2026-07-07 확정: 선불 약 5% 존재 = IN 스코프. **★2026-07-20 잔여 확인 해소(서팀장)**: 충전 입력 창구는 **PAY 화면 하나뿐**(게이트웨이 직접 경로 없음 — "업체에게 엔진 사이트를 오픈하지 말라고 해서 PAY에 넣은 것"), **입력 주체 = 인비토 내부**(입금 확인 후 담당자, 계정 2개 기발급). ⇒ **143 폐기 시 충전 경로 단절 → 슈퍼관리자 충전 입력 화면이 선택→필수로 승격**(Track D 설계 §4 전면 개정). 필요 작업 2건 = 강문희 수집엔진 소스 62 전환(선행) + 우리 화면 | 서팀장 → **해소(0720)** | **확정·필수화(설계 §4-1~4-5)** |
| ⑨ | RSRM_SalesMst 덤프 재요청 | 서팀장 | **해소(2026-07-07 수령 730행·§2-4·§4-2). 단가는 없음→한줄로 원장 확정(§3-3)** |
| ⑩ | RSRM_SalesStts replace 기준키(MsgType 포함) | 강문희 | **해소(2026-07-07): 일자+서버 전체 삭제 후 (DestDt,CustId,StoreId,MsgType) 재삽입·SysId 컬럼 추가(§4-2 3번)** |
| ⑪ | 수신 DB 물리 위치·접속 5요소 확정 + 0.0.0.0 인바운드 예외(2026-02-28 룰) 승인 | Harold | **원격 접속 로직 검증완료(2026-07-09)** — 근본 = DOCKER-USER에 **리턴 트래픽(ESTABLISHED) 허용 룰 누락** → 발송서버 SYN은 ACCEPT 통과해도 DB 응답(SYN-ACK) source가 화이트리스트 밖이라 `0.0.0.0/0 DROP`에 걸림 → **어떤 IP를 넣어도 접속 불가**였음(로컬 127.0.0.1은 FORWARD 미경유라 OPEN 착시). tcpdump로 SYN 재전송·SYN-ACK 0 확인. **비토 게이트웨이(139.150.81.213)를 화이트리스트에 넣어 동일 환경 실측 → established 추가 후 실접속 OPEN 확인**, systemd `pay-ingest-fw`에 established+139 영속화. **★해소(2026-07-15)**: 강문희 실 IP 회신 — 54/57/58 전부 `curl ifconfig.me` 실측 = 서버명 옥텟과 일치(58.227.193.54/.57/.58, 57은 docker 안이나 호스트 NAT라 동일) → **계정·방화벽 이미 반영 상태 = 추가 작업 0**. 유닛 파일 실측 확인(ACCEPT 4 + ESTABLISHED — 체인 순서 설계대로). |
| ⑫ | 강문희 발주 → 게이트웨이 접속 테스트 → 전환일 → 유입 검증(§7-4) | Harold(발주)·강문희(전환)·비토(검증) | **진행(2026-07-15)** — 강문희 회신: 54·57 프로그램 준비 완료(54=기존 java 복사+OLD 병행 유지+SysId 설정 반영 / 57=golang 전면 신규[한글·성능·자원 사유, 자체 검수 진행중] / 58=최다 사용량이라 57 검증 후 최후 적용). 운영실 테스트 3항목 요청(통계 실시간·결제→자동충전·이용자 추가→원부 반영, clibillid/storeid 한글 주의). **Harold 답신 발송(0715): 54·57·58 실 아웃바운드 공인 IP curl ifconfig.me 실측 요청(⑪ 전제) + SysId 값 형식 '54'/'57'/'58' 확인 + FillAmtHist 수집 엔진 새 DB 포함 확인** → **★0715(2) 강문희 실 IP 회신(전부 옥텟 일치·반영 불요·⑪ 해소) + SysId '54'/'57'/'58' 확정 + 신서버 단독 접속 구조(FillAmtHist 포함) 확인 → Harold "적용 시작 OK" 발신 → ★당일 54 병행 적재 개시 실측 통과: 당일 288행 유입(1분 push 실시간·InsTm 14:46)·SysId='54' 문자 그대로·CustId B-prefix 일치·한글 StoreId 7행 무결(향수식자재옥천점 등·깨짐 0). 다음 = 익일 143 마감 대조(DestDt='20260715': 143 `CustId LIKE 'B%'` vs 우리 `SysId='54'` COUNT 일치) → 운영실 테스트(충전·원부는 전환 단계) → 57(golang)→58.** 최종 전환일 직전에만 최신 dump 재복원+SysId 재백필(병행 중 불요). |
| ⑬ | **기존 업체 billID→senderkey 매칭** — 구조 확인(2026-07-10 Harold 설명): 매칭 = 게이트웨이에 사람이 수동 등록한 것 → 143 폐기와 무관. 잔여 확인 3개 = ①게이트웨이 등록 실절차·저장 위치(143 참조 아님 최종 확인) ②기존 매칭 전체 목록 추출(이관 대조용) ③서팀장이 휴머스온 웹 등록에 쓰던 계정 = 한줄로 IMC 연동 계정 동일 여부. 업체 발송 로직 수정 요구는 새 템플릿 시점 점진 전환(빅뱅 컨택 X) | 서팀장 즉답(필요 시 강문희) | 구조 확인 — 세부 3개 잔여 (2026-07-10) |
| ⑭ | **리셀러(더화이트) 접속 계정 정책** — 더화이트 계정 아래로만 열람 vs end-client 계정 병행 발급 vs 고객사 상이 템플릿 공유 허용 여부. 규정 확정 후 업체 안내 | Harold+서팀장 회의 (0705 핸드오프 §6-2와 통합) | 미결 (2026-07-10 회신으로 신규 등재) |

---

## 6. 최종 폐기 전 체크리스트 (Track F 게이트)

- [ ] Track A 배포 + 실측 4항목 통과
- [ ] Track B: 템플릿 이관 완료 + 에이전트 업체 한줄로 계정 전환 공지 + 레거시 템플릿관리자 접속 차단 후 2주 무클레임
- [ ] Track C: 게이트웨이 템플릿 연동 가동 (신규 템플릿이 레거시 없이 발송에 반영됨을 실측 1건)
- [ ] Track D: 적재 목적지 변경 완료 + 한줄로 Agent 발송결과가 PAY와 동일 수치임을 대조 검증(같은 기간 합계 비교) + PAY 접속 차단 후 2주 무클레임
- [ ] Track E: 인벤토리 11항목 전부 [대체완료/폐기확정/백업완료]
- [ ] invitobiz(usom) 웹발송 폐기: 42 조회계정 종료 공지 + 발송내역/정산 조회 대체·다운로드 제공 → 접속 차단 후 관찰 (발송·결제 이미 0 — 2026-07-16 진단)
- [ ] 전체 백업: MySQL 3388 전 DB 덤프 + **Oracle orcl(usom) expdp — invitobiz 발송내역 정본 SMSQ_SEND 포함(2026-07-16 확인, MySQL 백업만으론 누락)** + `/home` + `/www` + `/usr/local/nginx/conf` + crontab → 한줄로 서버 또는 로컬 보관, 복원 테스트 1회
- [ ] DNS: event-admin / event-admin-api / pay / invitobiz 레코드 정리 (폐기 or 이전처 변경)
- [ ] 관찰 기간 2주 (DNS 정리 후) → 다우클라우드 해지
- [ ] 절감 확정: 월 511,500원 + SSL 연 99,000원

---

## 7. 이력 로그

| 날짜 | 내용 |
|------|------|
| 2026-07-03 | 문서 최초 작성. PAY 앱 전체 분석(20MB 다운로드, 테이블 6개·적재 구조 확정). event-admin 프론트 SSL 유료→무료 전환 검증완료. Track A(usage_type+게이팅+발송ID 매핑) 코드완료·tsc 0. 서팀장 5문항 발신. 외주 1안/2안 메일 검토. |
| 2026-07-03 | 자가 검토 보강: A-3(가비아 인증서 환불 문의) 추가, Track D를 화면 2종(에이전트 전용 조회 / 웹·둘다 발송결과 Agent 구분)으로 명확화. |
| 2026-07-07 | 서팀장 5문항 회신 도착(첨부 3종). 정독 검토(§4-1): 문항 1/2/3/5 확인(54·57·58 · 1분 실시간 DB직결 · RemAmt+MsgType 7종 · 목적지 변경 가능 delete+insert 필수). ★업무절차 PPTX에서 양방향 구조 확정(정방향 통계/잔액 push + 역방향 충전 수집 RSRM_FillAmtHist) — 강문희 답변은 정방향만. 문항4 부분답(RSRM_Mem 270건만, RSRM_SalesMst 미전달=업체명·발송ID·PayTp 없음). 신규 열린질문 ⑧⑨⑩. Track D 차단→진행중. 흡수방식 권장=동일스키마 수신 DB+접속정보 제공(게이트웨이 코드 최소변경). |
| 2026-07-07 | Track D 상세 설계안 작성([2026-07-07-pay-absorption-track-d-design.md](2026-07-07-pay-absorption-track-d-design.md)). Harold 확인: 선불 약 5% 존재하나 충전 입력이 게이트웨이 직접에도 있음 → 잔액은 게이트웨이가 1분 push라 **흡수는 READ-only로 충분**(충전 화면 필수 아님, §4). **서팀장에 2차 체크 메일 송부 완료** — 회신 대기 항목 4건(아래 §4-2). |
| 2026-07-07 | **서팀장 2차 회신 전부 도착·반영**(§4-2): SalesMst 730행 수령(단가 없음→한줄로 원장)·선불 실재·replace 일자+서버 삭제·SysId 컬럼 추가·prefix B=54/C=57/D=58·충전은 게이트웨이 직접. |
| 2026-07-07 | **Track D Phase 1 수신 DB 구축완료**(런북 §7). Harold 승인(0.0.0.0 예외·포트 23388). invito `pay-ingest-db`(mariadb:10.11·143 실측 5.5.56이라 `--sql-mode=""` 호환) → dump 82MB 3테이블 복원·행수 대조 일치(934,232/730/7,026)·SysId ALTER+백필(54=801,720/57=7,468/58=125,044·NULL 0)·root@% DROP·sales×3 IP 계정·hanjul_ro SELECT-only·방화벽 systemd `pay-ingest-fw`(DOCKER-USER — ufw는 도커 우회 함정). 전환일 절차 = 최종 dump 3테이블 통째 재복원+SysId 재백필(replace 당일만 삭제라 기간 갭 방지). **정산 일괄 원칙(Harold)**: Phase 2 = 조회+슈퍼관리자 정산+거래내역서 웹/Agent 이용구분 한 번에(분리 배포 금지). 다음 = 강문희 발주(§7-3)→전환→유입 검증. |
| 2026-07-10 | **템플릿관리자 이관 서팀장 회신 반영(§2-3 — 첨부 구성도=강문희 예시)** — 신규 업체=senderkey INSERT로 즉시 가능(게이트웨이 수동 등록 소멸) / 기존 업체=게이트웨이 수동 매칭으로 발송 중 / 리셀러(더화이트) 정책 쟁점 등재(§5-⑭). 이후 Harold 설명으로 현행=완전 수동 3중 등록(휴머스온 웹→템플릿관리자→게이트웨이) 구조 확정, §5-⑬은 "매칭=게이트웨이 수동 등록·143 무관"으로 상태 갱신(세부 3개 = 서팀장 즉답 대상). 기존 업체 전환 = 새 템플릿 시점 업체별 점진(빅뱅 컨택 X) 방침. |
| 2026-07-10 | **한줄로 카카오 템플릿 관리 소스 정독(비토·Harold 지시)** — 5사실 확정(§2-3): 전사 단일 IMC 키 / IMC total 4,849 실측(2026-05-26 기록)=같은 계정 개연성 / 검수 자동화 완비 / import 2종 부재=Track B 개발 실체 / 발송 축 코드 0(표준 라인 미주입·비토 라인 주입). **베스트안 = IMC 원본 pull(레거시 DB 복사 폐기·재검수 쟁점 소멸 경로)**. 서팀장 사전 체크 12문항+방침 3 작성(§4-3) — 서팀장 의견 교환 후 재토의 예정. 강문희 질의는 그 후 잔여분만. |
| 2026-07-14 | **서팀장 8문항 회신 + 엑셀 2종(발신프로필 242·템플릿 4,336) 실측 + 강문희 구성안 PPT·QTmsg 매뉴얼 4.0 정독 → Track B·C 설계 확정**(§4-3 요지 참조). 1안(API) 채택 근거 = 알림톡 공식 경로(매핑 최상위)+기존 업체 무수정. 매핑 실체 = senderkey+코드 치환+라우팅 3기능 → upsert 5요소 payload. 원천 3분할(B_ 76% pull / bizp_·기타 24% 매핑 유지). 문서 3종 산출(자비스 지시서·강문희 제안서·Track B+C 설계). 다음 = ①강문희 제안서 발신(Harold) ②아난티 getSender 실측 ③자비스 착수. |
| 2026-07-15 | **한줄로 카카오 소스 확인(Harold 지시) + 강문희 제안서 Word 변환 + 서팀장 지시문 발신 준비.** 소스 확인 결과: 발신프로필 카톡인증 등록·승인 워크플로·검수 알림(kakao_alarm_users)·승인 감지(웹훅+5분 폴링+1h sync)·getSender 전부 기 구현 = 서팀장 질의 8건→**4건 축소**(①Bill_ID↔실업체 확정본[최우선] ②휴머스온 계정 교차 확인 ③수동 등록 컷오버 합의 ④업체 연락처+안내 패키지). **브랜드(sender_key) 열람 스코프만 미구현 확정**(alimtalk.ts:605 회사 전체 노출 정책) = Track B 유일 신규 개발. 친구톡 = 카카오 공식 폐지 확인 → 원 Q10 불요. 강문희 전달 = Word(Downloads/템플릿연동_1안_진행제안_인비토_20260715.docx) → 서팀장 검토 후 발신 경로 확정. 설계문서 §1·B-2·§5 동기 반영. |
| 2026-07-15 | **Track B 관문 통과 + import 2종 배포 + 아난티 847 pull 완료(파일럿 pull 단계 종결).** 아난티 getSender 실측 0000·@아난티·A = 같은 휴머스온 계정 확정(§4-3 관문 1). 슈퍼관리자 4 endpoint(debug 1콜·senderKey 연결·목록 probe·템플릿 pull) + CT-16 역변환(IMC snake→camel — 발송 대표링크 유실 7300 축 차단) 배포. 아난티 = 연결+847건 pull(APPROVED 827·KREJ 20·failed 0·중복 0). **잔여 = M4 실발송 1건·497vs827 집계 기준(서팀장)·M5 확대(B-3 계정·Bill_ID 선행)**. 상세 = 설계문서 §1·§6. |
| 2026-07-15 | **Track D 강문희 회신 + Harold 답신 발송(§5-⑫ 갱신).** 54·57 프로그램 준비 완료(54=java 복사·OLD 병행 / 57=golang 신규·검수 진행중 / 58=최후) — 실 IP 미포함이라 답신으로 54·57·58 실 아웃바운드 공인 IP 실측(curl ifconfig.me)+SysId 값 형식('54'/'57'/'58')+FillAmtHist 새 DB 포함 여부 확인 요청. 회신 도착 시 = IP 반영(계정 host+iptables, §7-2)→54 병행 적재 시작→운영실 테스트 3항목+§7-4 당일 grain 대조. |
| 2026-07-15 | **Track C 강문희 템플릿관리자 스펙 회신(PDF) → 관문 2 해소(Harold 확정 07-16).** 1안 확인·upsert 가능(키=(BILL_ID,외부템플릿코드) 확정 답신)·조회 전량 OK·54/58 송신측 구분(서버별 listen — 클라이언트 이원 발송)·reload 자체 해결·API키 화이트리스트·필드명 쟁점 소멸(표준 필드명 신규)·알림톡 senderkey 동봉 지원(친구톡 동일·54/58 전부). **삭제 기능 없음 확정** — 고아 행=대조 표시만·값 실수=동일 키 upsert 교정. 답신 = 삭제 불요+upsert 키+착수 시기 문의. 상세=Track B+C 설계 §1. |
| 2026-07-15 | **Track D 강문희 2차 회신 → "적용 시작 OK" 발신(§5-⑪ 해소·⑫ 갱신).** 실 IP 3종 실측 회신 = 전부 서버명 옥텟 일치(57=docker 안이나 curl ifconfig.me 동일) → 계정·방화벽 기 반영 상태 확인(유닛 파일 실측 — ACCEPT 4+ESTABLISHED 설계대로) = 추가 작업 0. SysId = '54'/'57'/'58' 문자 그대로(RSRM_SalesStts). 새 프로세스 = OLD salesd와 분리·접속정보 global 설정 신서버 단독(구서버 원천 차단) = FillAmtHist 수집 포함. 병행 기간 실충전은 기존 143 경로 담당(우리 DB에 새 충전 행 없음 = 이중 반영 위험 0). **다음 = 54 기동 연락 → 당일 RSRM_SalesStts SysId='54' 유입 + 143 동일 grain 합계 대조 → 운영실 테스트 3항목 → 57(golang)→58.** |
| 2026-07-15 | **★Track D 54 병행 적재 개시 — 당일 실측 통과.** "적용 시작 OK" 발신 당일 강문희 54 기동 → 우리 수신 DB 실측: 당일(20260715) 288행 유입·1분 push 실시간(InsTm 14:46)·SysId='54' 문자 그대로·CustId B-prefix 일치·**한글 StoreId 7행 무결(깨짐 0 — clibillid/storeid 주의 축 통과)**. 로컬 조회 주의 = sales 계정은 IP host 제한이라 컨테이너 안에서는 root 사용(`docker exec -it pay-ingest-db mysql -uroot -p`). 잔여 = 익일 143 마감 대조(§7-4) → 통과 시 강문희 공유 + 57 진행. |
| 2026-07-16 | **★레거시 143 디스크 100% 풀 사고 대응 + 서버 전수 점검·정리(§5-①②·§3-E 갱신).** 증상=서수란 팀장 event-admin(템플릿관리자) 로그인 "서버와의 통신 실패" → 근본 `/dev/sda1` 100%(백엔드 8081 전 요청 500·로그 09:42 줄 중간 멈춤이 단서). 정체=구 invito 웹발송(invitobiz=tomcat6_b 8070·usom 앱·Oracle orcl)의 8개월치 발송 Agent 로그 184G. **진단=발송 0(7월 SMSQ_SEND 실카운트 0)·결제 0·42계정 조회전용**(정본 Oracle SMSQ_SEND·발송내역 조회만). 아키텍처 확정(§5-①)=kkotemp-agent(event-admin 검수알림·MySQL·/home/event/libs·유지) vs 웹발송 Agent 6개(Oracle·/home/invitoMsg/invitoAgent_*·정지). 조치=Oracle/nginx 로그 truncate 15G + Agent 6개 정지(정확 PID·kkotemp 미포함·blanket pkill 금지) + Agent 로그 183G 삭제 + 2017 설치본·덤프·옛 웹로그 55G 삭제 → **100%→38%(≈253G 회수)** + /www/usom/web-log 자동삭제 크론(옛 usomLog.log.* 3일). 부산물=tomcat6(8080 빈 유휴)·8090 /python(死). 실사양=Xeon E5-2620 v4(2016)·32GB·447GB 단일디스크·CentOS7.3(EOL)·물리·가동 1625일 → **폐기 명분 재확인(연 618만원·EOL 보안)·리부팅 금지**. 잔여=폐기 전 42계정 공지+발송내역/정산 데이터 확보(§6). 상세=memory/project_2026_0716_legacy_usom_diskfull_agent_stop.md |
| 2026-07-23 | **★Track D Phase 2 부분 — 에이전트 발송통계 유형별 재개발 + 전환갭 백필(§9 신설).** ①웹/에이전트/테스트 탭 분리 + **유형별(SMS/LMS/MMS/카카오알림톡, MsgType S/L/M/K) 재개발 3화면**(고객사 StatsTab·슈퍼 AdminDashboard·사용자 ResultsModal). pay-stats CT를 `GROUP BY … MsgType`으로 재작성(회사=기간×유형·슈퍼=기간×회사×유형·경량 byType). 매핑=agent_send_id=CustId 실측 확정. 코드완료·tsc0(BE·FE)·vitest900·Codex1R(2정정: results 에이전트=회사전체 집계라 관리자 전용 격리[company_user·filter_user_id 제외]·StatsTab sent=0 성공률 `-%` 조건부). 웹/테스트 축 무접촉(회귀 0). env=`PAY_STATS_DB_USER=paystats`(설정 확인). ②**★58 성공 484만 미적재 사고**: 게이트웨이 58 성공 1,957만(SMS 320만+LMS 1,529만+MMS 47만+카카오 62만) vs 우리 원장 1,474만 → **전환갭(초기 dump 07-07 → 라이브 적재 58=07-14·54=07-10) 사이 58=07-08~13(6일)·54=07-08~09(2일)이 62에 통째 누락**(계정마다 ~25% 부족=결손일/전체일). **강문희 불필요 — 143(옛 수신 DB `sales`@3388)에 그 기간 남아있어 143→62 자체 백필**(mysqldump `CustId LIKE 'B/D%'` 결손일 → 62 로드 + SysId 백필). 백필 후 유형합 게이트웨이 일치. **★교훈: 적재 목적지 전환은 dump 시점~라이브 시작 사이 날짜가 누락된다(§7-1이 예고). 옛 수신 DB가 그 기간 원본을 보유하므로 거기서 백필. 게이트웨이 "58 성공"=유형별 성공 합산.** 잔여=배포(BE·FE build:safe+pm2)·57 갭 확인·강문희 전환갭 FYI·슈퍼 정산합산(billing 별도 Phase). 상세=memory/project_2026_0723_pay_agent_stats_tabs.md |
