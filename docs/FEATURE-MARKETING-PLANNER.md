# 마케팅 플래너 (AX 월간 대행) — 기능 상설 SoT

> **호출어 = "마케팅 플래너".** 이 문서가 정체성·불변 원칙·구조·요금·단계·이력을 **전부 소유한다.**
> STATUS는 경로만 갖는다. 여기 있는 것을 다른 문서에 복사하지 않는다.
>
> **역할 분담** — 이 문서 = *지금 무엇이 있고, 어길 수 없는 것이 무엇이고, 다음이 무엇인가*(상설·현재형).
> [2026-08-12 설계서](2026-08-12-ax-marketing-planner-design.md) = *왜 그렇게 정했나 · 실측 근거 · 미확인 원장 · 뒤집힌 판단*(시점·근거형).
> 근거를 다시 파기 전에 설계서를 먼저 연다 — 이미 실측으로 닫힌 질문이 많다.

---

## §1 착수 전 필독 순서

1. **§2 정체성** — 세 번째 시계가 무엇인지. 여기를 건너뛰면 여정·자동마케팅과 축이 섞인다.
2. **§3 불변 원칙** — 어길 수 없는 것.
3. **§4 구조** — 파일·테이블·엔드포인트 실물.
4. **§6 단계** — 지금 어디까지 왔고 다음이 무엇인가.
5. 요금을 건드리면 **§5**, 제작 품질을 건드리면 [설계서 §4](2026-08-12-ax-marketing-planner-design.md).

---

## §2 정체성 — 세 번째 시계

**담당자의 한 달을 결재로 바꾸는 대행 서비스.** 월간 캘린더에 자기 행사를 기입하고 채널을 체크하면,
월간 브리핑을 승인하는 것만으로 제작·발송·결과 브리핑까지 무인으로 돈다.

| 축 | 시계 | 트리거 |
|---|---|---|
| 여정 | 사건 | "방금 했다" |
| 자동마케팅 | 상태 | "지금 상태다" |
| **마케팅 플래너** | **달력** | **"그날이 왔다"** |

**축은 셋, 실행 엔진은 하나다** — 플래너는 자동마케팅 실행 축을 재사용한다(§4-4). 새 발송 엔진을 만들지 않는다.

**AX 판정 조건 둘** — ①브리핑이 결재 서류 수준(소재 실물 프리뷰 + 타겟 수 실쿼리 + 예상 크레딧·발송비)
②승인 후 무인 완주(제작→발송→결과 브리핑). 하나가 빠지면 AX가 아니라 예약 발송기다.

---

## §3 불변 원칙 (어길 수 없는 것)

### 3-1. 미승인 = 미발송 = 미차감 (fail-closed)
어떤 경로도 이 게이트를 우회하지 않는다. 승인 없이는 제작도 없다 — 따라서 원가도 차감도 0이다.

### 3-2. 문안은 당일, 소재는 승인 후
문자 문안은 **행사 당일 발송 전** 생성한다(T스팸필터 등 통신사 필터 때문에 사전 생성이 무의미).
그래서 **승인 대상은 계획(채널×시점×형식)이지 문안이 아니다.** 소재류(이메일·DM·인앱)만 승인 후 제작한다.
⛔ 사전 생성 문안을 다시 들여오지 마라 — [설계서 §11](2026-08-12-ax-marketing-planner-design.md)에서 접은 안이다.

### 3-3. 혜택은 고객사 기입 verbatim
혜택·수치는 캘린더 기입 칸에서 고객사가 직접 쓴다. AI는 그 값을 그대로만 쓴다(AI 혜택 생성 금지 원칙).

### 3-4. 알림톡은 정보성 안내 채널이다 — 광고가 아니다
플래너에서 알림톡의 자리는 행사 홍보가 아니라 **참여 접수 확인·행사 시작 안내(신청자)·결과 안내**다.
검수 자체가 광고성 문안을 반려하므로 이건 품질이 아니라 **통과 조건**이다. 검수는 AX가 대행한다(§4-5).

### 3-5. 재료가 없으면 정직하게 잠근다
채널 가용성은 고객사 데이터가 결정한다 — 정답표 하드코딩 금지. 잠글 때는 **사유를 그 자리에서 말한다.**
**판정 실패(쿼리 오류)는 열어주지 않는다**(fail-closed) — 못 확인한 것을 가능으로 두면 발송 단계에서 터진다.

### 3-6. 타겟은 실쿼리, 예측으로 고르지 않는다
브리핑의 모든 수치는 DB 실쿼리다. 추정 수를 결재 서류에 올리는 순간 승인이 무의미해진다.
상품 "추천"도 판매 실데이터 상위 제안이지 자동 선정이 아니다.

### 3-7. 크레딧 축은 둘뿐이다
**제작물(원가 연동)** + **대행(가치 연동)**. 세 번째 축을 만들지 마라(§5).

### 3-8. 이중 진실에는 대조 워커를 함께 만든다
플래너 터치포인트 ↔ 자동마케팅 실행 원장은 두 진실이 된다. **승인됐는데 미등록 / 취소됐는데 실행 잔존**을
잡는 대조 워커는 Phase 3의 선택이 아니라 필수다(6원칙 ③).

---

## §4 구조

### 4-1. 파일별 소유

| 파일 | 소유 |
|---|---|
| `backend/utils/marketing-planner.ts` | 판정 규칙 CT — 입력 검증 · 시점 계산(순수) · 예상 크레딧 산식 · 채널 라벨 |
| `backend/utils/planner-channel-gate.ts` | 채널 5종 가용성 판정 CT (전부 기존 신호 재사용 · fail-closed) |
| **`backend/utils/planner-approval.ts`** | **결재 CT (Phase 2)** — 브리핑 조립 · 승인 게이트 · 차감 멱등키 · 결재 토큰 · 통지 문구 · 제출/승인 전이 |
| `backend/routes/marketing-planner.ts` | 엔드포인트 · 트랜잭션 · 서버측 잠금 재검증 · 503 분기 |
| `frontend/pages/MarketingPlannerPage.tsx` | 월간 캘린더 · 행사 기입 모달 · 잠금 사유 · 실시간 합계 · 결재 배너 |
| **`frontend/pages/PlannerBriefPage.tsx`** | **결재 화면 (Phase 2)** — 요약 4카드 · 행사별 채널·시점·대상 수·크레딧 · 승인 확인 모달 |
| `frontend/constants/ai-operator-modules.ts` | 타일 배열(소비처 3곳 공유 — 한 곳 수정이 전부에 반영) |
| `backend/utils/marketing-planner.test.ts` · `planner-approval.test.ts` | 계약 테스트 10건 + 17건 |

### 4-2. 테이블 (2026-08-12 운영 CREATE 완료 · 각 12컬럼 실측)

| 테이블 | 역할 | 핵심 |
|---|---|---|
| `planner_events` | 행사 원장 | `plan_month`(YYYY-MM) · 기간 · `benefit_text`(고객사 기입) · `products` jsonb · `status` 9종 |
| `planner_touchpoints` | 터치포인트(행사×채널×시점) | `channel` 5종 · `timing_rule` jsonb · `est_credits` · `status` 8종 · `lock_reason` |
| **`planner_monthly_approvals`** (2026-08-13 신설 · **배포 후 CREATE**) | 월간 승인 원장 | `UNIQUE(company_id, plan_month)` · `status` 4종(pending·approving·approved·cancelled) · `agency_credits` · `est_snapshot` jsonb · **`event_ids` uuid[](제출 스냅샷 = 승인 대상)** · **`approve_attempt` uuid(시도 소유권)** · `deduct_idempotency_key` · `token`·`token_expires_at` |

**승인 순서 = 게이트 → 선점(서류 동봉) → 지문 재확인 → 차감 → 확정.**
①**선점이 서류를 함께 돌려준다**(`event_ids`·`agency_credits`·`plan_hash`). 승인은 그 값으로만 한다 —
선점 전에 읽은 값으로 확정하면 그 사이 재제출된 다른 서류를 집고도 옛 계획을 승인하게 된다.
②선점 뒤 **지문을 다시 맞춘다**. 어긋나면 선점을 풀고 재결재를 요구한다.
③차감 원장(`ai_credit_transactions`)에 그 달 멱등키가 있으면(=복구) **잔액·월 경계를 다시 묻지 않는다** —
대행료가 빠져 잔액이 줄었는데 같은 게이트를 다시 요구하면 돈만 빠진 달이 영영 안 닫힌다.
제작비 부족은 **제작 시점의 보류·통지**가 처리한다(§5 정책 — 크레딧 예약 축을 새로 만들지 않는다).
④확정은 `status='approving' AND approve_attempt=내 시도`에만 건다. 차감 전 끊긴 시도는 **10분 lease**로 회수된다.

**status CHECK 실측(2026-08-12 운영)** — `planner_events` = draft·briefed·approved·producing·scheduled·done·reported·cancelled·re_brief /
`planner_touchpoints` = planned·locked·producing·ready·scheduled·sent·skipped·hold_credit. **Phase 2 전이는 이 안에서만 움직여 DDL 추가가 없다.**

⛔ **발송 예정일은 저장하지 않는다** — 조회 시 `computeTouchpointDate`로 계산한다. 저장하면 행사 기간을 고쳤을 때
두 값이 갈린다(이중 진실). 컬럼 상세 = [SCHEMA.md](../status/SCHEMA.md) 61·62행.

### 4-3. 엔드포인트 (`/api/marketing-planner`, 고객사 인증)

| 경로 | 하는 일 |
|---|---|
| `GET /availability` | 채널 5종 가용성 + 사유 + 예상 제작 크레딧 (기입 모달 체크박스 소스) |
| `GET /events?month=YYYY-MM` | 그 달 행사 + 터치포인트(발송 예정일 계산 포함) + 그 달 결재 상태(배너 축) |
| `POST /events` · `PUT /events/:id` | 기입·수정. **행사+터치포인트 단일 트랜잭션** · 잠긴 채널 서버 재검증 |
| `DELETE /events/:id` | `draft`만 삭제 |
| **`GET /brief?month=YYYY-MM`** | 월간 브리핑 = 결재 서류(행사·터치포인트·대상 수 실쿼리·크레딧 합계·잔액·게이트·승인 상태) |
| **`POST /brief/:month/submit`** | 결재 올리기 — 승인 원장 `pending` + 행사 `briefed` + 결재 문자 1통(단일 트랜잭션 뒤 통지) |
| **`POST /brief/:month/approve`** | 월간 승인 — 게이트 → 선점 → 차감 → 확정 |
| **`GET /approval/:token`** | 결재 링크 착지(**인증 없음** — 어느 달인지만 정하고 결재 화면으로 보낸다. 승인은 로그인 사용자만) |

수정 가능 상태 = `draft`·`briefed`·`re_brief`. **승인된 달에 나중에 담은 행사**는 `submit`을 다시 불러 계약에 넣는다 —
승인 화면이 "계획이 바뀌었다"를 띄우고, 다시 올린 뒤 승인하면 **대행료 추가 차감 없이**(멱등키 동일) 그 행사가 들어간다.

### 4-4. 채널 가용성 판정 — 전부 기존 신호 재사용

| 채널 | 판정 근거 |
|---|---|
| 메시징 | `callback_numbers` 보유 |
| 알림톡 | `kakao_sender_profiles` 보유 |
| 이메일 | `customers.email` 보유 고객 존재 |
| 모바일 DM | 요금제 `mobile_dm_enabled` (`plan-guard` — plan_code 하드코딩 금지) |
| 인앱 | `getInAppDisplayEligibility` (SDK 설치 판정 CT 그대로) |

새 판정 로직을 만들지 않는다. 채널이 늘면 그 채널의 기존 게이트를 찾아 잇는다.

### 4-5. 알림톡 검수 대행 (Phase 3)

담당자에게 "등록하러 가라"는 가드는 대행이 아니다. 흐름 = 정보성 문안 생성 → 검수 자동 제출 →
상태 추적 → 반려 시 재생성·재제출 1회 + 사유 통지 → APR 후 발송 예약 편입.
**API 전 구간이 이미 존재한다** — `createAlimtalkTemplate` · `requestInspection` · `getRecentlyModifiedAlimtalkTemplates`
+ 30분 동기화 워커 · `getAlimtalkCommentFile`(반려 사유). 신규 개발은 오케스트레이션뿐.

**리드타임 = 5영업일**(검수 실소요 2~3일 + 반려·재검수 1회 여유). 담당자를 막는 선이 아니라
**AX가 검수를 맞출 수 있는가**의 판정선이다. 못 맞추면 재사용 APR 템플릿 폴백 → 없으면 그 터치포인트만 자동 제외.

---

## §5 요금

| 축 | 기준 | 값 |
|---|---|---|
| 제작물 | AI 호출 **원가** | 이메일 완성 50 · DM 생성5+발행100 · 인앱 게시 100 · 스튜디오 이미지 2/장 (단가 진실 = `getCreditCost`) |
| **대행** | 대체하는 **인건비·리테이너** | **월간 승인 = 1000** (source `planner-monthly-agency` — 단가는 `CREDIT_COST_MAP`이 소유) |

⛔ 그 source를 `OPERATION_SOURCES`에 넣지 않는다 — 거기 들어가면 잔액이 음수로 내려가는 축이 되어
"마이너스·자동충전 금지" 확정이 코드에서 무너진다.

- **차감 시점** — 대행 1000은 월간 승인 시(멱등키 `planner:{회사}:{YYYY-MM}`), 제작물은 각 제작 시점.
- **승인 게이트** — 잔액 ≥ (1000 + 그 달 예상 제작 합계).
- **월 중 소진** — 그 터치포인트만 보류 + **발생 즉시 통지**(조용한 증발 금지) + 충전 후 재개 1클릭. 마이너스·자동충전 금지.
- **취소 환불** — 그 달 제작·실행 0건이면 전액, 1건이라도 있으면 없음. 일할 계산 없음(규칙이 단순해야 분쟁이 없다).
- **자연 게이트** — 1000이 TRIAL·BASIC(750)을 걸러내 **PRO(100만/2,400)부터 트라이 가능**. 인위 게이트 없음.
  ⚠ 자격 플래그 비대칭 — 자동마케팅 자율 발송 자격(`advanced_access_enabled`)은 PRO가 false다.
  **플래너 발송은 사람이 승인한 발송이라 그 플래그를 요구하지 않는다**(별도 진입 경로 · 영향표 선행).

산정 근거·시나리오표 = [설계서 §3](2026-08-12-ax-marketing-planner-design.md).

---

## §6 단계 (착수 원장)

| Phase | 내용 | 상태 |
|---|---|---|
| 0 선행 확인 | 요금제 실값 · 상품 데이터 축 · 검수 소요일 · 소진 정책 · 메뉴명 · 검수 API 범위 | **★2026-08-12 전량 종결** |
| **1 플래너 코어** | 행사 CRUD 캘린더 · 터치포인트 · 가용성 잠금 · 예상 크레딧 · 메뉴 개편 | **★2026-08-12 배포완료 + DDL 실행완료** · 화면 실측 3건 대기 |
| **2 브리핑·결재** | 월간 브리핑 화면 · 결재 링크 토큰 · 1000크레딧 차감(멱등) | **★2026-08-13 코드완료** — 배포 후 DDL 1건(§4-2) · 실측 대기 |
| 3 실행 배선 | 자동마케팅 엔진 훅(영향표 선행) · 소재 대행 제작 · 알림톡 검수 대행 · **대조 워커** | 미착수 |
| 4 깔때기·브리핑 | 참여 동의 체인 · 결과 브리핑(자기 개선 루프 Phase 3와 겸용) | 미착수 |

**Phase 2·3은 크레딧 차감·발송 실행·결재 토큰이 걸리는 쓰기 경로 → Codex 대상**(0811 확정 기준).
Phase 1은 읽기·CRUD뿐이라 대상이 아니었다.

---

## §7 이력 색인

| 시점 | 내용 | 근거 |
|---|---|---|
| 2026-08-11 | 발단 — ERP 도입 논의에서 출발해 "월간 마케팅 대행"으로 수렴. 백오피스는 만들지 않고 프론트오피스만 | 설계서 §0 |
| 2026-08-12 | **설계서 완성** — 확정 원장 12건 · 요금 두 축 · 품질 전제 · 구조 · 단계 · 미확인 원장 | [설계서](2026-08-12-ax-marketing-planner-design.md) |
| 2026-08-12 | **Phase 0 실측 전량 종결** — 요금제 실값(PRO=100만/2,400) · 상품 데이터 축 둘 · 검수 5영업일 · 소진 정책 · 메뉴명 · 검수 API 전 구간 실존 | 설계서 §9 |
| **2026-08-12** | **Phase 1 배포완료** — CT 2본 + 라우트 5 + 화면 1 + 타일 개편 3건 + 계약 테스트 10건. **DDL 2건 운영 실행완료**(12컬럼×2 실측). BE·FE tsc 0 · vitest 155파일 2,304건 | 이 문서 §4 |
| **2026-08-13** | **Phase 2 코드완료 (Codex 적대 검토 1R = critical 0 · high 5 전량 정정)** — 다섯 중 셋이 한 뿌리였다: **승인 시도에 주인과 범위가 없었다.** 상태 문자열 하나로 소유권을 흉내 내 동시 요청이 서로를 덮어썼고, 차감 뒤 확정이 끊기면 잔액이 줄어 재승인 게이트에 막혀 돈만 빠진 달이 남았고, 승인 대상이 "그 달 전체"라 서류에 없던 행사가 함께 승인됐다. 구조로 닫았다 = **attempt 소유권 + 제출 스냅샷(`event_ids`) + 차감 원장 우선 복구 경로**(추가 편입 전용 분기는 삭제 — 재제출 하나로 합쳐졌다). 나머지 둘 = 대상 수 실패를 `deferred`와 구분해 fail-closed(`error`) · 터치포인트 조회·삭제에 회사 조건 직접 부여. **2R = high 5 · medium 1** — 셋이 또 같은 뿌리(승인이 서류에 결박되지 않는다)라 다시 구조로 닫았다: **선점이 서류를 함께 돌려주고**(`event_ids`·`agency_credits`·`plan_hash`) 승인은 그 값으로만 한다 · **`plan_hash`로 내용까지 고정**(ID만 같고 혜택·기간·채널이 바뀐 계획이 승인되던 구멍) · **10분 lease**로 차감 전 끊긴 시도 회수. 그 밖 = 크레딧 원장 조회에 회사·유형·source 조건. **기각 2건**(근거 = 이 표 아래 §8) | 결재 CT 1본 + 엔드포인트 4 + 결재 화면 1 + 캘린더 배너 + 계약 테스트 23건. BE·FE tsc 0 · vitest 156파일 2,321건. **DDL 1건은 배포 후.** 실측이 뒤집은 것 셋 = ①결재 통지는 **문자 + 화면 두 경로**(알림톡 통지 템플릿 미등록 · 플랫폼 이메일은 자체 메일 서버가 없어 stub) ②토큰은 **DB 저장 랜덤**(무상태 서명은 1회성·폐기를 못 만든다) ③취소·환불은 **Phase 3**(고객이 자기 차감을 되돌리는 크레딧 축이 코드에 없다 — 지금 되돌림은 슈퍼관리자 수동 조정) | 이 문서 §4 · [설계서 §5-3](2026-08-12-ax-marketing-planner-design.md) |

---

## §8 남은 것

- **화면 실측 3건**(Phase 1) — ①타일 진입 → 행사 기입 → 캘린더 바 표시 ②잠긴 채널 사유 표시 ③채널 체크에 따른 합계 변동
- **DDL 1건**(Phase 2) — `planner_monthly_approvals` CREATE. **배포 후 실행**, 그 전까지 브리핑은 503으로 막힌다
- **실측 4건**(Phase 2) — ①결재 올리기 → 결재 문자 수신 → 링크로 브리핑 착지 ②승인 → 대행 1000 차감이 크레딧 이력에 **1건**(더블클릭해도 1건) ③승인 뒤 행사 추가 → "계획이 바뀌었습니다" 안내 → 다시 올려 승인 → **추가 차감 0** ④만료된 링크 → 캘린더로 착지 + 만료 안내
- **Phase 3 착수** — 실행 배선(자동마케팅 훅 영향표 · 소재 대행 제작 · 알림톡 검수 대행 · 대조 워커) + **월 중 취소·환불**(크레딧 환불 축 신설이 선행 — 지금은 슈퍼관리자 수동 조정뿐)
- **알림톡·이메일 결재 통지** — 알림톡 통지 템플릿 등록 후 문자와 병행(자동마케팅 `notifyOperatorAdmins` TODO seam과 같은 축) · 이메일은 플랫폼 발송 경로가 생겨야 한다
- **Codex 2R 기각 2건(등재만 — 다시 꺼낼 때 이 근거부터 본다)**
  ①**크레딧 예약(reserve)으로 게이트↔차감 TOCTOU를 없애라** → 기각. 게이트 통과 뒤 다른 작업이 잔액을 쓰면 제작 여력이 준다는 지적은 사실이지만, 그 상황의 처리는 **제작 시점 보류 + 즉시 통지**로 이미 확정돼 있다(§5). 예약 축을 만들면 "제작물·대행" 두 축 원칙(§3-7)에 세 번째가 생긴다. 단 지적의 후반부(복구가 변동 잔액으로 게이트를 다시 본다)는 수용해 **복구 경로의 게이트를 제거**했다.
  ②**토큰 착지 라우트가 마이그레이션 오류를 302로 숨긴다 → 503으로 답하라**(medium) → 기각. 그 라우트는 **문자 링크를 눌러 브라우저로 도착한 담당자**의 착지점이라 JSON 503을 보여줄 자리가 아니다. 대신 로그에 `DB_MIGRATION_PENDING` 표식을 남겨 모니터링이 구분하게 했다.
- 참여 동의의 법적 문구(광고 수신동의와 구분) — 약관 검토
- 기존 마케팅 캘린더(1년 시즌 AI 설계) **전면 흡수** — 지금은 [AI 추천으로 채우기] 진입 연결만. 등재분 이관 방식 미설계
- `cdp_events` 참여 유형 추가 시 소비처 영향표(Phase 4 전)

---

## §9 뒤집힌 판단 (다시 꺼내지 않기)

- **D-1 PDF 결재** → 스케줄러 화면 결재로 대체.
- **브리핑에 문자 문안 전문 포함** → 폐기(당일 생성이라 결재 대상이 아니다).
- **AX 조율은 원가 0이므로 무과금** → 폐기. AX는 대행 제작 주체다.
- **요금제 슬롯(월 행사 수) 차등** → 철회. 크레딧 경제가 자연 게이트.
- **알림톡 리드타임 가드로 담당자를 막기** → 폐기. 검수까지 AX가 대행한다.
- **"상품 데이터가 없다"** → 오판이었다. 거래·행동 축은 전 provider 수집 중이고, 카탈로그 축만 provider별로 다르다.

---

## §10 관련 문서

- 시점 설계 근거·미확인 원장 = [2026-08-12 설계서](2026-08-12-ax-marketing-planner-design.md)
- 실행 엔진 = [자동마케팅](FEATURE-AUTOMARKETING.md) · 성과 환류 = [자기 개선 루프](FEATURE-AI-LEARNING.md)
- 소재 제작 = [이미지 스튜디오](FEATURE-IMAGE-STUDIO.md)(실데이터 조립 공식의 원형) · 알림톡 검수 = [템플릿](FEATURE-ALIMTALK-TEMPLATE.md)
- 데이터 = [자사몰 연동](FEATURE-CDP-INTEGRATION.md) · 스키마 = [SCHEMA.md](../status/SCHEMA.md) 61·62행
- 화면 기준 = `status/lessons/LESSONS_FRONTEND.md` 디자인 최소 기준
