# 정산 — 기능 상설 SoT

> **호출어 = "정산"**.
> 이 문서가 **정산의 구조·불변 원칙·이력**을 소유한다. STATUS.md는 상태와 잔여만 갖고 여기를 참조한다.
> **시점별 설계 근거는 설계서 6종이 계속 소유한다**(§9) — 여기엔 그 결론과 링크만 둔다. 정보 하나는 소유 문서 하나.
>
> 등재 = `status/SOT-INDEX.md` §0 · 원칙 = memory `feedback_feature_doc_owns_history`

---

## §1 착수 전 필독 순서

1. **§2 불변 원칙** — 어기면 **틀린 금액이 고객에게 나가거나 국세청에 두 번 나간다**
2. **§3 구조** — 발행 코어 하나를 누가 어떻게 부르는가
3. 작업 축에 해당하는 §4~§7
4. 그다음 §8 이력에서 같은 판단을 언제 어떻게 내렸는지

⚠ 이 기능은 **돈과 국세청 문서**를 다룬다. 코드 변경은 `codex_review_after_code_change` 대상이고, DB 컬럼은 `information_schema` 검증이 선행이다.

---

## §2 불변 원칙

### 2-1. 차단 기준은 "틀린 금액이 나가는가" 하나다

차단을 늘리는 방향이 항상 안전한 게 아니다. 금액에 영향 없는 것(음수 대기·계정 미상·요금제 이력 없던 기간)으로 발행을 막으면 **마감일에 아무도 청구서를 못 뽑는다**. 0726에 그렇게 막았다가 정상 발행이 죽어 되돌렸다(Codex 5차 #3·6차 ②가 그 산물이었다).

### 2-2. 절사는 **항목줄에서 1회**

단가가 소수(22.80·7.20)라 `수량 × 단가`가 소수로 떨어진다. 총액에서 한 번만 절사하면 안 된다 — 청구서 1페이지 항목표와 2페이지 상세에 각각 금액 열과 합계가 있어 **고객이 어느 쪽을 세로로 더해도 공급가액과 맞아야** 한다.
반올림이 아니라 **절사**인 이유는 고객에게 불리한 방향으로 1원도 넘기지 않기 위해서다. `Math.floor` 직접 호출 금지(부동소수점으로 1원이 깎인다) — `utils/money.ts floorWon`.
계정별 발행이 합산과 1원 다른 것은 **의도**다.

### 2-3. 단가는 부가세 **제외** 기준으로 저장한다

`companies.unit_price_basis` DEFAULT = `'vat_excluded'`(0727 실측). 단가가 부가세 포함으로 입력돼 있는데 코드가 그것을 공급가액으로 놓고 VAT를 또 더하면 **이중과세**다(0726 §9-1에서 4곳 + 표기 2곳 발견). 변환은 `utils/unit-price.ts` CT 하나만 지난다.

### 2-4. 한 회사·한 기간에는 **발행 하나**

중복 검사 축은 **회사 + 기간**이다. `COALESCE(user_id, nil)`로 비교하면 회사 정산과 계정별 정산이 서로를 못 막아, 같은 달을 두 번 청구하거나(웹 발송분 이중) 반대로 에이전트·테스트·스팸·크레딧이 통째로 빠진다.
판정 문은 **하나다** — `readBillingPeriodConflicts`(겹치는 발행 + 수동 정산완료). 발행 사전검사·잠금 안 재검사·정액 발행·미리보기가 전부 이것을 쓴다(★2026-08-04 통합. 그전에는 3벌로 복제돼 있었고 미리보기에는 아예 없어 "미리보기 통과 → 발행 실패"가 났다).

### 2-5. 발행 코어는 `utils/billing-issue.ts` **하나뿐**

단건 발행(`POST /generate`)과 일괄발급 배치가 같은 함수를 부른다. 배치가 로직을 복사하면 **복사본은 반드시 갈라진다.** 경로별 옵션을 넣어 코어를 가르는 방향은 불수용(0804 Codex high 판정 근거).

### 2-6. 통지 추적행은 **메일보다 먼저** 만든다

발송이 성공한 뒤에야 추적행을 만들면 "이 장이 통지됐는가"의 진실이 메일 뒤에만 생겨, 중간에 무엇이 어긋나도 상태가 남지 않는다. 부분 발송·중복 발송·고아 PDF·커넥션 고갈이 전부 그 하나에서 나온 가지였다 — 덧댄 장치(preflight·advisory lock·발송 직전 재확인·렌더본 추적)를 아무리 붙여도 다음 구멍이 계속 났다.
**행을 먼저 만들자 덧댄 장치가 전부 삭제됐다.** 코드가 늘지 않고 줄었다.

### 2-7. 컨펌 행은 `manual_wait`으로 만들고 **메일이 나간 뒤에만** 승격

`pending`으로 먼저 만들면 미전달 건의 세금계산서가 자동 발행된다.

### 2-8. 세금계산서가 `ready` 이상이면 **재발행 금지**

지우고 다시 만들면 국세청에 같은 건이 두 장 나간다. 잘못된 금액은 삭제·재발행이 아니라 **수정세금계산서**로 정정한다.
그래서 이 차단은 **재발행만이 아니라 모든 삭제**에 걸린다(★2026-08-05 [B-0805-2](../status/BUGS.md) — 그전에는 재발행 분기 안에만 있어 사유를 적은 일반 삭제가 통과했고, 계산서가 정산과 끊긴 채 남았다).
삭제에 사유를 받는 규칙("틀린 금액이 나갔으면 지우고 다시 보낸다")은 **계산서가 나가기 전**의 이야기다 — 그 뒤에는 지울 수 있는 장이 아니다.

### 2-9. 세금계산서 발행은 **대표 1명 불변**

참조 수신자는 issued 트랜잭션의 pending 기록 + 락 밖 재전송 패스로 처리한다(`taxbill_email_resends`). 인라인 재전송·웹훅 확정자 복원 금지 — 웹훅 304는 관측·ready 재큐잉만, `markFailed`는 CAS.

### 2-10. 수신자 원장은 `billing_recipients` **하나**

그전엔 뱃지가 `billing_contacts`를, 개별 메일이 `companies.contact_email`을 봐서 **같은 회사인데 일괄발급과 개별 재발송의 수신자가 달랐다.** 발송 모달의 수신자 칸을 **비워야** 참조가 함께 간다.

### 2-11. 080·부가서비스는 **원장 상태가 곧 청구 상태**

활성 = 파생 / 비활성 = 0줄 / 매핑 없음 = 발행 차단. **계약값·귀속을 항목 행에 복사하지 않는다.** 금액은 **공급가 저장**(VAT는 청구서가 파생). 반영·취소·발행은 **같은 회사 잠금**을 지나고, 판독은 서명·재검산을 통과한 것만 받는다.

### 2-12. 과거분 소급 발행 금지

5·6월은 수기 청구가 끝나 시스템 재발행 시 이중청구다(5월 후불 204만건·6월 20.6만건 규모). 레거시 id는 반드시 `periodCampaignIds`(기간조건) 축으로 — `eventIds`로 보내면 기존 정상분이 발송 월과 무관하게 전량 계상된다.

### 2-12-A. 회사 단위 직렬화는 **CT 하나**가 소유한다 — 잠금은 두 겹

정산 쓰기(발행·최소과금·수동완료·080 반영·080 취소·매핑 이동)는 전부 `utils/billing-lock.ts`를 지난다.
그전에는 같은 잠금이 **7벌로 복제**돼 있었고, 발행에만 회사 행 잠금을 더했더니 나머지가 옛 축에 남아
**"발행은 막았는데 수동완료는 통과"**가 성립했다(★2026-08-05 · [B-0805-1](../status/BUGS.md)).
- ① `advisory(company, 'billing')` — 옛 키 그대로. 키를 정규화하면 **배포 순간** 옛 코드가 든 잠금과 갈려 그 창에 진짜 경합이 생긴다.
- ② 회사 행 `FOR NO KEY UPDATE` — `$1::uuid` 캐스트가 표기를 정규화하고, 요금제 변경 경로(`UPDATE companies SET plan_id`)와 충돌해 **발행과 요금제 변경이 비로소 직렬화된다.** `FOR UPDATE`가 아닌 이유 = 자식 INSERT의 `KEY SHARE`까지 막아 집계 시간이 무관한 쓰기로 전파된다.
- 다건은 **정렬 고정**(교착 방지)인데 그 **정렬은 DB가 만든다** — `JOIN companies ON c.id = raw::uuid ORDER BY c.id`. JS에서 정규화해 정렬하면 표기가 하나 늘 때마다 순서가 다시 뒤집힌다(대소문자 → 중괄호 → 하이픈 생략으로 같은 부류가 세 번 났다). **PG의 uuid 파서를 손으로 흉내내지 않는다.**
- 인라인 복사 = 8번째 복제본.

### 2-13. 직접선택(`manual_wait`) 발급은 **컨펌 뒤에만** 큐에 오른다

`ready`는 팝빌 발행 큐다. 작성일자를 지정하는 순간 5분 워커가 국세청 문서를 만든다.
자동 정책은 컨펌 또는 기한 도래를 지나는데 **직접선택만 관문이 없어**, 날짜를 넣으면 업체 확인 없이 발행됐다
(2026-08-05 실측 — 한국고용노동교육원 08-03, 컨펌 기록 없이 발급 완료).
막기만 하면 부서가 발행일자를 통보하는 회사(시세이도류)가 영영 발행 불가가 되므로,
**관리자가 근거를 적고 컨펌을 대신 기록하는 창구**(`admin-confirm`)를 같은 폭으로 함께 연다 — 차단 기준은 §2-1 하나다.
대리 기록은 `manual_wait` 건에만 열린다. 자동 정책 건까지 열면 기한 전 발행을 사람이 앞당기는 길이 된다.

### 2-13-A. **발행 환경은 행에 적는다** — 나중에 추측하지 않는다

2026-08-05 운영 전환 전에 발행된 12장이 **국세청에 안 나갔는데** 장부는 `issued`, 화면은 `발행 완료`였다(20,066,393원).
어느 환경으로 나갔는지가 어디에도 없어 **승인번호 모양과 시각으로 추측**해야 했다. 그래서 `taxbill_issues.is_test`에
발행 시점의 `IsTest`를 그대로 적는다. 되돌리기(`reissue-production`)의 판정 축은 그 표식 하나다 —
승인번호 모양으로 가르는 판정은 팝빌 규칙이 바뀌면 조용히 틀린다.
테스트베드 문서는 운영에서 **찾을 수 없다**(완전 분리 환경) — 재발송·수정발행은 잠그고 뱃지로 드러낸다.
같은 문서번호로 운영에 다시 태우는 것은 중복이 아니다(운영에 그 번호가 없다). 같은 장에 운영 발행 행이 있으면 거부한다.

⛔ **`is_test`는 발행 전에는 목표, 발행 후에는 결과다.** 되돌릴 때 `is_test=false`를 함께 적고, 워커 claim이
`AND is_test = 현재환경`으로 **자기 환경과 같은 행만** 집는다. 요청 시점에 환경을 확인하는 것만으로는
enqueue와 소비 사이(재기동·설정 변경)가 열려 있어, 되돌려 둔 건이 그 사이 테스트로 나갈 수 있었다 —
그 구간이 뿌리다(Codex가 같은 지적을 두 번 했다). 환경이 어긋나면 그 행은 집히지 않고 `발급 대기`에 남는다.
NULL 관용(`COALESCE`)은 두지 않는다 — 표식 없는 행을 양쪽 환경에 열어 격리를 되돌린다.
되돌리기는 **`kind='original'`만**이다. 수정 장은 당초 승인번호가 테스트베드 것이라 운영에 그 원본이 없다.
화면도 서버와 **같은 화이트리스트**로 판정한다(`kind === 'original'`·`is_test === false`) — 표식이 없으면
`환경 미확인`으로 드러내고 전부 잠근다. 모르는 것을 안다고 다루지 않는다.

### 2-14. 미수신 대응은 **재발행이 아니라 재발송**이다

발행이 끝난 계산서를 다시 보내는 창구가 없으면 담당자가 쓸 수 있는 것은 [수정발행]뿐이고,
그건 국세청에 문서를 **한 장 더** 만든다(§2-8과 정면으로 부딪힌다).
`resendIssuedTaxbillEmail`은 `issued` 행의 기존 문서번호로 팝빌 `sendEmail`만 부르고 **어떤 상태도 쓰지 않는다.**
거래내역서도 같다 — 발송 완료분 재발송은 `send-email`(409 재발송 확인)이고, 미발송분 복구는 컨펌 경로다. 둘은 다른 문이다.

수신자 축은 **발행 때 쓴 것과 같아야 한다** — 발행은 `billings.user_id`로 해석하고 거기서 **NULL은 "회사 단위 장"이라는 유효한 값**이다(계정 미상이 아니다). 다른 축을 `COALESCE`로 끌어오면 회사 단위로 발행한 계산서가 계정 담당자에게 간다.
정산 행이 지워진 계산서(FK SET NULL·추적행은 CASCADE로 동반 삭제)는 **물러설 축이 없으므로 자동 폴백하지 않고 주소를 직접 받는다.** 국세청 문서 통지를 추측으로 보내지 않는다.
⚠ 고아는 §2-8 차단을 모든 삭제로 넓히기 전에 생긴 것이 남아 있을 수 있다([BUGS.md](../status/BUGS.md) B-0805-2) — 그래서 이 분기는 유지한다.
실패는 **분류해서 돌려준다** — 게이트 닫힘 503 · 전 주소 실패 502 · 상태·수신자 422 · 미분류 500(`TaxbillResendError`).
전부 422로 내리면 팝빌 게이트가 닫힌 것도 SDK가 죽은 것도 "입력 오류"로 기록되어 알림·재시도 판단이 막힌다. 유일한 재발송 창구라 그 누락이 오래간다.

### 2-15. draft 삭제·재발행은 **화면에서**, psql `DELETE` 금지

`ai_credit_requests.billed` 되돌림이 API 경로에만 있다(0725에 고친 결함).

### 2-16. 문서의 행 높이는 **내용에서 만든다** — 자르지 않는다

거래내역서 표의 칸은 길이 제한이 없는 값(계정명·`발송ID / 발급명`·항목명)을 받는다. 행 높이를 고정해 두면
넘치는 값이 두 줄로 흘러 다음 행을 침범하므로, 그때마다 **자르는 쪽으로** 막게 된다 — 0731 말줄임이 그것이고
2026-08-06에 고객이 "구분값이 잘려 대조를 못 한다"고 접수했다(더화이트).
폭을 넓히는 방향으로는 닫히지 않는다. 실측(8pt 맑은고딕)으로 가장 긴 라벨이 111.1pt인데,
표 495pt 안에서 나머지 8열을 실제 필요폭까지 깎아도 구분 칸 상한이 약 105pt다. 임계값만 옮기고 다음 회사에서 재발한다.

⛔ **`lineBreak: false`는 줄바꿈을 막지 못한다.** `width`가 함께 있으면 그대로 흐른다
(0806 실측 — width 37pt에 긴 숫자를 넣으면 옵션 유무와 무관하게 `doc.y`가 21.28pt = 두 줄 이동).
0726에 금액 칸을 지킨다고 넣은 그 옵션은 **동작한 적이 없고** 값이 칸보다 짧아서 안 터졌을 뿐이다.
⇒ 어느 칸이 흐르는지 **전제하지 않는다.** 한 행의 칸을 목록으로 먼저 만들고, **재는 옵션과 그리는 옵션을 같게 해서**
전부 잰 뒤 그 최대값으로 행 높이를 만든다(`contentRowHeight` — 1페이지 항목표와 2페이지 상세표가 같은 함수를 쓴다.
최소 높이만 22/18로 다르다. 둘로 나누면 한쪽만 또 굳는다). 한 줄 행은 예전 높이 그대로라 대부분 문서의 쪽수가 안 바뀐다.

⚠ **보장의 범위는 "새 페이지의 표 영역에 들어가는 행"까지다**(Codex 4R medium). 행이 경계를 넘으면 페이지를 한 번
더할 뿐 **행 자체를 쪼개지는 않는다** — 한 행이 그 영역보다 크면 그때는 넘친다. 그 영역은 상세표 663pt(760 − 97)·
항목표 520pt다.

그 상한이 성립하려면 **줄 수가 칸 폭으로만 결정돼야 한다.** `varchar(100)`은 글자 수 제한이지 줄 수 제한이 아니라서,
값 안에 개행이 들어 있으면 64자짜리도 63줄이 되어 663pt를 넘는다(Codex 5R medium). 그래서 칸에 넣기 전에
`toSingleLine`으로 개행·제어문자를 지운다 — 발급명(`cust_name`)처럼 **우리가 형식을 통제하지 못하는 값**이 이 칸에 들어온다.
그러면 원천은 `users.name varchar(100)`·`login_id varchar(50)`·발급명이고, 100자 계정명이 구분 칸(안쪽 83pt)에서
약 114pt라 표 영역에 닿지 않는다. 컬럼을 크게 늘리면 그때는 행 분할이 선행 과제다.

### 2-17. 정산월은 **사람이 정하는 라벨**이고, 이름일 뿐이다 (★2026-08-20 · 서수란 0819 접수)

정산 한 건이 "몇 월분인가"를 시스템이 추측하지 않는다. 그전에는 `billing_year`·`billing_month`를 시작일의 역월로
자동 파생해서, 중간정산(7/16~8/15)이 "7월 정산"이라 불리는데 8월 기준으로 입력한 부가서비스가 그 이름의 장에
실렸다(접수 증상 ①②는 같은 뿌리의 두 얼굴 — 역월 정산에서는 두 추측이 늘 같은 답이라 숨어 있었다).

- **파생 소유자는 `resolveBillingLabelMonth` 하나** — 기본값 = 종료일의 역월, 허용 = 정산 기간에 걸친 역월뿐(밖이면 422),
  계산은 `'YYYY-MM-DD'` 문자열 절단만(Date 파싱의 TZ 잠복 결함 제거). 계약 고정 = `billing-label-month.test.ts` 14건.
- **★0820 재오픈 정정 — 추가 청구 항목의 귀속 축 = 청구월 = 정산월이다.** 처음 배포는 "라벨은 이름일 뿐"이라며
  항목 선택을 기간 겹침으로 뒀는데, 중간정산(7/16~8/15 "8월 정산")이 두 역월과 겹쳐 **7월분까지 쓸어 담았다**
  (서수란 시세이도 실측 — 캡쳐로 재오픈). 담당자의 운영 모델은 "N월로 입력한 항목은 N월 정산에"다.
  선택 2곳(발행 코어·최소과금)과 차단·표시 5곳(매핑 이동·반영 취소·최소과금 목록·KT 반영 2)이 전부
  `make_date(billing_year, billing_month, 1) = period_month` 한 축이다(소스 스캔 계약이 겹침 잔존 0을 고정).
  역월 정산은 겹침과 월일치가 같은 답이라 동작 무변화. 부수 개선 — 옛 겹침은 "8월 정산 발행 후 7월 KT 반영"을
  오탐 차단해 7월분을 영영 못 넣었는데, 월일치는 허용한다.
- **금액·기간 중복검사는 여전히 `billing_start`/`billing_end` 축** — 발송분 집계와 발행 하나 불변식(§2-4)은 라벨과 무관하다.
- **입력된 달의 라벨 정산이 영영 안 만들어지면 그 항목은 미소비로 남는다** — 겹침 축 시절의 "기간 비겹침 고아"가
  "라벨 부재 고아"로 이름만 바뀐 같은 성질. 처방은 §7 미소비 잔존 감시(별건)가 소유한다.
- **총 정산표의 월 필터 축 = 이 라벨**(제품 계약) — 중간정산 장은 라벨 월로 통째 귀속되고, 발송일 기준 안분이 아니다.
  장 단위 합산이라 귀속이 어느 달이든 누적 총액은 불변이다.
- **수량 조정 재발행은 지운 장의 라벨을 승계한다** — 안 하면 시작월을 골랐던 장이 재발행에서 조용히 종료월로 바뀐다.
  승계 라벨은 발행 전에 검증하고 기간 밖이면 기본값 폴백(삭제 커밋 뒤 422로 죽는 창을 두지 않는다 — Codex 1R 수용).
- **일괄발급·최소과금은 역월 기간만 받으므로**(`isWholeMonthPeriod`·월 단위 모달) 기본값이 옛 파생과 같은 값이다 — 무변화.

### 2-18. 080 고정료의 근거는 명세서가 아니라 **발행**이다 (★2026-08-21 · 서수란 접수 · Codex 6R approve)

이용료·KT 부가서비스는 명세서와 무관한 월정액인데 근거가 [반영] 행에 묶여 있어, 명세서를 반영하지 않은 달은
고정료가 통째로 빠졌다(게스코리아 8월 실측 — 매핑 활성 전 고객사 공통. 이번 달처럼 명세서에 미납분이 섞여
반영 자료로 못 쓰는 달이 실제로 존재한다). 발행 코어가 정산월마다 활성 매핑에서 근거 행(`080_base` ·
supply_amount=0)을 자동 생성·소비하고, 금액은 발행 시점 원장에서 파생한다(§2-11 그대로). **명세서의 역할은
통화료 하나로 줄었다**(`080_call`은 통화료만 파생 — 고정료를 남기면 이중 계상).
- 생성 조건 = 같은 번호·달에 `080_base/080_fee/080_svc`가 없고 **소비된 `080_call`도 없을 때만**(구 파생이
  그 장에 고정료를 이미 청구한 달의 분할 2차 재청구 차단 — Codex 1R high). 옛 fee/svc가 있는 달은 그 행이
  근거(파생 스킵 축 = `has_base_row` — 두 규칙이 짝, `send-usage-aggregation.test.ts`가 계약 고정).
- **최소과금 정액 발행은 고정료(>0) 있는 활성 매핑 회사를 거부한다**(`MIN_CHARGE_080_MAPPING_ACTIVE`) —
  자동 생성을 지나지 않는 경로라 그대로 두면 고정료 영구 누락. 통화료 축은 기존 안전핀(스냅샷 파생 거부)이
  소유하므로 조건에 넣지 않는다(0/0/통화료만 매핑의 교착 차단 — Codex 2R high).
- 수기 항목과는 독립이다 — 080 대체 수기 입력(이용료·부가서비스)은 이중청구라 금지. 통화료만 수기 허용
  (그 달 명세서 반영은 건너뛴다). 잔존 미소비 혼합 상태(소비된 call + 미소비 fee/svc)는 생성 경로가 0804에
  폐기돼 새로 생길 수 없고, 배포 시 공존 0건을 SQL로 확정했다.

### 2-19. 계산서 작성일자 — 워커는 날짜를 기다리고, 변경은 "외부 호출 0 증명" 건만 (★2026-08-21 · Codex 6R approve)

- **발행 claim에 작성일자 도래 게이트**(`issue_date <= KST 오늘` · NULL은 집어서 markFailed로 드러낸다) —
  자동 정책(익월 1일)은 언제나 미래 작성일자를 만드는데 claim이 날짜를 안 봐서, 컨펌 직후 발행을 시도하고
  팝빌 -11002009(미래 작성일자)로 failed에 멈췄다(라프레리 실측 — 중간정산 종료월 기준이라 7월분이 9/1
  익익월로 밀린 것이 발단). 이제 미래 날짜는 그날 자동 발행된다.
- **작성일자 변경 창구**(`PUT /taxbill-issues/:id/issue-date` + 화면 모달) — 허용은 **외부 호출이 없었음이
  행 자체로 증명되는 2종뿐**: `invoicer_mgt_key` 엄격 NULL(채번은 외부 호출 직전에만 일어난다 — 내구 증거) /
  error에 `-11002009`(registIssue 검증 거절의 완결 응답 = 미등록 확정). 그 외 전부 거부·팝빌 대사 안내.
  ⛔ **팝빌 조회로 부재를 증명하는 관문을 되살리지 마라** — 1R~4R에 걸쳐 통신 실패 fail-open → ABA →
  빈 키·환경 불일치·지연 완료가 차례로 나왔다. 외부로 나간 적 있는 요청의 부재는 로컬에서 완전하게 증명할
  수 없다는 것이 이 설계의 뿌리다. 판정·쓰기는 행 잠금 안 단일 시퀀스.
- **[재시도]는 error를 지우지 않는다**(status만 ready로) — 지우면 -11002009 건이 변경 자격을 잃고 새 게이트가
  미래 날짜를 안 집어 복구 불능이 된다. 실패 사유 표시는 failed일 때만(화면). 컨펌 동기화는 superseded 행을
  덮지 않고 0건이면 경고 로그 후 진행(taxbill_issues가 진실 축). 문서번호는 변경 후에도 유지(멱등 재시도 계약).

---

### 2-20. 계산서 비고(PO번호)는 **컨펌 행이 장마다 갖고, 발행 패스가 싣는다** (★2026-08-21 · 시세이도 0805 접수 구현)

0805에 설계만 적고 "남은 것"에 둔 채 **같은 접수의 다른 건(대리 컨펌)만 완료**돼, 0821 서수란 팀장님이 "업체 확인 기록에
PO를 적으면 계산서 비고에 들어가나"를 물을 때까지 미착수였다. 완료 보고에서 남은 축을 분명히 하지 않은 기록 결함이다.
- **값의 자리** = `invoice_confirmations.taxbill_remark`(150자) — 부서 PO는 장마다 다르므로 회사 설정이 아니라 컨펌 행.
  **필수 여부** = `company_billing_settings.require_taxbill_remark` — 켜면 작성일자 지정·변경에서 비고가 비면 422(`TAXBILL_REMARK_REQUIRED`).
- **입력 창구 2** = [작성일자 지정](직접선택 회사 · 컨펌 현황 행의 날짜 옆) · [작성일자 변경] 모달(기존 값 기본 표시, 비우면 지움).
  같은 통보로 오는 값이라 화면을 나누지 않는다. 업체 확인 기록(`confirm_note`)은 근거 메모일 뿐 계산서로 가지 않는다.
- **발행** = `ISSUE_ROW_SQL`이 컨펌 행의 비고를 읽어 `payload.remark1`에 싣는다. 사유 2·4 수정 장은 `remark1`이 당초 작성일자
  기재 의무라 **비고는 `remark2`**로 내린다(의무를 밀어내지 않는다). 수정 장은 같은 컨펌 행을 가리켜 같은 PO가 따라간다.
- ⛔ **"비고 필수" 게이트는 발행 패스(`processOne`)에 있다 — 라우트 검사는 안내용 앞단일 뿐이다**(Codex 1R high).
  입력 라우트에만 두면 자동 정책(confirmed·due→ready)·플래그 켜기 전 ready·컨펌 연결 없는 건이 비고 없이 나간다.
  발행 패스가 `ISSUE_ROW_SQL`에서 회사 플래그를 함께 읽어 **문서번호 채번 앞**에서 멈춘다(failed) — 채번 전이라
  `invoicer_mgt_key`가 NULL로 남고, 그래서 [작성일자 변경] 화이트리스트(§2-19 ①)로 비고를 넣어 바로 복구된다.
  채번 뒤에 멈추게 만들면 그 건은 복구 불능이다. 자동 정책 회사가 플래그를 켜면 첫 발행이 여기서 멈추고 담당자가
  [작성일자 변경]에서 비고를 넣는 흐름이 된다(설정 저장에서 조합을 막지 않는 이유 — 막으면 전환 전 ready가 빠진다).
  **게이트 범위 = 원본 장 + 미채번 행뿐**(Codex 2R) — 채번된 행(submitted 재확인·실패 재시도)은 이미 외부에 문서가
  있을 수 있어 getInfo 대사보다 먼저 failed로 덮으면 실발행 건을 지우고, 수정 장은 같은 컨펌 행의 비고를 따르며
  PO 판정이 원본 때 끝나 막으면 복구 경로가 없다. **채번된 원본**은 비고가 비면 registIssue 전에 getInfo로 외부
  실재를 먼저 본다(Codex 3R) — 실재하면 그대로(중복 → 자가치유), 미등록·조회 실패면 failed(비고 요구).
  ⛔ **비고만 바꾸는 요청도 §2-19 화이트리스트 안에서만**(Codex 4R — 3R에 "날짜가 같으면 비고만 저장"을 열었다가 접었다).
  외부 문서가 있는데 선대사 타임아웃으로 failed가 된 행에 비고를 적으면 다음 발행이 중복 거절 → 자가치유로 끝나
  **로컬엔 PO가 있고 실제 문서엔 없는** 조용한 불일치가 된다. 외부 호출 이력이 있고 거절 확정이 아닌 행은 날짜든
  비고든 로컬에서 바꾸지 않는다 — 처방은 대사 후 정산 삭제·재발행. (-11002009 거절 확정 건은 화이트리스트라 [작성일자
  변경]에서 비고를 넣는다.) 계약 테스트는 중괄호 경계로 실제 분기를 잘라 검증한다(문자열 존재 검사는 분기 이동을 못 잡는다).
- **컨펌 행 id는 회사 격리 JOIN으로 맞춰진 값(`matched_confirmation_id`)만 쓴다**(Codex 2R critical) — raw
  `confirmation_id`로 발행 확정을 쓰면 JOIN 격리가 후속 쓰기에서 열린다(타사 컨펌이 issued·이 회사 팝빌 키). raw id는
  있는데 같은 회사 행이 없으면 채번 앞에서 멈춘다(대사 안내).
- **컨펌 행은 같은 회사의 활성 행만 잇는다**(`ic.company_id = t.company_id` — 발행 행 SQL·목록·변경 라우트 전부).
  변경 라우트는 그 행을 FOR UPDATE로 잠가 유효 비고를 계산하고, 저장 1건(RETURNING)을 확인한 뒤에만 ready로
  올린다 — 비고를 보냈거나 필수 회사인데 둘 곳이 없으면 422. 키가 없는 요청은 기존 PO를 보존한다(두 라우트 동일).
- 신설 컬럼 읽기는 전부 `to_jsonb`(ALTER 전 화면·발행 무사고) · 계약 = `taxbill-popbill.test.ts` 비고 배치 4건 +
  `billing-route-invariants.test.ts` 경로 계약 5건(게이트 위치·채번 순서·회사 격리·저장 확인·키 보존).

## §3 구조

### 3-1. 축

| 축 | 테이블 | 비고 |
|---|---|---|
| 청구서(장) | `billings` | `scope` = `combined`/`by_user`/`common` · `batch_id`(묶음) |
| 항목 | `billing_items` | 행마다 `user_id`·`agent_id`·`store_id`·`channel` |
| 추가 청구 | `billing_extra_items` | 080·부가서비스·최소과금. 소비 마커 = `billed_billing_id` |
| 수신자 | `billing_recipients` | 유형별·복수. 대표 1 + 참조 N |
| 통지·컨펌 | `invoice_confirmations` | **메일 1통 = 1행** |
| 세금계산서 | `taxbill_issues` | 원본 + 수정 축 |
| 일괄발급 | `billing_bulk_jobs` · `_items` | 진행률의 원천 |
| 수량 조정 | `billing_qty_adjustments` | 회사×기간 축이라 삭제·재발행에도 살아남는다 |
| 수동 완료 | `billing_manual_completions` | 우리 정산으로 못 내는 회사의 그 달 기록 |

### 3-2. 발행 흐름

```
대상 산출(listUnbilledPostpaid) → 적격 필터(filterBillableCompanies)
  → 발행 코어(issueBilling) → 장 분할(splitBillingSheets)
  → 통지 적재(createAndSendConfirmations: 행 먼저 → 메일)
  → 컨펌/이의 → 기한 도래 → taxbill_issues ready → 팝빌 발행
```

- **미발급 대상 제외 규칙** = `manual_billing`(수동 정산) · `min_charge_supply`(최소과금 정액) · **`status='terminated'`(해지 — ★0804)**. 셋 다 **목록에는 뜨고 담기에서만 빠진다** — 그 달 처리 여부를 사람이 봐야 하기 때문이다. 판정의 단일 진실은 `filterBillableCompanies`이고 job 생성·item 실행 직전에도 다시 지난다.
- **발송은 컨펌 경로 하나**(★0804) — 옛 `send-email`은 `emailed_at`만 찍고 추적행을 안 만들어, 그 순간 그 청구서가 컨펌·이의신청·세금계산서 흐름에서 통째로 빠졌다. 화면 진입점 2곳(목록·상세)을 `retryUnsentConfirmations`로 통일했다.
- **미리보기(`/preview`)는 발행과 같은 함수·같은 차단**을 쓴다. 발행 확인 모달이 이것을 먼저 보여주고, 차단 사유가 있으면 발행 버튼이 잠긴다(★0804 배선 — 그전에는 호출부가 0건이었다).
- ⛔ **"확정"이 두 축에 같은 이름으로 있다**(2026-08-05 · 서수란 질의로 드러남). 헷갈리면 담당자가 헛단계를 밟는다.
  - **화면 [확정] 버튼** = `PUT /billings/:id/status`로 `billings.status`를 `draft → confirmed`. **순수 UPDATE라 부수효과가 없다** — 메일도 세금계산서도 안 만든다.
  - **발행 큐를 움직이는 확정** = 고객이 컨펌 메일에서 누르는 동의. 워커가 `invoice_confirmations.taxbill_status IN ('confirmed','due')`인 건만 `ready`로 옮겨 `taxbill_issues`를 만든다. **관리자 [확정]은 이 큐를 움직이지 않는다.**
  - 발송 경로(`invoice-confirm.ts`)는 `billings.status`를 보지 않는다 ⇒ **확정은 발송의 전제 조건이 아니다**(관례이지 잠금이 아니다).
  - [확정]의 고유 효과는 **삭제 잠금 하나뿐**인데, 삭제 가드가 `상태 ≠ draft` **또는** `메일 발송됨`이라 **발송하는 순간 어차피 잠긴다**. 확정이 추가로 버는 것은 "아직 안 보낸 청구서를 미리 잠그는 것"뿐이다.
  - **추가 과제(착수 판단 = Harold)** — 화면에서 [확정] 버튼을 발행 흐름에서 걷어낸다. 컬럼째 폐기가 아니라 버튼·흐름에서만 빼는 것이고, `status='paid'`(수금)가 실제로 소비되는지 확인이 선행한다.

### 3-3. 청구 축 14종

웹 4(SMS·LMS·MMS·카카오) + 에이전트 4 + 테스트 2 + 스팸 2 + 요금제 + AI 크레딧.
**컬럼으로 담을 수 없다** — 목록은 `유형별` 버튼으로 열고, 상세 모달이 서버 `lines`(PDF·이메일과 같은 `buildInvoiceLines`)를 그대로 보여준다(★0804. 그전에는 SMS·LMS 두 컬럼만 있어 `both` 회사에서 틀려 보였다).

---

## §4 성능 — 근본 원인은 선택률 오추정이었다

회사 1곳 발행에 2분이 걸렸다. **인덱스는 원인이 아니었다**(존재 확인). 청킹도 아니었다 — `IN` 목록을 200개로 쪼개 배포했더니 44.3초 → 50.6초로 **개선 0**이었다.
EXPLAIN 실측 = `type: ALL` · `key: NULL` · `filtered: 49.60`. **IN 값이 5개일 때도 풀스캔**이었다. 개수가 아니라 선택률 추정이 원인이라 청킹으로는 닿을 수 없었다.
처방 = `app_etc1 IN` 집계 2곳에만 `FORCE INDEX(idx_app_etc1_sendreq)`. 결과 **50,643ms → 6,696ms(7.6배)**.
> 두 번째 스캔 폐기는 **하지 않는다**(종결) — 상세축이 854ms라 이득이 1초 미만인데 런타임 교차검증(`diffBillingRowsVsDayData`)을 잃는다. 그 값에 돈 검증을 팔지 않는다.

---

## §5 팝빌 (세금계산서)

- 연동·수정발행 구현·배포 완료(`d4430454`). ENV 5키 등록, 게이트 ON, 웹훅 수신 200 실측.
- **테스트/운영은 완전 분리 환경**이라 운영 전환 후에도 테스트베드가 남는다 → 테스트베드로 끝까지 검증한 뒤 마지막에 `POPBILL_IS_TEST=false`.
- ⛔ **실청구 컨펌 메일을 보내기 전에 테스트 절차를 끝내야 한다.** 지금 상태(게이트 ON + `IS_TEST=true`)로 실청구를 흘리면 컨펌 클릭이 없어도 기한(발송+3일) 도래 시 워커가 실고객 장을 **테스트베드로** 자동 발행한다. 입구는 사람이 열지만 **입구가 열린 뒤는 전자동**이라 멈출 지점이 설계상 없다.
- 한계 = 팝빌 사이트에서 수동 발행·수정한 문서는 우리 문서번호가 아니라 웹훅 매칭이 안 된다(raw 로그에만 남는다).
- ★ **2026-08-05 운영 전환 완료** — `POPBILL_IS_TEST=false` + `pm2 restart`. 검증 = 값 `false` · `.env` 수정 12:29:59 · 프로세스 기동 12:30:04(수정보다 뒤 = 새 값 적재 확정) · **런타임 확정 = PM2 로그 `[팝빌] SDK 초기화 — ★운영★`(12:30:45)**.
  ⛔ **`/proc/PID/environ`으로는 확인할 수 없다** — `dotenv`가 기동 후 메모리에 넣는 값이라 프로세스 환경변수에 안 잡힌다. 판정은 **파일 값 + 파일 mtime + 프로세스 기동 시각** 셋을 비교한다.
  전환 시점의 큐 = `pending` 29 · `ready` 2(크로커다일 25,248 / 유에스소프트 2,319,247) · `manual_wait` 1 · `issued` 11(전환 전 발행분이라 **전부 테스트베드**).
  ⇒ `pending` 29건은 기한(2026-08-08) 도래 시 순차 자동 발행된다.
- ★ **첫 실전 발행 확인 — 프로웨어 3,979,014원 · `issued` 12:45:15 · 승인번호 `2026080141000203000034ac`.** 전환이 실제로 동작한다.
- ⛔ **`failed`는 자동 재시도 대상이 아니다**(2026-08-05 실측). 크로커다일 25,248 · 유에스소프트 2,319,247이
  팝빌 인프라 504(`TokenService` Gateway Time-out)로 실패했는데, 12:45 워커 패스가 신규 건은 발행하면서
  그 2건은 건드리지 않았다. `invoice_confirmations`는 `ready`인데 `taxbill_issues`가 `failed`로 남아 있으면
  **사람이 재시도를 걸어야 한다.** 자동 회수 경로 신설은 별건(§7).
- **승인번호는 웹훅이 아니라 발행 응답·`getInfo` 재수집으로 확정된다** — 웹훅 로그 0건인데도 발행분 전부
  `nts_confirm_num`이 채워져 있었다. 설계상 `issued` 승격의 단일 소유자가 발행 패스이고 웹훅은 보조 신호다.
  ⇒ 웹훅 미수신이 발행을 막지 않는다. **방화벽 조치도 불필요** — 웹훅은 443으로 들어오고 그 포트는 공개 서비스라 이미 열려 있다.
  0805 이전 웹훅 0건의 원인은 방화벽이 아니라 **콜백 URL 미등록**이었고(운영 등록 완료), 인증서 상태 웹훅은
  핸들러가 미지 이벤트도 200 `"OK"`로 받으므로 등록해도 무해하나 **처리는 하지 않는다**(만료 알림은 팝빌 사이트 축).
- **2026-08-05 실측 — 전환 전 테스트베드로 나가던 이유는 우리 설정 한 줄이었다.** `packages/backend/.env`의 `POPBILL_IS_TEST`(`POPBILL_ENABLED=true`). 자격증명 3종도 레포 루트가 아니라 **그 파일**에 있다.
  - 판정 코드 = `utils/taxbill-popbill.ts:63` `isTest: process.env.POPBILL_IS_TEST !== 'false'` — **미설정도 테스트베드**가 안전 기본이다(단위 테스트가 이 계약을 고정).
  - ⇒ **팝빌 계정을 정상 서비스로 전환하고 포인트를 충전한 것과 우리 발행 환경은 별개 축이다.** 계정이 운영이어도 이 값이 `false`가 아니면 테스트베드로 초기화된다(로그 `SDK 초기화 — 테스트베드 / ★운영★`).
  - 전환 = 값 `false` + `pm2 restart all`(재기동 시 `.env` 재적재). **선행 = 8/4 테스트베드 발행 1건의 내용 확인**(공급자·공급받는자·합계·부가세·품목 줄, 그리고 우리 문서번호로 웹훅이 매칭됐는지 = 워커가 만든 건인지).

---

## §6 특례 (080 · 부가서비스 · 최소과금)

- **080** = KT 명세서 판독 → 원장 파생. AI 판독값으로 돈을 움직이므로 **검산과 반영을 서버가 결속**한다 — 정규형에 만료형 HMAC 서명을 발급하고, 반영은 서명 검증 후 **검산을 서버에서 재실행**한다. 화면 비활성화는 우회된다.
  - `NaN → JSON null → Number(null)=0`으로 "판독 불가"가 0원으로 살아나는 것 차단(안전 정수만 인정·반올림 금지)
  - 합계·건수를 "안 보이면 0"으로 두면 그 대조가 통째로 fail-open
  - 검산 항목이 서로 종속이면 오분류를 못 잡는다(VAT 비율 같은 **독립 축**을 하나 더)
- **부가서비스** = 수기 입력(`kind='manual'`). 자동 집계하지 않는 이유 = "서비스 제외" 같은 판단이 사람 몫이라 자동으로 세면 틀린다.
- **최소과금** = 정액 발행. 사용량 발행과 겹치면 이중청구라 일괄발급 담기에서 구조로 뺀다.

---

## §7 남은 것

**발행 전 필수** — 없음(0804에 §9-3 유형별 수량 버튼이 닫히면서 0726 §9의 4건이 전부 종결).

**개발 미착수**
- 삭제·재발행 원자화 — 지금은 preflight로 실패 창만 좁힌 두 트랜잭션. 완전 원자화는 발행 코어를 외부 트랜잭션에 참여시키는 공사(0804 Codex H3 불수용 기록)
- outbox/재발송 체계 — 메일 발송 후 DB 실패 시 죽은 링크. 복구 경로는 삭제 후 재발행(수용 위험으로 기록)
- 회사 반복 항목(에이스하드웨어 5만 정액) · 발행 그룹(`by_agent` 흡수, §9-6 공사 큼)
- `/preview` relation 부재 매핑 · 대기 게이트 완성(채널별 종결 판정 정규화 선행) · 여정 후불 미청구 통로
- 발송ID 단가 자동 매핑(`cost_per_*` 미충전 — 새 발송ID마다 그 달 청구가 막힌다)
- **과거 미소비 추가항목의 영구 미청구 성질**(★0820 관찰) — `billing_extra_items` 선택이 발행 기간과의 겹침이라, 미소비인 채 다음 발행 기간과 안 겹치는 달(예: 7월분 미소비 + 다음 발행이 8/1~8/31)은 어느 청구서에도 조용히 안 실린다. 미소비 잔존 감시(경고 목록)가 처방 후보
- ~~정산 목록 응답 세대 미관리~~ → **0820 같은 세션에 닫음**(Codex 2R이 1차 불수용 근거의 사실 오류를 잡았다 — 선택 유지 계약은 **화면 내 검색·페이징만**이고[아래 0806 절], 서버 필터 변경은 가지치기가 목록 도착 **후**에만 돌아 응답 전 전환 창에서 옛 목록·옛 선택으로 일괄 확정·발송이 가능했다). 2R의 요청 순서(seq) 가드는 3R이 다시 깼다 — 순서는 **귀속**이 아니다(전환 요청 실패 시 옛 달 목록 잔존 · 벌크 종료 재조회가 옛 클로저 필터로 나감). 최종 구조(5R approve) = **귀속(키) + 최신성(세대) 두 축**: 목록 상태가 자기 키(`billingsKey`)를 들고, 요청 파라미터는 항상 ref의 호출 시점 최신 키에서 만들며(옛 클로저 경유 호출도 현재 필터로 나간다), 상태를 쓰는 것은 `마지막에 시작한 요청 AND 키 현재 일치`(`isCurrent`) 하나뿐이고, 그 요청의 실패는 빈 목록(fail-closed), 일괄 버튼·`runBillingBulk` 입구는 `적재 키 = 현재 키`일 때만 산다. 키만으로는 같은 키의 두 재조회가 직렬화되지 않아(4R) 세대를 다시 얹었다 — 두 축은 직교라 하나가 다른 하나를 대체하지 못한다

**★0818 미적재 환불 회계 — 실행 단위 채무 원장** (브랜드 세션에서 발견 · 전 채널 축이라 여기가 소유)

미적재 환불 항아리는 `(회사, 참조유형, 참조ID, 유형, 원인키)` **누적**이다. 그런데 AI 캠페인은 **같은 `campaign id`로 재발송**되고, 실행마다 자기 실패분만 같은 원인키로 넘긴다. 그래서 1회차가 N건 환불해 두면 2회차의 N건은 "이미 갚았다"로 판정돼 `ok:true / refunded:0`으로 끝나고 **그 실행분 차감이 영구히 남는다**(덜 갚는 방향 — 회복 가능).

⛔ **원인키에 실행 ID를 붙여 우회하지 마라 — 0818에 시도했다가 되돌렸다.** `mysql-refund-sweeper`가 같은 미적재분을 **평문 `notloaded`로 다시 청구**하는데, `prepaidRefund`의 상한은 `총차감 − 총환불`이지 정당 실패액이 아니다. 항아리를 나누면 즉시 경로와 sweeper가 각각 갚아 **과환불**이 된다(6,000 차감·5,000 적재 → 1,000 + 1,000). 덜 갚던 것을 더 갚는 쪽으로 옮기는 짓이다.

**정답 방향** = 채무를 `(campaign_id, message_type, refund_key)` 정규화 원장(또는 실행별 obligation ID)으로 옮기고, **즉시 경로와 sweeper가 하나의 원인 합계로 정산**하게 한다. 착수 시 영향표 대상 = `prepaidRefund` 전 호출부 · `mysql-refund-sweeper` · `direct-send-worker` 재시도 루프 · `refund-pending`.

**0818에 함께 닫은 것**(이 축의 인접 결함 — 배포분에 포함)
- `both`는 차감이 2축(문자+BRAND)인데 예외 catch가 `Math.max` 하나로 미적재를 계산해 **BRAND 차감이 통째로 남았다** → 축별 정산으로 분리, 축마다 독립 try
- 미완 채무 슬롯이 캠페인당 하나라 두 축이 함께 실패하면 앞 기록이 사라졌다 → `markRefundPendingAxes`(행 잠금 + 병합 + 단일 UPDATE), 워커 `clear`/`defer`에 CAS
- 미완 채무가 **원인키를 안 들고 다녀** 워커가 `CANCEL` 채무를 `NOT_LOADED` 항아리로 갚았다 → `refundKey` 관통
- 성공 경로 부분환불 3곳이 `prepaidRefund`의 `ok:false`를 성공으로 처리 → 반환값 확인 + 미수 기록
- 조립 실패가 차감 **뒤**에 나던 순서를 **앞**으로 돌렸다(preflight) — 규격 위반이 돈을 움직이기 전에 끝난다

**요금제 무료 메시징 이월분** (0805 Codex 3R · [설계서](2026-08-05-plan-free-messaging-design.md) §7-A)
- 청구서 2페이지 상세에 **실제 성공·무료·청구 수량 분리 표시** — 지금은 수량이 실제 성공인데 금액만 공제 후라 고객이 세로 검산을 못 한다(발행 금액 자체는 정확). 고정 좌표 PDF라 레이아웃 변경 위험이 있어 분리했다
- 발송과 `billing_type` 전환 사이 직렬화 또는 **발송별 과금 모드 영속화** — 이 기능 이전부터의 성질이라 별건

**Harold 결정 대기** — `G` 유형 과금 분류 · 요금제 이력 크레딧 스냅샷 소급 ALTER
- **기발행분 품목 거래일자 공란**(★2026-08-05) — 아래 정정 **이전에 나간 문서**는 품목 줄의 월·일이 비어 있다. 소급이 아니라 **수정세금계산서 축**이라(§2-8) 손댈지는 Harold 판단이다. 대상 건수는 `taxbill_issues` `status='issued'` 집계로 확인.
- **해지 회사 목록 제외 · 정산 목록 검색·페이징**(★2026-08-06 Harold 지시 · 코드 완료) — 미발급 목록 41개사 중 28개가 담기지 않는 회사(해지)라 화면이 그것들로 덮였다. 서버 목록 쿼리에서 뺐다. ⚠ **해지 회사의 미청구분은 이제 이 목록에 안 뜬다** — 남았다면 단건 발행으로. 정산 목록은 15개씩 페이징 + 고객사·계정 검색(화면 안에서만 좁힌다). 헤더 체크박스는 **이 페이지** 기준(일괄발급과 같은 규약) — 선택은 페이지·검색을 넘어 유지되고 일괄 실행은 안 보이는 선택까지 포함한다.
- ~~**계산서 비고(PO) — 시세이도 접수**(★2026-08-05 접수, 임은지).~~ **★2026-08-21 구현 완료 — §2-20**(서수란 재질의로 미착수가 드러남). 아래는 당시 설계 원문. 브랜드(계정)마다 컨펌 후 PO를 통보받아 계산서 비고에 실어야 한다. **비고1 칸은 문서에 실재 확인**(금강제화 발행분). 발행 단위는 이미 `by_user`+`manual`이라 `by_agent` 공사 불필요.
  설계 = `invoice_confirmations.taxbill_remark`(장마다 값이 다르다 — 회사 고정값이 아니다) + `company_billing_settings.require_taxbill_remark`(비면 작성일자 지정 차단). 입력은 [작성일자 지정] 한 자리에서 날짜와 함께(같은 통보로 오는 값이라 화면을 나누지 않는다). 발행 패스가 `remark1`로 싣는다.
  ⚠ **완료 조건 = 첫 발행 1건에서 PO가 실제로 찍히는지 확인.** 값이 채워진 문서를 우리가 본 적이 없다(수정발행 실전 기록 0).

**0806(2) 접수에서 갈라 둔 것** (§8-5)
- **거래내역서 엑셀 첨부** — 더화이트 접수 본문의 대안 갈래("엑셀 첨부되어 본인들이 내역을 확인할 수 있게"). 기대 동작 칸은 PDF 전체 출력이라 이번 축에서 분리했다. 착수하면 구분 칸은 `resolveBillingScopeLabel` CT를 그대로 쓴다(PDF·모달·메일과 같은 값이어야 한다)
- **거래내역서 발송 지연 — 80분이 어디였는지는 미확정** — §8-5. 코드가 보장하는 것은 "`sendMail`이 60초 타임아웃 전에 성공한 요청이면 그 인계는 응답 전에 끝난다"는 구조뿐이고(타임아웃 경로는 반례 — 응답 뒤에 끝날 수 있다), 08-06 기록은 그것과 **일치할 뿐 그 요청이 실제로 보냈음을 증명하지 않는다.** **남은 것은 업체가 받은 메일 원문(헤더 포함) 확보** 하나다 — Received 체인만이 하이웍스 발신 큐와 수신측 중 어디서 멈췄는지를 가른다. 그 전에는 **우리 구간이라고도 외부라고도 쓰지 않는다**
- **불변 발송 시도 기록**(★0806 · Codex 4R·5R·6R이 같은 부류를 세 번) — 지금은 "그 요청이 실제로 보냈는가"를 **사후에 확정할 방법이 없다.** `emailed_at`은 가변이고 쓰는 경로가 여럿이며, 성공한 발송에는 장별 로그 마커가 없고, 응답 본문(`targeted`·`summary.sent`)은 남지 않는다. 그래서 접수가 올 때마다 정황을 쌓아 추정하게 된다. 필요한 것은 `billing_id`·호출 출처(요청/잡)·SMTP 결과·시각을 담은 **덮어쓰지 않는 행** 하나다. 그것이 생기면 §8-5 같은 다층 서술이 필요 없어진다. (DDL 축이라 별건)
- **에이전트 구분 칸에서 발송ID를 뺄지** (Harold 제안 0806) — 발급명만 쓰면 실측 80.0pt로 한 줄에 들어간다. 판정 축은 **같은 발급명을 쓰는 발송ID가 있는가** 하나다(있으면 두 행이 화면상 같아져 이번 접수가 오히려 안 닫힌다). 발송ID 병기는 0731 접수의 명시 요구이기도 하다([handoff](2026-07-31-billing-detail-storeid-handoff.md)). 확인 문 = `RSRM_SalesMst`의 `CustNm` 중복 집계

**0807에서 갈라 둔 것** (§8-6)
- **팝빌 `getInfo` 대사 기반 실패 장 폐기** — 로컬 데이터로는 "국세청에 문서가 없다"를 증명할 수 없다. `getInfoAsync`·`judgeInfoState`는 이미 있으니 그것을 취소 경로에서 부르고, **미존재가 확정된 경우에만** 폐기한다(3xx·305·조회 실패는 전부 거절 = fail-closed). ⚠ 크로커다일 건은 `registIssue`가 거부돼 팝빌에 문서 자체가 없을 것이라 `getInfo`가 오류를 낸다 — 그 오류코드로 "미존재"를 확정할 수 있는지 실측이 선행이다
  ★**0807(2) 보강(Codex 2R high — 상한 도달로 이 별건에 승격)**: "승인번호 없음 = 외부 문서 없음"이 성립하지 않아, **늦은 304가 도착하기 전에 취소→재발행이 앞서는 창은 세대 토큰 DDL로도 안 닫힌다**(외부 부재는 로컬로 증명 불가 — Codex도 자인). 이 별건이 닫아야 할 것 셋 — ①취소·실패 행의 **대사 상태 내구 기록**(getInfo 결과를 행에) ②**발행 직전(processOne) 재검사** ③생성·삭제·웹훅·claim의 **billing 단위 직렬화**. 삭제 가드의 `failed` 확대도 여기 묶인다(대사 없이 막으면 실패 장 정정 경로가 전면 불능). 그 전까지의 방어 = 0807(2) 증거 내구화(§8-6) — **증거가 도착한 순간부터**만 막는다
- **`mobile@invitocorp.com` 발행 메일 참조 — ★2026-08-08 배포완료**(ENV 등록·`pm2 restart --update-env` 실행 확인 · 실측 = 다음 발행 1건에서 mobile@ 수신). ENV `TAXBILL_ARCHIVE_BCC`(쉼표 구분)를 `enqueueTaxbillArchiveCopies`가 발행 확정 **COMMIT 뒤** best-effort로 참조 재전송 큐에 싣는다 — 대표·고객 참조 중복 제외 · 어떤 실패도 던지지 않고 `[아카이브미기록]`(issueId·mgtKey·주소)으로만 남김 · 형식 무효 ENV는 `[아카이브설정오류]` 1회 경고. 처음 형태(확정 트랜잭션 안 SAVEPOINT 격리)는 Codex 1R이 깼다 — 복구 실패가 트랜잭션을 오염시키고(25P02) 아카이브 전용 조회 거절이 확정을 롤백시킨다 ⇒ 트랜잭션 밖으로 구조 전환(SAVEPOINT·임계 조회 삭제). ⚠ **서버 ENV `TAXBILL_ARCHIVE_BCC=mobile@invitocorp.com` 등록 전에는 동작하지 않는다.** Codex 2R 상한 종결 — 남긴 판정 2건:
  - (2R high · **위험 수용**) COMMIT과 아카이브 기록 사이 프로세스 중단 = 그 장의 아카이브 사본만 무음 유실. 고객 무영향(문서·고객 참조는 확정 트랜잭션 내구)·수동 재발송 창구로 복구 가능. 내구화(outbox+대조)는 "불변 발송 시도 기록" 별건과 같은 부류라 그 별건이 소유.
  - (2R high · **기존 축 별건**) 재전송 큐(`taxbill_email_resends`)에 발행 환경(`is_test`) 표식이 없다 — 0731 신설 때부터의 성질로 고객 참조 행도 같다. 환경 전환을 넘긴 pending 행이 다른 환경으로 소비될 수 있다(0805 전환 완료·재전환 계획 없음이라 실위험 낮음). 닫으려면 DDL — `is_test` 컬럼 + UNIQUE 확장 + 소비 claim 필터(발행 장부 `is_test`와 같은 규약).
  - ★0808 실측: mobile@는 `billing_recipients`에 **인비토(자사) 대표 수신자로만** 등록 — ENV 등록·배포 전에는 다른 회사 발행분이 mobile@로 나가지 않는다(08-08 자동 발행 13건 실측으로 확인). 대신 **참조 재전송 구조가 이미 있다**(`taxbill_email_resends` + `processTaxbillEmailResends`가 팝빌 `sendEmail`로 같은 문서를 재발송, 멱등 UNIQUE·재시도 상한 완비). ENV `TAXBILL_ARCHIVE_BCC`를 순수 함수 `selectTaxbillResendTargets`가 cc에 합친다. **대상을 두 갈래로 나누는 것이 핵심** — 기록 실패 시 고객 참조가 있으면 기존대로 발행 확정을 미루고, 우리 아카이브 주소 때문에는 절대 미루지 않는다(§2-1). DDL 0
- **공개 이의신청이 승인번호 가드를 우회**(Codex 2R 범위 밖) — [invoice-public.ts:242](../packages/backend/src/routes/invoice-public.ts)가 `ready`만 보고 `cancelled`를 써서, 0807에 세운 "승인번호 있는 `ready`는 재확인 큐라 취소 불가"를 지나친다. 고객이 누르는 경로라 우선순위 높음
- **`taxbill_issues` 상태 전이 CAS 통합** — 지금은 웹훅·워커·라우트가 각자 조건으로 쓴다. 상태 버전(또는 소유 토큰) 축이 없어 ABA를 못 잡는다. DDL 축이라 별건
- **사유 1 묶음 단위 취소** — 부(-)·정(+) 쌍을 원자적으로 함께 처리. 지금은 개별 취소를 막기만 했다

**실측 대기** — 실회사 드라이런(발행→PDF→메일→삭제) · 거래내역서 MMS 308,043건 · 0804 배포분 6종 · 팝빌 테스트 절차 5단계 · ~~자동 발행 첫 실행~~(**2026-08-08 실측 완료 — 13건 전부 성공**, §8-6)

---

## §8 이력 색인

| 시점 | 무엇 | 검증 |
|---|---|---|
| **0821 코드완료·배포 대기** | **080 고정료 자동 파생 + 계산서 작성일자 정합**(§2-18·§2-19 신설 — 서수란 0821 접수 2건: 게스코리아 "명세서 없으면 080 기본 설정이 빠진다" + 라프레리 "9/1 계산서를 8/20자로"). ①`080_base` 근거 행 자동 생성·`080_call`은 통화료만·legacy 스킵 축 교체·최소과금 고정료 매핑 거부 ②발행 claim 작성일자 KST 게이트 + 작성일자 변경 창구(허용 = 엄격 NULL·-11002009 2종 화이트리스트 — 팝빌 조회 관문은 시도 후 폐기, §2-19 ⛔) + [재시도] error 보존 + 컨펌 동기화 superseded 보호. 화면 = [작성일자 변경] 모달·실패 사유 failed 한정 표시·080 모달 문구 | backend tsc 0 · frontend tsc 0 · vitest 4파일 296건(신규 계약: 파생 3·소스 스캔 6) · **Codex 적대 6R approve**(1R high 3 → 2R high 4 → 3R high 2 → 4R high 3·medium 2 → 5R high 2·medium 2 전건 수용·불수용 2건 근거 기록 → 6R 지적 0) · DDL 0 |
| **0821(2) 코드완료·배포 대기** | **계산서 비고(PO번호) — 시세이도 0805 접수 구현**(§2-20 — 서수란 재질의 "업체 확인 기록에 PO를 적으면 들어가나"로 미착수 발견). 컨펌 행 `taxbill_remark` + 회사 `require_taxbill_remark` · 입력 = 작성일자 지정·변경 2창구 · 발행 remark1(수정 장 remark2) · to_jsonb 읽기·503 안전망 · 설정 체크박스 | backend·frontend tsc 0 · vitest 정산 5파일 322건(신규 payload 4·소스 계약 4) · **DDL 2 실행완료(배포 전 선행 — 컬럼 추가뿐이라 옛 코드 무영향)** · **Codex 적대 5R**(1R critical 0·high 3·medium 1 → 게이트를 발행 패스로 이동 / 2R critical 1·high 2 → matched id 전파·게이트 범위 축소 / 3R high 1 → 채번 원본 getInfo 선대사 / 4R high 1 → 비고만 우회 폐기 / 5R high 1 = **기존 취소 경로 부채 → [BUGS B-0821-2](../status/BUGS.md) 별건**, medium 2 수용. 상한 초과로 정지·보고) |
| **0820(2) 재오픈 정정 · 코드완료** | **추가 청구 항목 귀속 축 = 청구월 = 정산월**(§2-17 개정 — 서수란 재오픈: 시세이도 "8월 정산"에 7월분 131만원 합산 실측). 선택 2곳 + 차단·표시 5곳을 `make_date(billing_year,billing_month,1) = period_month` 한 축으로. 역월 정산 동작 무변화. **Codex 적대 5R approve** — 1R 이관 차단을 마커 진실 축으로(소비행은 라벨 무관 금지) · 1R 라벨 건너뜀 고착 차단(`monthFullyCovered` — 달 전체가 발행 기간에 덮이면 반영 거부) · 2R 수동 정산완료 기간도 덮임에 합산 · 3R·4R 스키마 부재(42P01·42703)를 skipped로 삼키지 않고 503(CT `db-migration-error`) | backend tsc 0 · 정산 12파일 206건(신규 계약 16: 월일치 소스 스캔 2·덮임 8·수동완료 UNION·42P01/42703 스캔 등) · DDL 0 |
| **0820 코드 완료·배포 대기** | **정산월 = 사람이 정하는 라벨**(§2-17 신설 — 서수란 0819 접수 "중간정산 정산 월 설정"). 파생 소유자 `resolveBillingLabelMonth`(기본 종료월·기간 밖 422·문자열 절단) · 발행 코어+최소과금 INSERT 2곳 통일 · 재발행 라벨 승계(검증 후 폴백) · 목록 월 필터(`month` 1~12) · 폼 정산월 선택(두 역월에 걸칠 때만)+확인 모달 표기+기간 변경 시 선택 초기화. 기발행분 영향 0(역월 초과 장 0행 실측) | backend·frontend tsc 0 · `billing-label-month.test.ts` 12건(신규) · 정산 테스트 12파일 194건 · Codex 적대 5R **approve**(1R high3·medium1 → 2R이 1차 불수용의 사실 오류를 잡음 → 3R 필터 귀속 구조 전환 → 4R 세대 축 보강 → 5R 지적 0) · DDL 0 |
| **0814 배포완료** | **에이전트 단가 공백 목록(`GET /agent-price-gaps`)을 발행 게이트와 같은 기준으로 정정** — 실적(성공>0) 있는 유형만 · 부달 재전송은 원 발송ID로 귀속 · 실적 미확인 시 **fail-open**(`usage_known:false`) · 단가 컬럼 자체가 없는 유형은 `unpriceable` 별도 표시 | backend tsc 0 · `agent-price-gaps.verify` 18건(신규) · 프론트 소비처 grep 0 · DDL 0 · 아래 §8-7 |
| 0725 | 정산 재구축 착수 · 서수란 접수 6건 | 커밋 `d19f48fd` |
| 0726 | **발행 단위(scope) 신설** + Codex 7라운드 정정 · 부가세 기준 전환 · 절사 · 요금제 일할 | 1,137→1,426 테스트 |
| 0728 | **일괄발급·컨펌·세금계산서** 전 구조 + 발행 코어 추출 | Codex 5R SHIP · vitest 1,435 |
| 0729 | 통지 파이프라인 재구성(§2-6) · 브랜드메시지 청구 축 CT | vitest 1,468 |
| 0730 | 팝빌 연동·수정발행 · 특례 축 A(080·부가서비스·최소과금) | `d4430454` |
| 0731 | 수신자 원장 일원화 · 세금계산서 복수 수신 · 레거시 예약 누락 정정 · 구분 칸 | Codex 5R·4R |
| 0804(1) | 서수란 접수 6건 — 080 원장 파생 · 최소과금 검색 · 컨펌 창구 · 이의 통지 · 수량 수정 발행 | DDL 6·ENV 2 |
| **0804(2)** | **미완료 정비 + 서수란 접수 2건** | 아래 §8-1 |
| **0808 배포완료** | **발행 메일 아카이브 참조**(ENV `TAXBILL_ARCHIVE_BCC` — 확정 COMMIT 뒤 best-effort로 재전송 큐 합류) | backend tsc 0 · vitest 142파일 2,101건 · 회귀 주입 3회 검출 · **Codex 2R 상한 종결**(1R high 2·medium 1 → 뿌리 하나[트랜잭션 안 배치]로 구조 전환 전건 수용 / 2R high 2 → 위험 수용 1·기존 축 별건 1 · medium 수용) · DDL 0 · **ENV 등록 실행완료**(2026-08-08 · `.env` tail 확인 · `pm2 restart all --update-env`) · §7 해당 항목 |
| **0807(2) 배포완료** | **재시도 관문 Codex 종결(2R 상한) — 웹훅 cancelled 증거 내구화 + 재발행 길목 4곳 차단 + 재시도·취소 양수 화이트리스트 + 운영 재발행 dup에 failed** | backend tsc 0 · vitest 141파일 2,086건 · 회귀 주입 2회 검출 확인 · PREPARE 7문 실검증(Harold 실행) · **Codex 2R**(1R high 2·medium 1 → 수용 2·부분 1 / 2R high 2 → 수용 1·별건 승격 1) · frontend 무변경 · DDL 0 · 아래 §8-6 추기 |
| **0807 배포완료**(2026-08-08) | **실패 계산서 재시도 관문 + `ready` 취소 가드 2종 + `cancelled` 종단 보호** — 폐기 창구는 접었다(로컬 데이터로 외부 부재를 증명 못 한다) | backend tsc 0 · frontend tsc 0 · vitest 141파일 2,085건 · 신규 SQL 2건 PREPARE 실검증 · **Codex 2R**(1R high 3 전건 수용 → 2R high 3·medium 1로 방향 전환) · ~~재시도 관문은 Codex 미실시~~(→ 0807(2)에서 실시·종결) · DDL 0 · 아래 §8-6 |
| **0806(2) 배포완료**(2026-08-08) | **거래내역서 PDF 구분값 말줄임 폐기 + 발송 지연 조사(귀속 미확정 · 코드 변경 없음)** — 서수란 접수 2건 | backend tsc 0 · vitest 140파일 2,060건(신규 7) · 폰트 폭·줄높이 실측 · **Codex 3R**(1R high 1 → 뿌리 수정 · 2R·3R approve·결함 0) · DDL 0 · 아래 §8-5 |
| **0806 배포완료** | **품목 거래일자 + 테스트베드 12장 운영 재발행 + 해지 목록 제외 + 정산 목록 검색·페이징** | backend tsc 0 · frontend tsc 0 · vitest 140파일 2,053건 · Codex 3R(critical 0·high 4·medium 2 → 수용 5·부분 1·불수용 1) · DDL·백필 실행완료 · **재발행 12건 실전 완료** · 아래 §8-4 |
| **0805 배포완료** | **재오픈 2건 정정 + 총 정산표 + 확정 축 정리 + 팝빌 운영 전환** | backend tsc 0 · frontend tsc 0 · vitest 138파일 2,003건(신규 11) · 신규 SQL PREPARE 실검증 · 영향표 실측 · 팝빌 `★운영★` 로그 · 아래 §8-2 |
| **0805(2)** | **서수란 접수 5건 + 발행분 삭제 가드 확대** — 최소과금 판정 축 · 컨펌 관문 · 대리 컨펌 · 계산서·거래내역서 재발송 · 목록 일괄 처리 · §2-8을 모든 삭제로 | backend tsc 0 · frontend tsc 0 · vitest 139파일 2,038건(신규 3) · 프론트 safe-build(lazy 청크 36건 실존) · 실측 2건(회사 요금제 행·요금제 이력) · **Codex 7R**(critical 1·high 6·medium 4 → 수용 7·부분 1·불수용 3. 4R은 approve·결함 0. 불수용 3건은 뿌리 하나라 [BUGS.md](../status/BUGS.md) B-0805-1로 통합 후 그 뿌리째 정정) · 신규 SQL `PREPARE` 실검증 + 잠금 순서 운영 DB 실측 · 아래 §8-3 |

### 8-7. 0814 상세 — 진단 목록이 게이트와 다른 기준을 쓰면 소음이 된다

`agent-price-gaps`는 "마감일에 어느 단가가 비었는지 회사를 하나씩 발행해 보며 찾는" 일을 없애려고 0726에 만든 진단 API다. 그런데 **"단가 NULL"만** 보고 목록을 만들었다 — 발행 게이트(`priceBillingRows`)는 **`success > 0`일 때만** 막는데 목록은 실적을 안 봤다.

그래서 0729에 브랜드 단가 컬럼(`cost_per_brand`)이 늘어나자, **브랜드를 쓰지도 않는 발송ID가 전부 목록에 올라왔다.** 0814 실측 190행 중 대부분이 그 소음이었고, 정작 급한 **"전 유형 미설정" 20행**(SMS조차 비어 발송만 있으면 즉시 발행 차단 — 진소프트·플랫에이·한국마사회 등)이 그 안에 묻혔다.

**정정 =** 목록의 좁힘 규칙을 게이트와 같은 식으로 맞췄다. 축 파생은 `BILLING_TYPES`, MsgType→유형키는 `agentUsageKey` CT 재사용(매핑 두 벌 금지). 실적 조회는 `pay-stats.queryAgentUsedTypes()` 신설 — 전 기간(과거 기간 재발행에도 그 단가가 필요하므로 기간을 자르지 않는다).

**좁히는 방향은 fail-open이다.** 수집 DB 미설정·조회 실패면 전부 보여주고 `usage_known:false`를 함께 돌려준다. 공백을 숨기면 "채울 것 없음"으로 읽히고 마감일에 이유 없이 막힌 발행을 찾아다니게 된다 — 그쪽이 훨씬 위험하다.

⛔ **부달 재전송 귀속을 빠뜨리면 안 된다.** 중계 계정 행은 `StoreId`에서 원 발송ID를 뽑아 귀속한다. 안 그러면 그 경로로만 보낸 유형이 목록에서 사라지는데 발행은 막힌다.
⛔ 실적은 있는데 **단가 컬럼 자체가 없는** 유형(`KS`·`KL` 카카오 대체발송)은 입력으로 못 고치고 발행은 똑같이 막힌다 → `unpriceable`로 분리 표시. 축에 유형을 추가해야 풀린다.

일반화 = **"무엇이 나를 막는가"를 답하는 화면·API는 막는 쪽 코드와 같은 판정을 써야 한다.** 교훈 = [LESSONS_BACKEND](../status/lessons/LESSONS_BACKEND.md) 핵심원칙.

### 8-6. 0807 상세 — 실패 장은 "국세청에 문서가 없다"가 아니다

**접수가 아니라 점검에서 나왔다**(Harold 지시로 자동 발행 동작 확인 중 발견).

- **`failed` 폐기 창구를 만들려다 접었다.** 실패 장을 없애는 길이 [재시도]뿐이라 목록에 영원히 남아
  잘못 눌리도록 유혹하는 것이 출발점이었다. 그런데 **`failed`에는 두 가지가 섞여 있다** —
  팝빌에 문서가 아예 없는 것(`registIssue` 거부)과 **발행은 됐는데 국세청 전송만 실패한 것(305)**.
  우리 로컬 데이터로는 그 둘을 못 가른다. 승인번호는 그 사실의 **그림자일 뿐**이라 305는 승인번호가 없다.
  증명 없이 내리면 **실제 문서를 숨긴 채 다른 관리키로 중복 발행**할 길이 열린다(Codex 적대검증 2R).
  ⇒ 권위 있는 판정은 팝빌 `getInfo` 대사뿐이고 그건 취소 경로에 외부 호출을 들이는 별건이다(§7).
- **대신 재시도에 관문을 세웠다** — 위험한 것은 **수정(취소·정정) 장 재시도** 하나다. 그 문서는
  이미 국세청에 있는 원본을 건드린다. 사유와 명시 확인이 있어야 통과하고, 판정은 UPDATE 안에서 한다
  (밖에서 읽고 쓰면 그 사이 상태가 바뀐다). **원본 장 재시도는 무변경** — 안 나간 청구서를 같은 문서번호로
  다시 보내는 것이라 안전하다.
- **조사에서 드러난 기존 결함 둘을 함께 닫았다.**
  - **승인번호가 있는 `ready`가 취소 대상이었다** — 웹훅이 304를 관측하면 `failed`를 `ready`로 되돌리며
    승인번호를 적는다. 그 행은 이미 나간 문서의 **재확인 큐**지 발급 대기가 아니다.
    정상 `ready`(워커가 `confirmed`→`ready`로 만든 것)는 승인번호가 없어 영향이 없다.
  - **사유 1(기재사항 착오정정)은 부(-)·정(+) 두 장이 한 쌍**인데 한쪽만 취소할 수 있었다.
- **`cancelled`를 종단 상태로** — 웹훅 갱신문에 `status <> 'cancelled'`를 넣었다. 사람이 사유를 적고 내린
  판단을 늦게 도착한 외부 신호가 뒤집으면 사유가 지워지고 재시도 버튼이 되살아난다.
  상태를 덮는 대신 `[팝빌웹훅][대사필요]`로 크게 남긴다 — 조용히 지나가면 아무도 모른다.
  (→ **0807(2)에서 형태 정정** — WHERE 제외는 승인번호까지 버려서, CASE 분기로 상태·사유만 지키고
  승인번호는 기록하는 증거 내구화로 바꿨다. 아래 추기)
- ⚠ **자동 발행(컨펌 없이 3일) 정책은 아직 한 번도 실행된 적이 없다**(2026-08-07 실측).
  `issued` 33건 중 32건은 고객 컨펌이고, 나머지 1건(한국고용노동교육원)은 기한보다 **이틀 앞서** 나간
  0805 결함 건이다(§8-3 컨펌 관문으로 이미 닫힘). `pending → due` 전이만 실행 이력이 없고,
  그 뒤 구간(`due → ready → 발행`)은 매일 돌고 있다. **첫 실행 = 2026-08-08 09:00 · 13건.**
  기한 = `min(발송일 KST +3일 09:00, 익월 10일 00:00)` — 3일 경로는 09:00, 캡은 00:00으로 시각 기준이 다르다.
  ★**2026-08-08 실측 — 첫 실행 성공.** 13건 전부 `issued` · `is_test=f` · 승인번호 운영 형식(`41000203`) ·
  09:00 틱 10건 + 09:05 틱 3건(발행 패스 회당 10건 제한 그대로 — 정상). 참조 재전송 큐도 2건 `sent`.
  mobile@ 수신분은 없음 — mobile@는 `billing_recipients`에 **인비토(자사) 대표 수신자로만** 등록돼 있고
  인비토는 이날 발행 대상이 아니었다(전 건 아카이브 수신은 §7 `mobile@` 별건 착수 후).

**0807(2) 추기 — 재시도 관문 Codex 종결(2라운드 상한 도달).**

- **1R ① [high] 수용 — 웹훅 `status <> 'cancelled'`가 증거까지 버렸다.** 취소가 먼저 커밋된 경합에서
  늦은 304의 승인번호(외부 발행의 유일한 증거)가 콘솔 로그에만 남고, cancelled 제외 멱등과 만나
  같은 건의 새 원본이 나갈 수 있었다. ⇒ **cancelled 행도 갱신문이 지나가되 상태·사유는 CASE 첫 분기가
  지키고 승인번호만 기록**(증거 내구화). 그 증거가 있는 건은 **재발행 길목 4곳**(워커 moved CTE ·
  작성일자 지정 `TAXBILL_RECONCILE_REQUIRED` 409 · 정산 삭제 가드 · 운영 재발행 dup)이 fail-closed로
  막는다. 0행 fallback SELECT는 제거 — 구조를 고치니 덧댄 장치가 사라졌다.
- **1R ② [high] 부분 수용 — `expected_status`는 ABA를 못 잡는다.** `taxbill_issues`에는 세대 컬럼도
  `updated_at`도 없다(실측 18컬럼) — 토큰 CAS는 DDL 선행이라 §7 전이 CAS 별건이 소유. 실피해
  (외부 실존 문서의 취소→재발행)는 ①의 증거 내구화가 차단.
- **1R ③ [medium] 수용 — 음수 판정을 양수 화이트리스트로.** 무관문 재시도 = `kind='original'`만
  (수정 장은 확인+사유), 취소 = 원본 또는 수정 코드 2·4·6만(우리 생성 코드 1·2·4·6 중 1은 부·정 쌍).
  미지·손상 값은 409(`TAXBILL_KIND_UNKNOWN`) fail-closed.
- **2R ① [high] 수용 — 운영 재발행 dup 체크에 `failed` 추가.** 같은 billing의 운영 원본이 `failed`여도
  (305 = 팝빌에 문서 실존 가능·승인번호 없음) 테스트베드 행을 운영 `ready`로 되돌릴 수 있었다 —
  서로 다른 관리키의 실문서 중복. failed의 올바른 처치는 그 행의 [재시도]다. 실위험은 현재 0에 가깝다
  (`is_test=true` 잔여 0 — 0806 재발행 12장 전부 완료·되돌리면 false 전환).
- **2R ② [high] 불수용·별건 승격 — "승인번호 없음 = 외부 부재" TOCTOU.** 늦은 304 도착 전에
  취소→재발행이 앞서는 창은 세대 토큰으로도 못 닫는다(Codex 자인). §7 getInfo 대사 별건 보강으로 승격
  (대사 상태 내구 기록 · 발행 직전 재검사 · billing 단위 직렬화). "대사 전 재발행 전면 금지" 처방은
  getInfo 경로가 없는 지금 넣으면 0805에 닫은 결함(취소 후 재발행 영구 불능)이 되살아나 불수용.

### 8-5. 0806(2) 상세 — 서수란 접수 2건(더화이트·무주덕유산리조트)

두 건은 성질이 다르다. 하나는 문서의 결함이고, 하나는 **코드 변경 없이 답한 건**이다 —
후자는 "우리 구간에 결함이 없다"를 확정한 것이 **아니다.** 확정할 기록이 없다는 것이 이 건의 결론이다.

- **PDF 구분값 말줄임(더화이트)** — §2-16. 뿌리는 고정 행 높이였고, `lineBreak: false`가 막아준다는 전제가
  1페이지 항목표에도 같이 있어 함께 닫았다(전수 grep — 그쪽은 폭 여유가 커서 아직 안 터졌을 뿐이다).
  일자 칸의 남는 폭 21pt를 구분으로 옮겼다(일자 55→34 · 구분 70→91, 합이 같아 유형 이후 7열 좌표 무변경) —
  `B0228 / 제이씨패밀리`(79.1pt)·`B0082 / 금강제화`(63.1pt)가 한 줄에 들어간다. 그전에는 셋 다 잘렸다.
  ⚠ **접수 본문의 "엑셀 첨부" 갈래는 착수하지 않았다** — 기대 동작 칸이 PDF 전체 출력으로 좁혀져 있다(§7).
- **발송 시점(무주덕유산리조트 08-06)** — 업체 수신이 11:35, 우리 기록이 10:15:27이었다. 코드 변경 없음.
  발송 경로(`POST /bulk/retry-confirmations`)는 `transporter.sendMail`을 `Promise.race`로 기다린다.
  ⛔ **200 응답과 `emailed_at`만으로는 성공을 단정할 수 없다**(Codex 4R high). `emailed_at`은 SMTP **앞에서** 찍히고,
  60초 타임아웃 경로는 발송 불확정인 채로 COMMIT하며, 이 엔드포인트는 `targeted=0`도 `mailFailed>0`도 200으로 돌려준다.
  판정은 **두 층으로 갈라서** 말해야 한다. 축을 하나씩 더하며 결론을 붙드는 방식은 3라운드 연속으로 반례가 나왔다 —
  **지난 사건의 귀속을 사후 흔적으로 확정하려 한 것 자체가 뿌리다**(Codex 4R·5R·6R이 같은 부류).
  - **구조 사실(코드가 보장한다)** — **`sendMail`이 타임아웃 전에 성공으로 완료된 요청**은 그 인계가 응답 전에 끝난다.
    ⛔ 반례는 **타임아웃 경로 하나**다 — `Promise.race`는 60초에 경주만 끝낼 뿐 `sendMail`을 취소하지 못하므로,
    그 경로에서는 **응답이 나간 뒤에 인계가 끝날 수 있다**(그래서 로그가 `[발송불확정]`이다. Codex 9R medium).
    이 건은 961ms라 그 반례에 해당하지 않는다.
  - **그 건의 귀속(정황이고 확정이 아니다)** — `emailed_at` 10:15:27이 그 요청의 처리 창 안이고, 그날
    `[일괄발급]` 실패 마커 아홉 가지(적재실패·장없음·이미적재·정합실패·PDF실패·중복차단·발송불확정·확정실패·메일실패)가
    0건이며, 이 장에 대한 `send-email` 호출도 하루 전체에 없다. **다만 확정은 아니다** — `emailed_at`은 가변이고
    쓰는 경로가 여럿(일괄발급 러너·`send-email` 무조건 덮어쓰기)이며, **성공한 내부 호출에는 장별 마커가 없어**
    로그로 배제되지 않는다. 응답 본문(`targeted`·`summary.sent`)은 어디에도 남지 않는다.
  ⇒ 업체에 답할 수 있는 것은 **"`sendMail`이 타임아웃 전에 성공한 요청은 응답을 돌려주기 전에 메일 서버로 넘긴다(구조)"**와
    **"그날 기록은 그것과 일치한다(정황)"**까지다. 앞 문장은 **모든 클릭에 대한 보장이 아니다** —
    `targeted=0`·`mailFailed>0`·타임아웃 경로도 같은 200을 돌려준다.
    "80분이 전부 외부 구간"이라고 단정하지 않는다 — 그 확정은 업체 메일 원문의 Received 체인이 한다(§7).
  ⚠ 이 한계는 서술로 메울 것이 아니라 **기록이 없어서 생긴 것**이다 ⇒ 불변 발송 시도 기록이 §7 과제다.
  ※ 자동 발행 승격(`markConfirmationDelivered`)은 회사 정책이 `manual`이면 하지 않는 것이 정상이다 — 실패가 아니다.

### 8-4. 0806 상세 — 테스트베드 12장은 **국세청에 안 나간 것**이었다

운영 전환(KST 12:30) 전에 발행된 12장(**20,066,393원**)이 국세청에 등록되지 않은 채 장부는 `issued`,
화면은 `발행 완료`였다. **어느 환경으로 나갔는지가 어디에도 없어** 승인번호 모양(`8888…` vs `41000203…`)과
시각 경계로 추측해야 했고, 홈택스 미조회로 뒷받침한 뒤에야 확정됐다. ⇒ §2-13-A(발행 환경을 행에 적는다).

- **재발행 실행 완료**(2026-08-06 KST 13:10~13:15) — 12장 전부 성공. 문서번호는 그대로 두고 같은 번호로
  운영에 나갔다(테스트와 운영은 분리 환경이라 중복이 아니다). 승인번호만 `41000203…`으로 새로 찍혔다.
- **이중발행 아님 — 팝빌 `search`로 확정.** 7~8월 운영 발행분이 **총 29장**이고 우리 장부 `issued` 29건과 1:1이다.
  회사·계정별로 정확히 한 장씩이고 `8888…` 문서는 운영 목록에 하나도 없다.
  업체가 말한 "2장"은 **문서 두 장이 아니라 메일 두 통**이었다(어제 무효분 + 오늘 정식분).
  ⇒ 고객 대조 축 = **승인번호**. 8이 반복되면 무효, `41000203`이 정식.
  ⚠ 공급받는자 상호가 우리 회사명과 다른 곳이 여럿이다(나인=바이와이제이 · 크로커다일 알파=던필드알파 ·
  송지오옴므=송지오인터내셔널 · 금강제화 상설점=신환 · 메트로시티=엠티콜렉션 · 제주한라병원=한라의료재단).
- **고객이 무효 문서를 이미 받았다** — 테스트베드 발행도 수신자 주소를 그대로 넣어 보낸다.
  연락 온 곳만이 아니라 **12곳 전부에 갔다고 본다.**
- ⛔ **크로커다일에 실패한 취소 장이 남아 있다**(`failed` · -3,903,325 · 승인번호 없음). 이중발행으로 보고
  수정발행(취소)을 시도했다가 실패했다. **[재시도]를 누르지 않는다** — 지금 국세청에 있는 알파 장(`…366e`)이 맞는 문서다.
  ⚠ **2026-08-07 정정** — 이 항목의 옛 서술("당초 승인번호가 테스트베드 것이라 운영에 원본이 없어 실패")은
  **실측과 다르다.** 당초승인번호는 `20260801410002030000366e`로 **운영 형식**(`41000203…`)이고,
  팝빌이 준 사유는 `-11001056: 당초 (세금)계산서 국세청 전송이 완료되지 않아 수정(세금)계산서를 발행할 수 없습니다`였다.
  원본이 없어서가 아니라 **국세청 전송이 아직 안 끝나서**다.
  ⛔ 그래서 **"재시도해도 아무 일도 일어나지 않는다"는 전제가 깨졌다.** 08-07 15:21에 NTS 웹훅이 대량 도착해
  전송이 끝났다면 지금 재시도는 **성공하고 정상 문서가 취소된다.** ⇒ 재시도 관문을 세웠다(§8-6).
- **품목 거래일자** — payload `detailList`에 금액만 넣고 공급 연월일을 안 보내 문서 품목 줄의 월·일이 빈 칸으로
  나갔다(금강제화 발행분 실물 확인). 팝빌 문서로 `purchaseDT`·`yyyyMMdd` 확정. 작성일자를 **한 변수**로 만들어
  거기서 파생시킨다 — 따로 계산하면 문서 상단과 품목이 갈리고 그건 수정발행으로만 고친다(Harold 확정: 일치해야 한다).
  ⚠ 이 정정 **이전에 나간 문서**는 그대로 빈 칸이다 — 소급은 §2-8 축이라 손댈지는 Harold 판단(§7).

### 8-3. 0805(2) 상세 — 서수란 접수 5건

접수 5건 중 셋(④⑤①)이 **같은 모양**이었다 — 판정 축이 진실보다 좁거나, 길을 하나로 합치면서 남은 쪽을 안 만들었다.

- **최소과금 발행이 요금제 미가입 회사를 거부**(씨티케이이비전) — 가드가 `companies.plan_id` **존재**만 봤다.
  체험이 끝나면 Cron이 `plan_id`를 FREE(월정액 0)로 강등하므로 그 값은 남는다(실측: `plan_code=FREE` ·
  `monthly_price=0` · `subscription_status=trial_expired`). 화면의 "요금제 미가입"은 `plan_id`가 비었다는 뜻이 아니라
  `plan_code='FREE'`라는 뜻이다(`plan-guard.ts` `isUnsubscribed`).
  막아야 하는 것은 구독료와 정액이 같은 달에 겹치는 이중청구 하나뿐이고, 월정액 0원은 겹칠 것이 없다.
  ⇒ 판정을 **그 기간 요금제 청구액**(발행 코어와 같은 `loadPlanChanges → buildPlanSegments → sumPlanSegments`) **+ 현재 월정액**
  두 축의 합집합으로 바꿨다. 기간 축은 "그 달엔 유료였다가 지금 강등된 회사"를, 현재 축은 "유료인데 이력이 유실된 회사"를 잡는다.
  판정은 **advisory lock 안**에서 한다 — 밖에서 하면 소급 요금제 변경이 그 사이에 들어와 구독료가 있는 달에 정액이 나간다.
  실측으로 이 회사는 이력이 `07-20 → FREE(0원)` 한 행뿐이라 두 축 모두 0이고 통과한다.
  ⚠ **두 축이 0이어도 이력이 손상됐으면 그 0을 믿지 않는다**(Codex 1R high) — 그 달엔 유료였는데 이력이 끊겨 기간 축이
  0으로 나오고 지금은 FREE인 회사가 그대로 통과한다. 판정을 새로 만들지 않고 **발행 코어가 쓰는 `evaluatePlanHistoryGate`를
  그대로 지난다**(앞 공백은 사실이라 통과, 끊긴 이력·"플랜은 있는데 이력 0건"만 차단). 사용량 집계가 도는 동안 들어온
  소급 변경은 커밋 직전 `planChangesFingerprint` 재대조가 잡는다 — 원장 지문은 요금제 이력을 감시하지 않는다.
- **날짜 지정이 컨펌을 건너뜀** — §2-13. `taxbill_status='ready'` 쓰기 3곳 중 **직접선택 경로만** 관문이 없었다.
- **대리 컨펌 신설** — §2-13. 컬럼 2개(`confirmed_by_admin`·`confirm_note`) ALTER. 목록 조회는 `to_jsonb`로 읽어
  ALTER 전에도 현황판이 깨지지 않는다(`plan-guard`의 `advanced_access_enabled`와 같은 규약).
- **미수신 재발송 창구 부재** — §2-14. 거래내역서는 0804에 발송 경로를 컨펌 하나로 합치면서 **화면 진입점만** 사라졌다
  (서버 `send-email`은 그 뒤 컨펌 토큰 확보·승격까지 갖춰 걷어낸 이유가 남아 있지 않다). 계산서는 창구 자체가 없었다.
- **발급 대기 취소 신설**(★같은 세션 추가) — `ready` 장을 되돌리는 경로가 **고객 이의신청 하나뿐**이라, 담당자가 발행 직전 금액 오류를 발견해도 5분 뒤 워커가 국세청으로 보냈다. 정산 삭제는 취소 수단이 아니었다(`billing_id`만 SET NULL, 장부는 `ready` 그대로라 그대로 발행됐다).
  `POST /taxbill-issues/:id/cancel` — **`ready`만** 받는다(`submitted`는 팝빌에 올라갔을 수 있어 되돌리면 거짓 취소, `issued`는 §2-8 축). 장부를 `cancelled`로 내리고 추적행을 **`manual_wait`으로** 내린다 — `ready`로 두면 워커는 `confirmed`·`due`만 집으니 다시 발행되지 않고 §2-8 가드는 `ready`를 막으니 고쳐서 다시 낼 수도 없는 막다른 상태가 된다.
  ⛔ 함께 닫은 것 — 워커·작성일자 지정의 멱등 판정(`NOT EXISTS`)이 `kind='original'` 행의 **존재**만 봐서, 한 번 `cancelled`가 된 추적행은 **영원히** 새 장부를 못 만들었다(이의신청 건도 같았다). 두 곳에 `status <> 'cancelled'`를 넣었다 — 진행 중·완료 행은 그대로 막으므로 이중 발행은 없다.
- **정산 목록 일괄 확정·발송** — 새 벌크 엔드포인트를 만들지 않고 기존 엔드포인트를 건별 순차 호출한다.
  서버에 벌크 문을 하나 더 두면 발행 코어와 중복 발송 확인(409)을 우회하는 두 번째 길이 생긴다.
  ⚠ `bulk/retry-confirmations`는 **한 통도 못 나가도 `success: true`**를 돌려준다(금액 불일치·PDF 장애·이메일 미등록).
  HTTP 상태로 세면 막힌 장이 "발송 완료"로 잡혀 아무도 다시 안 본다 ⇒ 판정 축은 `summary.sent`다.
  이미 발송된 장은 일괄 대상에서 빼고 개별 [재발송](409 확인)으로 보낸다.

### 8-2. 0805 상세 — 재오픈 두 건은 **같은 뿌리**였다

두 접수 모두 0804에 고친 자리에서 다시 났고, 원인이 같다 — **한 축만 고치고 다른 축을 예외로 남겼다.**

- **수량 조정(제주한라병원 LMS 9,438→9,435)** — 음수 판정 키가 `채널·유형·발송ID·단가`인데
  인쇄 줄(`buildInvoiceLines`)은 `채널·유형·단가`다. **판정이 인쇄보다 좁으면 가려진 음수를 더 잡는 게 아니라
  정상 조정을 오거부**한다. 조정 행의 `agent_id`가 NULL이라(실측) 조정만 홀로 떨어져 합계가 -3이 됐다.
  0804에 계정(userId) 축을 같은 이유로 뺐는데 **발송ID 축을 안 뺀 것**이 재오픈이다. ⇒ 키에서 발송ID 제거.
  판정 축 = 인쇄 줄 축. **테스트가 0건이던 함수라 회귀 테스트 9건 신설**(`billing-qty-adjust.test.ts`).
- **080 추가청구(리스킨)** — 0804에 "청구 여부·귀속은 매핑 원장이 진실"로 바꾼 것이 **현행 종류(`080_call`)에만**
  적용되고 옛 종류(`080_fee`·`080_svc`)는 자기 값으로 계속 살았다. 리스킨은 `080_call`이 없어
  `legacy_superseded`에도 안 걸리고, `blocks_issue`는 `080_call`만 보므로 발행도 안 막혔다 ⇒ 매핑을 인비토로
  옮겨도 리스킨에 9,000+4,000이 계속 실렸다(실측 2행).
  ⛔ 그냥 매핑을 보게 하면 **원장 도입 전 옛 행까지 막혀 과소청구**가 된다. 지금 매핑 JOIN이 회사로 조인해
  **"타사로 옮겨졌다"와 "매핑이 아예 없다"를 구분하지 못하는 것**이 진짜 문제였다 ⇒ `map_exists_any`(번호 기준
  존재 여부)를 하나 더 읽어, **매핑이 있는데 이 회사 것이 아니면 옛 행도 청구하지 않는다.** 매핑이 없으면 종전대로.
  영향표 실측 = 미청구 옛 행 24건 전부 매핑 존재, 그중 타사 이전은 **리스킨 2행뿐**이라 나머지 22행 무변화.
  화면에도 `moved_to_other_company`로 사유를 드러낸다(조용한 제외 금지).

**왜 리스킨은 안 옮겨졌나 (0805 추적 — 마지막 한 걸음만 남음)**

0804에 "매핑의 회사를 바꾸면 미소비 항목 행도 같은 트랜잭션에서 함께 옮긴다"를 넣었는데 리스킨은 안 옮겨져 있었다
(매핑은 인비토, 항목 행은 리스킨). 실측 타임라인이 원인을 하나로 좁힌다.

| 시각(KST) | 무슨 일 |
|---|---|
| 08-03 14:46:22 | 매핑 생성(리스킨) |
| 08-03 14:48:05 | 옛 반영기가 항목 2행 생성(`080_fee`·`080_svc`, 리스킨) |
| 08-04 09:52:48 | 매핑을 인비토로 수정(`updated_at`) |

항목 행이 매핑 수정보다 **하루 앞선다** ⇒ 수정 시점에 대상 행이 있었다. 그리고 이동이 안 도는 다른 경로는 전부 배제된다 —
`relinked`는 참(회사가 바뀜) · 409였다면 매핑도 롤백됐을 것 · 삭제 후 재등록이었다면 `updated_at`이 `created_at`과 같을 것.
⇒ **남은 설명은 "08-04 09:52에 이동 로직이 아직 배포 전이었다" 하나다.** 항목 종류가 옛 `080_fee`·`080_svc`인 것도
같은 방향이다(0804부터 신규 생성은 `080_call`뿐인데 이 행들은 08-03 생성).

확정 방법 = `git log -S'미소비 반영분도 함께 옮긴다' -- packages/backend/src/routes/billing.ts`의 커밋 시각이
08-04 09:52 KST보다 뒤인지. **뒤면 일회성이고 추가 수정이 필요 없다** — 지금은 이동 로직이 배포돼 있고,
남은 잔재 2행은 위 `map_exists_any`가 청구에서 뺀다. 앞이면 이동이 돌았는데도 안 옮겨진 것이라 그때 다시 판다.

⚠ **한계(기록)** — `map_exists_any`는 "사람이 매핑을 지웠다"와 "원장 도입 전 행이라 매핑이 없다"를 구분하지 못한다.
둘 다 `false`가 되어 옛 값으로 청구된다. 지금은 미청구 옛 행 24건 전부 매핑이 있어 실제로 걸리는 건이 없다.

**총 정산표(신설)** — 소유자 전용 집계. `utils/audit-log.ts`에 `isSettlementOverviewViewer`
(ENV `SETTLEMENT_OVERVIEW_VIEWER_IDS`, 기본 `ceo` — 감사 로그·AI 학습과 같은 규약, 별도 env) +
`GET /api/admin/billing/overview{,/access}` + `SettlementOverviewModal`(정산 목록 헤더 진입점).
원천은 `billings` 하나이고 **미납 기본은 누적**이다 — 월로 끊으면 지난달 못 받은 돈이 화면에서 사라진다.
상단 카드는 아래 표의 합에서 만든다(따로 세면 둘이 갈린다). DDL 0.

**확정 축 정리** — §3-2의 두 "확정" 혼동(서수란 질의)에 대한 화면 조치. 버튼을 발행 액션과 구분선으로 가르고
라벨을 `청구 확정`으로, 툴팁에 "수금 관리용이며 발송·세금계산서와 무관"을 적었다. **상태 전이 로직은 무변경** —
`draft → confirmed → paid`가 수금 축이고 [수금완료]가 총 정산표의 미납에서 빠지는 그 축이다.

### 8-1. 0804(2) 상세

- **기간축 차단 판정 3벌 복제를 `readBillingPeriodConflicts` 하나로 통합**하고 미리보기가 같은 문을 보게 했다(§2-4). "미리보기 통과 후 발행 실패" 종결.
- **`/preview` 화면 배선** — 만들어진 뒤 호출부가 0건이던 엔드포인트. 발행 확인 모달이 금액·차단 사유를 먼저 보여주고 차단 시 발행을 잠근다.
- **유형별 수량 버튼** — 목록 SMS·LMS 2컬럼 → 버튼(§3-3).
- **해지 회사 일괄발급 제외**(서수란) — 목록엔 뱃지로 남기고 담기에서만 뺀다. **해지 시각 컬럼이 없어**(status만 바꾸고 `updated_at`은 다른 수정으로도 갱신) "해지 이후 기간만" 판정이 불가능하기 때문이다. 미청구분은 단건 발행으로.
- **발송 경로 통일**(서수란) — §3-2. 죽은 "거래내역서 목록"(`billing_invoices`) 섹션도 제거했다(만드는 진입점이 화면에 없어 영원히 비던 잔재).
- Codex 2R — critical(게이트 JOIN 테넌트 격리) 수용, high(TOCTOU 2건) 불수용·[BUGS.md](../status/BUGS.md) B-0804-1 기록.

---

## §9 설계서 (시점별 근거 — 이 문서가 대체하지 않는다)

| 축 | 문서 | 읽는 범위 |
|---|---|---|
| 발행 단위·정정·성능 | [2026-07-26-billing-scope-and-corrections-design.md](2026-07-26-billing-scope-and-corrections-design.md) | §0-A(확정값) → §9(이월) · 절사 = §9-5-A · 속도 = §9-9 |
| 일괄발급·컨펌·세금계산서 | [2026-07-28-bulk-invoice-confirm-taxbill-design.md](2026-07-28-bulk-invoice-confirm-taxbill-design.md) | §9(종결) · §7-0(팝빌 요지) · §4-2(0804 3종) |
| 브랜드메시지 청구 | [2026-07-29-brand-message-billing-design.md](2026-07-29-brand-message-billing-design.md) | §7(남은 것) · 구조 = §1~§5 |
| 특례·발행 그룹 | [2026-07-30-billing-extras-and-groups-design.md](2026-07-30-billing-extras-and-groups-design.md) | §2-4(원장 파생) → §9 → §7(확인 5건) |
| 구분 칸(웹 계정명·발송ID) | [2026-07-31-billing-detail-storeid-handoff.md](2026-07-31-billing-detail-storeid-handoff.md) | 전체(짧다) |
| 0725 착수 기록 | [2026-07-25-billing-restructure-handoff.md](2026-07-25-billing-restructure-handoff.md) | 필요 시 |

컬럼 = [SCHEMA.md](../status/SCHEMA.md) 해당 절 · 활성 버그 = [BUGS.md](../status/BUGS.md)
