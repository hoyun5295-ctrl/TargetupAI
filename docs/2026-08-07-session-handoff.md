# 2026-08-07 세션 인계 — 접수 4건 + 정산 점검

> ✔ **2026-08-08 종결 — 착수 항목 4건 전부 닫히고 전량 배포됐다. 이 문서로 다시 진입하지 않는다.**
> 남은 것은 **실측 3건**(§0 하단)과 **별건 §3**(착수 판단 = Harold)뿐이다.
> 완료분의 구조·이력은 각 기능 문서가 소유한다 — 정산 = [FEATURE-BILLING.md](FEATURE-BILLING.md)(§7 남은 것 · §8-6 0807·0807(2) 이력).
> 화면 축 2건(슈퍼관리자 알림·임시보관 채널)의 경위 = [LESSONS_FRONTEND](../status/lessons/LESSONS_FRONTEND.md) 핵심원칙 상단 2항.

---

## 0) 배포 상태 — 이것부터 확인한다

**2026-08-08 전량 배포완료** — 아래 5건 + ENV `TAXBILL_ARCHIVE_BCC` 등록(`pm2 restart all --update-env`).
이 인계의 착수 항목은 남지 않았다. 남은 것 = **실측**(아래)과 §3 별건(착수 판단 = Harold).

| 변경 | 코드 | Codex | 배포 |
|---|---|---|---|
| 정산 거래내역서 PDF 구분값 말줄임 폐기 | 완료 | 11R 종결 | **배포완료**(커밋 `1a2921c9`) |
| **모바일DM 발행 중지·재개** | 완료(+0808 4R 잔여 반영) | **4R 종결 — 잔여 0** | **배포완료** |
| **실패 계산서 재시도 관문 + 0807(2) 정정** | 완료 | **2R 종결(상한 도달 — 수용분 반영·1건 별건 승격)** | **배포완료** · 이력 = [FEATURE-BILLING §8-6 추기](FEATURE-BILLING.md) |
| **③ mobile@ 발행 메일 아카이브(0808)** | 완료 | **2R 종결(상한 — 판정 2건은 §7 별건)** | **배포완료**(ENV 등록 포함) |
| **④ AI자연어 수신자 필드(0808)** | 완료 | 대상 외(UI·표시 축 — 0804 확정 범위) | **배포완료**(frontend `build:safe` 포함) |

**실측 대기 3건** — ①다음 발행 1건에서 mobile@ 아카이브 수신(로그에 `[아카이브미기록]`이 없어야 정상)
②수정(취소·정정) 장 [재시도]에 확인 모달·사유 요구가 뜨는지(크로커다일 건 — **이제 코드가 막는다**)
③직접타겟발송 AI 자연어로 조건 추출 후 목록 열·`%변수%`에 조건 필드가 뜨는지.

---

## 1) 다음 세션 첫 작업 — 순서 고정

### ① 실패 계산서 재시도 관문 Codex — **0808 완료(2R 상한 종결)**
결과·취사 판단·정정 내용 전부 = [FEATURE-BILLING §8-6 "0807(2) 추기"](FEATURE-BILLING.md)가 소유.
수치 = backend tsc 0 / vitest 141파일 2,086건 / 회귀 주입 2회 검출 / PREPARE 7문 실검증(Harold) / DDL 0.
**②③④ 전부 0808 완료 — 이 인계의 착수 항목은 남지 않았다.** 남은 것 = 통합 배포(+ENV 등록·frontend build) · §3 별건(착수 판단 = Harold).

### ② 모바일DM 중지 — Codex 4R 잔여 → **0808 반영 완료(잔여 0)**
- **[high] 교차 테이블 경쟁** — 확정해 둔 판단대로 잠금 공사 대신 **응모 제출 길목**을 막았다.
  `submitEventResponse`의 INSERT가 **같은 문장 안에서** `dm_draw_runs` 존재를 판정해(lucky_draw만,
  가드 축은 별도 boolean 파라미터 — 이중 문맥 42P08 부류 회피) claim 이후 응모를 409로 거절한다.
  `draw_at`은 축이 아니다 — claim 전 응모는 추첨 모수에 들어가므로 막을 이유가 없다.
- **[medium] 실패 사유 보존** — tagged outcome으로. `stopDm`·`resumeDm`이 전이 UPDATE와 **같은 문장·같은
  스냅샷**에서 사유 태그(`ab_running`/`drawn`/`not_published`/`not_stopped`/`not_found`/`race`)까지 돌려주고,
  문구는 CT `DM_TRANSITION_BLOCK_MESSAGES`가 소유한다. 별도 사유 조회 `findDmTransitionBlock`은 **삭제**
  (구조를 고치니 덧댄 장치가 사라졌다). 스냅샷상 원인이 없는 0행(`race`)은 단정 없이 새로고침만 안내.
- ✅ medium 1건(모달에 "중지 중에도 예정 추첨은 실행되고 그 뒤엔 재개 불가")은 0807에 반영 완료.
- 검증 = backend tsc 0 / vitest 141파일 2,089건(신규 3) / 회귀 주입 2회 검출 확인 / DDL 0 / frontend 무변경.

### ③ `mobile@invitocorp.com` 발행 메일 참조 — **0808 구현 완료(Codex 2R 상한 종결)**
경위·판정·잔여 별건 2건 전부 = [FEATURE-BILLING §7 해당 항목·§8 색인 0808행](FEATURE-BILLING.md)이 소유.
요지 — 설계와 달리 **확정 트랜잭션 밖(COMMIT 뒤) best-effort**로 확정됐다(트랜잭션 안 SAVEPOINT 격리는
Codex 1R이 깼다). ⚠ **서버 ENV `TAXBILL_ARCHIVE_BCC=mobile@invitocorp.com` 등록 전에는 동작하지 않는다.**

### ④ 직접타겟발송 AI자연어 수신자 필드 — **0808 구현 완료** (접수 · 임은지 08-05)
확정 축 그대로 구현했다 — **`filter`가 참조한 필드**(그 키가 곧 조건 필드)가 서버 샘플과 프론트 `meta`를
함께 채워 미리보기와 발송 목록이 같은 값을 본다.
- 서버: `previewMatching`이 filter 키를 **FIELD_MAP(단일 기준)**으로 풀어 샘플 SELECT에 싣고
  (`custom_*`은 JSONB 경로 · FIELD_MAP 밖 키는 fail-safe 제외 · `sms_opt_in` 제외),
  라벨 목록 `sampleFields`(FIELD_MAP `displayName` 파생)를 응답에 더했다. 소비처 3곳(자연어·saved-segments·
  onboarding) 모두 가산적, 렌더러 4곳은 고정 컬럼이라 무영향.
- 프론트: AI 적용 시 `meta`를 filter 키 + phone으로 구성(라벨 = 회사 스키마 `enabledFields` 우선 →
  서버 `sampleFields` — 별도 라벨 테이블 없음), 샘플 표에 조건 필드 열·헤더 추가.
- 신규 테스트 `ai-segment-generator.test.ts` 2건(JSONB 경로·fail-safe 제외 계약 — 회귀 주입 검출 확인).
  Codex 대상 외(UI·표시 축 — 0804 확정 범위). 실측 = 화면에서 자연어 조건(예: VIP 등급) 추출 후
  목록 열·%변수% 확인 1건.

---

## 2) 서버에서 확인된 사실 (코드와 무관 · 실측 기록)

- **2026-08-08 09:00에 13건 자동 발행 첫 실행.** 워커 생존·팝빌 `★운영★`·end-to-end 동작 08-07 확인 완료.
  `pending → due` 전이만 실행 이력이 없다. 상세 = [FEATURE-BILLING §8-6](FEATURE-BILLING.md)
- **거래내역서 발송 지연(무주덕유산리조트) 80분은 미확정.** 완료 조건 = 업체 메일 원문 Received 체인 확보.
  코드가 보장하는 것은 "`sendMail`이 타임아웃 전에 성공한 요청이면 인계가 응답 전에 끝난다"까지다
- **구분 칸에서 발송ID를 뺄지** — Harold 제안. 판정 축은 **같은 발급명을 쓰는 발송ID가 있는가** 하나다.
  확인 문 = `RSRM_SalesMst`의 `CustNm` 중복 집계(MySQL). 아직 실행 전

---

## 3) 별건 (이번 세션에서 발견 · 착수 판단 = Harold)

정산 축은 전부 [FEATURE-BILLING §7](FEATURE-BILLING.md)에 등재했다. 그 밖:

- **`publish`·`send-to-target` 핸들러에 `canAccessDm`이 없다** — 같은 회사 안에서 남의 DM을 발행·발송할 수 있다(기존 결함)
- **A/B 시작·수정의 원자성 부재** — `updateAbTest`/`startAbTest`가 검증과 전이를 분리해, 동시 요청으로
  타사 published page UUID를 running 테스트에 넣을 수 있다
- **발송 intent 직렬화·예약 취소 부재** — DM을 중지해도 이미 예약된 발송은 나간다. 캠페인에 `dm_id`가 없어
  나중에 취소할 대상을 찾지도 못한다. 중지 모달에 그 한계를 명시해 뒀다
- **추첨 claim 실패 시 run 행 잔존** — 그 추첨의 재시도가 영구 차단된다
- **여정·자동마케팅 본문에 붙여넣은 내부 DM URL** — 일반 URL로 재단축돼 중지 후에도 발송된다

---

## 4) 이번 세션에서 배운 것 (반복 방지)

- **상태값 하나를 만들면 그것을 소비해야 하는 축이 몇 개인지 먼저 센다.** DM 중지는 넷이었다(공개 노출·발송·
  A/B 실행·이벤트 수명주기). 축마다 가드를 덧대면 라운드마다 새 구멍이 난다 — **충돌 상태에서 전이 자체를 거절**하는 쪽이 가드를 늘리지 않는다
- **`failed`·`ready` 같은 상태 이름이 외부 사실을 보장하지 않는다.** `failed`에는 "팝빌에 문서가 없다"와
  "발행됐는데 전송만 실패했다"가 섞여 있다. 외부 사실은 외부에 물어야 한다
- **지난 사건의 귀속을 사후 흔적으로 확정하려 하지 않는다.** 축을 하나씩 더해도 반례가 계속 나온다 —
  기록이 없어서 생긴 한계는 서술로 못 메운다(⇒ 불변 발송 시도 기록이 과제가 됐다)
- **전수 정정은 코드만이 아니라 문장에도 적용한다.** 한 문단만 낮추고 같은 주장을 하는 다른 세 곳을 남겼다가
  Codex 4·5·7라운드에서 연속으로 잡혔다
- **템플릿 리터럴 안 SQL 주석에 백틱을 넣지 않는다**(문자열이 끊긴다) · **정규식 리터럴에 제어문자를 직접 적지 않는다**(파일이 바이너리로 잡힌다) — 둘 다 이번에 실제로 밟았다
- **소스 스캔 테스트의 경계는 고정 길이가 아니라 다음 선언까지**로 잡는다. 주석이 늘자 슬라이스가 검사 대상 앞에서 잘려 거짓 실패가 났다
