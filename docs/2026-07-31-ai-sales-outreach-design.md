# 2026-07-31 AI 영업 아웃리치 설계서 (슈퍼관리자 · ceo 전용)

> 상태: **설계 초안 — Harold 검토 대기.** 코드·스키마 미확인 상태에서 작성한 설계이며, CT 재사용 맵·테이블 초안은 구현 착수 시 소스 grep(승인 후)·`information_schema`로 확정한다.
> 근거 대화: 2026-07-31 Harold↔AI 논의. 기존 발판 = 0722 영업용 테스트발송 3채널(배포완료).

## 0. 한 줄 요약

Harold가 영업 대상 업체 리스트만 넣으면, 백그라운드 워커가 업체 홈페이지를 분석해 한줄로 산출물(문안·이메일·모바일 DM·인앱 시안·스튜디오 이미지)을 그 브랜드 맞춤으로 제작하고, Harold가 승인 큐에서 미리보기·수신처 수정 후 **클릭 1회**로 이메일 또는 데모 문자를 발송하는 내부 영업 도구. "우리 서비스 좋습니다"가 아니라 **"귀사 브랜드로 이미 만들어 봤습니다"**를 들고 가는 구조.

## 1. 배경·목적

- 영업 병목: 사람 손으로 업체별 샘플을 만들어 보내는 속도가 한계. 0722에 만든 영업용 테스트발송(수동 3채널)을 **리스트 단위 자동 제작 + 승인 발송**으로 확장한다.
- 사용자: Harold 1인 (ceo 전용 메뉴).
- 제품이 곧 데모: 받는 쪽이 여는 순간 한줄로의 실물 산출물을 본다. 문안·DM·이미지 전부 "한줄로AI로 만든 예시"임을 명시한다.

## 2. 확정된 결정 (2026-07-31 대화 합의)

| 항목 | 결정 |
|---|---|
| 위치 | 슈퍼관리자(sys.hanjullo.com) 내 ceo 전용 메뉴. ENV 허용 목록 게이팅(Operator `AI_OPERATOR_ALLOWED_USERS` 방식과 동일 패턴, 예: `SALES_OUTREACH_ALLOWED_USERS`). 미허용 계정은 메뉴 자체 미노출 + 서버측 라우트 거부 |
| 발송 방식 | **전자동 발송 금지.** 백그라운드는 제작까지만. 발송은 승인 큐에서 Harold 클릭 1회가 유일한 경로 |
| 수신처 | 크롤로 추출한 이메일은 초안일 뿐 — 승인 큐에서 **Harold가 수정 가능** |
| 문자 채널 | 전화번호를 아는 업체는 **데모 문자** 발송 가능. 본문 = 한줄로AI 생성 문안 + 모바일 DM 링크 + "본 문자는 한줄로AI가 생성한 문안과 모바일DM 예시입니다" 고지 |
| 이미지 | 스튜디오 다운로드 개방 없이 **서버 저장 경로 직접 사용** (내부 도구이므로) |
| 첨부파일 | 기본 OFF(스팸 점수 가중). 업체별 스위치로만 ON |
| 발신 도메인 | hanjul.ai 본체와 **분리** (거래 메일 평판 보호). 도메인명 = Harold 확정 필요(§13) |
| 혜택 수치 | AI가 %·원·쿠폰 등 구체 혜택 생성 금지(기존 룰 그대로). 중립 문구 + 큐에서 Harold 수정 |

## 3. 전체 흐름 (파이프라인)

```
[0] 양식 학습 (1회 + 갱신)
    Harold 샘플 세트 → 스타일 가이드 저장 (구성 순서·톤·이미지 스타일·블록 패턴)
        ↓
[1] 리스트 등록 (Harold — 유일한 수동 입력)
    "업체명, 홈페이지 URL[, 이메일][, 전화번호]" 여러 줄 붙여넣기
        ↓
[2] 웹 분석 (워커)
    홈페이지 크롤 → 브랜드 프로필 추출(업종·톤·주력 상품·색·제휴/대표 이메일)
        ↓
[3] 샘플 제작 (워커)
    문안 → 스튜디오 이미지 → 모바일 DM(생성→저장→hlj.kr 발급) → 이메일 HTML → 인앱 시안(프레임 렌더 PNG)
        ↓
[4] 초안 조립 (워커)
    제안 메일(맞춤 서두+인라인 이미지+DM 링크) + 데모 문자 초안 → 상태 'ready'
        ↓
[5] 승인 큐 (Harold — 클릭 1회)
    카드 미리보기 → 수신 이메일 수정 / 채널 선택(이메일·문자·둘 다) → [보내기]
        ↓
[6] 발송·이력
    결과 기록 · 동일 업체 재발송 차단 · 회신 수동 표시
```

- 각 단계 실패는 그 단계에서 멈추고 카드에 사유를 표시한다. 단계별 개별 재시도 버튼. 자동 완화·대체 발송 없음(0건=차단 원칙과 동일 정신).
- [2]~[4]는 업체별 독립 진행 — 한 업체의 실패가 다른 업체를 막지 않는다.

## 4. 화면 설계 (신규 메뉴 = Journey Builder 동급 다크 퀄리티 의무)

메뉴: 슈퍼관리자 > **"AI 영업"** (가칭 — §13). 탭 3개.

### 4-1. 대상 보드
- 상단: 리스트 등록 영역 — 여러 줄 붙여넣기 1회(형식: `업체명, URL[, 이메일][, 전화]`), 등록 즉시 파이프라인 시작. 추가 입력 단계 없음.
- 본문: 업체 카드 그리드 — 상태 칩(등록→분석중→제작중→검토대기→발송됨→회신/실패/건너뜀), 진행 단계 표시, 실패 카드는 사유 + 재시도 버튼.

### 4-2. 승인 큐 (핵심 화면)
카드 1장 = 업체 1곳.
- 좌측(미리보기 탭): ①제안 메일 렌더 ②모바일 DM(단말 프레임, hlj.kr 실링크) ③문안 텍스트 ④인앱 시안(단말 프레임 PNG) ⑤스튜디오 이미지
- 우측(발송 패널):
  - 수신 이메일 — **수정 가능 입력칸** (크롤 추출값은 초안, 출처 표시: "홈페이지 하단에서 추출" / "미발견 — 입력 필요")
  - 전화번호 — 있으면 문자 채널 활성
  - 채널 선택: 이메일 / 문자 / 둘 다
  - 메일 제목·서두 인사말 — 수정 가능
  - 첨부 스위치(기본 OFF)
  - 버튼: [산출물별 재생성] [테스트(내게 보내기)] [보내기] [건너뛰기]
- [보내기]는 20크레딧+ 확인 모달 규칙과 동일 결의 사전 확인 모달(수신처·채널 최종 표시) 1회.
- 미리보기 = 실제 발송본과 **같은 조립 함수**로 렌더(미리보기용 별도 조립 금지 — 이중 진실 금지).

### 4-3. 발송 이력
- 발송 건 리스트: 업체·채널·수신처·발송 시각·결과·회신 표시(수동 토글)·재발송 차단 상태.

## 5. 데이터 모델 초안

> **초안이다.** 구현 착수 시 컬럼·타입을 확정하고, DDL 실행 전 `information_schema` 검증(db_column_verify_before_code) + 코드 배포 후 마이그레이션 순서(migration_after_deploy) + catch의 `DB_MIGRATION_PENDING` 503 분기(db_alter_safety_net)를 전부 적용한다. PG 신규 4테이블, 기존 테이블 변경 0.

| 테이블 | 역할 | 주요 컬럼 초안 |
|---|---|---|
| `sales_outreach_prospects` | 대상 업체 원장 | id, company_name, homepage_url, extracted_email, manual_email(승인 큐 수정값 — 발송은 manual 우선), phone, status, brand_profile(jsonb: 업종·톤·색·주력상품·근거 발췌), fail_stage, fail_reason, created_at |
| `sales_outreach_assets` | 업체별 산출물 | id, prospect_id, kind('copy'\|'email_html'\|'dm'\|'inapp_png'\|'studio_image'), payload(jsonb: 본문/경로/hlj.kr URL/원본 참조 id), regen_count, created_at |
| `sales_outreach_sends` | 발송 이력(멱등 원장) | id, prospect_id, channel('email'\|'sms'), recipient, subject, sent_at, result, provider_ref, **dedupe_key(홈페이지 도메인+채널 UNIQUE)** |
| `sales_outreach_style_guides` | 양식 학습 결과 | id, version, source_samples(jsonb: 샘플 세트 참조), guide(jsonb: 산출물별 구성 규칙·톤 규칙), active |

- 재발송 차단 = `dedupe_key` UNIQUE 제약으로 DB가 강제(화면 검사만으로 안 막는다). 기본 90일 — 기간 해제는 별도 확인 모달 경유로 키에 회차 부여.
- 발송 상태 전이는 서버가 강제: `ready` 상태가 아니면 발송 API 거부.

## 6. 백엔드 구성

- 신규 라우트: `routes/sales-outreach.ts` — 전 엔드포인트에 ceo 게이팅 미들웨어(기능 개방 시 보조 조회·재생성·테스트발송 EP까지 전수 격리 — 0721 역할 격리 교훈).
- 신규 워커: 아웃리치 파이프라인 워커(단계별 상태 머신, 기존 워커 패턴 준수 — 구현 시 grep으로 기존 워커 골격 확인 후 동형 작성).
- 신규 CT: 크롤·브랜드 프로필 추출, 스타일 가이드 적용 조립 등 헬퍼는 전부 `utils/`에 CT로 신설(인라인 금지). 발송 조립 함수는 미리보기와 발송이 공유.

### 6-1. 기존 자산 재사용 맵 (0722 영업 테스트발송 = 직접 발판)

> 아래는 메모리(2026-07-22 기록) 기반 — 구현 착수 시 현재 코드로 재확인한다.

| 필요 기능 | 재사용 대상 | 비고 |
|---|---|---|
| 이메일 실발송(마케팅 footer 없이) | 0722 email test-send 경로 — 저수준 `sendEmail()`(company-smtp-client 계열) 재사용. `POST /api/email/campaigns/:id/test-send` 전례 | 단, 아웃리치는 고객 SMTP가 아니라 **우리 전용 발신 도메인 SMTP** — 발신 설정만 신규, 발송 함수는 재사용. (광고) footer 미부착은 0722 Harold 명시 의도와 동일 판단 유지 여부 §10에서 확정 |
| 이메일 HTML 산출물 | 0722 export-html 경로(EMAIL_FOOTER_SLOT 제거·이미지 절대화 처리 완비) | 이메일 자산 생성의 후처리로 재사용 |
| 인앱 시안 PNG | 0722 인앱 "테스트저장"(html-to-image, 웹 블러 배경·앱 폰 프레임 실물 렌더, `captureMode`) | 서버측 배치 렌더로 전환 필요 여부 확인(현재는 브라우저 캡처) |
| 모바일 DM 생성 | DM AI 생성 경로(0716 AI 퍼스트) + 발행 URL 발급(hlj.kr — URL 발급은 모바일 DM 전용) | 블록 생성→저장→발급을 서버측에서 호출하는 내부 진입점 필요 |
| 스튜디오 이미지 | 0731 슈퍼관리자 예시 배치 생성 전례(크레딧 미차감·공개 서빙) | 동일 방식: 영업 생성분 **크레딧 미차감**, 원가만 인지(이미지 1회 약 352원) |
| 문안 생성 | 기존 AI 문안 생성 경로 + 스타일 가이드 프롬프트 고정부 | 혜택 수치 금지 룰 그대로 적용 |
| 문자 발송 | 기존 QTmsg 발송 경로(LMS) | 발신번호·(광고)·080 = §9·§10 |

- 크레딧·과금: 영업용 생성·발송 전량 **미차감**(내부 도구). 원가(AI 호출·이미지·문자 단가)는 이력에 기록만.

## 7. 웹 분석(크롤) 설계

- 서버측 fetch: 메인 페이지 + 회사소개/About + 푸터 우선. robots.txt 존중, 업체당 페이지 수 상한(기본 5), 타임아웃·요청 간격 두고 순차.
- 추출 항목: 업체명 대조, 업종, 톤·컬러, 주력 상품/진행 중 이벤트(문안 소재), 제휴/대표 이메일(mailto·푸터·contact 페이지 패턴), 전화번호(참고용 — 문자 발송은 Harold 입력·확인 번호만).
- 이메일 오추출 방어: 추출값은 항상 "초안+출처 발췌" 표시. 발송은 승인 큐에서 Harold가 눈으로 확인한 뒤에만. 미발견 시 '이메일 필요' 상태로 발송 비활성.
- 브랜드 프로필에는 **근거 발췌**(어느 페이지 어느 문장에서 업종·톤을 읽었는지)를 함께 저장 — 승인 큐에서 Harold가 맞춤화가 헛짚었는지 즉시 판별.
- 타사 로고·사진 원본을 산출물에 삽입하지 않는다 — 브랜드명 텍스트 + 스튜디오 생성 이미지 중심. (저작권·상표 방어)

## 8. 생성 규칙

- 양식 = `sales_outreach_style_guides` 활성 버전이 프롬프트 고정부. Harold 샘플 세트에서 학습(산출물별 구성 순서·블록 패턴·문단 톤·이미지 스타일). 샘플 세트 추가 시 버전 갱신.
- **혜택 수치(%·원·쿠폰·무료) 생성 금지** — 실존 브랜드에 가짜 혜택이 붙는 사고 차단. 중립 표현("신규 고객 혜택 안내" 수준)까지만, 필요 시 Harold가 큐에서 수정.
- 모든 산출물에 "예시(시안)" 표기 — 받는 쪽이 실제 진행 중 캠페인으로 오인하지 않게.
- 데모 문자 고정 구조:
  `[한줄로 소개 1줄] + AI 생성 문안(브랜드 맞춤) + 모바일 DM 링크(hlj.kr) + "본 문자는 한줄로AI가 생성한 문안과 한줄로AI로 제작된 모바일DM 예시입니다" + 수신거부 안내(§10 확정 후 문구)`
- 제안 메일 고정 구조:
  `맞춤 서두(그 업체 사이트에서 본 것 1~2가지 언급) + "한줄로AI로 귀사 브랜드 예시를 만들어 봤습니다" + 인라인 이미지(스튜디오·인앱 시안) + 모바일 DM 링크 + 서비스 소개 1문단 + 발신자 서명(회사·연락처) + 수신거부 링크 + (스위치 ON 시 첨부)`
- 사용자 노출 문구 규율 그대로: 모델명·내부 코드명 노출 금지, 고객 언어만.

## 9. 발송 설계

### 이메일
- 전용 발신 도메인(§13 확정) + SPF/DKIM/DMARC 설정 후 개시. hanjul.ai 발신 인프라와 계정·IP 분리.
- 일일 상한 서버측 강제(기본 30건 — §13), 신규 도메인 워밍업 기간은 더 낮게 시작.
- 수신거부 링크 필수 포함, 수신거부 원장 기록 → 이후 등록 시 자동 '발송 불가' 상태.

### 문자 (데모 문자)
- 기존 QTmsg 라인·LMS. 발신번호 = 사전등록 번호 중 지정(§13).
- (광고) 표기·080 무료수신거부 부착 여부 = §10 법률 확인 결과에 따라 확정. 확정 전 문자 채널은 개발하되 발송 스위치 잠금.

### 공통
- 발송 경로는 승인 큐 [보내기] 단 하나. 워커·스케줄러에서 발송 함수 호출 자체가 불가능한 구조로(발송 함수는 승인 컨텍스트 필수 인자).
- [테스트(내게 보내기)] = Harold 본인 주소·번호로만(대상 업체 주소로 테스트 발송 불가). 도달 가능 임의 테스트값 생성 금지 룰 준수.
- 발송 직전 서버 재검증: status='ready' + dedupe_key 미존재 + 수신처 형식 유효.

## 10. 컴플라이언스 (착수 전 Harold 확인 필요 — 미검증 항목)

> 아래는 법적 판단이 필요한 항목으로, AI가 단정하지 않는다. 확인 전에는 관련 스위치를 잠근 채 개발한다.

1. **정보통신망법 제50조(영리목적 광고성 정보 사전 동의)** — 업체가 공개한 제휴제안 창구로 보내는 서비스 제안 메일이 광고성 정보 예외에 해당하는지. 이메일 (광고) 표기 부착 여부 판단 포함.
2. **데모 문자** — 사전 동의 없는 번호로의 발송 가부, (광고)·080 무료수신거부·발신번호 사전등록 요건. Harold가 "아는 번호"(기존 거래·명함 교환 관계)의 범위 정의.
3. 개인 전화번호·이메일 = 개인정보 — 수집 최소화, 발송 목적 외 사용 금지, 수신거부·삭제 요청 처리 절차.

## 11. 안전장치 요약

| 위험 | 장치 |
|---|---|
| 오발송(잘못된 주소·엉뚱한 브랜드 시안) | 전자동 발송 금지 + 승인 큐 육안 확인 + 추출 출처 표시 + 사전 확인 모달 |
| 중복 발송 | `dedupe_key` DB UNIQUE — 화면·워커 어디서도 우회 불가 |
| 본체 메일 평판 오염 | 발신 도메인·계정 분리 + 일일 상한 + 수신거부 원장 |
| 가짜 혜택 부착 | 혜택 수치 생성 금지(프롬프트+발송 전 서버 검사) |
| 미리보기≠발송본 | 동일 조립 함수 공유(이중 진실 금지) |
| 크롤 실패·미발견 | 정직하게 실패 표시, 자동 완화 없음 |
| 권한 | 전 엔드포인트 ENV 게이팅(보조 조회·재생성·테스트발송 포함 전수) |

## 12. 구현 단계

| Phase | 범위 | 비고 |
|---|---|---|
| 1 — 핵심 루프 | 게이팅+메뉴 뼈대 · 리스트 등록 · 크롤·분석 워커 · 문안+스튜디오 이미지+이메일 HTML 생성 · 승인 큐(수신처 수정·테스트발송) · 전용 도메인 이메일 발송 · 이력 | 이것만으로 영업 가동 가능 |
| 2 — 데모 완성 | 모바일 DM 자동 생성+hlj.kr 발급 · 인앱 시안 PNG · 첨부 스위치 | DM 링크가 pitch의 핵심 — Phase 1 직후 바로 |
| 3 — 문자·성과 | 데모 문자 채널(§10 확정 후 개방) · 회신 표시·성과 보드 · 후속 메일 시퀀스(별도 논의) | |

- 각 Phase 종결 = tsc 0 + Codex 검증 + Harold 실측 1건(§9 실측 원칙 — 발송 도구이므로 실발송 1건 검증 시나리오 포함).

## 13. Harold 확정 필요 항목 (착수 전)

1. **샘플 세트** — DM hlj.kr 주소 / 이메일(계정+제목) / 문안 텍스트 / 스튜디오·인앱 시안(계정+시안 이름) / 각 샘플이 가정한 업체명+홈페이지 URL. (1세트로 시작 가능, 업종 다른 2~3세트면 정확도 상승)
2. 발신 전용 도메인명 (예: hanjul-hello.kr 류 — Harold 결정)
3. 문자 발신번호(사전등록 번호 중 지정) + 080 적용 여부
4. §10 법률 확인 진행 방식
5. 일일 발송 상한 기본값 (제안: 30)
6. 메뉴명 확정 (가칭 "AI 영업")
7. ENV 키명·허용 계정 (제안: `SALES_OUTREACH_ALLOWED_USERS=ceo`)

## 14. 검증 상태 선언

- 이 문서는 설계 초안이다. §6-1 재사용 맵은 2026-07-22 메모리 기반이며 현재 코드로 미재확인. §5 테이블은 초안이며 `information_schema` 미검증. 구현 착수 승인 시: ①소스 grep 승인 요청 → ②재사용 맵 확정 → ③DDL 초안 + 검증 SQL 제공 순서로 진행한다.
