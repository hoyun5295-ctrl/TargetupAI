# 전송자격인증제 — 인증기준 대조 및 개발 원장 (2026-08-18)

> 호출어 **"전송자격인증"**. 이 문서가 이 트랙의 확정 사실·항목별 대조·개발 목록·제출물을 소유한다.
> 근거 문서 3종(절대경로) — 매 세션 다시 받지 않는다.
>
> | 문서 | 경로 |
> |---|---|
> | 가이드라인 2차 개정 (인증기준 원문) | `C:\Users\ceo\OneDrive\문서\카카오톡 받은 파일\전송자격인증제_고시_내_인증기준_준수를_위한_가이드라인_2차+개정.pdf` |
> | 신청 체크리스트 v7 | `C:\Users\ceo\OneDrive\문서\카카오톡 받은 파일\전송자격인증+신청+체크리스트_v7.pdf` |
> | 개발사항 및 정의서 (박성용 과장 정리) | `C:\Users\ceo\OneDrive\문서\카카오톡 받은 파일\전송자격인증제_개발사항 및 정의서.docx` |
>
> 읽는 법 — PDF는 `pdftotext -enc UTF-8 -layout`, docx는 `PYTHONIOENCODING=utf-8 python` + `python-docx`(본문은 `paragraphs`, 캡처 목록은 `tables`).

---

## 1. 제도 개요 (확정 — 원문 실측)

- 주관 = 방송미디어통신위원회. 제출처 = `antispam@korea.kr`(운영 문의) · `srt@kisa.or.kr`(인증기준 문의).
- 제출물 = ①신청서 ②구비서류 ③신청 체크리스트. **대용량 첨부가 아닌 일반 첨부**로 제출.
- 심사 2단계 — **서류 심사**(화면 캡처·양식·설명자료로 기준 준수 확인) → **현장 심사**(시스템 실물 확인).
  제출서류로 확인이 어려우면 보완 또는 **부적합** 판정.
- ⚠ **인증 후 신청서 기재사항이 바뀌면 인증을 다시 신청해야 한다.**
- ⚠ **서비스 유형별(웹페이지·API·솔루션·모듈) 증빙을 각각 제출**해야 한다. 우리는 셋 다 해당한다(§2).

체크리스트 항목은 18개(0.1~5.2)이고, 그중 **2.1부터가 시스템/개발 축**이다(박성용 과장 확인).
0.x~1.3은 서류 축(신청서·등기·이용약관·사무실 증빙·불법스팸 발송 방지계획서).

**신청 구분 (★Harold 확정 0818)** — **기존 특부가사업자(대량문자 발송 사업자)** · **KCUP 전송자격인증 취득**.

## 2. 우리 서비스 유형 판정

| 유형 | 해당 | 근거 |
|---|---|---|
| 웹페이지 발송 | ○ | hanjul.ai 발송 화면 |
| API 발송 | ○ | app.hanjul.ai SDK·API |
| 솔루션/모듈(사설발송) | ○ | 싱크에이전트 · 비토 게이트웨이 Agent |

⇒ **증빙을 세 벌 준비해야 한다.** 가이드라인은 "솔루션(웹GUI·독립 구축형)은 웹페이지 발송과 동일한 인증기준을 적용"한다고 못 박았다.

**심사 트랙 (★Harold 확정 0818)** — **서비스 유형은 웹페이지 발송이 맞고, 계정 발급은 계약 후 역발급 B2B로 한정**한다.
따라서 2.1 계정관리는 `①-2 계약 등을 통한 본인확인`으로 소명한다. 가이드라인 【설명】이 "계약관계를 통한 계정 발급 시 신원을 확인해야 하며, 중복 가입을 통제하는 절차를 마련해야 합니다"로 이 경로를 인정한다.
실측 뒷받침 — **공개 회원가입 라우트가 0건**이다(`routes/`에 `/signup`·`/register` 공개 경로 없음. 계정은 슈퍼관리자·영업이 생성).

## 2-1. 계정 구조 실측 (2026-08-18)

**회사당 계정 수 분포** (활성·system 제외, 총 130개사)

| 계정수 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 9 | 23 |
|---|---|---|---|---|---|---|---|---|---|
| 회사수 | **104** | 3 | 9 | 4 | 5 | 2 | 1 | 1 | 1 |

- 80%가 계정 1개다. 다만 **계정 수 ≠ 사용자 수** — 담당자가 원래 1명인 회사면 1인 1계정을 충족한다.
- 반대 방향의 증빙이 나왔다 — **23개·9개·7개로 나눠 쓰는 회사가 실재**한다. "기업회원은 실제 발송자별 계정 발급" 요건을 시스템이 지원하고 실사용 사례가 있다는 근거로 제출할 수 있다.

**공유 의심 계정** (계정 1개 회사 중 최근 90일 접속 IP 5개 이상)

고운세상(해지)·psy5868(테스트)을 빼면 **7곳** — 룰루레몬 12 · 숭실원격 10 · 쇼메 10 · 중평알앤에스 9 · 폴라초이스 7 · 방통대 6 · 아르뉴 5.
⚠ IP 분산이 곧 공유는 아니다(한 사람이 사무실·집·모바일을 쓰면 3~4개). 확인 대상일 뿐 판정이 아니다.
130개사 중 7곳이면 **계정 분리 안내로 감당되는 규모**다.

**★상시 계측기** — 접속 인계([접속 인계](2026-08-18-session-takeover.md))가 남기는 `login_session_conflict` 감사 로그가 곧 공유 계정 탐지기다.
같은 계정으로 동시에 들어오려는 시도가 기록된다. 종전에는 뒷사람이 앞사람을 말없이 밀어내 흔적이 없었다.
2.1 소명자료로 "공용 계정을 탐지·통제하고 있다"의 근거가 된다.

## 2-2. 해지·정지 고객사 계정 실측 → 결함 확정·수정 (2026-08-18)

**해지 14곳 · 정지 4곳 = 18개 계정이 전부 `users.is_active = true`.**
그중 **지금 로그인 가능한 계정 5개** — `testmart`(해지인데 계정 active) · `tosstest`·`isoi`·`isoics`·`kcptest`(정지, 계정 전부 active).
나머지 13개는 `users.status='dormant'`라 막혀 있었는데, **구조가 막은 게 아니라 해지 라우트가 손으로 맞춰둔 것**이었다.

**뿌리** — 로그인 게이트가 `users.status`만 보고 `companies.status`는 SELECT에도 없었다.
해지 라우트(`admin.ts` `DELETE /companies/:id`)는 계정을 dormant로 전파하지만 ①그 라우트를 안 거친 경로가 있고(`testmart`)
②`suspended`로 바꾸는 코드는 **아예 없다**(grep 0건 = 수동 SQL) ③세션 무효화는 어디에도 없었다.
⇒ 전파에 기대면 경로가 늘 때마다 샌다. **게이트를 회사 축으로 올린 것이 처방이다**(§4-A).

⚠ 상태 표기 자체도 부정확하다 — 고운세상은 계약 해지인데 `status`가 `active`/NULL이고, 자연인(아이소이)은 해지인데 `suspended`다.
그리고 **`companies`에 해지 시각 컬럼이 없다**(SCHEMA.md) — "언제 회수했나"의 근거가 감사 로그밖에 없다.

---

## 3. 항목별 대조 (코드 실측 기준 · 2026-08-18)

| 항목 | 기준 요구 | 우리 상태 | 근거 |
|---|---|---|---|
| **2.1 계정관리** | 본인확인·중복가입 차단·**발신번호 회선 수 제한(개인 무선3/유선5, 외국인 무선2, 법인 무선4/유선 n=종사자수)**·등록·변경·삭제 이력 | **부분** — 번호 중복 등록 차단만 있다. **회선 수 제한 없음**. 등록·변경·삭제 **이력 없음** | `admin.ts:1297` · `manage-callbacks.ts:118`(중복만) / `manage-callbacks.ts` `audit_logs` 0건 |
| **2.2 국외 이용자** | 국외 IP 차단·화이트리스트·VPN/프록시 통제·예외 승인 이력·**모든 접근 경로**(웹·관리자·API·모듈) | **없음** | STATUS §3 TODO "슈퍼관리자 IP 화이트리스트" 미착수 |
| **2.3 부정사용 차단** | 결제정보↔계정 연계·명의 불일치 보류/소명·처리 결과 계정별 기록 | **부분** — 결제 원장은 있다(`payments`·`deposit_requests`·`balance_transactions`). 보류·소명 절차 화면 없음 | SCHEMA.md |
| **3.1 방화벽** | 경계 방화벽·정책·**우회/비가시 경로 없음**·정책 변경 이력 | **부분 + 위험** — §6 참조 | SERVERS.md |
| **3.2 접근권한 부여** | 권한분류표(R/W/D)·시스템↔문서 일치·승인자/사유/일시·공용계정 제한 | **부분** — 시스템 권한은 있다(`user_type`·`role`·`hidden_features`·`store_codes`). **권한분류표·승인 이력 없음** | SCHEMA.md `users` |
| **3.3 접근권한 변경** | 인사이동·퇴사 시 즉시 회수·변경 이력 대장·정기 점검 | **없음**(대장·근거자료 축) | — |
| **3.4 다중 인증(MFA)** | 전 이용자 로그인 MFA·등록/변경/재설정 이력·**실패 초과 시 계정 잠금** | **부분** — **슈퍼관리자만 TOTP**. 고객사 사용자는 ID/PW뿐. 계정 자동 잠금 없음(있는 건 (IP,ID) 30분 임시차단) | `totp` = `routes/auth.ts` 단독 · `login-block.ts` |
| **3.5 추가 인증(발신)** | 발송 시 발신번호↔계정 연계 확인(SMS OTP/ARS)·재인증 기준·사설발송 대체통제 | **없음** | — |
| **4.1 이용자 로그** | 접속·인증·발송·과금 로그 **1년 이상**·변조/삭제 방지·백업 | **부분** — 로그인 성공/실패·세션(IP·UA)·발송 이력·과금 원장 존재. **보관기간 정책 문서 없음** | `audit_logs` · `user_sessions` · MySQL 발송 |
| **4.2 시스템 로그** | 관리자 행위·장애·**개인정보 조회/수정/삭제/다운로드 이력**·DB/API/인증 시스템 포함 | **부분** — 관리자 행위는 `audit_logs`. **고객DB 엑셀 다운로드 이력 0건** | `customers.ts:203` `/download`에 `audit_logs` 0건 |
| **4.3 로그 점검** | **월 1회** 정기 점검·점검 기록(일자·점검자·대상·결과·조치)·이상징후 후속조치 | **없음**(운영 절차) | — |
| **5.1 조치 및 결과확인** | 스팸신고 확인 절차·이용자 정지/해지·**조치 시 이용자 고지**·이력 보관 | **부분** — 계정 상태(`locked`/`dormant`)는 있으나 자동 전환·고지 발송 없음 | `auth.ts` statusMessages |
| **5.2 금칙어 차단 체계** | 발송 요청 시 키워드·URL·전화번호 **다중 조합(1~5개)** 자동 탐지 → 차단/보류·**KISA 차단정보 적용**·탐지 로그 | **없음** | 우리 "스팸필터"는 통신사 판정 **유료 테스트**(`routes/spam-filter.ts`), `copy-spam-risk.ts`는 AI 생성용 로컬 18단어 가드로 "실제 판정 보장 아님"이 주석에 명시 |

---

## 4-A. ★착수 완료 — 계약 종료 계정 차단 3단 (2026-08-18 · 코드 완료·배포 대기)

§2-2의 결함을 닫았다. DDL 0.

| # | 무엇 | 어디 |
|---|---|---|
| ① | **로그인 게이트를 회사 축으로** — `terminated`·`suspended`면 계정 상태와 무관하게 403 + 감사 로그(`login_blocked` / `company_*`) | `utils/company-access.ts`(신규 CT) · `routes/auth.ts` |
| ② | **상태 변경이 세션까지 전파** — 해지 시 소속 계정 dormant + **접속 중 세션 무효화** | `utils/session-manager.ts` `invalidateCompanySessions` · `routes/admin.ts` |
| ③ | **회수 이력** — `audit_logs`에 `company_terminated`(처리자·일시·잠근 계정 수·끊은 세션 수) | `routes/admin.ts` |

**⛔ 이 축의 함정 2개**
1. **`SELECT u.*`에 `c.status`를 별칭 없이 넣으면 계정 상태가 회사 상태로 덮인다**(같은 컬럼명이 둘이면 뒤엣것이 이긴다) → 계정 상태 게이트가 통째로 오작동한다. `c.status AS company_status` 강제. 소스 불변식 테스트가 고정.
2. **`companies.status`의 NULL은 활성이다**(SCHEMA.md) → `!== 'active'` 부정 비교를 쓰면 상태 미설정 고객사가 **전부 로그인 불가**가 된다. 차단할 값만 열거하는 긍정 비교로만 판정.

검증 — backend tsc 0 · 전체 2,673건 통과 · 신규 계약 9건. 부정 비교 주입과 별칭 제거 주입 각각에서 기대한 테스트가 깨지는 것을 확인하고 원복했다.
⚠ **운영 계정 5개는 SQL로 직접 고치지 않는다.** 배포로 코드가 막은 뒤 슈퍼관리자 화면에서 상태를 정리하는 순서다.

**추가 과제(범위 밖 · 기록만)** — 회사 상태 `locked`·`dormant`는 이번 차단 열거에 넣지 않았다(현재 실측 0건). SDK·API 발송 경로가 해지 고객사를 막는지는 별도 인증 축이라 따로 봐야 한다(2.2가 "API·모듈 연동 서버 등 모든 접근 경로"를 요구한다).

## 4. 개발 필요 목록 (규모순)

1. **금칙어 차단 체계 (5.2)** — 신규. 발송 요청 경로 전부가 지나는 한 곳에 게이트. 차단정보 CRUD·다중조합 판정·차단/보류·안내 팝업·탐지 로그·KISA 차단정보 반입.
   ⚠ 게이트는 화면이 아니라 **발송이 시작되는 길목**에 둔다(직접발송·AI캠페인·여정·자동마케팅·API·에이전트 전 경로).
2. **추가 인증 = 발신 인증 (3.5)** — 신규. 발송 시 발신번호↔계정 연계 확인. 박성용 과장 안 = 등록 담당자 SMS OTP·세션 24시간 유지.
   ※ 가이드라인이 **인증 생략 가능 범위**를 명시한다 — 템플릿 고정·인증번호·시스템 자동발송 등 이용자 개입 없는 발송, 쿠폰/선물하기 등 서비스 제공 발송. **자동마케팅·여정 자동발송이 여기에 해당하는지 판단이 필요하다.**
3. **다중 인증 (3.4)** — 고객사 사용자 MFA. **설계는 §4-B가 소유**.
4. **발신번호 회선 수 제한 (2.1)** — 이용자 유형(개인/외국인/법인) 축 신설 + 유선/무선 구분 + 초과 시 보류·차단. 증빙서류 업로드·관리자 승인. 등록·변경·삭제 이력.
5. **국외 IP 차단·화이트리스트 (2.2)** — 웹·관리자·API·에이전트 **전 경로**. 예외 승인 화면과 이력.
6. **개인정보 처리 이력 (4.2)** — 고객DB 조회·수정·삭제·**다운로드** 감사 로그. 다운로드가 가장 시급하다.
7. **부정사용 보류·소명 (2.3)** — 의심 결제 보류 상태와 처리 화면.
8. **조치 고지 (5.1)** — 정지/해지 시 이용자 자동 고지(SMS/이메일) + 이력.

## 4-B. 다중 인증(3.4) 설계 — Harold 원안 + 기준 대조

**Harold 원안 (0818)**
슈퍼관리자에서 계정별 **로그인 담당자 휴대폰번호 등록**(복수 가능) → 로그인 시 **인증받을 번호를 선택** →
그 번호로 **1800-8125 발신, 6자리 숫자 인증번호**를 알림톡(승인 후) 또는 문자로 발송 → 입력해 통과하면
**그 계정은 24시간 유효**, 그 안에 재로그인하면 인증 없이 통과. MOTP 앱은 쓰지 않는다.

**재사용 가능한 실물 (실측)**
- `1800-8125` = 이미 `SYSTEM_SMS_CALLBACK` 기본값으로 시스템 발신에 쓰인다(`routes/internal-alert.ts:19`)
- **인증 전용 발송 라인이 이미 있다** — `sms_line_groups.group_type='auth'` / `SMSQ_SEND_11`, `getAuthSmsTable()`을 여러 곳이 쓴다

**⚠ 기준과 부딪히는 지점 — 계정당 복수 번호**
가이드라인 3.4·3.5에 같은 문장이 두 번 나온다: *"다수의 이용자가 공동으로 사용할 수 있는 인증수단은 인증기준에 부적합합니다."*
2.1은 *"1인 1계정 원칙은 기업 1개당 1계정이 아니라 실제 발송자 1인당 1계정"*, *"공용 계정 사용… 운영이 불가"*.
⇒ **계정 하나에 번호 여럿 + 로그인 시 선택**은 "한 계정을 여러 사람이 각자 폰으로 쓰는 구조"로 읽힌다.

**★Harold 확정 (0818) — 주 번호 1개로 간다.**
계정당 유효한 인증번호는 **언제나 하나**다. 로그인 시 번호를 고르는 단계가 없다(고르는 순간 공동 사용 구조가 된다).
교체는 슈퍼관리자가 하고 이력을 남긴다. 담당자가 여럿이면 번호가 아니라 **계정을 나눈다**(2.1 1인 1계정과 함께 맞는다).
⇒ 등록 목록 테이블이 필요 없어졌다. `users`에 주 번호 컬럼 하나 + 변경 이력은 `audit_logs`.

**비토가 정한 부분 (원안에 없던 것)**
- **24시간 유효는 같은 기기·같은 IP 대역 조건**을 붙인다. 3.5가 "접속환경 변경 시 재인증"을 명시하고, 무조건 24시간을 믿으면 탈취 계정이 하루를 그냥 쓴다.
- 인증번호 발송분은 **고객사에 청구되지 않게 과금 제외**.
- 알림톡 템플릿 승인 전까지는 문자, 승인 후 알림톡 우선·실패 시 문자 폴백(발송 실패 = 로그인 불가라 단일 장애점을 만들지 않는다).
- 로그인 실패 초과 시 **계정 잠금 + 경고 안내**를 동반한다(현재는 (IP,ID) 30분 임시차단뿐이라 기준 ④를 못 채운다).

## 4-C. 발신 인증(3.5) 적용 범위 — 확정

**여정 · 자동마케팅 · 마케팅 플래너는 발송 건별 추가인증 대상이 아니다.**
근거 = 가이드라인 3.5 주의사항: *"내용 수정이 불가한 템플릿, 인증번호, 정형화된 시스템 안내, **시스템 자동발송 등 이용자 개입이 없는 발송** 건에 대해서는 인증을 생략할 수 있습니다."*

⚠ 다만 심사는 "그럼 누가 그 발송을 지시했나"를 묻는다. 답은 **여정 활성화·자동마케팅 ON·플래너 승인 시점의 인증 이력**이고,
그 조작이 전부 로그인 세션 안에서 일어나므로 §4-B의 24시간 MFA로 충족된다. **추가 개발 없이 소명자료로 처리한다.**

## 5. 개발이 아닌 축 (문서·운영·인프라)

- 이용약관 불법스팸·역무제공거부 조항 발췌 · 사무실 확보증명 · **불법스팸 발송 방지계획서**(5.1 조치 절차·3.5 재인증 기준이 이 문서에 들어가야 한다)
- 계정 목록 및 권한분류표(R/W/D) · 조직도 · 접근권한 변경 이력 대장
- 방화벽 장비·정책 화면 · **네트워크 구성도(특부가 관련 전 설비)**
- 로그 보관 정책 · 로그 점검 양식(월 1회) · 이용료 납부 내역 관리대장
- 심사용 화면 캡처 8종 = 정의서 표(발신번호·IP·MFA·발신인증·로그·부정사용·금칙어·스팸조치)

## 6. ★ 심사에서 걸릴 위험 — 먼저 손봐야 한다

**3.1 방화벽 ③ "관리 편의성을 위한 우회 접속 또는 비가시성 경로가 없는가?"에 정면으로 걸리는 실측 항목이 있다.**
`SERVERS.md` 기록 — **.62의 `pay-ingest-db` 23388 포트는 Docker가 ufw를 우회(DOCKER-USER 규칙 미적용)해 실제로 외부에 열려 있다.**
현장 심사에서 포트 스캔·구성도 대조를 하면 바로 드러난다. 신청 전에 닫는 것이 맞다.

같은 절의 `.54`(1696일 무재부팅·커널 2021년, 실트래픽 단독 부담)도 현장 심사 대상 설비다.

## 7. Harold님 확인 필요

1. **MFA 복수 번호 방식** — §4-B. 원안(계정당 복수 번호 + 로그인 시 선택) 그대로 갈지, 추천안(주 번호 1개)으로 좁힐지.
2. **착수 순서** — §4의 나머지 8건을 한 번에 갈지, 심사 일정에 맞춰 나눌지.
3. **공유 의심 7곳** — 계정 분리 안내를 영업이 언제 돌릴지(§2-1).

*(0818 해소: 신청 구분 = 기존 특부가 + KCUP 취득 §1 · 심사 트랙 = 웹발송 + 계약 역발급 §2 · 자동발송 인증 생략 §4-C)*

## 8. 이력

- **2026-08-18 신설** — 문서 3종 정독 + 코드 실측 대조. 개발 8건·문서 축 6건·심사 위험 1건 도출.
- **2026-08-18(2)** — Harold 확정 3건 반영(신청 구분·심사 트랙·자동발송 제외). 계정 구조 실측(§2-1)과 해지 계정 실측(§2-2)으로 **결함 1건 확정 → §4-A 3단 수정 코드 완료**(DDL 0 · 계약 9건 · 결함 주입 확인). MFA 설계 = §4-B(복수 번호 방식만 결정 대기).
