# 자동마케팅 (Continuous Operator) — 기능 상설 SoT

> **호출어: "자동마케팅"** — 이 이름을 부르면 이 문서다.
> 기능 상설 SoT 3호(여정·싱크에이전트와 같은 체계). **이 문서가 자기 이력을 소유하고 STATUS는 참조만 한다.**
> 상태·잔여는 STATUS §2 카드가, 시점별 설계 근거는 [0803 타겟팅 재설계 설계서](2026-08-03-automarketing-targeting-redesign-design.md)가 소유한다.
> 목적 한 줄 = **조건이 발생하면 그 조건에 맞는 대상이 정확히 뽑혀 나간다. 두루뭉술·어바웃 없음**(2026-08-03 Harold).
> 그리고 그 위에 하나 더 — **기능이 우선이다.** 정확도를 이유로 기능을 끄는 것은 수정이 아니다(2026-08-03 Harold).

---

## §0 정체성 — 여정과 무엇이 다른가 (2026-08-04 Harold 확정)

**시계의 주인이 다르다.**

| | 여정 | 자동마케팅 |
|---|---|---|
| 시계의 주인 | **고객** — 고객이 무언가를 한 순간 그 사람 시계가 시작된다. 언제 나갈지 우리는 모른다 | **우리** — 우리가 정한 시점이 오면 그때 조건에 맞는 사람을 모은다. 언제 나갈지 미리 안다 |
| 조건의 모양 | 방금 무엇을 **했다**(사건) | 지금 어떤 **상태다** / 지난번과 무엇이 **달라졌다** |
| 나가는 모양 | 사람마다 다른 시각, 한 명씩 | 한 회차에 다 같이 = 캠페인 1건 |
| 문안 | 한 번 정하면 안 바뀐다(관계는 일관돼야 한다) | 회차마다 새로 만든다(그때 계절·분위기·변화를 말한다) |
| 목적 | 관계 관리 | 반복 캠페인 |

⛔ **주기는 설정값이지 정체성이 아니다.** 매일·매주·매월 어느 쪽이든 위 성격은 같다.

**자동마케팅만 가진 어휘 = 회차와 회차 사이의 변화.** 여정에는 "지난달"이라는 개념이 아예 없다 —
사람마다 시계가 따로 돌아 구간을 자를 기준이 없기 때문이다. 자동마케팅은 회차라는 시간 격자를 갖고 있어
"지난 회차와 지금 사이에 무엇이 달라졌나"를 물을 수 있고, 그건 담당자가 실제로 쓰는 말이다 —
"이번에 등급 오르신 분", "요즘 발길 끊긴 분", "오랜만에 다시 오신 분". 명부를 아무리 필터링해도 안 나온다.

**새 축을 더하기 전에 답할 질문 하나** — 이 조건은 "방금 무엇을 했다"인가, "지금 어떤 상태다"인가.
전자면 **여정이 구조적으로 더 잘한다**(담은 그 사람에게 세 시간 뒤 한 번 ≫ 일주일치를 모아 월요일에 같은 문자).
자동마케팅에 넣으면 못 하게 막는 게 아니라 **더 못 하게 하는 것**이고, 그러면 기능을 나눈 의미가 없다.
⛔ 장바구니 포기·상품 조회·배송 시작은 이 기준으로 **여정 전용**이다.

겹치는 축은 나쁘지 않다. 생일은 여정이면 "그분 생일 일주일 전에 그분께", 자동마케팅이면 "이번 회차 생일자를
모아 이달 프로모션과 함께"다 — 목적이 다른 다른 마케팅이라 고객사가 고른다. 둘 다 켰을 때 두 번 나가는 것만
조절하면 되고 그게 [설계서 §6](2026-08-03-automarketing-targeting-redesign-design.md) 미결 1이다.

---

## §1 착수 전 필독 순서

1. **§12 남은 개발** — 다음에 무엇을 할지는 여기만 본다(유일한 착수 원장).
2. **§0 정체성** — 새 축을 더할지 말지가 여기서 갈린다.
3. **§2 불변 원칙** — 어기면 오발송·과금 사고다.
4. **§3 구조** — 무엇이 어디서 결정되는지.
5. **§4 대상 판정 계약** — 이 트랙의 핵심.
6. 작업 도메인 `status/lessons/LESSONS_BACKEND.md` 핵심 원칙 + `LESSONS_META`.
7. 이력·뒤집힌 판단·실측 사실 = §7·§8·§10.

---

## §2 불변 원칙 (어기면 사고)

1. **대상 판정은 AI가 하지 않는다.** AI는 자연어를 축·파라미터로 옮기는 자리(등록 1회)와 문안 생성에만 쓴다.
   회차마다 목표를 다시 해석하면 지난달과 같은 조건이라는 보장이 없다.
2. **보여준 수 = 통지한 수 = 나가는 수.** 셋이 다르면 하나는 거짓이다. 세는 곳·뽑는 곳·보내는 곳이
   같은 WHERE·같은 게이트·같은 범위를 쓴다.
3. **0건 자동완화 금지.** 대상이 0이면 제안을 만들지 않고 **사유와 함께 담당자에게 알린다**(조용한 0건 금지).
4. **AI가 구체 혜택(%·원·쿠폰·무료)을 만들지 않는다.** 미편집 placeholder가 남으면 발송 출구에서 차단한다.
5. **예측으로 타겟을 고르지 않는다.** 추천·개인화·send-time에만 예측을 쓴다.
6. **모르는 값은 권한·범위를 넓히는 쪽으로 해석하지 않는다.** 승격 오판은 비인가 발송이고, 제한 오판은 보류에 그친다.
7. **돈이 걸린 검사는 실측값으로, 검사와 예약은 한 트랜잭션에서.** 제안 시점 수치로 통과시키고 실제로 더 보내면 안 된다.
   - **차감이 확정되기 전에 상태를 전진시키지 않는다**(2026-08-05 · §7-7). 그리고 **예외가 안 나는 것은 성공의 증거가 아니다** —
     `deductCreditSafe`는 잔액 부족·영구 실패에도 throw하지 않는다. 성공 판정은 반환값으로만 한다.
8. **정확도를 보장할 수 없으면 보내지 않는다 — 다만 기능을 끄는 것은 별개 판단이고 Harold 결정 사항이다.**

---

## §3 구조 — 무엇이 어디서 결정되나

```
등록(모달·자연어·시나리오·캘린더)
  → continuous_operators 1행 (목표 문장 + 채널 + 주기 + 대상 계약)
  → [워커 1분 주기] next_run_at 도달 = 제안 생성 사이클
       services/ai-orchestrator.ts orchestrate()
         · 계약 있으면: 축→SQL 컴파일(AI 해석 버림)   ← 결정성
         · 계약 없으면: recommendTarget(AI)로 filters 생성  ← 옛 방식
         · 대상 수 = utils/operator-audience.ts 단일 문(안전필터·미클릭·피로도·매장범위 포함)
         · 0건이면 제안 미생성 + 사유 통지
         · 문안 생성 → 스팸 검증 → 통과 시 status='scheduled'(자율) 또는 'pending'(승인 대기)
       사전 통지(담당자 문자) — 제안 생성 시점, lead분 전
  → [워커] scheduled_send_at 도달 = 발송 패스
       utils/continuous-operator.ts dispatchProposalSend()
         · 매장 범위 해석 → 조건 컴파일 → staging 적재(실측 건수)
         · 실측 건수·현재 단가로 상한·예산 재검사(자율 경로, 한 트랜잭션)
         · createDirectSendCampaign → 직접발송 파이프라인 공유
```

**핵심 파일**

| 파일 | 소유하는 것 |
|---|---|
| `utils/automarketing-segment.ts` | 세그먼트 계약 레지스트리(축 11종 · 근거 판정 · SQL 조각 · 상태/변화 구분). DB import 0 순수 |
| `utils/operator-cycle-snapshot.ts` | **회차 스냅샷** — 변화 축의 유일한 근거. 표 준비 판정·기준선 유무·갈아 끼우기 |
| `utils/operator-audience.ts` | 대상 판정 **단일 문** — 게이트 해석·매장 범위·컴파일·카운트 |
| `utils/operator-recipients.ts` | WHERE 조각 **단일 빌더**(`buildAudienceWhere`) + count/명단/staging 3형태 |
| `utils/continuous-operator.ts` | 오퍼레이터 CRUD · 제안 생성 사이클 · 발송 패스 · 담당자 통지 |
| `services/ai-orchestrator.ts` | 제안 조립(타겟·문안·검수·비용·성과) |
| `routes/ai.ts` | `/operator/*` 라우트 — 축 목록·타겟확인 명단·제안 상세 |

---

## §4 대상 판정 계약 (이 트랙의 핵심)

**축은 계약이고 두 갈래다.**

- **상태 축 6종** — 명부 한 장으로 판정한다. `all` · `dormant` · `recent_buyers` · `vip` · `birthday` · `new_customers`.
- **변화 축 5종**(2026-08-04 신설) — 지난 회차 스냅샷과 비교해 판정한다. `grade_up`(등급이 오른 고객) ·
  `first_purchase`(처음 구매한 고객) · `returned`(다시 돌아온 고객) · `went_quiet`(발길이 끊긴 고객) ·
  `spent_more`(지난번보다 많이 산 고객).

각 축은 `resolve`(그 회사에서 쓸 수 있는가)와 `build`(SQL 조각)를 갖고, 변화 축은 `needsCycleBaseline`을 더 갖는다.

**회차 스냅샷 계약**(변화 축의 전부 — `operator_cycle_snapshots` · 2026-08-04 Codex 1R로 확정)

- **문은 둘뿐이다. 전체 교체는 폐기했다.**
  ① 기준선 보충(`ensureCycleBaselineRows`, DO NOTHING) — 없는 사람만 지금 모습으로 추가. 최초 기준선과
  매 회차 신규 고객 보충이 같은 문이다(안 하면 기준선 이후 등록된 고객이 변화 축에서 영구 제외된다).
  ② 발송분 갱신(`advanceCycleSnapshotForPhones`, DO UPDATE) — **실제로 발송된 수신번호의 사람만** 갱신.
  전체 교체는 A매장 한 명의 발송이 B매장 고객·피로도 제외자의 변화까지 소진시켰다(발송 범위 ≠ 갱신 범위).
- **갱신 시점 = 캠페인 종결 확인 뒤.** 큐 수락은 발송 성공이 아니다 — 발송 큐 장애로 전량 실패해도 변화가
  소진돼 다음 회차에 조용히 사라진다. 발송 커밋 마커(`campaign_id`)와 같은 UPDATE에서 대기 마커
  (`meta.cycleSnapshotPending`)를 심고, 오퍼레이터 워커의 **종결 대조 패스**(`settlePendingCycleSnapshots`)가
  캠페인 상태를 보고 처리한다: `completed` → 발송 큐 실수신 번호(`dest_no`, `app_etc1`=캠페인 id)로 그
  사람들만 갱신 / `failed`·`cancelled` → 갱신 없이 해제(변화 보존) / 진행 중 → 대기. 갱신 실패는 마커가
  남아 다음 패스가 재시도한다(수렴). 부분 실패는 자동으로 정확하다 — 큐에 실린 번호만 갱신된다.
  보호 영역(direct-send)은 **읽기만** 한다 — 쓰기는 스냅샷 표·제안 마커뿐.
- 짝은 **고객 행 id** + **회사 결합**으로 맞춘다. 전화번호로 이으면 같은 사람이 매장별로 여러 행인 회사에서
  A매장 행이 B매장 행의 과거와 비교된다. 지난 회차 이후 새로 생긴 행은 다음 보충 때 기준선이 생기고,
  그 전까지는 변화로 잡히지 않는다(모르면 안 보내는 방향).
- **구매 파생 전환 3종(첫 구매·복귀·구매 증가)은 구매창 가드 필수** — 마지막 구매일이 그 스냅샷의 관측
  시점 이후일 때만 발화한다. 과거 주문 뒤늦은 연결(backfill)·옛 날짜 보정은 카운트·금액을 움직이지만
  이 구간의 행동이 아니다. 구매 증가 최소 금액은 0을 받지 않는다(0이면 변화 없는 전원이 대상).
- **첫 회차는 기준선만 심고 대상 0 + 사유 통지.** 기준선 기록 실패도 통지한다(정상 "제안 없음"과 섞지 않는다).
  화면의 첫 회차 안내는 서버 판정(`awaitingBaseline` 응답)을 쓴다 — 오퍼레이터가 있다고 기준선이 있는 게 아니다.
- **저장 검증은 근거 판정만**(`assertSegmentUsable`) — 컴파일로 검증하면 변화 축이 "지난 회차 없음"에 걸려
  등록 자체가 안 된다. 판정과 컴파일은 다른 질문이다.
- 표가 아직 없으면 **컴파일 전에** 멈춘다(`isCycleSnapshotReady`) — 라우트 3곳이 503 `DB_MIGRATION_PENDING`으로 받는다.
- ⛔ 이 표는 **사실을 복사하지 않는다.** 없던 사실(과거의 모습)을 기록하는 것이라 미러가 아니다.
  단 "스냅샷 상태 ↔ 캠페인 결과"는 두 진실이라 종결 대조 패스가 잇는다(6원칙 ③).
- **비교할 항목을 스냅샷에 하나 더 담으면 축이 하나 더 열린다** — 축마다 장치를 만들지 않는다.

- **정답표를 갖지 않는다.** 무엇이 가능한지는 그 회사 데이터가 런타임에 정한다. 근거가 없으면 **사유와 함께 잠근다.**
  잠긴 축은 숨기지 않는다 — 숨기면 담당자는 왜 없는지 알 수 없다.
- **축이 정해지면 SQL이 정해진다.** 같은 축·같은 파라미터·같은 데이터면 언제 돌려도 같은 명단(결정성).
- **정의를 두루뭉술하게 두지 않는다.** 휴면 = 마지막 구매일이 기준 이전 **AND 구매 기록 있음**(미구매는 휴면이 아니다).
  상위 등급 = 회사가 확인한 등급 순서의 최상위 급(순서를 안 정했으면 잠근다 — 여정 등급 판정 공유).
- **모르는 축 값은 fail-closed.** "계약 없음"과 "알 수 없는 계약"을 같은 값으로 뭉개면 미지의 값이 전체 고객으로 열린다.
- **대상 모드는 하나다.** 계약(`segment_key`)과 옛 축(`target_hint`)은 공존할 수 없다 — 계약이 이기고 힌트는 해제된다.

**게이트(조건이 아니라 안전장치)** — 미클릭 제외 · 발송 피로도 · 안전필터(활성·수신동의·수신거부·무효번호) · 매장 범위 ·
**여정 겹침 제외**(2026-08-04 Harold 확정 — `companies.automarketing_exclude_journey` opt-in. 켜면 여정 진행 중
`status='active'`인 고객에게 자동마케팅이 안 나간다. 피로도가 "몇 통"을 막는다면 이것은 "무슨 말"의 충돌을 막는다.
기본 꺼짐 = 겹침 허용 = 배포 시 동작 변화 0. 설정 = 회사 설정 화면, 피로도 토글 아래).
게이트는 객체 하나로 넘긴다. 포지셔널 인자로 넘기던 시절에 화면이 `null, null`을 넘겨 게이트가 통째로 빠졌다.

**매장 범위** — 오퍼레이터를 만든 계정의 **현재 DB 권한**으로 해석한다.
⛔ `users.user_type` 실측값은 `admin` · `user` · `system`이다. **`company_admin`·`company_user`는 JWT 변환값이라 DB에 없다.**
토큰 어휘로 판정하면 제한 사용자가 전사 권한으로 승격된다. 소유자가 없거나 비활성(`is_active`·`status='active'` 미충족)이면 보류한다
— 회사 해지는 그 회사 전 사용자를 `status='dormant'`로 바꾸므로 이 조건 하나로 해지 경로도 닫힌다.

---

## §5 미반응자 리마인드 — **2026-08-04 되살림** (구현 완료)

**대상 = 1차 실수신 번호 ∩ 지금 안전필터 ∩ 미클릭(1차 발송 시각 이후) ∩ 피로도 ∩ 매장 범위.**
2026-08-03 보류 사유("1차 수신자 집합을 모른다")가 발송 큐 원장으로 해소됐다 — 새 표는 만들지 않았다.

**구조** (전부 `continuous-operator.ts` — 1R Codex 13건 반영판)
- **코호트 원천** = `readCampaignQueuedPhones` — MySQL 발송 큐 `dest_no`, `app_etc1` = **campaigns.id**(직접발송
  경로 실측). settle(변화 축 회차 마감)과 **같은 문**. 발송월 + 지금월 LOG 합집합(월 경계), 하이픈 제거 후 숫자만.
  ⛔ **리마인드는 성공 코드만**(`successOnly` — `SUCCESS_CODES`): 실패(결번·꺼짐)·대기 번호에 또 보내면
  받은 적 없는 리마인드다. settle은 전체(큐 적재 = 발송 경계 — 의도된 차이). selectFields에 DISTINCT 금지
  (`smsSelectAll`이 `_sms_table` 리터럴을 앞에 붙여 문법 오류가 된다 — 중복 제거는 JS Set).
- **예약**(`scheduleSequenceReminder`) — 1차 발송 성공 직후 **admin_review('스팸 검증 중')로 먼저 durable하게**
  넣고, 스팸 검증은 분리 실행(5분 상한)으로 돌린다 — 검증 큐가 전역 직렬(건당 25~90초)이라 동기로 기다리면
  발송 워커·승인 HTTP가 멈춘다. 통과 = CAS 승격(검증 중 사유 그대로일 때만 `scheduled` — 사람 판단을 안 덮는다) /
  미통과·시간초과·크래시 = admin_review 잔존 + 통지(fail-closed — 검증 없이 자동 발송으로 못 넘어간다).
  ⛔ AI 재작성 금지(maxRetries 0). 문안은 EUC-KR 안전화(`stripIncompatibleEmojis`)를 지난다.
  미클릭 기준 시각 = **1차 발송 커밋 직후 캡처**(예약 함수 진입 시각이 아니다 — 그 사이 클릭도 반응이다).
  예상 비용 = 리마인드 채널 단가로 재산정(1차 값 복사 금지 — SMS→LMS 승격 시 표시≠실차감).
- **발송**(`dispatchProposalSend` 코호트 분기) — 조건을 다시 컴파일하지 않는다(보류의 원인이었다).
  1차 캠페인 `status='completed'` 필수(아니면 취소+통지). 코호트 phone semi-join 위에 안전필터·미클릭·
  피로도·매장 범위 게이트가 일반 발송과 같은 문으로 얹힌다. 혜택 placeholder·080·MMS·발신번호·상한·예산
  재검사도 같은 경로. **보류기의 격리 장치 3종(매 패스 수렴 UPDATE·due 제외·발송 진입 차단)은 전부 제거** —
  fail-closed는 dispatch의 primary_campaign_id 부재 취소 한 곳이 소유한다(장치가 남으면 반쪽 되살림).
- **화면 명단도 코호트**(routes/ai.ts recipients) — target 재컴파일 금지(화면 ≠ 실발송이 된다).
- **끄면 미발송 예약분도 취소**(updateOperator) — "껐는데 나갔다"는 과다 발송 클레임이다.
- **리마인드는 스냅샷 settle 마커를 심지 않는다** — 같은 코호트 재접촉이지 새 회차가 아니다.

**한계(기록)** — 미반응 판정은 클릭뿐이라 링크 없는 문자면 코호트 전원 재발송(정의상 정확 — 담당자가 링크로 정한다) /
admin_review 리마인드의 수동 승인 = 즉시 발송·스팸 재검 없음(수동 승인 = 사람 책임, 1차와 같은 기존 계약) /
예산 SUM이 `created_at` 축이라 D+N 리마인드 비용이 생성일에 계상되고 발송일엔 빠진다(전 제안 공통 구조 축 — 등재).

---

## §6 화면

- 발송 대상 선택 = `SegmentPicker`. 축 목록·라벨·사유·파라미터 정의를 **서버가 준다**(프런트에 목록을 두지 않는다 —
  두면 그 회사에서 안 되는 축까지 늘 열려 보인다). 잠긴 축은 자물쇠와 사유를 함께 보여준다.
- 축을 고르면 **그 자리에서 지금 기준 대상 수를 실측**해 보여준다. 추가 입력 없음(클릭 1회).
  그 수는 발송이 쓰는 조건·게이트와 같은 기준이다.
- 옛 축(`target_hint`)이 살아 있는 오퍼레이터는 "지금은 예전에 지정한 OO 축으로 나갑니다"라고 표시한다.
  **무관한 수정(이름·예산)에서는 옛 축을 건드리지 않는다** — 사용자가 "자동 판단"을 명시적으로 고른 경우에만 해제한다(3상태).
- 관리자가 남의 오퍼레이터를 편집하면 **그 소유자 기준**으로 대상 수를 센다(화면 수 ≠ 실발송을 막는다).

---

## §7 이력 색인

| 시점 | 무엇 | 검증 | 근거 |
|---|---|---|---|
| 2026-05-19 (D176) | Continuous Operator 신설 — 제안·동의 흐름 | — | SCHEMA D176 절 |
| 2026-07-02 | 스케줄 컬럼 의미 전환(`next_run_at` = 생성 시각) · 담당자 통지 2종 | 마이그레이션 UPDATE 4 실측 | LESSONS_BACKEND |
| 2026-07-07 | 마케팅 캘린더 완비 — `target_hint` 축 6종 도입(자연어 지시) | ALTER 실행 | autosend-policy |
| 2026-07-12 | 축 일반 개방(C-4) · 죽은 설정 제거(C-2) · 중지 시 예약 취소(C-1) | — | — |
| 2026-08-03 | **타겟팅 재설계** — 계약·결정성·단일 문·매장 범위·리마인드 보류 | tsc 0 / vitest 136파일 1,947건 / 계약 검증 38건 / Codex 적대검증 8R | §8 |
| 2026-08-04 | **정체성 확정 + 변화 축 5종 신설** — 회차 스냅샷 도입, 사건 반응 축은 여정으로 확정 | tsc 0 양쪽 / vitest 136파일 1,965건(신규 15) / 주입 검증 2건 실패 확인 후 원복 | §0 · §7-3 |
| 2026-08-04 | **리마인드 되살림 + 등록 계약 필수화 + 여정 겹침 제외** — 코호트(발송 큐 원장)·분리 스팸 검증 / 등록 1회 AI 축 매핑·시나리오 프리필 / 회사 opt-in 게이트 | tsc 0 양쪽 / vitest 136파일 1,982건 / Codex 1R 13건·2R 6건 | §5 · §7-5 |
| **2026-08-04 배포완료** | 위 전량 배포 + DDL 2건 실행완료(`operator_cycle_snapshots` CREATE · `companies.automarketing_exclude_journey` ADD). **남은 DDL 0** | Harold 배포·ALTER 실행 확인 | — |
| 2026-08-05 | **§12-1 매장 미배정 사유를 화면이 쓰게** — 서버가 주던 `blockedReason`을 화면 2곳이 버리고 있었다 | frontend tsc 0 · 소비처 전수 grep(서버 4곳·화면 2곳) | §7-6 |
| 2026-08-05 | **§12-5·6 무과금 마감 + 동시 생성** — 죽은 `try/catch`가 무과금 발송을 `sent`로 마감했고, 제안 생성에 오퍼레이터 단위 직렬화가 없었다. 정산 축을 발송 상태에서 분리 | backend tsc 0 · vitest 137파일 1,992건(신규 10) · 신규 SQL 5문 `PREPARE` 실검증 · `pg_indexes`·과거 정산 대조(7/7) 실측 · **Codex 적대검증 2R**(1R 3건 전량 수용 · 2R 4건 = 수정 1·실측 1·범위 밖 2) | §7-7 |

### §7-6 2026-08-05 매장 미배정 사유 (§12-1)

서버는 매장 미배정을 **네 곳**에서 말하고 있었고, 그중 세 곳은 이미 화면·통지가 받고 있었다 —
`routes/ai.ts:2816`(제안 상세 명단)은 `basisLabel`로 내려 `ProposalDecisionCard`가 Source caption에 그리고,
`ai-orchestrator.ts:433`(제안 생성)은 throw가 `countError`로 잡혀 0건 게이트가 사유와 함께 통지하고,
`continuous-operator.ts:1861`(발송 패스)은 `admin_review`로 돌리며 담당자에게 통지한다.
**못 받던 곳은 `routes/ai.ts:1529`(target-recipients)의 `blockedReason` 하나**였는데, 그 응답을 쓰는 화면이 둘이었다.

- `SegmentPicker.tsx` — `total` 0을 그대로 받아 **"지금 기준 0명 · 수신거부·발송 피로도까지 걸러낸 실제 발송 대상입니다"**.
  걸러낸 결과가 아닌데 걸렀다고 말한다(§2 불변 원칙 2·3).
- `AiOperatorPage.tsx` — `success === false`만 오류로 보고 사유를 지나쳐, 빈 배열을 받은 공용 모달이
  **"조건에 맞는 대상이 0명입니다. 조건을 넓히거나 고객 데이터를 확인해주세요."**를 그렸다.
  원인이 조건이 아닌데 **조건 완화를 유도**한다 — 0건 자동완화를 금지한 원칙이 막으려던 바로 그 방향이다.

**고친 방식** — 사유 문장은 서버가 소유하고 화면은 그리기만 한다(화면이 문장을 지어 붙이면 서버가 사유를 늘릴 때
화면만 거짓이 된다). 공용 `TargetRecipientsModal`은 손대지 않고 **호출부에서 막았다** — 그 모달은 여정·이메일·인앱·DM이
함께 쓴다. `SegmentPicker`는 자기 표시 영역이 있어 잠금 안내를 직접 그리고, `AiOperatorPage`는 그 문장으로 throw해
모달의 기존 오류 표시 경로를 탄다. 백엔드·DDL 무접촉.

⛔ **판정 순서** — 화면의 blocked 분기는 `awaitingBaseline`보다 **먼저**다. 서버도 매장 판정을 기준선 판정보다 먼저 한다.
뒤집으면 매장이 없는 계정에 "첫 회차에 기준을 잡습니다"라는 다른 안내가 나간다.

**남은 관찰 2건**(이번 범위 밖 — §11로)
- `AiOperatorPage`는 `operator_id`를 안 보내 **열람자 기준**으로 센다. 제안 소유자 기준으로 세는 `ai.ts:2816`과 축이 다르다.
- 변화 축 + 신규 등록이면 화면이 서버 호출 자체를 생략해(기준선이 없다는 게 정의) 등록 시점에는 매장 사유가 안 보인다.
  발송 시점 통지로는 나가므로 침묵은 아니다.

### §7-7 2026-08-05 무과금 마감 · 동시 생성 (§12-5·6)

**§12·§11이 적어둔 "sending 복구 이중 차감 가능성"은 실재하지 않았다.** 두 차감 호출부가 같은 멱등키
(`continuous-operator-send:{proposalId}`)를 쓰고, `ai-credit-tx.ts`가 `BEGIN → FOR UPDATE → 잠금 후
idempotency_key 재확인 → ON CONFLICT (idempotency_key) DO NOTHING` 순서라 두 번 빠지는 경로가 없다.

**실재한 것은 정반대 방향이다 — 무과금 발송이 `sent`로 마감됐다.**
`deductCreditSafe`는 잔액 부족(`[CREDIT][SKIP]`)에도 영구 실패(`[CREDIT][MISS]`)에도 **throw하지 않고 정상 반환**한다.
그래서 발송 경로의 `try/catch`는 **발화할 수 없는 죽은 분기**였고 `creditDeducted`가 항상 참이 되어 `status='sent'`가 됐다.
정지복구는 `status='sending'`만 조회하므로 그 순간 재차감 기회가 영영 닫힌다. 주석이 선언한 계약
("차감 실패면 sending 유지 → 정지복구가 재차감 → 유실 0")이 통째로 죽어 있었다.

**뿌리는 반환값이 없다는 것이었다.** `deducted:false`가 세 상황(멱등 중복 · 크레딧제 미적용 · 크레딧 행 없음)에서
똑같은 `empty`로 나와 호출부가 "돈이 빠졌는가"를 물을 방법 자체가 없었다. ⇒ `DeductResult.skipReason`으로 셋을 가르고,
`deductCreditSafe`가 **차감 확정 여부**를 돌려주게 했다. `true` = 차감됨·이미 차감됨·차감 대상 아님 / `false` = 돈이 안 빠졌다.
죽은 `try/catch`는 삭제했다(구조를 고치면 덧댄 장치가 사라지는 방향).

- 소비처 32곳 중 반환값을 쓰는 곳은 0이라 나머지 31곳은 동작이 그대로다(`void → boolean`은 하위 호환).
- **정상 회사·정상 발송은 변화 0.** 크레딧제 미적용(`not_applicable`)과 재시도 중복(`duplicate`)도 `true`라 곧바로 `sent`다.

**그런데 그 첫 처방으로는 결말이 안 바뀌었다**(Codex 1R high). 차감 미확정이면 `sending`을 유지해 정지복구가
재차감하게 했는데, 그 **정지복구가 상태를 먼저 `sent`로 바꾼 뒤 차감을 시도**한다. 그 사이 실패하거나 프로세스가
죽으면 그 행을 다시 찾을 길이 없다 — 30분 유예만 만들고 무과금은 그대로 확정된다.

**진짜 뿌리는 발송 상태와 정산 상태가 한 축에 묶여 있다는 것이었다.** ⇒ **축을 나눴다.**
발송 커밋과 **같은 UPDATE**에서 `proposal_json.meta.chargePending` 마커를 심고, **상태는 정상대로 `sent`로 마감한다**
(화면이 "발송 중"이라 거짓말하지 않는다). 정산은 마커가 durable하게 들고 있다가 `settlePendingCharges` 대조 패스가
**상태와 무관하게** 집어 같은 멱등키로 확정될 때까지 재시도하고, 확정되면 마커를 지운다.
회차 스냅샷의 `cycleSnapshotPending` + `settlePendingCycleSnapshots`와 **같은 형태**다 — 새 장치가 아니라 기존 패턴 재사용.

- 그 결과 `reconcileStuckSending`의 **크레딧 보강 블록은 사라졌다.** 상태 마감만 남는다(구조를 고치면 덧댄 장치가 없어진다).
- 첫 처방의 `'sending'` 유지와 `auto_execute_reason` 덧댐도 함께 사라졌다.
- 마커 UPDATE 자체가 실패하면 대조 근거가 없다 ⇒ 담당자에게 알린다(회차 마감 실패 통지와 같은 자리).

**동시 생성(§12-5)** — `openProps` 확인과 INSERT 사이에 잠금이 없고 그 사이에 수십 초짜리 AI 호출이 있다.
동시 `run-now` 두 건이 둘 다 "예약 없음"을 읽고 둘 다 INSERT하면 **같은 회차 2회 발송·2회 과금**이다(멱등키가 제안별이라 막지 못한다).

처방은 **두 겹**이다. ①INSERT를 `VALUES`에서 `SELECT ... WHERE NOT EXISTS`로 바꿔 **확인과 삽입을 한 문장**에 넣었다
— 창이 수십 초에서 문장 내부로 줄고, **인덱스가 아직 없는 배포~DDL 창에서도 실질 방어가 된다**(Codex 1R high).
②부분 UNIQUE 인덱스가 최종 방어로 남는다(완전 동시 실행은 스냅샷이 갈려 ①의 WHERE를 둘 다 통과할 수 있다).
`shouldSkipProposalGeneration`은 AI 비용을 아끼는 빠른 경로로 남는다.

⛔ **INSERT만 막으면 구멍이 남는다** — `scheduled`로 들어오는 경로는 셋이다(전수 grep).
INSERT · **야간 승인**(`approveProposal`이 `sending → scheduled`로 되돌린다) · 리마인드 CAS 승격.
리마인드는 인덱스 조건에서 빠지므로 안전하고, **야간 승인은 걸린다** — `23505`를 받아 승인을 거절하고
`admin_review`로 되돌리며 `total_approved`도 되돌린다(승인 안 됐는데 카운트가 남으면 그 통계가 거짓이다).
이 건은 Codex가 못 찾았고 내가 `status='scheduled'` 진입 경로를 전수로 훑다가 찾았다.

**run-now 안내 순서도 바꿨다**(Codex 1R medium) — 예약 확인이 기준선 안내보다 **먼저**다.
변화 축은 첫 회차 뒤 기준선이 계속 존재해서, 순서가 반대면 이미 예약된 회차가 있어도
"비교 기준을 잡았습니다"가 나가 담당자가 실제 예약을 못 본다.

```sql
CREATE UNIQUE INDEX ux_operator_proposals_one_scheduled
  ON operator_proposals (operator_id)
  WHERE status = 'scheduled'
    AND COALESCE(proposal_json->'meta'->>'is_reminder', 'false') <> 'true';
```

**2026-08-05 실행완료** — `pg_indexes` 조회로 `ux_operator_proposals_one_scheduled` 설치 확인(Harold 실행). **남은 DDL 0.**
(인덱스가 없는 동안에도 위 ①의 `WHERE NOT EXISTS`가 방어하므로 배포~DDL 창이 비어 있지 않다.)

착수 전 실측으로 닫은 선행 조건 — 위반 행 0(`GROUP BY operator_id HAVING COUNT(*)>1`) · 조건식 정상 평가(전 행 NULL → `false`) ·
리마인드가 `meta.is_reminder=true`를 실제로 달고 저장됨(`:2368` 코드) · `operator_proposals`의 UNIQUE는 기본키 `id` 하나뿐
(`pg_indexes` 실측 4행)이라 대상 미지정 `ON CONFLICT`가 다른 제약을 삼키지 않는다 ·
**신규 SQL 2문(조건부 INSERT · 정산 대조 SELECT)은 운영 DB에서 `PREPARE`로 구문·타입·JOIN 검증 통과**
(실행 없이 파싱·계획만 — `tsc` 통과가 SQL 유효를 보장하지 않으므로 이 단계를 별도로 거친다).

⚠ **`scheduled` 행이 현재 0이라 인덱스가 막는 장면은 운영 데이터로 검증할 수 없다**(§10 실사용 0). 검증은 코드와 단위 테스트가 진다.

### 7-7-1. Codex 적대검증 2라운드 판정 (라운드 상한 도달)

2R = `needs-attention` · high 4건. **처리 = 수정 1 · 실측으로 닫힘 1 · 범위 밖 2.** 라운드 상한(2회)이라 여기서 멈췄다.

- **수정(기아)** — 정산 대조가 정렬 없이 `LIMIT 20`이라 영구 실패 20건이 앞자리를 차지하면 뒤에 쌓인 미정산
  발송이 한 번도 시도되지 않는다. ⇒ 시도 직전에 `meta.chargeAttemptAt`(UTC ISO)을 남기고 **미시도 우선 →
  오래 안 해본 순**으로 정렬한다. 캐스팅 없이 문자열 비교라 형식 오류로 패스가 죽지 않는다.
- **실측으로 닫힘(과거 미정산 행)** — "`sending` 0건"은 **배포 전에 이미 `sent`로 마감된 무과금 행을 증명하지 못한다**는
  지적이 옳았다(종전 코드는 차감 실패도 `sent`로 마감했다). 대조 결과 **캠페인이 커밋된 제안 7건 전부**
  크레딧 원장에 `continuous-operator-send:{id}` 키가 있다(분모 7 · 정산 7). 마커 없는 사각지대에 남을 과거 행은 0이다.
- **범위 밖 2건은 §11로** — 캠페인 커밋과 정산 의무의 원자성 · `pending` 승인 시 기존 예약 미확인.

⛔ **알려진 한계(다시 적어둔다)** — 인덱스가 없는 동안 완전 동시 실행은 스냅샷이 갈려 `WHERE NOT EXISTS`를
둘 다 통과할 수 있다. 그래서 인덱스가 최종 방어다. 이건 결함이 아니라 **DDL을 배포 직후에 돌려야 하는 이유**다.

**한계(기록)** — 이 인덱스는 `pending` 다건을 막지 않는다. 그건 기존 계약이 허용하는 상태다
(`operator-proposal-dedup.test.ts`가 "pending/admin_review만이면 skip 안 함"으로 명시). 사람이 두 건을 각각 승인하면 두 번 나간다.

### §7-1 2026-08-03 재설계가 실제로 바꾼 것

- **WHERE 조각 4벌 → 1벌.** count·명단·staging·예측 분모가 같은 조각을 쓴다. 게이트를 빠뜨릴 자리가 사라졌다.
- **대상 수 단일 문 신설**(`operator-audience.ts`). filters를 직접 받는 문을 없애 우회를 구조로 막았다.
- **세그먼트 계약 레지스트리 신설** — 자연어 지시 6종 → 근거 판정 + SQL 고정.
- **결정성** — 계약이 있으면 회차마다 AI가 다시 해석하지 않는다.
- **fail-open 4곳 폐쇄** — 대상 수 실측 실패 시 AI 추정치로 발송 / 미지의 축을 전체로 강등 / 계약 저장 실패를 성공 처리 /
  0건 통지 기록을 효과보다 먼저.
- **매장 범위** — 미리보기만 적용되고 실발송은 전사였다. 세는 곳·뽑는 곳·보내는 곳을 같은 범위로.
- **돈** — 발송 직전 실측 건수·현재 단가로 상한·예산 재검사, 검사와 예약을 한 트랜잭션에서.
- **조용한 0건 제거** — 제안 생성 시점 0건에 사유 통지(7일 쿨다운, 대상이 잡히면 창 재개방).

### §7-2 Codex 적대검증 8라운드 요지

라운드가 갈수록 지적의 성격이 옮겨갔다 — 1~4R은 이번 변경이 만든 결함, 5~6R은 이 트랙이 드러낸 기존 구조 결함
(매장 범위·역할값), 7~8R은 발송·과금 파이프라인 전반. **대상 선정 축 자체는 3R 이후 새 결함이 나오지 않았다.**

같은 부류가 두 번 이상 나온 것(= 구조부터 고쳐야 한다는 신호):
- **원자성** — 등록·수정·예산 예약이 각각 2단계였다. 전부 한 문장/한 트랜잭션으로 모으자 보상 코드가 사라졌다.
- **단일 문 우회** — WHERE를 모았는데 리마인드가 우회했고, 게이트를 모았는데 포지셔널 인자가 흘렸다.
- **DB 어휘 vs 토큰 어휘** — `company_admin`이 DB에 없다는 사실 하나가 권한·통지 두 곳을 동시에 망가뜨리고 있었다.

### §7-3 2026-08-04 변화 축 신설이 실제로 바꾼 것

- **정체성이 먼저였다.** 축을 늘리는 것이 아니라 "무엇을 여기 넣고 무엇을 여정에 두는가"의 기준(§0)을 세웠다.
  기준이 서자 처음 제안했던 축 셋 중 둘(장바구니·상품 조회)이 애초에 이 기능 것이 아니었음이 드러났다.
- **회차 스냅샷 하나로 축 다섯이 열렸다.** 등급 상승만을 위해 등급 관측 표를 만드는 것과 공사가 비슷한데
  얻는 것이 다섯 배다. 비교 항목을 하나 더 담으면 축이 하나 더 열린다.
- **파라미터 화면 일반화** — 종전 화면은 `days` 하나만 그려서 금액 조건이 화면에 없었다. 축이 정의한 만큼
  전부 그리게 바꾸고, 프런트에 있던 빠른 선택값 상수(`QUICK_DAYS`)를 계약 쪽으로 옮겼다(축마다 자연스러운
  구간이 달라 화면이 목록을 들면 금액 축에 '30일' 버튼이 뜬다).
- **테스트 표본 정정** — `cart_abandoned`가 "미등록 축" 표본으로 쓰이고 있었다. 그 이름으로 축을 신설하면
  fail-closed 불변식이 **초록인 채 의미만 사라진다**(메시지를 안 보는 `toThrow()`라 더 조용하다).
  표본을 영구 금지 축(예측 기반)으로 바꾸고 메시지까지 단정하게 했다.
- **DDL 상태 실측** — "3컬럼 대기"로 알려져 있었으나 `segment_key`·`segment_params`·`zero_target_notified_at`
  전부 이미 실행돼 있었다. 이번 표(`operator_cycle_snapshots`)와 여정 제외 컬럼까지 2026-08-04 실행완료 — **남은 DDL 0**.

### §7-4 2026-08-04 Codex 1라운드 — 12건, 뿌리 4개 (개별 수정이 아니라 구조 수정)

같은 부류가 반복되면 구조부터 고친다는 원칙이 이번에도 맞았다. 12건 중 10건이 뿌리 4개로 묶였다.

| 뿌리 | 반복된 지적 | 구조 수정 |
|---|---|---|
| R1 근거 판정과 컴파일이 한 함수 | 저장 500 · 명단 500 · 화면 오안내 (3회) | `assertSegmentUsable` 분리 — 저장은 근거만 묻는다 |
| R2 "변화가 이 구간에서 일어났는가"를 안 물음 | backfill 오발송 (첫 구매·복귀·증가) | `cycleCompare` 구매창 가드 공통 옵션 |
| R3 스냅샷 범위 ≠ 발송 범위 (전체 교체) | 매장 밖 소진 · 신규 고객 영구 제외 (2회) | 전체 교체 폐기 → 보충(DO NOTHING)·발송분 갱신(DO UPDATE) 두 문 |
| R4 큐 수락 = 발송 성공으로 오인 | 전량 실패 시 변화 소진 · 갱신 실패 삼킴 (2회) | 종결 대조 패스(settle) — 마커 durable·재시도 수렴 |
| 단독 | 최소 금액 0원 · 회사 결합 없음 · 기준선 실패 침묵 | min 1 · `s.company_id` 결합 · 실패 통지 |
| [범위 밖] | 동시 run-now 제안 중복(이전부터 있던 구조) | `continuous-operator.ts:674` — 착수 판단은 Harold님 |

검증 = 전체 1,981건(신규 16) / 주입 2회(fail-closed 축·구매창 가드) 실패 확인 후 원복 / `recordCycleSnapshot` 잔존 0.

**2라운드(증분 — 고친 줄+직접 호출부만, 여기서 라운드 종료)** — 12건 = 수용 6 · 불수용 3 · 잔여 등재 3.

수용(반영 완료): 발송 커밋 마커 실패 = 재시도+담당자 통지(F2) · settle 조회를 종결 상태로 한정(기아 차단, F4) ·
completed+큐 0건 = 마커 유지·재경보(F5) · 기준선 판정보다 근거 판정 먼저(F7a) · 명단 경로 503 분기(F7b) ·
기준선 실패 통지 false 처리(F8).

불수용(사실은 맞고 처방은 기각 — 근거):
- **"completed는 통신사 성공이 아니다"(F1)** — 맞다. 그러나 통신사 단계 실패 번호를 재발송하는 경로는 이
  시스템 어디에도 없다. 변화 축만 실패 번호를 다음 회차에 다시 잡으면 전 발송 경로와 다른 동작이 된다.
  **적재 성공 = 발송**이 이 시스템의 경계이고, settle이 닫는 것은 전량 실패(적재 0)다 — 의도된 제품 판단.
- **"동일번호 매장별 전 행 갱신"(F6b)** — 발송은 사람 단위로 한 번 나갔고 그 사람의 변화는 알린 것이다.
  변화 축은 사람 단위 마케팅이라 사람 단위 소진이 맞다.
- **"화면 debounce 경합"(F7c)** — reqSeq 가드 + 효과 시작 시 리셋이 실존, 코드 재확인으로 기각.

잔여 등재(별도 처리 — 지금 안 닫는 근거):
- **같은 날·혼합 backfill**(F3 잔여) — `recent_purchase_date`가 시각 없는 date라 날짜보다 촘촘한 비교가
  불가능하다. 제대로 닫으려면 구매 원장 창 합산인데 그건 리마인드 트랙(원장·발송결과 축)과 같은 공사다.
- **발송→settle 사이(분 단위)의 새 변화 소진**(F6a) — 막으려면 발송 시점 값 보관(2세대 스냅샷)이 필요해
  기계가 늘어난다. 창이 분 단위라 잔여로 수용.
- **[범위 밖] 매장 미배정 blockedReason을 화면이 미소비**(`ai.ts:1526`·`SegmentPicker.tsx:90`) — 0명으로 표시.

### §7-5 2026-08-04 마무리 2건 — 리마인드 되살림 · 등록 계약 필수화

**리마인드** = §5가 소유(구조 전문). 요지 — 하드 차단 제거, 코호트는 settle과 같은 문(`readCampaignQueuedPhones`),
스팸 검증은 예약 시점 1회(AI 재작성 금지), 모든 실패류 통지.

**마무리 2건의 Codex 1R = 13건 반영** — 뿌리는 또 하나였다: **보류를 풀 때 보류기의 장치를 전부 걷지 않았다**
(격리 3종 잔존 = 예약은 되는데 다음 패스가 전부 정지). 그 외 = 코호트 SQL 문법(DISTINCT 위치)·성공 코드 필터·
동기 스팸 블록·명단 코호트 불일치·비용 복사·명시 선택 덮어쓰기·시나리오 오프리필·OFF 미취소 등 — 상세 §5.
불수용 4(저장 전 확인 단계 = 1클릭 제품 판단 / confidence threshold / 수동 승인 즉시 발송 = 기존 계약 /
settle 큐 적재 경계). [범위 밖] `ai-credit.ts:119` deductCreditSafe가 실패에도 resolve(기존 구조 — 등재만).

**2R(증분 — 여기서 라운드 종료)** = 닫힘 7 · 미닫힘 6 전부 재수용·반영: 미클릭 기준을 캠페인 커밋 직후로 /
리마인드 수신 수·비용 = 실발송 실측(claim 시점 값 금지) / 스팸 실패 통지도 CAS(사람 판단과 모순 통지 금지) /
'검증 중' 좀비 대조 sweep(10분 초과 = 확인 요청 전환+통지) / `target_hint` 명시 선택(캘린더)에 매핑 비개입 /
OFF 취소 실패 = 재시도+통지 / run-now 조회 회사 결합 / 문안 안전화를 저장 문(등록·수정)으로.
부분 불수용 3 — OFF와 sending의 경합(이미 발송 진행 = 되돌릴 수 없음, 문서화된 한계) · run-now 안내 조회
실패의 503 승격(성공한 실행을 실패로 보이게 한다 — 기능 우선) · 승인 시 편집 본문(userSelection)의 EUC-KR
미안전화(전 제안 공통 기존 경로 — 리마인드 신설분 아님, 별도 등재).

**등록 계약 필수화(설계서 §5-B ③)** — 결정성이 기본 등록 경로에 닿지 않던 마지막 구멍.
- **자연어·오늘의 추천**(모달을 안 거치는 1클릭 경로): 서버가 **등록 1회에 한해** AI로 목표를 축에 매핑
  (`suggestSegmentForObjective`). 후보는 그 회사에서 지금 열려 있는 축뿐(정답표 금지 그대로), 목록 밖·확신
  없음 = null = 자유 해석 등록(매핑 실패로 등록을 막지 않는다 — 기능 우선). 파라미터는 계약 범위로 절단.
  무엇으로 고정됐는지 응답(`appliedSegment`)에 실어 화면이 즉시 토스트로 알린다 — 몰래 고정 금지.
  ⛔ **모달을 거친 등록에는 매핑이 개입하지 않는다**(`segment_choice_seen`) — 화면이 보여준 "자동 판단" 선택을
  서버가 덮으면 화면이 거짓말이 된다. 매핑된 축이 저장 불가(표 미생성 등)면 매핑을 버리고 자유 해석으로 등록.
- **시나리오**: 축과 정확히 일치하는 카드만 계약 프리필(휴면 60일·신규 30일·전체·생일·상위 등급) —
  모달 SegmentPicker에 선택된 채 떠서 담당자가 확인 후 저장(§4-3의 "AI 제안 → 확인 → 계약" 그대로).
  결합 조건(VIP+휴면)·축에 없는 대상(승급 근접·상품 관심·포인트)은 프리필 없음.
- **1클릭 원칙 유지** — 어느 경로에도 추가 입력·중간 단계가 늘지 않았다.

---

## §8 뒤집힌 판단 (같은 실수 반복 금지)

**⑤ 여정 축 목록을 자동마케팅 축 목록으로 그대로 옮기려 했다 — 틀렸다(2026-08-04 Harold 지적).**
설계서 §4-2 표가 여정이 세운 축을 그대로 나열했고 나는 그것을 착수 목록으로 읽었다. 여정 축이라고
자동마케팅 축인 것이 아니다. **가르는 기준은 §0이고, 그 기준이 없으면 두 기능이 같은 일을 해서 나눈 의미가 사라진다.**

**⑥ "월간이 자동마케팅의 정체성"이라 잡았다 — 틀렸다(Harold 지적).** 매주 도는 반복 마케팅도 자동마케팅이다.
주기는 설정값이지 정체성이 아니다. 정체성은 **시계의 주인**이다.

**⑦ "등급 상승은 결정성을 깨서 자동마케팅에 넣을 수 없다"고 판정했다 — 틀렸다.**
여정의 결정성 개념(같은 조건이면 언제 돌려도 같은 명단)을 그대로 갖다 댔는데, 자동마케팅은 회차형이라
**결정성의 뜻이 "같은 회차·같은 데이터면 같은 명단"**이다. 이번 달 생일자가 지난달과 다른 것은 정상 동작이다.
→ 다른 기능의 원칙을 옮길 때 그 원칙이 **그 기능의 시간 축에서도 같은 뜻인지** 먼저 확인한다.

**⑧ 운영 데이터로 결함을 판정했다 — 근거가 아니었다(Harold 지적).** 이새 원장이 얇은 것을 보고
"휴면 축이 이미 어긋나 있다"고 보고했는데, 실사용 고객사가 0인 시스템에서 그 숫자는 결함의 증거가 못 된다.
그 원장이 얇은 것은 싱크에이전트 1.5.7의 알려진 결함이고 원인·처방이 이미 확정돼 있었다.
그리고 그 증상을 자동마케팅 화면에 신선도 표시로 덧대려 했는데 **그게 땜질이다** — 근본은 1.7.0 설치와 재싱크다.
→ **결함 여부·수정 여부는 코드로 판정한다.** 운영 데이터는 표본이 있을 때만 근거가 된다.

**⑨ 요약 컬럼과 원장의 괴리 0을 "정확하다"로 읽었다 — 틀렸다.** 둘은 같은 문(`sync-ingest` → 구매요약 재계산)에서
갱신되므로 **같은 누락을 공유한다.** 일치는 "정확하다"가 아니라 "같은 문을 지난다"까지만 증명한다.

**① "축 6종이 정답표다" — 부분적으로 틀렸다.** 필드·값 축은 이미 런타임 판정이었다(`detectActiveFields`가
실제 값 있는 컬럼만 AI에 주고 DISTINCT 실측값 200개까지 준다). 틀린 것은 **사건 축**이지 필드 축이 아니었다.
→ 코드를 끝까지 읽기 전에 구조를 단정하지 않는다.

**② `orchestrate` 호출자가 하나뿐이라고 단정했다 — 틀렸다.** grep 패턴이 `orchestrate\(`라
`const fn = flag ? orchestrateWithAI : orchestrate` 형태의 **참조**를 놓쳤다. 실제로는 공용 제안 경로도 쓴다.
→ 축을 grep할 때 호출 형태만 보지 않는다(별칭·참조가 축을 은폐한다 — 0731 브랜드 표시 축 교훈의 재발).

**③ "1차 수신자 명단이 어디에도 없다" — 틀렸다(Harold 지적).** PG staging만 보고 단정했는데
**발송결과(MySQL SMSQ)에 `app_etc1` 기준으로 남아 있다.** 새 표를 만들 뻔했다.
→ "없다"고 쓰기 전에 그 데이터를 이미 갖고 있을 만한 다른 저장소를 먼저 본다.

**④ 리뷰 권고를 그대로 받아 기능을 두 번 멈췄다(Harold 지적).** 리마인드 전면 차단, 그리고 부분 스키마 폴백 제거
(배포~DDL 구간에 자동마케팅 수정 저장이 전부 503이 되는 상태였다).
→ **Codex의 사실 지적은 검증해서 받고, 처방은 내가 다시 정한다. 기능을 멈추는 처방은 Harold 결정 사항으로 올린다.**
Codex의 권고는 일관되게 "막아라·끄라·배포하지 마라" 쪽으로 기울어 있다 — 리뷰어로서 안전한 기본값이지 상품 판단이 아니다.

---

## §8-1 영구 판정 기준 (2026-08-03·04 Harold 확정 — 이 트랙의 의사결정 규칙)

- **기능이 우선이다.** 정확도를 이유로 기능을 끄는 것은 수정이 아니다. 막아야 하면 사유와 대안을 함께 올린다.
- **Codex의 사실 지적은 검증해서 받고, 처방은 내가 다시 정한다.** 권고는 일관되게 "막아라·끄라·배포하지 마라"로
  기울어 있다 — 리뷰어의 안전한 기본값이지 상품 판단이 아니다. 기능을 멈추는 처방은 Harold 결정 사항.
  (사실 지적 자체의 정확도는 높았다 — 역할값·staging 스키마·로그인 게이트·`orchestrate` 공용·코호트 SQL 문법·
  격리 장치 잔존까지 전부 실측으로 확인됐다.)
- **결함 여부는 코드로 판정한다.** 실사용이 0인 시스템에서 운영 데이터는 근거가 못 된다(§8 ⑧).
- **다른 기능의 원칙을 옮길 때는 그 기능의 시간 축에서도 같은 뜻인지 먼저 확인한다**(§8 ⑦ — 결정성의 뜻이 갈렸다).

---

## §10 실측으로 확정된 사실 (재질의 금지)

착수할 때마다 다시 묻지 않는다. 값이 바뀌었을 근거가 생기면 그때만 재실측한다.

| 사실 | 값 | 왜 중요한가 |
|---|---|---|
| `users.user_type` 실측 분포 | admin 126 / user 101(매장 배정 7) / system 75 | **`company_admin`·`company_user`는 JWT 변환값이라 DB에 없다.** 토큰 어휘로 권한을 판정하면 제한 사용자가 전사로 승격된다(SCHEMA users 행 등재) |
| `campaign_send_staging` | phone·name만(customer_id 없음) + 워커가 수신거부·중복 삭제 후 전체 삭제 | 사후 조인으로 1차 코호트를 복원할 수 없다 → 코호트 원천은 발송 큐(§5) |
| 자동마케팅 실사용 고객사 | **0** (오퍼레이터 37건 전부 인비토 내부 계정 · 활성 2건도 인비토 · 승인 이력 4건) | 파괴적 변경 창이 열려 있다. 동시에 **운영 데이터로 결함을 판정할 수 없다**(§8 ⑧) |
| 회사 해지 / 로그인 게이트 | 해지 = 전 사용자 `users.status='dormant'`(admin.ts) / 로그인 = `is_active AND status='active'`(auth.ts) | 매장 범위 판정이 이 조건을 그대로 써야 해지 경로가 함께 닫힌다(§4) |
| 구매 원장 보유 회사 | 이새에프앤씨 1곳(185만 행) · **상품코드·상품명 0건** · 매장코드 100% · 금액 99.96% | 상품 파라미터는 코드상 근거가 있으나(`sync-ingest`가 적재) **검증 표본이 없다** — 0건은 통과가 아니다 |
| `customers` 요약 ↔ `purchases` 원장 | 괴리 0(2026-08-04 실측) | 정확성의 증거가 **아니다** — 둘은 같은 문에서 갱신돼 같은 누락을 공유한다(§8 ⑨) |

---

## §12 남은 개발 — 다음 세션 착수 목록 (이 절이 유일한 착수 원장)

> 왜 미뤘는지의 근거는 §7-4·§7-5·§11이 갖는다. **무엇을 할지는 여기만 본다.**
> 진입 = 호출어 **"자동마케팅"** → 이 절부터. 운영을 막는 건은 **0**이다(전량 배포완료 후 잔여).

| 순 | 무엇 | 범위 | 착수 조건·근거 |
|---|---|---|---|
| ~~1~~ | ~~매장 미배정 사유를 화면이 쓰게~~ — **2026-08-05 완료**(§7-6). 소비처는 `SegmentPicker` 하나가 아니라 **둘**이었다(`AiOperatorPage`도 같은 응답을 쓴다) | `SegmentPicker.tsx` · `AiOperatorPage.tsx` | **배포완료**(커밋 `08605ab9` — 0806~0808 배포에 실려 나감) |
| 2 | **상품·매장 조건 파라미터** — `purchased` 계열에 상품·매장 선택 | `automarketing-segment.ts` + `Record<string, number>` 타입을 backend 5·frontend 4에서 확장 | ⛔ **상품 데이터 실측 0건** — 데이터가 들어오는 회사가 생긴 뒤. 지금 만들면 검증 표본 0인 코드가 9파일에 퍼진다 |
| 3 | **backfill 정밀도** — 같은 날·혼합 backfill이 변화로 오인될 수 있다 | `cycleCompare` 구매창 가드 → 구매 원장 창 합산으로 | `recent_purchase_date`가 시각 없는 date라 날짜보다 촘촘한 비교 불가. **리마인드가 판 원장·발송결과 축과 같은 공사** — 그 트랙을 다시 열 때 함께 |
| 4 | **발송→마감 사이 창** — 분 단위 사이 새 변화가 소진될 수 있다 | 발송 시점 값 보관(2세대 스냅샷) | 기계가 는다. 창이 분 단위라 후순위 |
| ~~5~~ | ~~동시 `run-now` 제안 중복~~ — **2026-08-05 완료**(§7-7). 조건부 INSERT(한 문장) + 부분 UNIQUE 인덱스 + 야간 승인 `23505` 처리 | `continuous-operator.ts` · `routes/ai.ts` | **DDL 실행완료**(설치 확인) · 남은 DDL 0 |
| ~~6~~ | ~~sending 복구 이중 차감 가능성~~ — **실재하지 않았다**(멱등키 동일 + DB UNIQUE). 대신 정반대인 **무과금 `sent` 마감**을 찾아 고쳤다 | `ai-credit-tx.ts` · `ai-credit.ts` · `continuous-operator.ts` | 2026-08-05 완료(§7-7) · **배포완료**(커밋 `08605ab9`) |
| 7 | **예산 집계 축** — D+N 리마인드 비용이 생성일에 계상되고 발송일엔 빠진다 | 예산 SUM `created_at` 축 | 전 제안 공통이라 자동마케팅만 못 바꾼다. 정산·예산 트랙과 함께 |

**개발 아님 — 결정만 남은 것**: 기존 운영 오퍼레이터를 새 계약으로 옮길지(기본 = 옮기지 않음, 실사용 0이라 실질 영향 0 — 설계서 §6 미결 2).

**화면 실측 9건**(개발과 별개, 배포분 검증): 변화 축 잠금·사유 / 첫 회차 안내 / 파라미터 프리셋 / 자연어 축 고정 고지 / 리마인드 토글·문안 필수 / 설정 여정 제외 토글 / 0건 통지 / **매장 미배정 계정으로 축 선택 시 "0명" 대신 사유 노출**(0805) / **이미 예약된 회차에서 [지금 실행] 시 "이미 예약되어 있습니다"**(0805).

---

## §11 이 트랙이 발견했지만 범위 밖인 것 (착수 판단 = Harold)

- `routes/manage-users.ts:216` — DB 역할 `admin` 대신 `company_admin` 비교 → **관리자 계정 삭제 방지 무력화**(보안).
- 일회성 발송 경로 통합 — `preview-recipients`가 JWT 역할·피로도 없이 조회. 발송 파이프라인 보호 영역이라 영향표 선행.
- ~~`continuous-operator.ts` — 동시 `run-now` 제안 중복 · sending 복구 이중 차감 가능성~~ → **2026-08-05 해소·정정**(§7-7). 이중 차감은 실재하지 않았고, 실재한 것은 무과금 `sent` 마감이었다. **정지 판정 TOCTOU는 미착수로 남는다.**
- ~~`utils/ai-credit.ts:119` — `deductCreditSafe`가 잔액 부족·최종 실패에도 resolve~~ → **2026-08-05 해소**(§7-7). 여전히 throw는 하지 않되 **차감 확정 여부를 반환**한다.
- **캠페인 커밋과 정산 의무의 원자성**(Codex 2R high · 2026-08-05) — 캠페인이 먼저 커밋되고 그다음에 `chargePending` 마커·차감이 온다. 커밋 직후 프로세스가 죽으면 `campaign_id`도 마커도 없어 자동 대조가 불가능하다. 근본 처방은 **발송 직전 크레딧 fail-closed 예약 + 정산 outbox**인데, 이는 잔액 부족 시 **발송을 막는 정책 결정**을 포함하므로 §2 불변 원칙 8에 따라 Harold 결정이다. 발송 파이프라인 보호 영역이라 착수 시 영향표가 선행한다. ⚠ 이번 변경이 만든 구멍이 아니라 **외부 발송 커밋과 우리 원장이 두 시스템**이라는 기존 성질이다(6원칙 ③의 자리).
- **`pending` 승인이 기존 예약을 확인하지 않는다**(Codex 2R high · 2026-08-05) — 야간 승인은 인덱스에 걸리지만 주간 승인은 곧바로 발송이라 인덱스를 지나간다. 같은 회차에 `scheduled`와 `pending`이 공존하면 둘 다 나갈 수 있다. 다만 **"`pending` 다건 허용"은 기존 계약**이고 `operator-proposal-dedup.test.ts`가 명시하므로, 승인 시점 확인을 넣는 것은 승인 계약 변경이다 — 별건.
- ~~`routes/ai.ts:1526` · `SegmentPicker.tsx:90` — 매장 미배정 `blockedReason` 미소비~~ → **2026-08-05 해소**(§7-6).
- `AiOperatorPage.tsx`의 target-recipients 호출이 `operator_id`를 안 보내 **열람자 기준**으로 센다 — 제안 소유자 기준인 `ai.ts:2816`과 축이 다르다(2026-08-05 §7-6에서 발견, 미착수).
- 승인 시 편집 본문(`userSelection`)의 EUC-KR 미안전화 — 전 제안 공통 기존 경로(리마인드 신설분 아님).
- 예산 SUM이 `created_at` 축이라 D+N 리마인드 비용이 생성일에 계상된다(전 제안 공통 구조 축).

---

## §9 이 기능이 절대 하지 말 것

- **사건 반응 축을 여기 넣지 않는다** — 장바구니 포기·상품 조회·배송 시작은 여정이 구조적으로 더 잘한다(§0).
  새 축을 더하기 전에 "방금 무엇을 했다"인가 "지금 어떤 상태다"인가부터 답한다.
- 예측 점수로 타겟을 고르지 않는다.
- 0건일 때 조건을 자동으로 완화하지 않는다(어떤 경로에도 넣지 않는다).
- AI가 구체 혜택을 만들지 않는다.
- 원장을 복사하지 않는다 — 직독한다.
- 마이그레이션은 배포 뒤에. 워커는 트래픽과 무관하게 돈다.
- 정확도를 이유로 기능을 임의로 끄지 않는다 — 막아야 한다면 사유와 대안을 함께 올린다.
