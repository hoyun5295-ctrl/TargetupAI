# 대행발송 셀프 접수 — 기능 상설 SoT

> **호출어: "대행발송"** — Harold님이 대행발송을 언급하면 이 문서를 먼저 연다.
> 이 문서는 **대행발송의 현재 모습과 이력 색인**을 소유한다. 시점별 설계 근거·경위·실측 절차는 설계서가 소유하고 여기엔 링크만 둔다(`doc_ownership`).
> 상태·잔여는 STATUS §2 카드가 소유한다. 여기엔 **구조와 원칙**만 남긴다.
> ★2026-08-26 신설 — 배포·운영 중인 기능인데 상설 문서 없이 998줄 날짜 설계서가 그 역할을 겸하고 있었다(0803 등재 규칙 위반 상태였다).

| 시점 설계서 | 소유하는 것 |
|---|---|
| [2026-08-22 설계서 §0~§14](2026-08-22-agency-send-design.md) | 1차 구상 원문 + 구현 중 정정 경위. **§12 = 0823 판정·정정 5건**(예약 배관 위임·시각 상수 분리·변수 슬롯 번역) · **§14 = 당일 접수 건 재검사 폐지**(Harold 0823(2)) · §12-F 소유권 축 · §12-G 계약 10 |
| [같은 문서 §15~§17](2026-08-22-agency-send-design.md) | 0825 화면·입구 축 — 화면 개편 §15 · 헤더 두 문 §15-6 · **담당자 링크 승인 §16**(fragment 토큰·수용 위험 §16-3) · **요청서 원스텝 §17**(파서 계약 §17-5 · AI 매핑 §17-6 · 명단 미리보기 §17-7) |
| [같은 문서 §18](2026-08-22-agency-send-design.md) | **이메일 접수(챕터 2) 전량** — 구조 §18-2 · 자동 확정 조건 §18-3 · 파서 보강 §18-4 · 워커 절차 §18-5 · 멱등 3층 §18-6 · 수치 §18-7 · **회의론자 검증 15건 반영표 §18-8** · 갈린 지점 §18-9 · DDL §18-10 · 구현·검증 이력 §18-11-1 · **배포 후 실측 7 §18-12** · 수용 위험 §18-13 |
| [같은 문서 §21](2026-08-22-agency-send-design.md) | **메일 한 통 = 요청서 여러 건 + 양식 발송 ID 칸**(★0905 브레인스토밍 4역할 수렴) — 두 분류 §21-1 · 확정 규칙 §21-2 · **선행 결함 2건 §21-3**(회신 게이트 미집행 · 멱등 대조 키) · 다중 설계 §21-4 · **회귀 보증 기준본 8개 §21-5** · 갈린 지점과 접은 것 §21-6 · 수용 위험 8 §21-7 · 구현 순서 §21-8 · 실측 §21-9 |
| [OPS.md §2-2-D](../status/OPS.md) | **이메일 워커 운영 명령** — 가동 확인 · ENV · 정지 상태 확인과 재개 SQL · 긴급 정지 3단 · 현황 SQL · 경보 3종 · 상한 조정 위치 |

---

## 1) 대행발송이란 — 제품 정의

**사람이 하던 대행발송의 손을 전부 기계로 옮기되, 발송을 결정하는 손(승인)만 담당자에게 남긴다.**

- 경위 = Harold 2026-08-22 "직원 2명이 대행발송에 붙어 있다. 슈퍼관리자가 체크한 업체에만 열고, 담당자가 등록하면 스팸 검사·다듬기·테스트 문자·승인·예약·당일 재검사까지 기계가 돌게 하자."
- 흐름 = **접수 → 1차 스팸 검사(AI 다듬기 포함) → 담당자 테스트 문자 → 승인 → (발송일이 다르면) T-2h 재검사 → 큐 적재 → 발송.**
- **입구는 셋, 그 뒤는 하나다.** 어느 입구로 들어와도 같은 원장·같은 워커·같은 승인 게이트를 지난다.

| 입구 | 무엇 | 사람이 확인하는 자리 |
|---|---|---|
| 화면 접수 | 명단 업로드 + 문안 직접 입력(3단계 Composer) | 접수 화면 자체 |
| 요청서 원스텝 | 요청서 xlsx + 명단, 두 파일로 끝(§17) | 확인 화면(strict 스냅샷) |
| **이메일 접수** | hanjullo 메일함으로 같은 두 파일을 보내면 자동 접수(§18) | **확인 화면이 없다** → 접수 완료 회신 메일 + 담당자 테스트 문자가 그 역할 |

- 노출 = **메뉴는 모든 회사에 보인다(미끼).** 진입은 슈퍼관리자 스위치 AND 유료 요금제(`canUseAgencySend` 한 곳이 판정).
- 돈 = **전액 고객사 귀속.** 정산은 이미 "누구 ID로 나갔나"로 굴러가므로 이 축이 정산에 새로 할 일이 없다.

---

## 2) 불변 원칙 (어길 수 없는 것)

> 전문·경위는 설계서 §3(불변 11개)·§12-G(계약 10)가 소유한다. 여기는 **다시 손댈 때 반드시 지나는 것**만.

1. **⛔ 승인 없는 발송 0 · 발송일 당일 검사 없는 발송 0.** 워커가 죽어 검사가 안 돌았으면 그 건은 `expired`로 안내하고 나가지 않는다.
2. **⛔ 큐 적재는 당일 검사 통과 뒤 1회뿐이고, 적재는 직접발송 배관에 위임한다.** 이 축이 `campaigns` INSERT·`bulkInsertSmsQueue`를 직접 쓰면 선불 차감·수신거부 제외·부분 적재 롤백·재시도 멱등이 한꺼번에 빠진다(0823 실측).
3. **⛔ 정산 축에 새 값을 만들지 마라.** `send_type='direct'`로 그냥 탄다. 새 값은 청구·동기화 축에 없어 빠지고, 넣으려면 정산 CT를 건드리게 된다(0823에 실제로 그랬고 전량 원복).
4. **⛔ 접수 쓰기는 `createRequestCore` 하나다**(CT = `utils/agency-send-intake.ts`). 입구가 늘어도 검증·트랜잭션·적재 대조는 이 한 곳이다. 라우트에 코어를 다시 정의하지 마라(계약 테스트가 잡는다).
5. **⛔ 소유권은 두 축뿐**: `revision`(담당자 경로 CAS) · `lock_token`(워커 경로). **타임스탬프를 토큰·CAS 조건으로 쓰지 마라** — PG 마이크로초와 드라이버 밀리초가 왕복에서 어긋나 정상 소유자도 0행이 된다(0823에 배웠는데 **0826 새 파일에서 재현했다**).
6. **⛔ `final_test_at`은 통과 분기에서만 찍는다.** 시도 시각(`last_test_at`)으로 "당일 검사 통과"를 판정하면 차단된 문안이 검사 없이 예약된다.
7. **⛔ 문안 변수는 주소록 슬롯 4칸으로 번역해 넘긴다**(`agency-send-vars.ts`). 치환 CT는 값을 **DB 컬럼 이름**으로 찾으므로 변수명을 키로 넘기면 전 건이 빈칸으로 발송된다(에러도 로그도 없다).
8. **⛔ 확정 경로에서 AI를 부르지 마라.** 미리보기가 추천하고, 화면이 그 매핑을 조정값으로 되보내고, 확정은 조정값만 믿는다(`aiSuggest=false`). 이메일은 전 구간이 확정 경로다.
9. **⛔ 양식의 원본은 xlsx가 아니라 생성 스크립트다**(`packages/backend/scripts/build-agency-request-form.js`). 손으로 고치면 파서 계약(★0826(2)·(3) 통일 양식 = **업체 실물 복제** · 시트 "내용"+"고객리스트" 한 파일 · 라벨 별칭 · 구양식 시트 "요청서"도 계속 읽음)이 깨진다. 바꾼 뒤 **파서 왕복 실측**이 같은 커밋에 들어간다. **값 칸 안내문은 실물 원문만**(파서 PLACEHOLDER_VALUES와 짝) — 새 안내문을 값 칸에 만들면 목록이 늘어난다.
10. **⛔ 이 축의 모든 문구에 줄표 0**(모달·버튼·안내 문자·회신 메일 전부). 계약 테스트 = `em-dash-invariants.test.ts`.
11. **⛔ 일반 사용자(company_user)는 본인 접수만, 관리자(company_admin·super_admin)는 회사 전체**(★0826(2) 서수란 접수·Harold 확정). 고객 라우트에 행 접근 경로를 새로 만들면 소유자 술어(`created_by`)를 같은 커밋에 건다. 판정은 **JWT 어휘로만**(DB `users.user_type`과 어휘가 다르다 · SCHEMA users 절 함정).

**이메일 입구 전용 (§18)**

12. **⛔ 서버 메일을 지우지 않는다(DELE 0).** 처리 상태의 진실은 intake 원장 단독이다. POP3에는 폴더·읽음 개념이 없다. 이 메일함에 다른 POP3 클라이언트를 물리면 그쪽 삭제 설정이 메일을 지운다.
13. **⛔ 신원은 allowlist_only.** 하이웍스가 수신 인증 헤더를 안 붙이므로(0826 실측) **허용 목록 정확 일치가 전부**다. 메일 안의 `ARC-*`·`Authentication-Results`·`X-Authinfo`는 발신자가 위조 삽입 가능하니 신뢰 근거로 쓰지 마라.
14. **⛔ 신원 판정 전에 본문·첨부를 내려받지 마라**(TOP 헤더만 → 판정 → 선점 INSERT → RETR). 무인증 입구의 첨부 폭탄 방어다.
15. **⛔ 허용 주소는 귀속 사용자(`user_id`)를 반드시 갖는다.** 그 값이 접수의 `created_by`가 되고, 없으면 승인까지 다 끝난 건이 발송 직전 `dispatch_no_owner`로 죽는다.
16. **⛔ POP3 로그인 실패는 백오프 대상이 아니다.** 3연속이면 폴링을 멈추고 경보를 보낸 뒤 **사람이 재개**한다(1분마다 재시도 = 계정 잠금).
17. **⛔ 중복 차단 집합에 `queued` 미도래를 포함한다.** 종결로 읽으면 아직 나가지 않은 예약과 같은 명단이 한 벌 더 접수된다(이중 발송).
18. **⛔ 회신은 발신 주소 ∧ 활성 허용 목록 교집합에만 보낸다.** 위조 메일에 답해도 진짜 소유자에게 가고, 우리가 백스캐터 반사판이 되지 않는다. 미등록·판정 실패에는 회신 0.
19. **⛔ 청구 계정(귀속) 후보가 여럿이면 자동 선택 0**(★0827 §20). 같은 주소는 여러 귀속에 등록될 수 있고(활성 UNIQUE = `(email_norm, user_id)`), 여럿이면 요청서 "청구 계정" 지정으로만 확정한다(`matchBillingTarget` · 표시명 또는 로그인 ID **정확 일치** · 부분 일치 금지). 지정 못 하면 반려(회신에 계정 목록). "기본 계정"을 만들면 지정을 깜빡한 건이 엉뚱한 계정으로 청구된다.
20. **⛔ 예약이 잡힌 뒤의 취소는 큐를 지우되 캠페인 상태는 그대로 둔다**(★0828 `queueOnly`). 대행발송 캠페인은 생성 51밀리초 뒤 적재를 끝내고 `completed`가 된다(예약 시각은 큐 행의 `sendTime`이 든다). 상태 게이트(`scheduled`·`draft`)를 그대로 두면 **예약이 잡힌 접수를 아무도 취소할 수 없다**(B-0828-1). 반대로 상태를 `cancelled`로 바꾸면 **이미 나간 발송이 청구에서 사라진다** — 청구 선택기가 `status='completed'`를 요구하고, 수량은 큐의 성공 행 수로 세기 때문에 상태를 두고 큐만 지우면 나간 몫은 청구되고 막은 몫은 0원이 된다.
21. **⛔ 막을 것이 없었으면 「취소됨」으로 적지 않는다**(★0828 `alreadySent`). 큐에 대기도 픽업도 0이면 이미 전량 나간 것이다. 그때 취소로 확정하면 **고객은 받았는데 화면은 취소**가 된다. 원래 상태로 되돌리고 「이미 발송이 끝나 취소할 수 없습니다」를 알린다. ⛔ 그리고 `inspectAttemptCampaign`을 고쳐 발송 종결을 `stopped`로 보게 만들지 마라 — 적재가 끝나도 큐가 남아 있으면 더 나가므로 `live`가 맞고, 바꾸면 워커 D가 취소 건의 큐 회수를 놓친다.
22. **⛔ 회신 상한에 걸려도 침묵하지 않는다**(★0828 B-0828-2). 요청서를 고쳐 다시 보내는 것이 정상 흐름인데 답이 없으면 사용자는 무엇이 잘못됐는지 알 수 없다. 상한은 ENV로 조이되(`AGENCY_MAIL_REPLY_RATE_PER_HOUR` · 0 = 무제한 기본) 상한에 닿은 첫 회차에는 그 사실을 한 통 알린다.
23. **⛔ MMS 이미지는 규격이면 원본 그대로, 벗어나면 맞춰서 받는다. 맞추지 못한 파일만 파일별 사유로 반려한다**(★0828(2) 서수란 접수 `cmtclkuhe04iujnotbi3xbuu3` · **★0910 개정** = 임은지 접수 `cmttqx2gy0c8sjnotlvs441r1` · Harold 확정 "변환은 문제가 아니다"). 규격 = **JPG 실체(SOI 바이트 판정 · 확장자·MIME 불신) · 장당 300KB · 3장**(`LIMITS` 재사용). 맞춤은 `utils/mms-image-fit.ts` `fitMmsImage` **한 벌**이고 화면 접수(`POST /api/agency-send/mms-image`)·메일 접수(`prepareMailMmsImages`)가 같은 함수를 지난다. **같은 프로세스의 sharp로 한다 — 파이썬 서비스를 부르지 않는다**(0828의 "변환하지 않는다"는 변환을 파이썬 서비스에 맡기면 그 장애가 접수 장애가 된다는 근거였다. 그 근거가 없어졌다). ⛔ **조용히 바꾸지 않는다** = 화면 안내 토스트·슬롯 "자동 맞춤" 표시 · 메일 회신 "규격에 맞게 바꿔 붙인 이미지" 줄 · 담당자 테스트 MMS가 변환본이다. 이미지는 요청서와 **별도 첨부** 최대 3장(요청서 파일 안에 넣지 않는다). 요청서 "이미지 파일명" 칸은 **첨부가 있으면 무시**(순서 = 첨부 순서 · 접수 완료 회신이 순서를 고지 · 최종 확인 = 담당자 테스트 MMS)하고, **칸만 있고 첨부 0장이면 반려**(`image_not_attached`). 저장은 청구 계정 확정 뒤에만 하고 반려로 빠지면 그 자리에서 지운다. 코어에는 화면 접수와 같은 형태(**절대경로 문자열 배열**)로 넘긴다 = 발송·테스트 문자·청구 배관 무변경. **원본 파일명은 경로와 따로** `mms_image_names`(표시 전용 · 경로와 같은 순서)에 둔다 — 경로 배열에 이름을 섞으면 발송 배관이 그 모양을 읽게 된다. 옛 "메일 = SMS·LMS만"(설계서 §18-3 9)은 0828에 폐기.
24. **⛔ 회신번호가 고객마다 달라도 접수는 하나다**(★2026-09-12 · 임은지 접수 `cmtwbtpby00atjnlu0rh5c9jc`). 고객별 번호는 수신자 행(`agency_send_recipients.callback`)이 들고, 적재가 그것을 `campaign_send_staging.callback`으로 옮겨 `useIndividualCallback`으로 보낸다. **접수를 회신번호로 쪼개지 마라** — 쪼개면 스팸 검사·담당자 테스트 문자·승인이 종류 수만큼 반복된다(16종 = 16번이었다). 종전 근거였던 "적재 배관이 수신자별 회신번호를 나르지 못한다"(설계서 §17)는 **사실이 아니었다**: 발송 워커는 staging의 `callback`을 함께 읽고(`direct-send-worker` 청크 SELECT), 프로세서가 `resolveCustomerCallback`으로 행마다 싣는다. 그 경로는 고객DB와 무관하다. ⛔ 그리고 **발송 직전에 등록을 다시 확인한다** — 배관은 회신번호 등록 검증을 하지 않는다고 계약에 적혀 있고(`direct-send-core` 머리 주석) 호출부가 그 책임을 진다. 미등록이 섞이면 **대표 번호로 조용히 바꾸지 말고** 멈추고 알린다(`dispatch_callback_unregistered`). 양식 "내용" 시트가 원래 고객별 발신번호 기재를 안내하고 고객리스트 헤더에도 `매장전화번호 (회신번호)` 열이 있다 — 그 사용법이 표준이다. **★2026-09-13 보강** ①**이메일 입구도 같은 계약이다**: 종전에는 회신번호 열이면 "접수가 여러 건으로 나뉜다, 화면에서 하라"로 통째로 반려했는데 0912부터 사실이 아닌 사유였다(Harold "회신번호별로 나가야 해서 회신번호 열이 있는 것"). 지금은 화면 원스텝과 같이 대표 번호 = 첫 행, 고객별 번호 = 수신자 행으로 접수 1건이고, 회신 메일에 "고객별 N종"과 회신번호 빈 행 제외 건수를 적는다 ②**발송 직전 재검증은 대표 번호까지 본다**(고정 번호 방식의 유일한 번호 · 판정 함수는 접수 때와 같은 `getRegisteredCallbackSet`) ③재접수("같은 내용으로 다시 접수")와 승인 링크 화면도 고객별 번호를 나르고 종류 수를 보인다.
25. **⛔ 수신자 적재는 앱 메모리를 거치지 않는다**(★2026-09-12 · 남지현 접수 `cmtwlz0sf00kkjnlusipmsof8`). `agency_send_recipients` → `campaign_send_staging`은 **한 문장(INSERT SELECT)**으로 옮긴다. 종전에는 전량을 앱 배열 다섯 벌로 올려 되넣었고, 그것이 접수 상한(3만)의 실제 이유였다(근거로 적혀 있던 "엑셀 업로드 권장값"은 관례였다). **건수 상한을 다시 만들지 마라** — 같은 발송 배관은 직접발송에서 42만 건을 처리한 실적이 있다(0912 실측). 슬롯 치환은 `toSlotValues`와 같은 규칙을 SQL로 쓴다(`COALESCE(vars->>키, '')` · 키가 없으면 빈 문자열). **행수로 막는 상한은 전부 없앴다**(접수 상한·파서 행 상한 `MAX_LIST_ROWS = 0`) — 실질 방어선은 파일·메일 크기다(§4 "명단 크기"). ⛔ 시간과 메모리를 근거로 행 상한을 되살리지 마라: 이메일 워커는 백그라운드이고 겹침 가드(`running` + advisory lock)가 있어 한 통이 오래 걸려도 다음 틱이 겹치지 않으며, 100만 행 파싱 RSS 피크는 310MB로 접수가 끝나면 회수된다(0912 실측 · 서버 가용 46Gi). ⛔ **exceljs 스트리밍 리더로 바꾸지 마라** — 같은 실측에서 메모리를 5배 썼다(1,574MB 대 310MB). 엑셀이 반복 문자열을 한 표에 모으는 구조라 행 단위로 읽어도 그 표를 통째로 들고 있어야 한다. **★2026-09-13 ⛔ 같은 파일을 두 번 파싱하지 않는다**: 메일 워커는 API와 **같은 프로세스**라 동기 파싱이 도는 동안 모든 화면 요청과 발송 워커가 멈춘다. 종전 한 통 = 명단 전체 파싱 4회였다 → 요청서는 **시트만** 읽고(20만 행 파일 4,174ms → 18ms 실측) 사전 게이트가 읽은 명단을 분석(`analyzeOneStep` `pre`)에 넘겨 **명단 파싱 1회**(20만 행 약 4.5초 실측). POP3 수신도 조각마다 전체를 이어 붙이던 O(n²)를 조각 수집으로 바꿨다(`pop3-client.ts`).
26. **⛔ 같은 시도 키의 "시도 캠페인 조회 → staging 적재 → 캠페인 생성"은 시도 키 advisory lock을 쥔 채로만 돈다**(★2026-09-13 Codex 적대 1R high · `dispatchToPipeline`이 잠그고 `dispatchAttempt`가 본문). 소유권 토큰 확인은 그 순간만 본다. 적재가 lock 만료(30분)를 넘기면 복구가 건을 되돌리고, 다음 tick이 같은 시도 키로 들어와 **앞 실행이 만든 캠페인이 읽는 staging을 지우고 다시 쓸 수 있었다**. 못 잡으면 그 tick은 `approved`로 되돌리고 넘긴다(`dispatch_retry` · `ATTEMPT_BUSY`). 세션 잠금이라 프로세스가 죽으면 저절로 풀린다. 같은 라운드에서 함께 닫은 것 = ①컬럼 탐지(`hasAgencyColumn`)는 **조회 실패를 "컬럼 없음"으로 캐시하지 않는다**(굳으면 컬럼이 있어도 고객별 회신번호를 빼고 접수·적재한다) ②재접수용 수신자 조회(`GET /:id/recipients`)와 재접수 화면이 `callback`을 나른다(빼면 한 접수에 섞인 번호가 대표 번호 하나로 나간다).

---

## 3) 현재 구조 — 파일별 소유

| 파일 | 소유하는 것 |
|---|---|
| `utils/agency-send-state.ts` | 상태 머신(11상태)·전이표·시각 규칙. 리드타임 상수 = 화면 `MIN_LEAD_MINUTES`(180) / 이메일 `EMAIL_MIN_LEAD_MINUTES`(240) · 이메일 중복 차단 집합(`EMAIL_DUP_BLOCKING_SQL`과 판정 함수가 짝) |
| **`utils/agency-send-intake.ts`** | **접수 코어**(`createRequestCore`) + 원스텝 분석(`analyzeOneStep`) + 발송 창 조회 + 이력 기록. 입구 3 공통. `pre.minLeadMinutes`로 입구별 리드타임을 **코어가 집행** |
| `utils/agency-send-form.ts` | 요청서·명단 파서. ★0826(2) **통일 양식**(시트 "내용"+"고객리스트" 한 파일 · 업계 라벨 별칭 · 괄호 부연 제거 대조 · 플레이스홀더 빈칸 처리 · 한국어 시각 표기 · 문자타입 알림톡 반려 · `hasRecipientSheet`) + 구양식(시트 "요청서"+별도 명단) 하위호환 · 무헤더 감지와 열 이름 합성 · 열 점수표(`scorePhoneColumns` 한 벌 + 임계 둘: 화면 0.5 / 이메일 0.9+격차) · 회신번호 칸 해석 |
| `utils/agency-send-vars.ts` | 문안 항목 ↔ 명단 열 매칭(`resolveVarColumns`) + 주소록 슬롯 번역(`buildSlotPlan`) |
| **`utils/agency-send-preview.ts`** | ★0828(2) **실물 문장 조립 CT**(`buildRenderedSample` 1행 = 검사·테스트 문자 / `buildRenderedSamples` 상위 N = 상세 미리보기). 조립은 `prepareSendMessage` 한 벌 · 워커 옛 `buildSample`이 여기로 이동. 소비 = 고객 `GET /:id/preview`(소유자 술어) + 관리자 `GET /api/admin/agency-send/:id/preview`(super_admin) 둘 다 **같은 CT** |
| `utils/agency-send-worker.ts` | 5분 워커 A~F(1차 검사·당일 재검사·적재·만료·대조·lock 복구). 적재는 **DB 안 한 문장**(`INSERT INTO campaign_send_staging SELECT … FROM agency_send_recipients` · ★2026-09-12 불변 25)으로 옮긴 뒤 `createDirectSendCampaign`에 위임. 고객별 회신번호가 실렸으면 `useIndividualCallback`으로 보내고, 그 앞에 등록 재검증을 한다(불변 24) |
| `utils/agency-send-approve.ts` | 승인 효과 CT(한 트랜잭션). 입구 둘(로그인 화면·문자 링크)이 같은 함수를 지난다 |
| `utils/agency-send-cancel.ts` | ★0826(3) 취소 효과 CT. 입구 둘(고객 화면·슈퍼관리자 운영 취소)이 같은 함수를 지난다. cancelling 선점 → 큐 삭제 확인 → 확정 · tooLate만 되돌림 · 실패는 워커 F 인계 |
| `utils/agency-send-link.ts` | 담당자 링크 승인 토큰(번호별 서명 · fragment 주소 · 헤더 운반) |
| **`utils/mms-image-fit.ts`** | ★0910 **MMS 이미지 맞춤 CT**(`fitMmsImage` · 입구 둘 공용 = 화면 `POST /mms-image` · 메일 `prepareMailMmsImages`). 규격 JPG는 원본 그대로 · 밖이면 sharp로 회전·흰 배경·긴 변 1080·300KB 이하 순차 JPG · 허용 형식 목록 · 5천만 화소 상한 · 실패는 사유 반환(`describeMmsFitFailure`) · 고지 문구(`describeMmsFitNote`) · multer 한글 파일명 복원(`restoreUploadFileName`). 원본명 배열 정리 = `mms-image-util.ts` `alignMmsImageNames` |
| **`utils/agency-send-mail-worker.ts`** | **이메일 1분 워커** — UIDL 대조·선점·신원·상한·규격·분석·접수·회신·백오프·정지 |
| `utils/pop3-client.ts` | 자체 소형 POP3S 클라이언트(tls 위 6명령). ⛔ MIME 해석은 여기서 안 한다(`mailparser`의 일) |
| `utils/agency-send-email.ts` | 허용 발신자 정규화·판정(`resolveEmailSender` — 주소와 귀속 사용자 활성을 한 쿼리로 · ★0827 후보 0/1/2+ = `owner_inactive`/`ok`/`choose`) + **청구 계정 지정 대조**(`matchBillingTarget`·`normalizeBillingTargetKey`·`describeBillingTargets` — 등록 라우트의 겹침 예방과 같은 한 벌) |
| `utils/agency-mailer.ts` | 회신 발신(3값 `sent\|rejected\|unknown` · ENV 없으면 수신·접수까지 전면 잠금) |
| `routes/agency-send.ts` | 고객사 API(목록·접수·원스텝·상세·승인·문안 수정·시각 변경·취소). 코어는 CT에서 import. ★0826(2) **사용자 격리**(소유자 술어 `ownerParam` 한 모양 · 관리자 응답에만 접수 계정 JOIN) |
| `routes/agency-approve.ts` | 무로그인 링크 승인 API. ⛔ 노출은 문안·시각·건수·발신번호까지(명단·매핑·내부 식별자 금지) |
| `routes/admin.ts` | 회사 스위치 PATCH · **허용 이메일 4라우트** · 접수 현황(+`mailIntake` · ★0826(2) 내역 메뉴 재료 = source·레일 스탬프·고객사명·신청자명) · ★0826(3) **운영 취소**(`POST /agency-send/:id/cancel` · 확정 시에만 담당자 안내 문자) |
| **`components/agency/AgencyPreviewModal.tsx`** | ★0828(2) **치환 미리보기 모달 공용 한 벌**(고객 상세·슈퍼관리자 내역이 함께 쓴다). 좌 목록 10건 페이징 · 우 폰 화면 · 누르면 폰이 바뀐다. ⛔ 서버 조립 문장만 그린다(화면 치환 재구현 0) · ⛔ `createPortal`(부모 모달 `overflow-hidden`에 잘림 방지) |
| 프론트 | `pages/AgencySendPage.tsx`(목록) · `components/agency/*`(Composer·OneStepModal·Detail·api 미러·★0826(2) `AgencyProgressRail` 공용 레일) · `pages/AgencyApprovePage.tsx` · `components/admin/AgencyEmailSendersModal.tsx` · `components/admin/AgencyMailIntakePanel.tsx` · ★0826(2) `components/admin/AgencySendLedgerPanel.tsx`(슈퍼관리자 대행발송 내역) |
| 표시 라벨 | `components/agency/agency-send-api.ts`가 `STATUS_LABEL`·`SOURCE_LABEL` **단일표**를 소유. 새 상태·출처는 여기 등재가 같은 커밋 |
| **`components/agency/AgencyEventLog.tsx`** | ★0828(2) **진행 기록 표·문장 공용 한 벌**(`EVENT_LABEL` + 렌더). 고객 상세·슈퍼관리자 내역 상세가 함께 쓴다 — 화면마다 두면 "고객에게는 승인 번호가 보이는데 직원 화면에는 안 보이는" 상태가 된다(0828(2)에 실제로 발생). 새 이벤트 kind 등재가 같은 커밋 |

**DB** = `agency_send_requests`(+`source` · ★0910 +`mms_image_names` 표시 전용 · 코어가 존재 확인 뒤에만 싣는다) · `agency_send_recipients` · `agency_send_events` · `agency_send_email_senders` · `agency_send_email_intake` · `agency_send_mail_state`. 컬럼 원문·불변은 [SCHEMA.md](../status/SCHEMA.md) 해당 절.

---

## 4) 운영에 필요한 설정

| 축 | 값 |
|---|---|
| 회사 개통 | 슈퍼관리자 → 고객사 수정 → **대행발송 스위치**(`companies.agency_send_enabled`) AND 유료 요금제 |
| 이메일 접수 개통 | 같은 카드의 **허용 이메일 관리** 모달에서 주소 등록(**귀속 사용자 필수**). 활성 주소 0건이면 그 회사 메일은 전부 무시된다(모달이 경고를 띄운다) |
| 한 주소 = 청구 계정 여럿 | ★0827 같은 주소를 **계정마다 한 번씩** 등록하고, 두 번째부터는 **표시명 필수**(= 담당자가 요청서 "청구 계정" 칸에 적을 이름 · 겹치면 등록이 막힌다). 절차·안내 문안·현황 SQL = **[OPS.md §2-2-D ⑧](../status/OPS.md)** |
| ENV | `AGENCY_MAIL_ENABLED` · `AGENCY_MAIL_USER` · `AGENCY_MAIL_PASS`(메일 전용 비밀번호). 없으면 워커가 시작하지 않는다(부팅 로그 1회) |
| 운영 명령 | **[OPS.md §2-2-D](../status/OPS.md)** — 정지·재개·긴급 정지 3단·현황 SQL·경보 3종 |
| 경보 | `agency-mail-login-fail`(즉시·정지 동반) / `agency-mail-unknown-sender`(6시간 요약) / `agency-mail-poll-fail`(30분 정체) |
| **명단 크기** | ★2026-09-12 **행수 제한 없음**(불변 25). 실질 상한은 파일 크기 하나이고 두 입구가 같은 값이다 = 메일 첨부 합계 **50MB**(`MAX_ATTACH_TOTAL` · 메일 전체는 그 1.67배) · 화면 요청서 업로드 한 파일 **50MB**(`ONE_STEP_FILE_LIMIT`). 실측 = 명단 xlsx 6열 20만행 7.5MB · 50만행 18.7MB · 100만행 37.4MB(12열 약 2배). ⛔ 우리 값 위에 **메일 서버가 받아 주는 첨부 크기**가 따로 있다(하이웍스 상한 확인 중 · 확인되면 낮은 쪽에 맞춘다). **남은 비용(★0913)** = 명단 파싱 1회는 동기라 그동안 같은 프로세스의 API·발송 워커가 멈춘다(20만 행 약 4.5초 실측 · 100만 행은 행수 비례면 약 20초대로 **미검증**). 없애려면 메일 워커를 별도 프로세스로 분리해야 한다(운영 배포 변경 · 착수 판단 Harold) |

---

## 5) 이력 색인

> 경위·수치·정정 원문은 설계서가 소유한다. 여기는 **언제 무엇이 바뀌었나**만.

| 시점 | 무엇 | 원문 |
|---|---|---|
| 2026-08-22 | 설계 승인(크레딧 0 · 인디고 · MMS 포함 · 미끼 노출) · DDL 3테이블+컬럼 1 | 설계서 §0~§9 |
| 2026-08-23 | 6단계 전량 구현 · **§12 정정 5건**(예약 배관 위임 · 시각 상수 분리 · 변수 슬롯 · 취소 CAS · 소유권 축) · Codex 적대 6R · 배포 | 설계서 §12 |
| 2026-08-23(2) | **당일 접수 건 재검사 폐지**(같은 날 두 번 검사 금지 · Harold 지시) · 예약 생성 실측 확인 | 설계서 §14 |
| 2026-08-28 | **예약 취소 불능 정정**(`queueOnly`·`alreadySent`) · **발송 완료 상태 신설**(`sent` · DDL 1) · **이메일 회신 상한 ENV 이관**(기본 무제한) · 시스템 알림 문구 구조화 | [B-0828-1·B-0828-2](../status/BUGS.md) |
| 2026-08-25 | 화면 개편 §15 · **담당자 링크 승인 §16** · **요청서 원스텝 §17**(+양식 리디자인 §17-5) · 배포 | 설계서 §15~§17-5 |
| 2026-08-25(4~6) | 문안 항목 AI 매핑 §17-6 · 명단 미리보기 엑셀 뷰 §17-7 · 헤더 접수 입구 두 문 §15-6 | 설계서 §17-6~§17-7 |
| **2026-08-26** | **이메일 접수(챕터 2) 설계→구현→배포 하루 종결.** 브레인스토밍 4역할 + 회의론자 15건 → **하이웍스 실측으로 전제 2개 정정**(POP3만 · 인증헤더 미부착) → 신설 7파일 · DDL 4종 · 자체 적대 5R(실결함 7) | 설계서 §18·§18-11-1 |
| **2026-08-26(2)** | **통일 양식 + 사용자 격리 + 슈퍼관리자 내역 메뉴.** ①업계 실물 양식(카톡 수신 실측) 기준으로 세 입구 통일: 한 파일(내용+고객리스트) · 화면 슬롯 1개 · 이메일 1파일 표준(2파일 관용) · 배포 양식 재생성 ②사용자 격리(서수란 접수): company_user 본인만·관리자 전체 + 접수 계정 표시 ③슈퍼관리자 "대행발송 내역" 탭(진행 레일+접수구분+고객사·신청자) · DDL 0 · Codex 생략(Harold 지시)·자체 적대 검토 | 설계서 §19 |
| **2026-08-26(3)** | ①배포 양식을 **업체 실물 셀 단위 복제**로 정정(Harold 반려 "저걸로 통일=모양 그대로") ②**슈퍼관리자 운영 취소**: 취소 효과 CT 승격(`agency-send-cancel`) + admin API + 내역 탭 취소 버튼 + 확정 시 담당자 안내 문자 ⑧ | 설계서 §19-6·§19-7 |
| **2026-08-26(4)** | **보낼 시각 연도 필수 폐지**(서수란 첫 실사용 반려 → Harold "연도는 너무 과해"). 연도 없는 표기 = 올해 → 지났으면 내년(60일 이내만) → 그 밖은 과거 그대로(리드타임 반려). 빈 분("10시 분") 허용 | 설계서 §19-8 |
| **2026-08-26(5)** | ①시각 표기 확장(혼합·상대 날짜·시간대 낱말·반·쯤 · "밤 12시"는 반려 유지) ②승인 문자 = **단축 URL**(CT-40 · 만료 = 토큰과 동일) + 요청 건수 N건 ③허용 이메일 선택·목록 표시 "아이디 - 이름" | 설계서 §19-9 |
| **2026-08-26(6)** | **리드타임 180/240 → 40분 통일 + 미달은 자동 조정**(요청+30분 · 서수란 요청 · Harold 확정) · 접수 즉시 검사 기동 · 조정 고지 4곳(확인 화면·회신·담당자 문자 분기·변경 토스트) · 누를 수 없는 승인 링크 차단 · **이메일 일일 상한 기본 무제한**(ENV로 재조임) · DDL 1(`requested_at_original` · 폴백 있음) | 설계서 §19-10 |
| **2026-08-27** | **청구 계정 다중 귀속**(서수란 접수: 담당자 1명이 청구 계정 여러 개를 대신 요청하는 실무 수용). 활성 UNIQUE `(email_norm)` → `(email_norm, user_id)` · `resolveEmailSender` `choose` outcome · 요청서 "청구 계정" 칸(선택) + `matchBillingTarget` 정확 일치 · 회사 축 게이트를 귀속 확정 직후로 재배열 · 등록 라우트 표시명 필수·겹침 예방 · 모달 뱃지 · Codex 적대 2R 종결(§20-6) · **배포완료 + DDL(인덱스 교체) 실행완료** · 운영 실측 = §20-5 | 설계서 §20 |
| **2026-08-28(2)** | **①치환 미리보기**(서수란 접수 `cmtcle8gn04bnjnot641p0fvq`): T-2h 적재 전에도 접수 상세에서 상위 50건 실물 문장(발송과 같은 CT 조립) 검토 · 워커 `buildSample`을 `agency-send-preview.ts` CT로 승격 · `GET /:id/preview` · 파이프라인 무변경(승인 즉시 예약안은 불변 1·2 충돌로 불수용) **②메일 MMS 개통**(서수란 접수 `cmtclkuhe04iujnotbi3xbuu3`): 별도 첨부 3장 · 규격이면 접수·벗어나면 파일별 사유 반려 · 불변 23 신설 · 배포 양식 안내 개정 + 파서 왕복 실측 | [B-0828-5·B-0828-6](../status/BUGS.md) |
| **2026-09-02** | **목록 10건 페이징**(Harold 지시 "무한 스크롤을 페이징뷰로"). 대행발송 목록 4곳 전부 = 슈퍼관리자 내역·고객 접수 내역·메일 접수 반려 목록·등록 발신주소. 공용 CT `TablePagination` 재사용(10건 이하면 스스로 숨는다) + 화면 액센트용 `accent` prop 신설(기본값 = 기존 사용처 그대로라 회귀 0). ⛔ 필터·검색 변경 시 1페이지 리셋(안 하면 "검색했는데 빈 화면") · 새 접수·주소 등록 직후 1페이지 이동(새 항목이 맨 앞에 온다) · 승인 대기 묶음은 페이징하지 않는다(처리할 일감은 전부 보여야 한다). 백엔드 무변경 | [B-0902 세션](../status/BUGS.md) |
| **2026-08-30** | **승인 링크 보안 보강 배포완료**(충전 링크와 공통 세션): ①링크 승인 즉시 **담당자 전원 통보**(문구 ⑩ `buildLinkApprovedNotify` · 끝자리 표기 = 탐지 레버 · `notifyManager` 적재 건수 검사 동반) ②`recordAuditLog('agency_link_approved')`(ip·ua · 감사 화면 노출) ③서명 폴백 키 제거(fail-closed · B-0825-2 종결) ④승인류 IP 리미터(제품별 분 30) ⑤페이지 토큰 = `useApproveToken` 훅(fragment만 · 즉시 주소 제거 · 재진입 불일치 차단) + 스미싱 구분 고지. **실측 대기 = 다음 실접수 링크 승인 때 담당자 전원 통보 수신 확인** | 충전 설계서 §16 불변 10 |
| **2026-08-31** | **다듬기 무변경 회차를 기록에서 분리 · 배포완료**(Harold 지적 "다듬었다는데 담당자 문자가 안 왔다"). 짧은 문안은 모델이 원문을 그대로 돌려주는데 `checkRefined`가 그것을 통과시켜 **변경 0인 회차도 `refined`로 찍혔다** → 담당자 통보는 문안이 달라졌을 때만 나가므로 기록과 통보가 어긋났다(실측 = `content_version` 1 고정·`content_unchanged` t). 실제로 바뀐 회차만 `refined`, 무변경은 **`refine_nochange`** 신설(`AgencyEventLog` 라벨 동반 · `kind` CHECK 없음 = `pg_constraint` 실측 PK·FK뿐 · DDL 0). **통보 정책·재검사 동작은 무변경**(바뀐 문장이 없으면 보낼 것도 없다) | `agency-send-worker.ts` `runSpamRound` |

| **2026-09-05** | **메일 한 통에 요청서 여러 건 + 양식 `발송 ID` 칸 — 설계 승인(구현 미착수).** 접수 = Harold "담당자 한 명이 한 메일에 양식 5개를 보내는 곳이 있다. **두 분류가 있다**"(A = 계정마다 담당자가 나뉜 곳 · B = 요청 담당자가 한 명인 곳). 브레인스토밍 4역할(기획·백엔드·프론트·회의론자) 1차 + 교차 토론 1R 수렴. **파생 발견 2건**(다중과 무관한 기존 결함) = ①회신 상한 게이트가 객체 부정 연산으로 한 번도 닫힌 적 없음(불변 22 미집행) ②멱등 3층 대조 키가 사용자 원본 시각이라 자동 조정된 건을 못 잡음(운영 실측 7건 중 2건). **선행 커밋 2건 → 다중 커밋** 순서 확정. **당일 코드 완료 · DDL 0 · 배포 대기**(기준본 8개 대조 회귀 0 · 240파일 3776건 통과 · tsc 0). 구현하며 접은 것 2건 = 담당자 문자 순번(파일명이 이미 구별 신호라 불필요) · 화면 예고(메일 요청자는 화면에 안 들어온다) | 설계서 §21 |

| **2026-09-10** | **직원 접수 4건 · 배포완료 · DDL(`mms_image_names`) 실행완료 · 실측 대기** ①승인 링크 화면 제목 (광고) 중복 = 조건 없는 부착 → 발송과 같은 CT(`buildAdSubjectFront`·새 `startsWithAdMark`) · 화면 접수 미리보기 같은 모양 동반 ②양식 "문안 항목" 행 = 화면·메일 접수 구분(접수자 원문) ③**MMS 이미지 자동 맞춤 = 불변 23 개정**(화면·메일 모두 · `mms-image-fit.ts` · 조용한 변경 금지 고지 3곳) + 양식 "이미지 문자" 행 ④상세 받는 사람별 미리보기 = 원본 파일명(`mms_image_names`) + 눌러서 크게 보기(발송 결과 창과 같은 모습). 범위 밖 4건 = B-0910-10 | [B-0910-6~10](../status/BUGS.md) |
| **2026-09-12** | **직원 접수 2건 · 백엔드 배포 확인 · DDL(`agency_send_recipients.callback`) 실행완료 · 실측 대기 · ★0913 Codex 적대 1R high 3건 반영(불변 26 · 미배포)** ①회신번호가 고객마다 달라도 접수 하나(분할 근거였던 "적재 배관이 수신자별 회신번호를 못 나른다"가 사실이 아니었다 · 불변 24) ②적재를 DB 안 한 문장으로 옮겨 행수 상한 전량 제거 · 화면 접수의 조용한 절단(3만 번째 `break`) 제거 · 크기 상한 두 입구 50MB(불변 25 · §4) · 스트리밍 파서는 실측으로 기각(메모리 5배) | BUGS B-0912-5·6 · [0913 인계](2026-09-13-session-handoff.md) |

**뒤집힌 판단 (같은 자리에서 다시 틀리지 않으려고 남긴다)**

- **`send_type='agency'` 신설 → 전량 원복**(0823). 새 값이 청구·동기화 축에 없어 빠졌고, 넣으려니 정산 CT를 건드리게 됐다. 대행은 "그 계정으로 대신 눌러 주는 것"이라 정산에 새로 할 일이 없다.
- **담당자 테스트 문자를 인증 라인으로 발송 → 테스트발송 축으로 이설**(0823). `bill_id`가 없어 어느 계정 사용량에도 안 잡히고 있었다.
- **2차 다듬기가 1차보다 보수적**이었다 → 앵커(날짜·금액·번호·링크)만 남기고 문장을 새로 쓰는 방향으로(0823).
- **IMAP 전제로 설계 전량 작성 → POP3 확정판으로 재작성**(0826). 착수 게이트에 실측을 걸어 둬서 재설계가 아니라 분기 선택으로 끝났다. 교훈 = LESSONS_BACKEND 핵심 원칙(외부 능력 실측).
- **이메일 신원을 3값 fail-closed로 설계 → allowlist_only 확정**(0826). 하이웍스가 수신 인증 헤더를 안 붙인다는 실측 때문. 완충 = 상한 절반값 + 완료 회신이 진짜 소유자에게 가는 인지 경로.
- **Codex 리뷰 → 자체 적대 검토 5R**(0826 Harold 지시 "결함 0 나올 때까지"). 실결함 7건을 잡았고 오판 1건은 근거(기존 코드 4곳)로 자가 철회했다.
- **메일 MMS "변환하지 않는다"(0828) → 화면·메일 모두 자동 맞춤(0910)**. 0828의 근거는 "변환을 파이썬 서비스에 맡기면 그 장애가 접수 장애가 된다"였는데, 0910에 나는 그것을 "변환 자체를 하지 말라"로 읽고 문구 정정만 추천했다가 Harold 정정("변환하는 게 문제라고 생각하는 거냐"). 근거가 **방법**(외부 서비스)에 있으면 방법을 바꾸면 된다 — 같은 프로세스 sharp + 실패 시 지금과 같은 반려. 직원이 대행하던 시절 직원이 줄여 줬던 일이다.

**★0913 적대검토에서 확정됐으나 이번에 고치지 않은 것(medium 이하 · 등재만)** = ①예약 생성이 거절돼 캠페인이 안 생기면(잔액 부족 등) 매 5분 tick마다 staging 전량을 지우고 다시 쓰고, 끝내 만료돼도 staging 행이 남는다(나이 기준 정리 없음 · 0912 전 앱 메모리 경로도 같았다) ②승인 링크 외 화면(목록·상세 미리보기)은 대표 번호 하나만 보인다 ③미등록 번호로 만료된 건을 재예약하면 승인까지 다시 돌고 발송 직전에 또 멈춘다(재검증이 막으므로 오발송 없음) ④화면 접수 본문 상한 50mb가 큰 명단의 숨은 상한이다 ⑤SQL 슬롯 치환이 지수 표기 숫자(예: 1e21)에서 `toSlotValues`와 글자가 다를 수 있다 ⑥한 캠페인 100만 건은 직접발송 워커 한 루프를 오래 붙잡는다(OFFSET 페이징).
**축 밖 부채** = [B-0823-1](../status/BUGS.md)(워커 실패 결말) · [B-0825-6](../status/BUGS.md)(종결 건 명단 purge 경로 부재) · [B-0825-7](../status/BUGS.md)(5분 워커 겹침 가드) · 화면 접수 무헤더 명단(공용 업로드 파서라 축 밖 · 설계서 §18-4).
