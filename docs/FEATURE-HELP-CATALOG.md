# 기능 카탈로그 + 도움말 봇 — 기능 상설 SoT

> **호출어: "도움말 봇" 또는 "기능 카탈로그"** — Harold님이 언급하면 이 문서를 먼저 연다.
> 이 문서가 이 축의 정체성·불변 원칙·구조·이력을 소유한다. 상태·잔여는 §9.

## 0) 30초 요약 — 이 축이 무엇인지 다시 확인할 때

| 질문 | 답 |
|---|---|
| 이게 뭔가 | 우리 기능을 "고객이 하려는 일" 단위로 정의한 **원장 한 벌**. 그것으로 봇이 답하고 `/guide` 화면이 그려진다 |
| 왜 매뉴얼이 아닌가 | 사람이 쓰는 매뉴얼은 4개월 만에 멈췄고 3개월치 기능이 빠졌다(§2-2). 정의를 코드에 두고 테스트가 누락을 막는다 |
| 정의는 어디에 | `packages/backend/src/content/feature-catalog.ts` 한 파일. md도 DB도 아니다 |
| 사람이 쓰는 것 | `goal`·`keywords`·`steps`·`blockers` 넷뿐. 요금제 이름·크레딧 숫자·버튼 라벨은 **적지 않는다**(런타임이 만든다) |
| 지금 몇 개 | **39개 전부 본문 있음**(2026-08-22(2) 27개 승격). 빈칸(stub) 0. 새 기능을 빈칸으로 등재하면 만료일이 지날 때 빌드가 실패한다 |
| 봇은 누구에게 | **요금제 사용 중인 회사만**(서버 판정). 자리 = 우측 하단 |
| 봇이 지어내지 않는 이유 | 카탈로그에서 못 찾으면 **AI를 아예 부르지 않는다**. 답에 없는 경로·혜택 숫자·모델명이 있으면 버린다 |
| 돈이 드나 | 무료. 크레딧 표에 등록하지 않았다(미등록 = 0). 기록만 남긴다 |
| 다음에 뭘 하나 | 배포 후 §10-1 검증(39개 질문 육안) · 백지 계정 리허설(§10-3). §10-1·§10-2는 2026-08-22(2) 구현 완료 |
> 경위: Harold "우리 한줄로 AI 기능을 사용하는 방법 문의에 답변해주는 봇 · 온보딩이랑 도움말 둘 다 잘 응대하는 봇 · 매뉴얼은 완전 구형이라 새로 만들어야 한다 · **지금은 사용법 문의가 없다, 서비스받는 업체가 아직 없다** · 그래서 보유 기능을 처음부터 완벽하게 정의하고 그 토대로 답할 봇을 만든다 · 이것 또한 학습데이터에 넣어야 한다."
> 5역할 브레인스토밍(기획·백엔드·프론트엔드·디자이너·회의론자) 2라운드 수렴 + Harold 지시 2건 반영.

---

## 1) 한 줄 정의

**이것은 매뉴얼이 아니라, 봇과 안내 화면이 함께 읽는 단일 원장이다.** 우리 기능을 "고객이 하려는 일" 단위로 정의해 코드에 두고, 그 정의로 (가) 봇이 답하고 (나) 안내 화면이 그려지고 (다) 나중에 우리 AI 기능들이 참조한다. 정의는 한 벌뿐이다.

---

## 2) 확정 사실 (실측 · 재검증 불요)

| # | 사실 | 근거 |
|---|------|------|
| 2-1 | **실사용 고객사 0.** 문의 로그가 없어 "자주 묻는 질문"에서 출발할 수 없다. 이 봇의 1차 사용자는 첫 고객사의 첫 2주와 그 앞에서 시연하는 우리다 | Harold 2026-08-22 |
| 2-2 | 옛 매뉴얼 원고는 **폐기**. 2026-05-27 작성 9챕터인데 그 뒤 3개월 기능(고객 360·캠페인 대행·마케팅 진단·이미지 스튜디오·브랜드메시지·마케팅 플래너·모바일 DM) 언급이 **각 0회**. 사람이 갱신하는 방식은 4개월 멈춰 실패가 증명됐다 | `docs/manual/manual-content-v2.md` · `docs/manual/매뉴얼.md`("서팀장 캡처 대기 중" 상태 그대로) |
| 2-3 | 화면 라우트 **47개** · 모달 **58개** · 요금제 기능 키 **12개**(basic_send · customer_db · customer_db_view · target_send · ai_mapping · spam_filter · ai_messaging · ai_premium · auto_campaign · mobile_dm · auto_spam_test · ai_cdp) | `App.tsx` · `components/*Modal.tsx` · `plan-guard.ts:41-53` |
| 2-4 | **`docs/FEATURE-*.md` 11종은 봇 컨텍스트에 넣을 수 없다.** 테이블명·사고 이력·내부 식별자가 가득하다 | memory `feedback_no_internal_identifiers_in_external_documents` |
| 2-5 | 요금제 사용 판정은 이미 CT에 있다: `ACTIVE_PAID_PLAN_WHERE = plan_code <> 'FREE' AND subscription_status NOT IN ('expired','suspended')`. FREE는 "요금제 미가입(레거시 이관 중 유료 미전환)" | `plan-guard.ts:150-152` · 파일 머리 주석 10행 |
| 2-6 | 기존 온보딩 마법사 7단계가 있고 **AI 오퍼레이션 체험권(`ai_operator_trial_until`) 보유 회사만** 대상이다. 요금제와 무관한 별도 컬럼이고 슈퍼관리자가 수동 부여한다. 체험권이 없으면 `OnboardingCard`가 스스로 안 뜬다(설계대로 동작 · 403 화면을 만나지 않는다) | `onboarding.ts:41-58` · `OnboardingCard.tsx:1-12,50-60` · `plan-guard.ts:96-101` |
| 2-7 | 인터럽트 티어는 `z-[2000]`(ConfirmModal · CreditConfirmModal · CustomerDataGate). 토스트는 `z-[10000]`(우상단·우하단). 이미지 스튜디오는 `z-[2400]`·`z-[2500]`으로 인터럽트 위에 있다 | `ConfirmModal.tsx:109-112` · `ToastProvider.tsx:87` · `console-ui.ts:280` · `ImageStudioPage.tsx:661,703` |
| 2-8 | 동적 `import()` 리터럴은 **App.tsx 안에** 둬야 한다. 난독화 exclude가 `[node_modules, .nuxt, App.tsx, lazy-page.tsx]` 넷뿐이고, 벗어나면 청크가 안 만들어진다(0718 전 고객 다운 경위) | `vite.config.ts:8-14` |
| 2-9 | 크레딧 미등록 source는 `getCreditCost`가 0을 돌려준다 → 무료화에 과금 코드 변경 0 | `ai-credit-calc.ts:196-200` |
| 2-10 | 우리 테스트 게이트(pre-push)는 **클론당 옵트인**이고 vitest가 없으면 통과시킨다. `--no-verify`도 열려 있다. 백엔드는 ts-node 실행이라 배포 시점 빌드 게이트가 없다 | `.githooks/pre-push:3-24` |

---

## 3) 불변 원칙

1. **⛔ 코드가 강제하지 못하는 정의는 만들지 않는다.** 요금제 이름·크레딧 숫자·버튼 라벨을 정의에 **문자열로 적지 않는다.** 요금제 가부는 런타임 `canUseFeature` 판정으로만 말하고, 크레딧은 키 참조로 두고 렌더할 때 `getCreditCost`로 숫자를 만든다. 최근 3개월에만 요금제 경계가 세 번 움직였다(0720 · 0814 · 0822).
2. **⛔ 봇 컨텍스트에 들어가는 것은 이 카탈로그의 공개 필드뿐이다.** `docs/FEATURE-*.md`도, 화면 문구도, 내부 문서도 넣지 않는다(2-4).
3. **⛔ 매칭 실패면 AI를 호출하지 않는다.** 폐집합이라 지어낼 재료가 없는 것이 환각 차단의 1층이다. "모릅니다"가 틀린 답보다 낫다.
4. **⛔ 게이트는 필요조건이지 충분조건이 아니다**(2-10). 게이트가 건너뛰어진 기계에서도 제품이 거짓말하지 않게 **런타임 2층**을 함께 둔다: 응답 조립 직전에 경로 실존을 다시 확인하고, 없으면 버튼을 그리지 않는다.
5. **정의가 없는 기능도 목록에는 있다.** 미작성은 빈칸(stub)으로 **전수 등재**하고 만료일을 준다. 그래야 역방향 게이트가 첫날부터 전수로 켜지고, 봇이 "모릅니다" 대신 "이 기능은 여기서 시작합니다"까지는 답한다.
6. **정의는 한 벌이다.** 봇과 안내 화면(`/guide`)이 같은 원장을 읽는다. 화면에만 있고 봇은 모르는 설명이 생기면 그것이 결함이다.
7. **⛔ 봇이 회사 데이터를 답하지 않는다**(1단계). 수치 질문은 기존 AI 설명 화면이 소유한다. 어제 회사 격리 실사고가 난 자리에 경로를 하나 더 만들지 않는다.

---

## 4) 구조

### 4-1) 정의 단위 = 작업(Job)

화면 47개도 요금제 키 12개도 아니다. **"고객이 하려는 일"** 하나가 단위이고, 화면·모달·요금제는 그 일의 속성으로 매단다.

라우트 기준이 안 되는 근거: 고객이 보는 헤더 메뉴는 8개뿐인데 라우트는 47개다. 자동발송·세그먼트·모바일 DM은 메뉴에서 빠졌는데 라우트는 살아 있다(`ai-operator-modules.ts` 주석: "카드를 교체, 라우트는 유지"). 라우트에 정의를 묶으면 고객이 갈 수 없는 화면까지 목차가 된다.

### 4-2) 파일: `packages/backend/src/content/feature-catalog.ts` 한 개

12개를 도메인별 8파일에 흩으면 대부분이 항목 1~2개짜리 껍데기가 된다. **항목 30개를 넘으면 그때 쪼갠다.**
> 2026-08-22(2) 39개가 한 파일(약 1,000줄)에 있다. 쪼개기는 **하지 않았다**: 같은 세션에 본문 27개를 쓰면서 구조까지 바꾸면 검토 면이 두 배가 된다(범위 규율). 쪼개기는 "추가 과제"로 남긴다. 쪼갤 때 기준은 `JOB_GROUPS` 7묶음이고, 불변식 8번이 읽는 파일 경로(`content/feature-catalog.ts`의 `Omit<FeatureJob, 'sourceFile'`)는 그대로 둬야 한다.

```ts
export interface FeatureJob {
  id: string;               // 'send-direct'
  title: string;            // 고객 언어 명사구, 14자 이내        [사람]
  goal: string;             // 이걸 하면 무엇이 끝나는가, 40자 이내 [사람]
  keywords: string[];       // 사용자 어휘·구어·오탈자 5개 이상     [사람]
  steps: string[];          // 3~6줄                             [사람]
  blockers: { symptom: string; fix: string }[];  // 0~3개         [사람]
  entry: { path: string; via: string };          // 기계 대조
  planKey: FeatureKey | null;                    // plan-guard 타입 직접 import
  creditSource: string | null;                   // CREDIT_COST_MAP 키. 숫자 금지
  related: string[];                             // 최대 3
  status: 'ready' | 'stub';
  stubUntil: string | null;                      // 'YYYY-MM-DD'
  sourceFile: string;                            // 내부 전용. 노출 0
}

export type PublicFeatureJob = Omit<FeatureJob, 'sourceFile' | 'status' | 'stubUntil'>;
```

**사람이 쓰는 것은 넷뿐이다**: `goal` · `keywords` · `steps` · `blockers`. 나머지는 기계가 대조한다.

`blockers`가 이 체계의 핵심이다. 기능 설명이 아니라 **막히는 지점**이 봇의 실제 답변 내용이다. 옛 매뉴얼이 죽은 이유가 화면 구성표와 단계 나열만 있고 "왜 안 되는지"가 없어서다.

### 4-3) DB: 원장 1개

`help_questions(id, company_id, user_id, path, question, matched_ids text[], answered bool, created_at)`.
답한 질문과 못 답한 질문을 한 테이블이 가져야 **미답 비율**이라는 단일 지표가 나온다. 정의는 DB에 두지 않는다.

### 4-4) 봇

- **노출 대상 = 요금제 사용 중인 회사만**(★Harold 지시). 판정은 서버가 한다(`plan_code <> 'FREE'` + 구독이 만료·정지 아님, 2-5). 프론트가 요금제를 받아 판정하면 판정이 두 벌이 된다.
- **자리 = 우측 하단**(★Harold 지시). 토스트가 같은 자리를 쓰므로(2-7) 토스트가 뜨는 동안 런처가 비켜선다: ToastProvider와 인터럽트 3종이 `body`에 표시 속성을 달고 런처가 그 속성에 반응한다.
- **층 = 런처 `z-[1400]` · 패널 `z-[1500]`.** 인터럽트(2000) 아래여야 한다. 도움말이 크레딧 차감 확인창을 덮으면 사고다. 캔버스가 화면을 소유하는 `/dm-builder`·`/image-studio`는 런처를 띄우지 않는다(2-7의 2400·2500).
- **모드를 묻지 않는다.** 서버가 발송 이력 유무로 판정한다. 없으면 온보딩(시작 카드), 있으면 도움말(현재 화면 작업 + 막히는 지점).
- **화면 인지 = 라우트만**(1단계). 모달은 등록하지 않고, 대신 정의가 모달을 지칭하지 못하게 막는다. 6개만 등록해도 나머지 52개에서는 여전히 모르기 때문이다.
- 스크림·포커스 트랩·본문 스크롤 잠금 없음. 봇이 "여기를 누르세요"라고 하면 그대로 눌러야 한다.
- 껍데기에 `transform`·`filter`·`backdrop-filter` 금지.

**기존 온보딩 마법사와의 관계**: 대체하지 않는다. 진행률 원장도 새로 만들지 않고 마법사 것을 읽는다. 봇 카드가 `/onboarding?step=N`으로 딥링크하되, **그 회사가 마법사 대상이 아니면(체험권 없음, 2-6) 링크를 그리지 않고 봇이 직접 단계를 안내한다.** 봇이 체험권 게이트를 물려받으면 지금과 똑같이 "체험권 준 회사만 안내받는" 상태가 된다.

### 4-5) 답변 조립과 환각 차단 4겹

1. **폐집합 매칭.** 점수가 임계 미만이면 AI를 호출하지 않고 가까운 작업 3개와 "문의 남기기"만 낸다.
2. **컨텍스트는 후보 3개의 공개 필드뿐**(3-2).
3. **출구 코드 검사**(프롬프트 지시가 아니다): 응답 안 모든 `/경로`가 실제 라우트에 있는지 · 인용한 작업 id가 카탈로그에 있는지 · 기존 `detectBenefits`로 혜택 토큰(%·원·쿠폰·무료) 검출. 하나라도 걸리면 답변을 버리고 1번으로 되돌린다.
4. **요금제 문구는 모델이 쓰지 않는다.** `planKey`를 그 회사 기준으로 판정해 우리 문자열로 렌더한다.

### 4-6) 안내 화면 `/guide`

**원고 0장·스크린샷 0장.** 같은 카탈로그를 그리기만 한다. 톤은 라이트 인디고(읽고 검색하는 조회 화면). 지금 대시보드 푸터가 걸고 있는 3개월 낡은 정적 매뉴얼 링크를 여기로 돌린다.

### 4-7) 과금: 무료

`CREDIT_COST_MAP`에 등록하지 않는다(2-9, 코드 변경 0). 사용법에 돈을 걷으면 안 묻고 이탈한다.
**`/api/help/*`는 크레딧 헤더를 싣지 않는다** — 전역 인터셉터가 그 헤더를 잡아 차감 토스트를 띄우기 때문이다.
남용 방지 4층: 매칭 실패는 AI 호출 전 거절 · 캐시 재사용 · 회사당 일 30회·분 3회(초과 시 잠그지 않고 검색 결과만) · 입력 240자 상한.
**원가는 1단계 산출물로 실측한다**(`ai_call_log`에 전용 source. 차감 0, 기록 1). 무료로 학습된 뒤에는 유료로 못 바꾼다.

---

## 5) 게이트 (불변식 테스트 7종)

`packages/backend/src/utils/__tests__/feature-catalog-invariants.test.ts` 하나. 기존 `scanSources` 캐시를 재사용한다.

1. **경로 실존**: 모든 `entry.path`가 App.tsx 라우트 집합에 있다. **추출이 40건 미만이면 그 자체로 실패**(App.tsx가 분할되면 정규식이 조용히 0건을 돌려주고 게이트가 죽은 줄도 모른다).
2. **역방향 커버리지**: `SUB_MODULE_CARDS` 12장 + 헤더 메뉴 라벨 + `FeatureKey` 12개가 각각 최소 1개 작업에 나온다. 새 카드·새 요금제 키를 넣고 정의를 안 쓰면 빌드가 깨진다.
3. **금칙어 0**: 모델명 5종 · 이모지 · 줄표 · `D숫자` · `CT-숫자` · `Modal` · 테이블명 후보. 고객 문장이 이 파일에만 있으므로 **테스트 하나가 프로젝트 하한 전부를 강제한다.**
4. **요금제 키 유효**: `planKey`가 타입에 있고, 그 키로 판정하는 백엔드 파일이 실존한다.
5. **크레딧 키 유효 + 숫자 금지**: `creditSource`가 실존하고, 정의 전 문자열에 크레딧 숫자·요금제 이름이 없다.
6. **치수**: `goal` 40자 이하 · `steps` 3~6 · `blockers` 3 이하 · `keywords` 5 이상.
7. **stub 만료**: `stub`은 `stubUntil`을 갖고, 그 날짜가 지나면 실패한다. "나중에"를 주석이 아니라 날짜로 건다.

그리고 **런타임 2층**(3-4): 응답 조립 직전 경로 재확인 + 관리자용 상태 조회로 위반 건수를 관측한다.

---

## 6) 1단계 범위 (숫자로 못 박는다)

**작업 12개 · 신규 파일 8개 · 사람이 쓰는 문장 약 120줄.** 나머지 기능 전부는 stub으로 같은 날 등재한다.

| # | id | 고객이 하려는 일 | planKey |
|---|---|---|---|
| 1 | `sender-register` | 발신번호 등록하기 | null |
| 2 | `upload-customers` | 엑셀로 고객 명단 올리기 | customer_db · ai_mapping |
| 3 | `send-direct` | 주소록으로 첫 문자 보내기 | basic_send |
| 4 | `check-results` | 발송 결과 보기 | basic_send |
| 5 | `manage-unsubscribes` | 수신거부 확인하기 | basic_send |
| 6 | `credits-and-plan` | 요금제와 크레딧 이해하기 | null |
| 7 | `send-target` | 조건으로 대상 골라 보내기 | target_send |
| 8 | `write-copy-ai` | AI로 문안 받기 | ai_messaging |
| 9 | `view-customer` | 고객 한 명 이력 보기 | customer_db_view |
| 10 | `schedule-send` | 예약해 두기 | basic_send |
| 11 | `send-alimtalk` | 알림톡으로 보내기 | ai_messaging |
| 12 | `auto-marketing` | 자동 마케팅 켜기 | auto_campaign |

**산출물 8개**: 카탈로그 · 불변식 테스트 · 답변 조립 CT · 라우트 · 런처 · 패널 · `/guide` 페이지 · DDL 1건.
**기존 파일 수정**: App.tsx(마운트 + 라우트) · 대시보드 푸터 링크 · 인터럽트 3종과 토스트에 표시 속성 1줄씩.

**1단계에서 빼는 것**(팽창 시 잘라낼 첫 후보를 미리 지목): 모달 인지 · 요소 하이라이트 · 회사 데이터 지표 · 마법사 화면 정리 · 도메인별 파일 분할 · 검색 미러 테이블 · 나머지 35화면 본문 · 우리 AI 기능에 주입.

### 6-1) 인수 조건 3개 (이게 없으면 "완료"라고 하지 않는다)

1. **백지 계정 리허설 1일.** 카탈로그를 안 쓴 사람이 신규 계정으로 12개를 순서대로 끝까지 해보고 막힌 자리를 그대로 적는다. **`blockers`가 전부 추측인 상태를 깨는 유일한 실측이고, 실사용 0에서 얻을 수 있는 유일한 입력이다.**
2. **375px 실측 1회.** 우하단 런처와 토스트 동시 표시.
3. **크레딧 라벨 1:1 테스트 통과**(2026-08-22 이미 완료 — 게이트 방식이 실제로 굴러간다는 첫 증거).

---

## 7) 학습 데이터 (Harold 미확정 1건)

다섯 역할 전원이 **"봇이 먼저, 제품 전체는 나중"**으로 수렴했다. 검증 안 된 정의를 문안 생성·진단에 먹이면 틀린 안내가 봇 하나가 아니라 47개 화면에서 나온다(위험이 곱셈이 된다).

⛔ **`ai_training_logs`에는 넣지 않는다.** 그 원장은 성과 라벨로 정렬되는 **문안 코퍼스**이고 소비처가 하나다. 라벨이 있을 수 없는 안내 문장을 섞으면 문안 생성이 매뉴얼 문장을 마케팅 문안으로 뱉는다.

(나)를 열 때의 형태도 미리 정한다: **생성이 아니라 참조.** AI 응답 끝에 관련 작업 칩을 붙이는 데까지가 안전선이고, 사용법을 문장으로 지어내게 하지 않는다.

**Harold 확인 대기**: 이 정의를 봇만 읽게 할지, 문안 생성·AI Operator·진단도 읽고 "그럴 땐 이 기능을 쓰세요"라고 안내하게 할지.

---

## 8) 이 축이 실패하는 경로와 막은 형태

| 실패 경로 | 막은 형태 | 남은 구멍 |
|---|---|---|
| 정의가 또 낡아 4개월 뒤 매뉴얼과 같은 자리 | 역방향 게이트(카드·메뉴·요금제 키) + stub 만료일 | 게이트가 옵트인이다(2-10) → 런타임 2층으로 완화 |
| 없는 기능을 있다고 하거나 막힌 기능을 쓰라고 한다 | 출구 4겹 + 요금제를 런타임 판정으로만 말함 | 네 겹 중 둘이 코드 출구라 상대적으로 튼튼 |
| 실사용 0에서 만든 정의가 실제 질문과 어긋난다 | 맞히려 하지 않고 **틀린 것을 빨리 안다**: 미답 원장 + 주 1회 상위 5건 자동 보고 + 백지 계정 리허설 | 첫 고객사가 봇을 세 번 쓰고 안 열면 로그도 안 쌓인다 |
| 규모가 커서 착수했다 중단 | §6의 숫자 + 뺀 것을 이름으로 열거 | 팽창 1순위 = `/guide`, 2순위 = stub 본문 |

---

## 9) 상태·잔여

- 2026-08-22 설계 확정(브레인스토밍 수렴 + Harold 지시 반영) → **같은 날 1단계 구현 완료 · 배포 대기.**
  - 백엔드: `content/feature-catalog.ts`(ready 12 + stub 27 · `JOB_GROUPS` 7 · `NOT_DOCUMENTED_ROUTES`) · `utils/help-answer.ts`(매칭·프롬프트·출구 4겹·소한도·모델 호출) · `routes/help.ts`(`GET /context` · `GET /catalog` · `GET /catalog/:id` · `POST /ask` · `POST /questions`) · `app.ts` 마운트.
  - 프론트: `components/help/`(HelpDock · HelpPanel · HelpJobCard · help-api · help-ui) · `lib/surface-flags.ts` · `pages/GuidePage.tsx`(`/guide` · `/guide/:jobId`) · App.tsx(lazy 리터럴 + 마운트 + 라우트) · ToastProvider·ConfirmModal·CreditConfirmModal·CustomerDataRequiredModal에 표시 속성 1줄씩 · 대시보드 푸터 링크 → `/guide` · 온보딩 마법사 `?step=`.
  - 게이트 결과: 백엔드 tsc 0 · 프론트 tsc 0 · vitest 193파일 2,930건(신규 `feature-catalog-invariants` 8 · `help-answer` 9 · `credit-label-parity` 2) · 검출기 지적 1건(인디고 면 위 회색 글자) 정정 후 0 · 모델명·dialog·이모지·줄표 0(기존 줄표 불변식이 프롬프트 문자열의 줄표 리터럴을 잡아 정정).
  - 구현 중 정정: 회의가 낸 매칭(키워드 전체 일치만)은 "문자 어떻게 보내요"처럼 활용형을 못 잡았다 → 낱말 + 어간(끝 글자 제외) 점수로 바꿈. 카탈로그 문장에서 혜택 단어(할인·쿠폰)를 제거 — 모델이 그대로 따라 쓰면 출구 검사에 걸려 답이 버려진다.
  - **배포 전 DDL 1건(Harold, 서버 psql)**: 아래 §9-D.
- 남은 것 = §6-1 인수 조건(백지 계정 리허설 1일 · 375px 실측) · §7 Harold 답 · 봇 패널 톤 육안 판정(라이트 인디고 1벌로 냈다. 다크 화면 위에서 어색하면 그때 2벌째).
- **2026-08-22 밤 배포완료 · Harold 1차 육안 확인 완료.** 그 자리에서 나온 접수 3건 = §10.
- **2026-08-22(2) 발견성 축 구현 완료 · 배포 대기**(§10-2). 바뀐 파일 = `lib/help-open.ts`(신설) · `components/help/HelpDock.tsx` · `components/help/help-ui.ts` · `components/DashboardHeader.tsx` · `lib/surface-flags.ts` · `components/ToastProvider.tsx` · `pages/SegmentsPage.tsx`. 백엔드·DB 변경 0.
- **2026-08-22(2) 답변 품질 축 구현 완료 · 배포 대기**(§10-1). 백엔드 = `content/feature-catalog.ts`(27개 승격 · stub 0 · `/diagnosis` 비문서 사유 · send-direct 질문형 키워드 2) · `utils/help-answer.ts`(관련 기능 title 전달 · `direct` 답변 틀 · `HELP_DIRECT_MARGIN`) · `routes/help.ts`(`direct` 응답). 프론트 = `components/help/help-api.ts` · `HelpPanel.tsx`(direct면 산문 0 · 첫 카드 펼침). 게이트 = 백엔드 tsc 0 · 프론트 tsc 0 · vitest 193파일 2,934건(불변식 8 + 답변 조립 13 + 라벨 계약 2 포함). DDL 0.

### 9-D) 배포 전 실행 DDL (Harold · 서버 psql)

```sql
CREATE TABLE IF NOT EXISTS help_questions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  user_id uuid NULL,
  path varchar(200) NULL,
  question varchar(240) NOT NULL,
  matched_ids text[] NOT NULL DEFAULT '{}',
  answered boolean NOT NULL DEFAULT false,
  created_at timestamptz NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_help_questions_company_created ON help_questions (company_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_help_questions_unanswered ON help_questions (answered, created_at DESC) WHERE answered = false;
```

- `user_id`에 FK를 걸지 않는다(0728 `23503` 원칙 — 계정이 삭제돼도 질문 기록은 남는다). 테이블이 없어도 답변은 나가고 "문의 남기기"만 503(`DB_MIGRATION_PENDING`).
- 확인 SQL: `SELECT column_name FROM information_schema.columns WHERE table_name='help_questions' ORDER BY ordinal_position;` (8행)
- **선행 완료(2026-08-22)**: 고객 화면 내부 용어 노출 14건 정정(테이블명 13 + 내부 코드명 1) · 크레딧 라벨 1건 + 1:1 계약 테스트 신설. 게이트 방식이 굴러간다는 첫 증거를 확보했다.
- **별건(미착수)**: 청구·회사·관리 라우트의 같은 문구 24건. 슈퍼관리자 화면이 섞여 있어 고객 노출 여부 판정이 먼저다.

---

## 10) 다음 세션 접수 (2026-08-22 밤 Harold 육안 확인 결과)

배포 후 Harold님이 실제로 써 보고 낸 접수다. **착수 순서는 아래 그대로**(1번이 답변 품질의 대부분이다).

### 10-1) 답변 품질 — 빈칸 27개 본문 + 이름 지어내기 차단 + 답변 틀 → **2026-08-22(2) 구현 완료 · 배포 대기**

**실측 증상**(Harold 스크린샷): "이미지 스튜디오는 어떻게 써요?" → 봇이 시작 위치까지만 답하고 "구체적인 사용 단계는 이 안내에는 없는 내용입니다"로 끝났다. 그리고 **"퀵 캠페인"이라는 없는 이름을 만들어 냈다**(실제 화면 이름은 "원클릭 캠페인").

| # | 무엇 | 왜 | 어디를 고치나 |
|---|---|---|---|
| ① | **빈칸 27개에 본문(steps·blockers)을 채운다** | 1단계는 12개만 썼다. 나머지는 설계대로 "시작 위치까지만" 답한다 — 지어내지 않은 건 맞지만 사용자에겐 못 답한 것이다 | `content/feature-catalog.ts`의 `STUBS` → `READY`로 승격. 승격 시 `status:'ready'` + `stubUntil:null` + steps 3~6 + blockers ≤3(게이트 6·7이 강제) |
| ② | **관련 기능을 내부 식별자가 아니라 화면 이름으로 넘긴다** | 지금 `related`를 id 배열(`quick-campaign`)로 프롬프트에 넣어서 AI가 그 영문 id를 한국어로 옮기며 없는 이름을 지었다. **이건 구현 결함이다** | `utils/help-answer.ts` `buildHelpMessages` — `related`를 `[{id, title}]`로 바꿔 넣고, 프롬프트에 "관련 기능은 주어진 title 그대로만 부른다"를 명시 |
| ③ | **답변 틀을 고정한다** | 지금은 AI가 산문으로 풀어 써서 정의에 있는 순서가 뭉개진다. 본문이 있는 기능은 AI가 다시 쓰는 것보다 **정의를 그대로 보여주는 쪽이 정확하다** | 본문 있는 작업이 1순위로 매칭되면 모델 호출 없이 카드(한 줄 정의 → 어디서 → 순서 → 막히면)를 그대로 낸다. 모델은 **어느 작업인지 고르는 일과 여러 작업에 걸친 질문**에만 쓴다 |

③은 부수 효과가 크다. 모델 호출이 줄어 원가·지연이 함께 내려가고, 출구 검사에서 답이 버려지는 경우도 줄어든다.

**검증 방법**: 승격한 기능마다 "○○ 어떻게 써요?"를 한 번씩 물어 (가)순서가 나오는가 (나)없는 이름이 없는가를 본다.

#### 10-1-A) 구현 결과 (2026-08-22(2))

| # | 한 것 | 어떻게 |
|---|---|---|
| ① | **27개 전부 승격** | 화면 코드를 읽고 썼다. `steps`는 그 화면의 실제 버튼 라벨과 순서, `blockers`는 코드가 실제로 내는 차단 문구(`toast.error`·`disabled`·게이트)에서만 뽑았다. 코드에 근거 없는 막힘은 적지 않았다 |
| ② | **관련 기능 = 화면 이름** | `buildHelpMessages`가 `related` id를 카탈로그 title로 바꿔 넣고 프롬프트에 "이름을 줄이거나 바꾸거나 새로 짓지 않는다"를 명시. 프롬프트에 `id` 필드 자체를 넣지 않는다(테스트 고정) |
| ③ | **답변 틀 고정(`direct`)** | 본문 있는 작업이 **분명한 1순위**(2순위와 점수 차 ≥ `HELP_DIRECT_MARGIN`=4, 키워드 하나가 통째로 맞은 만큼)면 모델 호출 0 · 한도 소모 0 · 출구 검사 불요. 화면은 산문 없이 첫 카드를 펼친다. 모델은 1·2순위 차가 작을 때만 "어느 작업인지" 가리는 데 쓴다 |

**집필 중 드러나 정정한 사실 3건**(stub 때 틀려 있었다):
- `/ai-batches` · `/ai-explain` · `/voice-inbound` · `/push-campaigns` · `/calendar` 다섯 화면은 **고객 화면 어디에도 진입 버튼이 없다**(grep 0). 안내에 "메뉴에 버튼이 없습니다. 주소창의 주소 끝을 …으로 바꿔 엽니다"라고 그대로 적었다. 없는 버튼을 있다고 쓰는 것이 이 원장의 유일한 거짓말이다.
- `/diagnosis`는 **로그인 전 공개 진단**(로그인 화면 링크). 로그인 고객은 대시보드 "진단 시작" 카드로 받는다. `marketing-diagnosis`의 진입을 `/dashboard`로 고치고 `/diagnosis`는 `NOT_DOCUMENTED_ROUTES`에 사유를 적었다.
- 자동 스팸 검사(`auto_spam_test`)는 직접발송이 아니라 **자동 마케팅·마케팅 플래너가 문안을 확정할 때** 도는 것(`autoSpamTestWithRegenerate` 소비처 3곳). 진입을 `/continuous-operator`로 고쳤다. 요금제 키는 `plan-guard`에 있으나 그 키로 판정하는 호출부가 없다(플래그만 있고 게이트 배선 0) → **별건**으로 남긴다.

**남은 것 = 배포 후 육안**: 39개 제목으로 "○○ 어떻게 써요?"를 물어 (가) 카드가 펼쳐지는가 (나) 없는 이름이 없는가. 모델 경로(동점)로 가는 질문은 출구 검사가 막는지 같이 본다.

### 10-2) 봇 발견성 — 저게 도움말인지 알 수 없다 → **2026-08-22(2) 구현 완료 · 배포 대기**

**실측 증상**: "우측 아래 버튼은 봤지만 저게 도움말인지 어떻게 알겠냐?"
**Harold 지시 원문**: "테스트계정이라고 되어있는 업체명 우측으로 옮기면 공간확보되잖아" · "궁금한건 물어보세요 하면서 오른쪽 아래 자리잡는 것처럼 애니메이션" · "니가 말한대로면 무조건 겹친다니까?"

#### 10-2-A) 착수 전 실측 — 옮기려던 이유는 겹침이었고, 겹침의 원인은 런처 자리가 아니었다

| # | 사실 | 근거 |
|---|---|---|
| 가 | 공용 알림(`ToastProvider`)은 우측 **상단**(`top-4 right-4`)이다. 우하단 런처와 애초에 겹치지 않는다 | `ToastProvider.tsx:90` |
| 나 | 그런데 런처는 그 알림이 뜨면 `bottom-4` → `bottom-20`으로 **위로 비켜서고 있었다.** 겹치지도 않는 것을 피해 튀어오른 것 | `ToastProvider.tsx:55` → `HelpDock.tsx:31,63` → `help-ui.ts:14`(당시) |
| 다 | 진짜 우하단을 쓰는 화면은 **`/segments` 하나**다. 이 화면이 공용 알림을 안 쓰고 제 우하단에 알림을 따로 만들었다 | `SegmentsPage.tsx:585-597`(당시) |
| 라 | 같은 모양이 2곳 더 있으나 **둘 다 대상이 아니다**: `AlimtalkSendersSection`은 `/admin`·`/admin/alimtalk-senders`에서만 렌더되는데 런처가 `/admin`에서 안 뜬다. `AlimtalkTemplateFormModal`은 import하는 곳이 **0**인 죽은 파일(V2로 대체) | 소비처 grep |
| 마 | 하단 **가운데**(`left-1/2`)를 쓰는 3곳이 더 있다. 데스크톱에서는 우하단과 떨어져 있고, 375px에서 닿는지는 미검증 | `ManageCustomersTab.tsx:627` · `ManagePurchasesTab.tsx:402` · `CustomersTab.tsx:376` |

**"우하단 3곳"이라 보고했던 1차 집계는 과대였다**(라 때문에). 실제 겹침은 다.

#### 10-2-B) 확정 — 옮기지 않는다. 겹침을 없애고, 발견성은 연출이 맡는다

옮겼을 때의 손실이 실측으로 확인됐다: **`DashboardHeader`를 렌더하는 페이지는 대시보드 1개뿐이고 나머지 44개는 자체 헤더를 쓴다.** 옮기면 여정·설정·발송결과 등에서 도움말이 사라지고, 막힌 그 화면에서 물을 수 없게 된다(현재 구현은 보고 있는 경로를 같이 보내 그 화면 기능을 먼저 찾는다 · `matchJobs`).

| # | 무엇 | 구현 | 주의 |
|---|---|---|---|
| ① | **겹침 원인 제거** | `/segments`의 자체 우하단 알림을 공용 알림으로 되돌렸다(`showToast` 안쪽만 교체 · 호출부 9곳 무변경) | ⛔ 화면이 제 알림을 따로 만들면 그 자리에서 또 겹친다. 우하단은 런처 자리다 |
| ② | **비켜서는 장치 폐기** | `data-toast-open` 플래그 자체를 없앴다(`surface-flags.ts` 타입에서 제거 · `ToastProvider`의 호출 제거 · `HELP_LAUNCHER_WRAP_SHIFTED` 삭제) | ⛔ 소비처 0인 플래그를 남기면 다음 사람이 "겹치면 비켜선다"고 잘못 믿는다 |
| ③ | **티저 후 자동 접힘** | 진입 1.2초 뒤 문구가 펼쳐지고 4초 뒤 스스로 접힌다. 계정당 1회(`localStorage: help_teased_v1` + 모듈 잠금 이중). 누르면 즉시 접힌다 | ⛔ 폭 전환은 `transform: scale`이 아니라 **max-width**로 한다. transform이 걸린 조상은 그 안 `fixed`의 기준이 되어 패널 안 전체 화면 창이 어긋난다(`help-ui.ts` 규약) |
| ④ | **헤더 진입점 추가** | 세션 타이머 **왼쪽**에 "궁금한 건 물어보세요" 버튼. **옮기는 것이 아니라 추가**다 | Harold 2차 지시 "회사명이 길 수도 있잖아, 타이머 옆으로". 처음 업체명 옆에 뒀다가 옮겼다. ⛔ `menuItems` 목록에 넣지 않는다(메뉴가 아니라 유틸리티). 회사명 블록에는 새로고침(`window.location.reload`)이 걸려 있어 그 안에는 어떤 버튼도 넣지 않는다 |
| ⑤ | **열림 상태 단일 소유** | `lib/help-open.ts` 신설(`useSyncExternalStore`). 진입점이 둘이어도 창은 하나 | `eligible`도 같이 둔다 — 판정은 서버 하나고 그 응답을 받는 곳은 `HelpDock`뿐이다. ⛔ 헤더가 요금제를 따로 조회하지 않는다 |

⛔ 주의: 2026-08-22 확정된 "헤더에 진입점을 늘리지 않는다"(`DashboardHeader.tsx`)와 부딪히는 것처럼 보이지만, 그 확정은 **메뉴 항목**에 대한 것이고 이건 업체명 옆 유틸리티 자리다. 메뉴 목록에 넣지 않는다.

**게이트 결과**: 프론트 tsc 0 · vitest 193파일 2,930건 전부 통과 · `build:safe` 성공(lazy 청크 39건 실존) · 빌드된 CSS에 새 클래스 실존 확인(`transition-[max-width,opacity,padding]` · `max-width:240px` · `max-w-0` · `duration-500` · `ease-out`). 목업이 아니라 산출물에서 확인했다.

**남은 것 = 육안 1회**: 티저가 실제로 펼쳐졌다 접히는가 · 헤더 버튼과 하단 런처가 같은 창을 여는가 · 375px에서 하단 가운데 3곳(마)과 닿는가.

### 10-3) 백지 계정 리허설 1일 (§6-1 인수 조건)

카탈로그를 안 쓴 사람이 신규 계정으로 12개를 순서대로 끝까지 해보고 **막힌 자리를 그대로 적는다.** 지금 `blockers`는 전부 내가 코드를 보고 추론한 것이다. 실사용 고객 0인 상태에서 추측을 실측으로 바꿀 수 있는 유일한 방법이고 비용이 1일이다. 10-1 ①과 같이 하면 승격하는 27개의 blockers도 실측으로 쓴다.

### 10-4) 그 밖(순위 밖)

- 375px 실측(§6-1 ②) — 이제 볼 것은 공용 알림이 아니라 **하단 가운데 3곳**(10-2-A 마)과 우하단 런처가 좁은 폭에서 닿는지다.
- **별건(미착수)**: `components/AlimtalkTemplateFormModal.tsx`는 import하는 곳이 0인 죽은 파일이다(`AlimtalkTemplateFormV2`가 대체). 삭제 판단은 별도 과제 — 이번 세션에서는 손대지 않고 되돌렸다.
- §7 학습데이터 (나) 판단 — Harold 답 대기.
- 모달 인지·요소 하이라이트·칩 건수·회사 데이터 질문(2단계 목록 그대로).
