# SNS 채널 (기능 상설 문서 · 2026-09-21 신설)

> **호출어 = "SNS 채널" / "SNS 게시" / "인스타 연동"**. 이 문서가 이 기능의 **정체성·불변 원칙·구조·채널 현황·규격 맞춤 규칙·운영·남은 것·이력**을 소유한다. STATUS 는 세션 행으로 참조만 한다.
> **시점 근거 = [2026-09-17 설계서](2026-09-17-sns-publish-design.md)** — 회의록(§8)·예정 DDL(§3-1)·**실측 원문(§1-4 인스타 · §1-5 Threads)**·별건 목록(§9)은 그 문서가 소유하며 여기 복사하지 않는다. 두 문서가 다르면 **현재 코드가 진실**이다.
> 관련 상설 = [FEATURE-IMAGE-STUDIO.md](FEATURE-IMAGE-STUDIO.md)(소재 라이브러리 · 비율 변환 원칙) · [FEATURE-CDP-INTEGRATION.md](FEATURE-CDP-INTEGRATION.md)(OAuth 배관 선례) · [2026-09-01 AI 이미지 표시](2026-09-01-ai-image-notice-design.md)(표시 CT 계약) · [2026-08-21 오퍼레이터 표면](2026-08-21-operator-surface-tier-design.md)(화면 토큰).

---

## §0 30초 요약

- **무엇**: 회사가 자기 SNS 계정을 OAuth 로 연결하고(비밀번호 저장 0), 사진과 글을 **한 번** 쓰면 고른 채널마다 그 채널 규격으로 각각 올라간다. 성공은 **플랫폼 재조회로만** 확정한다.
- **어디**: `/sns` · AI Operator 허브 3행 3열 카드 `SNS 채널`.
- **채널(2026-09-23 1차-B)**: 인스타그램·Threads·페이스북 페이지·X = **연결·게시 코드 완료 · 영상 포함**(인스타 = 릴스). 카드가 실제로 열리는지는 **자격 ENV(+ X 는 월 상한 ENV)** 가 정한다(`sns-availability.ts`). 설계·결정 = [2026-09-23 1차-B 설계서](2026-09-23-sns-1b-design.md).
- **개방**: ENV `SNS_COMPANY_IDS` **회사 단위**. ENV 밖 회사도 **카드는 보이고**, 누르면 기능 안내 창이 열린다(숨기지 않는다).
- **상태**: S0·S1 배포·게이트 통과(자사 계정 연결 실동작). **S2·S3 코드 완료 · 게시 실측 대기.** **1차-B 코드 완료(0923) · 미배포 · 전 경로 문서 기준(raw 전).**
- **★ 0924**: 올릴 글 재설계(글·태그 반반 · 글 상자 꼬리 · 채널별 글 · AI 캡션 사진 초안 · 맞춤법 검사 · 자주 쓰는 태그) + 가장 편리한 채널 관리(확인할 것 띠 · 다시 연결 1탭 · 가장 나쁜 계정 배지 · 예약 블록 · 시각 바꾸기 · 글 고치기 · 불러와서 쓰기 · 쓰던 글 보존) + 안전 결함 12건. **코드 완료 · 미배포 · DDL 0.** 설계·결정 = [0924 설계서](2026-09-24-sns-channel-design.md)(Q1 가 · Q2 가 · Q3 나).

---

## §1 정체성

- **발송이 아니라 게시다.** 문자·알림톡은 받는 사람 목록이 있고 건당 과금이 있지만, SNS 는 대상이 없고 채널 계정 하나에 글이 올라간다. 그래서 수신자·스팸·발신번호 축이 통째로 없다.
- **한 번 쓰면 갈라진다.** `sns_posts` 1행 : `sns_post_targets` N행. **상태 머신의 주인은 target 이고** post 상태는 자식 집계 파생이다. "인스타는 올라갔는데 Threads 는 실패"가 정상 결과다.
- **우리 원장과 플랫폼, 진실이 두 곳이다.** 그래서 대조 워커가 처음부터 함께 있다(6원칙 ③).

---

## §2 불변 원칙 (어길 수 없는 것)

전량은 설계서 §2(20개)가 소유한다. **코드를 고치기 전 반드시 보는 8개**만 여기 둔다.

1. **저장 ≠ 연결.** 토큰을 저장하면 `pending` 이다. 플랫폼에 계정을 **다시 물어** 자격이 확인된 뒤에만 `active` 다. `pending`·`ineligible` 에 초록 0.
2. **전 조회에 `company_id` 조건을 직접 부여한다.** `(platform, external_account_id)` 만으로 회사를 정하는 함수를 만들지 않는다. 공개 콜백(deauthorize)은 회사별 후보를 전부 꺼내 각각 처리한다.
3. **성공은 플랫폼 재조회로만.** API 200 은 `submitted` 까지다. `published` ⇔ `verified_at` 있음. 화면 초록도 그 뒤에만.
4. **target 상태값 7개 고정**(`draft` `scheduled` `claimed` `submitted` `published` `failed` `cancelled`). `stage` 는 **기록 전용**이며 어떤 분기도 읽지 않는다. **`failed → scheduled` 전이 없다**(재시도 = 새 행).
5. **이중 게시 0 = 3겹.** `UNIQUE(company_id, idempotency_key)` + `FOR UPDATE SKIP LOCKED`·`lock_token` + **게시 호출 직전 소유권 재확인**. 메모리 잠금 금지.
6. ⛔ **원본을 멋대로 자르지 않는다**(Harold 확정 2026-09-21) — §5 가 전문을 소유한다.
7. ⛔ **고객 문안을 말없이 바꾸지 않는다.** 채널 상한을 넘어도 **자르지 않고** 넘었다는 사실만 돌려준다. 게시를 막는 것은 호출부다.
8. **AI 는 태그 생성기가 아니라 선택기다.** 회사 '자주 쓰는 태그'의 부분집합만 서버가 통과시킨다. **사용자 글이 유일한 면허다** — 링크·맨 도메인·금액·혜택 덩어리·문장 속 #태그를 `{{k#}}` 로 가리고 되돌리며(**하나라도 잃으면 폐기하고 폐기했다고 말한다** · 0924), 결과 링크 호스트 ⊆ 원문 · 없던 #태그 제거 · 출구 `stripUnauthorizedBenefits`. 글이 없고 사진만 있으면 사진 초안(숫자·링크·혜택·행사 낱말 문장은 뺀다 · 짧으면 결과 없이 한 줄 안내).

---

## §3 구조 — 파일별 소유

| 층 | 파일 | 소유하는 것 |
|---|---|---|
| 상수 | `utils/sns-constants.ts` | 플랫폼 4 · 상태 3종(계정·target·post) · `derivePostStatus` · ENV 게이트 · 오류 코드 |
| 어댑터 계약 | `utils/sns/adapter.ts` | `ISnsAdapter`(연결) · `ISnsPublishAdapter`(게시) · `capabilities` · 오류 형(型) |
| 채널 | `utils/sns/{instagram,threads,facebook-page,x}.ts` | **채널 1개 = 파일 1개.** 상수·endpoint·scope 를 그 파일이 직접 선언한다(화면 추론 0) |
| 계정 원장 | `utils/sns-accounts.ts` | 계정 CRUD · 자격 해석 · 화면 직렬화(**토큰 필드 없음**). ⛔ `company_integrations` 를 import 하지 않는다(정적 계약) |
| OAuth | `utils/sns-auth-state.ts` | state 서명·TTL·미래 시각 방어. 1회용 보장은 `sns_oauth_states` 행이 한다(두 겹) |
| 규격 판정 | `utils/sns-media-fit.ts` | **§5 규칙의 값 소유자.** 순수 함수(sharp 0) · ★1차-B 채널별 미디어 수용(`snsMediaBlockReason`) · 영상 판정(`planSnsVideoFit`) |
| 영상 판독 | `utils/sns-video-probe.ts` | ★1차-B ISO-BMFF 판독(길이·표시 크기·코덱·초당 프레임) · **moov 앞당김(무손실)** |
| 개방 판정 | `utils/sns-availability.ts` | ★1차-B 코드·자격 ENV·실비 월 상한 셋 다. 카드·연결·저장·워커가 이 함수 하나를 부른다 |
| 미디어 | `utils/sns-media.ts` | 원본 저장 · 소재 복사 · 게시본 굽기(JPEG 재판독 · ★target·미디어별 파일) · 경로 조작 차단 · ★영상 4MB 조각 세션(디스크만) · 서명 서빙 파일 해석 |
| 캡션 | `utils/sns-caption-rules.ts` | 확정본 조립 · 상한 판정 · 태그 판정(사유) · ★0924 본문 태그가 채널 태그 자리를 먼저 씀 · 두 번 붙지 않음 · 채울 자리 게시 불가 · 링크·태그 자리 판정. **전 경로가 이 함수를 지난다** · 화면 미러 = `sns-view.ts`(계약 테스트가 같은 표로 대조) |
| AI | `utils/sns-caption-ai.ts` | ★0924 모드(다듬기·사진 초안·잠금) · 가림·복원 · 세트 부분집합 강제 · 출구 차단기 · 다시 쓰기·길이 맞추기 · ★0925 사진 초안 = 사진 속 글 옮겨 적기(`transcribeSnsImageText` · 30분 캐시) → 그 글 + 회사 이름이 면허 |
| 맞춤법 | `utils/sns-spell-check.ts` | ★0924 모델은 후보만 · 위치·판정은 서버(원문 1회 · 최소 변경 구간 · 보호 구간 · 편집 거리) |
| 저장·게시 | `utils/sns-compose.ts` | ★0924 저장+예약 한 트랜잭션 · composeId(uuid v5) 중복 0 · expected 대조(409) · 글 고치기 대체(REPLACED) · 시각 바꾸기(NOWAIT) |
| 다시 시도 | `utils/sns-retry.ts` | ★0924 한 트랜잭션 · 나중 행 재계수(두 번 눌러도 한 번) · 최신 행 SQL 조각 · 실패 할 일(`snsFailureAction`) |
| 예약 시각 | `utils/sns-schedule.ts` | ★0924 ISO+오프셋 · 오프셋 없으면 한국 시간 · 60초 넘게 지난 시각 400 |
| 목록·기본값 | `utils/sns-posts.ts` | ★0924 예약/기록 가르기 · 묶음 상태 = 계정별 최신 행 파생 · 작성 기본값(지난번 채널) · ★0925 기록 10개씩 페이지(목록·개수 같은 조건) · 미디어 종류 |
| 확인할 것 | `utils/sns-attention.ts` | ★0924 끊김·확인 멈춤·자격 없음·연장 실패·올리지 못한 글·결과 모름(최대 3 + 총수) |
| 자주 쓰는 태그 | `utils/sns-tag-set.ts` | ★0924 `brand_kit.sns_tag_set` 더하기·빼기(행 잠금 · `jsonb_set` · 상한 50) · 형식 위반 옛 값 `invalid[]` · 씨앗 |
| AI 표시 판정 | `utils/sns-ai-notice.ts` | ★0924 `ai_declared` 또는 이미지 스튜디오에서 **만든** 소재(직접 올려 둔 소재는 아니다) |
| 쓰던 글 | 화면 `utils/sns-draft.ts` | ★0924 localStorage 7일 · 로그아웃·401 에서 삭제 · composeId 미러 |
| 서명 URL | `utils/sns-signed-media.ts` | HMAC + **상태 결박 판정**(`isSnsMediaUrlLive`) |
| 멱등키 | `utils/sns-idempotency.ts` | 94자 고정 키 · 과금 지문 |
| 워커 | `utils/sns-{publish,reconcile,token}-worker.ts` | 발행 30초 · 대조 30분 · 토큰 6시간 |
| 라우트 | `routes/sns.ts` | 인증 라우터 + **공개 라우터**(콜백·권한 회수·서명 미디어 · helmet 앞 마운트) |
| 화면 | `pages/SnsPage.tsx` · `components/sns/*` | 채널 카드 · 작성 구역 · 이력 · 채널 상세 창 · 소재 픽커 |
| 화면 사전 | `utils/sns-view.ts` | 배지 문구·색 · **배지 판정 함수**(증거 컬럼이 상태보다 앞선다) |

**테이블 5** = `sns_accounts` · `sns_oauth_states` · `sns_media` · `sns_posts` · `sns_post_targets`. DDL 전문은 설계서 §3-1. ⛔ `company_integrations` 재사용은 **금지**(provider 조건 없는 조회 2곳이 인앱 게이트를 뚫는다).

---

## §4 채널 현황

| 채널 | 연결 | 게시 | 비고 |
|---|---|---|---|
| **인스타그램** | 실동작(0920) | 코드 완료 · 실측 대기 | 상수 전량 실측 확정(설계서 §1-4). ⛔ **계정 ID 는 `/me` 값**(대시보드 표시값과 다르다) · ⛔ **`media_url` 저장 금지**(CDN 만료) · **삭제 API 없음**(사람이 지운 것을 대조 워커가 감지) |
| **Threads** | 실동작(0921) | 코드 완료 · **미검증** | 연결 경로만 확정(설계서 §1-5). **앱 ID·시크릿이 인스타와 별개** · 게시 경로(`/threads`·`/threads_publish`·`text` 파라미터)는 문서 기준 · ★영상 `VIDEO` 최대 5분 |
| 페이스북 페이지 | 코드 완료(0923) · **미검증** | 코드 완료 · **미검증** | Facebook Login · **로그인 1회 = 페이지 N행**(페이지 토큰 만료 없음) · 글·사진·여러 장(`attached_media`)·영상(`file_url`) · 권한 회수는 사람 id → `meta.profile.owner_user_id` · 실제 게시는 `publish` 안에서만 |
| X | 코드 완료(0923) · **미검증** | 코드 완료 · **미검증** | **PKCE** · 액세스 2시간 → **게시 직전 행 잠금 갱신** · 사진 4장·영상 조각 업로드 · **글 1건 $0.015 · 링크 $0.200 · 읽기 $0.005(공식)** → 확인은 게시 직후 1건(`immediate`로 변경) · 삭제 감지 제외 · **월 상한 ENV 없으면 닫힘** |

**영상(1차-B)** = 한 게시물에 1개 · 사진과 섞지 않는다 · **다시 인코딩하지 않는다** · 인스타·Threads 규격의 "moov atom at the front" 때문에 moov 가 뒤에 있으면 **상자 순서만 바꾼다**(위치표 보정 · 영상 바이트 불변) · 판정은 확인된 위반만 막는다(모르는 값은 통과) · 업로드 300MB(인스타 상한) · 4MB 조각.

스켈레톤도 **어댑터로 등록돼 있다** — 화면이 채널 목록을 지어내지 않고 서버가 준 것만 그리기 위해서다. 연결을 누르면 사유와 함께 막힌다.

---

## §5 ⛔ 규격 맞춤 — 원본을 멋대로 자르지 않는다 (Harold 확정 2026-09-21)

`utils/sns-media-fit.ts` 가 값으로 소유하고 **계약 테스트 6건이 잠근다.**

1. **허용 비율 안이면 아무것도 하지 않는다.** 손대는 것이 예외다. 폭 상한도 안 넘으면 픽셀 그대로 나간다.
2. **경계에 0.5% 오차를 둔다.** `1200x628` 은 1.9108 로 인스타 상한(1.91)을 0.04% 넘는데, 그걸로 여백을 붙이는 것이야말로 "멋대로"다.
3. **범위 밖이어도 자르지 않는다.** **가장 가까운 경계**로 pad 한다 — 3:4(0.75) 포스터는 1:1 이 아니라 4:5(0.8) 로 가서 여백이 최소다.
4. **`crop` 은 기본이 될 수 없다.** 사용자가 그 채널에 대해 명시적으로 고를 때만. 워커는 `mode:'pad'` 고정이다.
5. **여백을 무엇으로 채우는지는 `image-serve.ts fitToCanvas` 가 사진을 보고 고른다**(단색 배경이면 가장자리 색 · 복잡하면 블러 · 0908 비교 캡처로 확정). ⛔ SNS 에서 다시 짜지 않는다 — 같은 사진이 화면마다 달라진다.
6. **화면 문구에 "잘린다"가 나오지 않는다.** 잘리지 않으므로. 소스 스캔 테스트가 강제한다.
7. 게시본은 **별도 파일**이다(원본 불변 · `.opt` 캐시 미사용). **구운 파일을 다시 읽어** JPEG 가 아니면 게시를 시작하지 않는다.

사진을 올리는 즉시 화면이 채널마다 결과를 말한다 — "고른 채널 모두 사진을 그대로 올립니다" 또는 "Threads 는 규격이 달라 여백을 채워 올립니다. 사진은 잘리지 않아요."

---

## §6 운영

**ENV**(`packages/backend/.env`)

| 키 | 값 | 비고 |
|---|---|---|
| `SNS_COMPANY_IDS` | 회사 id 목록 · `*` = 전 회사 | **비면 기능이 닫힌다**(워커도 안 뜬다). 되돌리기 = 비우고 재시작 |
| `INSTAGRAM_CLIENT_ID/SECRET/REDIRECT_URI` | Meta 앱 Instagram 유스케이스 | 콜백 = `/api/sns/auth/callback/instagram` |
| `THREADS_CLIENT_ID/SECRET/REDIRECT_URI` | **인스타와 별개 값** | 콜백 = `/api/sns/auth/callback/threads` |
| `SNS_MEDIA_PATH` · `SNS_RENDER_PATH` | 원본·게시본 보관 | 생략 시 `./uploads/sns-media`·`./uploads/sns-render` · 영상 조각은 `SNS_MEDIA_PATH/_incoming/{회사}` (6시간 지나면 정리) |
| `FACEBOOK_PAGE_CLIENT_ID/SECRET/REDIRECT_URI` | ★1차-B Meta 앱 **Facebook 로그인** 이용 사례 | 콜백 = `/api/sns/auth/callback/facebook_page` · 권한 회수 = `/api/sns/deauthorize/facebook_page` |
| `X_CLIENT_ID/SECRET/REDIRECT_URI` | ★1차-B X 개발자 콘솔 OAuth 2.0 **Web App(기밀)** | 콜백 = `/api/sns/auth/callback/x` |
| `SNS_X_MONTHLY_POST_CAP` | ★1차-B X 이번 달 게시 상한(전 회사 합산) | **비었거나 0 이면 X 카드가 `준비 중`**(fail-closed). 콘솔 지출 상한이 두 번째 겹 |
| `SNS_FACEBOOK_PAGE_COMPANY_IDS` | ★0924 페이스북 페이지를 열 회사 id 목록 · `*` = 전 회사 | **비면 닫힘**(심사 전 채널은 명단 회사에만). 인스타·Threads 는 이 키와 무관 |
| `SNS_X_COMPANY_IDS` | ★0924 X 를 열 회사 id 목록 · `*` = 전 회사 | **비면 닫힘** · 월 상한 ENV 와 둘 다 있어야 열린다 |

**게이트 순서** = 인증 → `requirePlanFeature('sns_publish')`(FREE 만 차단) → ENV. ⛔ 뒤집으면 ENV 에 든 FREE 회사가 뚫린다.

**Meta 콘솔 설정** — Threads 는 콜백 **3칸이 전부 필수**이고, 리디렉션 칸은 **입력 후 제안을 클릭해 칩으로** 만들어야 저장된다(설계서 §1-5). 테스터 역할 + **초대 수락**이 OAuth 선행 조건이다(인스타·Threads 각각).

---

## §7 남은 것

| # | 무엇 | 상태 |
|---|---|---|
| 1 | **게시 실측 1건** — 사진 → 두 채널 → `게시됨` → 삭제 감지 | S3 게이트. **다음 세션 1순위** |
| 2 | **Codex 적대 검토** — DDL 경로라 규칙상 대상 | 미실행(내가 호출 불가 · Harold 실행) |
| 3 | ~~AI 캡션이 사진을 보지 않는다~~ **0924 해소** | 공용 AI 함수는 이미 사진을 받았다(0917 §9-11 보류 사유는 사실과 달랐다) · 사진 초안(Q2 가) |
| 4 | Threads **게시 경로 미검증** | 연결만 확정. 실측에서 다르면 `sns/threads.ts` 게시 블록만 교정 |
| 5 | Threads **데이터 삭제 콜백이 임시 주소** | 설계서 §9-9. 2차 심사에서 걸릴 수 있음 |
| 6 | 프로페셔널 아닌 계정의 `ineligible` 경로 | 그런 계정이 있어야 확인 가능 |
| 7 | ~~1차-B(릴스·페북·X)~~ **코드 완료 0923** · 2차(고객 개방·심사) | 1차-B 설계서 §8 실측 6건 · §9 미검증 9건 · §10 별건 5건 |
| 8 | **1차-B 배포 전 DB 확인 1회** — `sns_media`·`sns_post_targets` CHECK 제약에 `'video'` 가 막히지 않는지 | 1차-B 설계서 §6 SQL(Harold) |
| 9 | **1차-B Codex 적대 검토** — X 실비 경로 · 발행 워커 D1·D3 · 행 잠금 갱신 | 미실행(Harold) |
| 10 | **0924 배포 전 SQL 4건 + 추가 2건**(기본 키 · 예약·실패 행 · 결과 모름 후보 · 자주 쓰는 태그 · `cdp_assets` 컬럼 · `companies.company_name·brand_name`) | 0924 설계서 §10 · 완료 보고 |
| 11 | **0924 실측** — 사진 1장 + 자주 쓰는 태그 → 두 채널 → 채널별 글과 실제 게시물 대조 · 다시 시도 두 번 눌러 1건 · 예약 시각 바꾸기 | 배포 뒤(Harold) |
| 12 | **0924 Codex 적대 검토** — 저장·게시 트랜잭션(`sns-compose.ts`) · 다시 시도 · 해제 취소 · 좌초 회수 | 미실행(Harold) |

---

## §8 이력 · 뒤집힌 판단

| 날짜 | 무엇 | 왜 |
|---|---|---|
| 0917 | 전원합의체 수렴 · 설계서 | 명칭 `SNS 게시` 4:1 · 신규 테이블 5 · 상태 7 |
| 0920 | S0 raw 실측 · S1 배포·게이트 통과 | **계정 ID 가 화면값과 다르다**를 raw 가 잡았다 — 어댑터보다 curl 이 먼저여야 하는 이유의 실증 |
| 0920 | 명칭 `SNS 게시` → **`SNS 채널`** | Harold 확정. "기능을 계속 붙여 나갈 그릇" |
| 0920 | 허브 타일 위치 2행 → **3행 3열** | 만들고(자동제작) 다듬어서(이미지 스튜디오) 내보내는(SNS) 동선이 한 줄로 이어진다 |
| 0920 | **카드 숨김 폐기** | 1차 구현은 ENV 밖 회사에 카드를 숨겼다. Harold 정정 — **"없는 메뉴는 물어볼 수도 없다."** 카드는 보이고 누르면 안내 창 |
| 0920 | AI 자율 예측 타일 → AI 메모리 서브메뉴 | 1차 구현은 타일만 내리고 **입구를 안 냈다**(주소를 아는 사람만 쓰는 죽은 기능). Harold 정정 후 입구 실존을 계약 테스트가 잠근다 |
| 0921 | Threads 연결 실동작 | 앱 ID 가 인스타와 **실제로 별개**임이 확인돼 ENV 분리가 맞았다 |
| 0921 | **비율 처리가 설계보다 강해졌다** | 설계는 "잘림 표시 뒤 선택"이었으나 Harold 확정으로 §5 가 됐다. 경계 오차 0.5% 도 이때 들어갔다(1200x628 이 0.04% 초과로 여백이 붙던 것) |
| 0921 | 소재 라이브러리 픽커 누락 → 보강 | 설계서 §4-2 에 있었는데 구현에서 빠졌다. **`asset_id` 가 AI 표시 자동 부착의 유일한 근거**라 그냥 빠지면 안 되는 자리였다 |
| 0923 | **1차-B 코드 완료** — 영상(릴스·Threads·페북·X) · 페이스북 페이지 · X | [1차-B 설계서](2026-09-23-sns-1b-design.md). 뒤집힌 판단 = X 확인 `deferred → immediate`(공식 요금이 건당 $0.005 라 타임라인 대조가 더 비싸다) · 업로드는 nginx 를 바꾸지 않고 4MB 조각 · 발견 결함 3(D1 처리 5분 넘으면 영구 올리는 중 · D2 캐러셀이 마지막 사진 N장 · D3 컨테이너 무한 재생성 = B-0923-5) |
| 0925 | **사진 초안 A안 + 올린 기록 카드 그리드** | 배포 뒤 Harold 확인. 사진 초안이 포스터 문구 없이 장면만 묘사 → 사진 속 글을 먼저 읽어 면허로 쓴다(설계서 §12). 올린 기록은 카드 5 × 2 · 페이지 · 상세 창. 뒤집힌 판단 = '사진만으로는 사실을 알 수 없다'(0924) → 우리 고객 사진은 대부분 문구가 든 포스터다 |
| 0924 | **올릴 글 재설계 + 가장 편리한 채널 관리 + 안전 결함 12건** | [0924 설계서](2026-09-24-sns-channel-design.md). 브레인스토밍 2회 · Harold Q1 가(끊기면 예약은 기다림) · Q2 가(사진만 있으면 AI 첫 글) · Q3 나(자주 쓰는 태그 모두 켜고 시작). 뒤집힌 판단 = 인스타 태그 30 → 5(Meta @creators) · Threads 30 → 1 · AI 표시가 직접 올려 둔 소재에는 더 붙지 않음 · 끊김 때 예약을 미리 닫던 것 제거 · 칩을 글에 넣지 않고 꼬리로 보여 준다 |
| 0923 | 작성 구역 썸네일 깨짐 수정(B-0923-4) | 미리보기 주소가 인증 뒤인데 `<img src>` 로 불러 401. 주소는 인증 그대로 두고 화면이 헤더를 실어 blob 으로 받는다. **화면 미리보기용 인증 주소는 `<img src>` 에 그대로 넣지 않는다** |
