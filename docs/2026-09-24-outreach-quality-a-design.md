# AI 영업 산출물 품질 A: 홈페이지만으로 만든 결과물의 결함 6종 (2026-09-24)

> 접수: Harold 「홈페이지만 읽어서 해도 퀄리티를 끌어올려야 하는데 영 마음에 들지가 않네」 → 「A부터 설계 확정하고 구현까지」.
> 근거 실물: 톤28 잡 `ea70ffc6` (산출물 `/api/outreach/v/3cbb05142a` · DM `dm-DmvTXF7` · 카탈로그 `dm-Skt1knt`) 원문·이미지 5장 직접 확인 + Harold SQL(선택 행사 원문 · hero_fallback false · block_gate 7/0).
> 상설 = [FEATURE-SALES-OUTREACH.md](FEATURE-SALES-OUTREACH.md). B(네이버 스토어 전용 판독기)는 별도 설계.

## 1. 축 대조표 (결함 → 원인 확인 자리 → 처방)

| # | 결함(실물) | 원인(코드로 확인) | 처방 |
|---|---|---|---|
| A1 | 포스터 헤드라인 = "톤28" 뿐 | ① 첫 행사 문구가 숫자로 시작("2주동안…") → 숫자 앞 절단이 제목을 통째로 비움 ② 업체명 "톤28" 자체에 숫자 → 업체명이 든 제목·상품명이 전부 숫자 게이트(`posterTextOk`)에 걸림 ③ 첫 선택 행사 1건만 본다(`eventQuote: selected?.quote`) | 게이트는 **업체명을 가린 뒤** 숫자를 본다(`posterTextOkFor`) · 선택 행사 전부를 순서대로(쪼갠 제목 → 원문) 시도 · 상품명 부제는 용량·수량 낱말(숫자로 시작하는 토큰)을 떼고 판정 |
| A2 | 이벤트 카드: 제목이 말 중간에 잘림 · 혜택·기간 없음 · 버튼 "상품 자세히 보기" · 링크 홈 | 홈페이지 인용문 후보(origin crawl)는 **인용문 전체를 제목 칸 하나**에 넣고(`eventCardsOf`) 표준 조립이 `headlineFromCard` 18자에서 자름 · 기간 null · 본문 = 기간 줄뿐 | 분석 AI(기존 호출 1회)가 인용문과 함께 **제목·혜택·기간 세 조각**(원문 그대로)을 낸다 → 서버가 각 조각이 인용문의 부분 문자열인지 대조(불일치 = 조각 버림 · 인용문은 유지) → 카드 제목 = 조각 제목 · 기간 = 조각 기간 · 본문(`text`) = 혜택(면허 있을 때만 · 숫자 차단기 없는 preset 경로라 면허 없는 혜택은 싣지 않는다) · 행사 카드 제목 = 괄호 설명을 뺀 뒤 30자 낱말 경계(포스터·제목 계열은 18 그대로) · 슬라이스 없는 행사 카드의 버튼 대체 문구 = "행사 자세히 보기"(슬라이스 카드는 종전 "상품 자세히 보기") |
| A3 | 받는 사람 브라우저 탭 "[영업] 톤28" · 카톡 미리보기 "[영업 카탈로그] 톤28" | DM·카탈로그 `title` 접두(`produce.ts publishOutreachDm` · `catalog.ts publishOutreachCatalogDm`). 이 접두는 학습 후보 제외 표식이기도 하다(`sales-outreach-examples.ts` · `reference-skeleton-promote.ts`) | 제목 = 업체명 / 업체명 + " 카탈로그". 학습 제외는 **원장 연결**로 판정(`sales_outreach_assets` payload `dmId`·`catalogDmId`) + 옛 접두 판정 유지. 공용 DM 뷰어는 손대지 않는다 |
| A4 | 메일에 같은 행사 제목이 두 번 | 0923 메일 재구성의 "이번 시안에 담은 소식" 요약이 시안(표준 조립 행사 카드)과 겹친다 | 시안에 행사 카드(`so-std-event*`)가 있으면 요약 카드를 뺀다(평문은 시안이 없으니 유지) · 요약·제목은 쪼갠 제목·기간을 쓴다 |
| A5 | 포스터 배경에 읽어 온 상품에 없는 색조 팔레트·메이크업 붓 | 배경 지시(`posterStyleHint`)가 병·튜브·상자·포장만 금지하고 "minimal props" 허용 | 소품 금지로 바꾸고 화장품·미용 도구·다른 상품 전반을 명시 금지(돌·천·식물·물 같은 중립 재료만). 이미지 모델 결과라 효과는 다음 실측으로 확인 |
| A6 | 카탈로그 상품 쪽: 상품이 위쪽에 작게 · 아래 절반 빔 · 상품명 없음 · 글자 없는 풍경 사진 쪽 | ① 캡션 게이트가 숫자 0 → 용량 표기("50g")가 든 상품명이 거의 전부 탈락 ② 원본의 흰 여백까지 contain → 상품이 작다 ③ 슬라이스 선별이 분위기 사진(photo·other)도 싣는다 | ① 캡션 = 괄호·대괄호 제거 + 숫자로 시작하는 낱말(용량·수량·판번호) 제거 + 업체명 가린 게이트 ② 합성 전 균일 바탕 여백 잘라내기(sharp trim) ③ 카탈로그만 판정이 banner·product 인 슬라이스로 거른다(DM 행사 블록 선별 함수는 그대로) |

## 2. 지키는 선

- 새 문구를 지어내지 않는다: 세 조각은 인용문의 부분 문자열만(대조 실패 = 버림). 혜택 수치는 면허(미래 종료일) 있을 때만 본문에 싣는다.
- 이미지 안 글자의 혜택 게이트(할인·%·원·쿠폰 등)는 그대로. 숫자 완화는 업체명·용량 표기만.
- DDL 0 · 새 jsonb 키 = `event_quote.candidates[].parts {title, benefit, period}`(옛 후보에는 없다 = 종전 동작).
- 공용 CT: `headlineFromCard` 는 최대 길이 인자만 추가(기본 18 = 고객 입구 호출 3곳 무변경). DM 뷰어·`selectEventSlices` 무변경.

## 3. 영향표

| 바뀌는 것 | 소비처 | 영향 |
|---|---|---|
| `filterQuoteCandidates` 가 `parts` 를 싣는다 | 분석 단계 1곳 · 확인 화면(quote 표시 = 무변경) · `eventCardsOf` | 옛 후보 무변경 |
| `eventCardsOf`(crawl) 제목·기간·본문 | 제작(표준 조립 DM·메일 · 카탈로그 계획 · AI 골격 경로 `text` 본문) | 조각 없으면 종전 |
| `buildOutreachPosterTexts` 여러 출처 | 포스터 1곳 | `eventQuote` 단수 입력 유지(테스트 호환) |
| DM·카탈로그 제목 | 뷰어 `<title>`·og · 학습 후보 2곳 | 제외 판정을 원장 연결로 보강 |
| 메일 요약 카드 생략 | 메일 html | 평문 무변경 |
| 카탈로그 캡션·여백·슬라이스 | 카탈로그 1곳 | 계약 테스트 갱신(`세럼 50ml` → `세럼`) |

## 4. 추가 과제(이번 범위 밖 · 기록만)

- 공용 `cutAtWord`(produce.ts · 고객 입구 `campaign-customer-fill.ts` 도 사용)는 낱말이 한도에 딱 맞게 끝나도 그 낱말을 버린다("추석 맞이 톤28 윷놀이 행운의"를 13자로 자르면 "추석 맞이 톤28"). 행사 버튼 이름이 짧아져 60% 규칙에 걸리는 원인. 공용 CT 라 이번에는 호출부(행사형 대체 문구)에서 막았다.

## 5. 실측(배포 뒤 · Harold)

톤28 새 잡 1건(홈페이지만) → ① 포스터 헤드라인이 업체명이 아닌 행사명 ② DM 행사 카드에 온전한 제목 + 기간(+ 면허 있으면 혜택) ③ 탭 제목 "톤28" ④ 메일 행사 제목 1번 ⑤ 카탈로그 상품 쪽 상품명 캡션·상품 크기 ⑥ 포스터 배경 소품. 옛 잡은 다시 만들기 전까지 종전 산출물 그대로다(저장 시점 고정).
