# 여정(Journey) 재설계 설계서 — 2026-08-01

> **호출어: "여정 재설계 이어가자"**
> 이 문서가 여정 트랙의 SoT다. 상태·잔여는 STATUS §2 카드가 소유하고, 여기엔 **무엇이어야 하는가**와 **왜 그런가**를 담는다.
> 근거는 전부 코드 정독으로 확정한 것이다. **운영 DB 조회는 아직 하지 않았다** — SQL이 필요한 항목은 §10에 따로 모았다.

---

## §0 이 재설계가 나온 경위

2026-08-01 Harold 지시로 여정 트리거 전수 점검을 했다. 두 층이 나왔다.

**아래층(기계 결함) 13건** — 발화 불능·중복 과금·이벤트 유실. Codex 적대검증으로 전건 확인. §9에 목록.

**위층(구조 결함)** — Harold 지적으로 드러났다. 점검을 "기계가 고장났나"로만 봤는데, 실제 물음은 "이 도구로 마케팅이 되는가"였다. 확인 결과 네 가지가 성립하지 않는다.

1. **첫 구매와 재구매를 구분할 수 없다.** 트리거가 "구매했다"만 보고 몇 번째인지 안 본다. 두 번째 구매에도 "처음 구매해주셔서 감사합니다"가 나간다.
2. **여정이 무엇으로 끝나는지 정의가 없다.** 종료 신호는 "구매하면 끝" 옵션 하나뿐이고 기본이 꺼짐이다. 구매로 시작한 여정에 그걸 켜면 자기 진입 사유로 즉시 끝나서, 코드가 "진입일 다음 날부터만 인정"으로 우회해 뒀다(journey-executor.ts `isGoalConvertedSinceEntry`).
3. **연동을 했는데도 트리거가 안 돈다.** 싱크에이전트로 구매를 정상 적재하는 회사인데 구매 여정이 안 돈다(§2-2).
4. **만들고 나면 못 바꾼다.** 트리거 종류를 바꾸는 API가 없고, 저장 후 스텝을 늘리지도 줄이지도 못한다. 고객사가 200크레딧을 내고 사는 것이 사실상 "한 번 정하면 못 바꾸는 문자 예약"에 가깝다.

이 문서는 위층을 다시 세운다. 아래층 13건은 위층 결정에 따라 상당수가 자연 소멸하므로 §9에 보류로 둔다.

---

## §1 제품 정의 — 우리는 무엇인가

**고객사의 데이터를 받아 여정을 설계해 주는 AI 파트너다.** 데이터 없이 스스로 도는 여정은 없다. Braze도 같다.

여기서 두 가지가 따라 나온다.

- **데이터가 없으면 만들 수 없다고 말해야 한다.** 지금은 만들어지고 켜지고 0건으로 돈다. 그게 제일 나쁜 형태다 — 고객사는 켜 뒀다고 믿고, 우리는 아무것도 안 보낸다.
- **데이터가 풍부할수록 더 좋은 여정을 설계해 준다.** 이게 우리가 파는 것이다. 트리거 목록을 늘리는 게 아니라, 받은 데이터로 무엇을 할 수 있는지 알려 주는 쪽이 제품이다.

---

## §2 전제 — 데이터 게이트

### §2-1 연동 두 경로

| 경로 | 무엇이 들어오나 | 어디에 쌓이나 |
|---|---|---|
| 싱크에이전트 (ERP·POS) | 고객, 구매이력, (신설) 예약 | `customers`, `purchases`, (신설) `reservations` |
| 자사몰 연동 (웹훅·SDK) | 주문, 행동(조회·장바구니·결제) | `cdp_events` + `customers` 증분 |

### §2-2 지금의 비대칭 — 이 재설계의 핵심 원인

확정 사실(grep):

- `cdp_events`에 쓰는 코드는 **두 곳뿐**이다 — `cdp-events.ts:183`(`trackEvent`), `:481`(`ingestBrowserEvents`).
- 자사몰 주문(`cdp-orders.ts syncOrder`)은 `trackEvent`로 `purchase` 이벤트를 남기고 `customers` 요약도 증분한다. **양쪽 다 쓴다.**
- 싱크에이전트(`routes/sync.ts`)는 `purchases` 원장에 넣고 `updateCustomerPurchaseAggregates`로 `customers` 요약만 재계산한다. **`cdp_events`를 건드리는 코드가 한 줄도 없다**(`sync.ts` 내 `trackEvent`·`cdp_events` grep 0건).

그런데 트리거 8종이 보는 곳이 갈린다.

- 구매·예약·배송·장바구니 → `cdp_events`만 본다.
- 신규가입·휴면·생일·포인트 → `customers`만 본다.

**결과: 싱크에이전트로 구매를 정상 적재하는 회사는 구매 여정을 못 쓴다.** 데이터가 없어서가 아니다. DB에 구매가 들어와 있고 화면에 총구매액도 보이는데 여정만 못 본다.

Braze와의 차이도 여기다. Braze는 데이터가 들어오는 문이 하나고 그 이벤트가 곧 트리거 원천이다. 우리는 문이 둘인데 트리거는 한쪽만 본다.

**결정: 어느 문으로 들어오든 같은 트리거가 물린다.** 트리거는 원천(`cdp_events` / `purchases` / `customers`)이 아니라 **의미**로 정의한다.

### §2-3 게이트 판정 — 우리는 정답표를 갖지 않는다

**이 절이 이 설계의 중심이다.** 우리는 도구를 제공하고, 무엇이 가능한지는 고객사가 준 데이터가 정한다.

고객사마다 여는 범위가 다르다. 전부 주는 곳도 있고 이름·전화번호만 주는 곳도 있다. 그러니 **"이 트리거는 이 필드 여섯 개를 요구한다"고 우리가 못 박을 수 없다.** 그 회사가 준 것 안에서 무엇을 비교할 수 있는지를 **런타임에 찾아내는 구조**여야 한다.

원칙은 §1과 같다 — 데이터가 없으면 못 만든다고 말한다. 다르게 적용될 뿐이다.

- 트리거를 **열 수 있는가**도 회사별 계산이다.
- 신규인지 기존인지 **판정할 수 있는가**도 회사별 계산이다.

두 번째를 빼먹으면 "만들 수는 있는데 엉뚱한 사람에게 나가는" 상태가 된다(§3-0-2).

**고객사에 새 필드를 요구하지 않는다.** 주면 더 정확해지고, 안 주면 남은 것으로 판정하고, 아무것도 없으면 못 한다고 말한다. 그 판정 결과를 화면이 그대로 보여준다 — 이 회사에서 이 여정이 되는가, 무엇을 근거로 하는가, 안 되면 무엇을 더 주면 되는가.

자산: `utils/company-data-profile.ts`가 회사별 `customers` 컬럼 충족률을 이미 계산한다(1시간 캐시). 지금은 AI 문안 변수 선정에만 쓴다. **여기에 `purchases`·`cdp_events`·(신설)`reservations` 보유 여부를 붙여 판정 근거 계산기로 확장한다.** 신규 CT를 만드는 게 아니라 소비처를 늘린다.

판정은 "컬럼이 있나"가 아니라 **"그 판정을 할 만큼 있나"**로 한다. 예: 구매 주기 이탈은 그 고객 구매가 3건 이상이어야 성립한다.

---

## §3 트리거 정의 15종 (확정)

정의는 넷을 모두 채워야 트리거로 인정한다 — **진입 조건 / 종료 신호 / 재진입 규칙 / 필요 데이터**. 넷 중 하나라도 못 채우면 열지 않는다.

### §3-0 두 갈래로 나눈다

지금 8종은 성격이 섞여 있어 정의가 안 잡힌다.

- **상태 전이형** — "이전에 A였는데 지금 B가 됐다". 이전 상태를 알아야 판정된다.
- **사건 발생형** — "방금 이 일이 일어났다". 지금 일어난 일만 보면 된다.

현재 코드는 전이형을 "현재 상태가 B다"로만 판정한다. 그래서 첫 구매를 못 잡는다(구매한 건 알아도 첫 번째인지 모른다).

**해법: 진입 원장(`journey_entry_ledger`)을 일반화한다.** 지금 신규가입이 쓰는 "이 식별자를 전에 본 적 있나"가 이미 전이 판정 구조다. 여기에 "그때 상태값이 무엇이었나"를 얹으면 나머지 전이형이 같은 방식으로 잡힌다.

### §3-0-2 "우리가 처음 본 사람"과 "신규 고객"은 다른 말이다

현재 코드는 앞의 것을 뒤의 것으로 쓴다. 둘은 **이관이 끼는 순간 갈라진다.** 그리고 이관은 예외가 아니라 모든 고객사의 첫 단계다.

같은 원인이 트리거마다 다른 얼굴로 나온다. 신규가입에서는 10년 단골이 신규가 되고, 첫 구매에서는 3년 전 구매가 오늘 첫 구매가 되고, 휴면 복귀에서는 과거 거래가 오늘 복귀로 보인다.

**판정 방식: 우리가 신호를 정하지 않는다. 그 회사가 준 것 중 "전에도 고객이었다"를 말해주는 값을 찾아 쓴다.**

찾을 수 있는 후보(요구사항 아님 — 있으면 쓰고 없으면 다음으로 내려간다):

| 우선 | 근거 | 의미 |
|---|---|---|
| 1 | 고객사가 주는 가입일 | 있으면 가장 정확. 표준 필드에 없으므로 **주는 회사만** 해당 |
| 2 | 구매 흔적 | 구매횟수·최근구매일·총구매액·마지막구매일 / 구매 원장 행 / 과거 구매 이벤트 |
| 3 | 거래의 결과값 | 포인트 보유 · 등급이 기본등급이 아님(기본등급은 회사 설정을 받은 곳만) |
| 4 | 없음 | **판정 불가.** 신규가입 계열 여정을 잠그고 사유를 보여준다 |

4번을 "명단에 없으니 신규"로 처리하면 10년 단골에게 환영 문자가 나간다. **판정 불가는 정직하게 불가라고 말한다.**

**식별은 사람 단위로 한다.** 지금 신규 판정 키가 회사 + **매장코드** + 전화번호다. 싱크로 들어온 사람은 매장코드가 있고 자사몰로 들어온 사람은 없어, 같은 전화번호인데 다른 사람으로 본다. 싱크를 쓰던 회사가 자사몰을 붙이는 순간 기존 고객 전원이 신규가 된다. **판정 키에서 매장코드를 뺀다** — 회사 + 정규화 전화번호, 이메일도 함께 본다. (고객 행 자체의 병합은 별건.)

### §3-0-3 관측 시각과 발생 시각을 구분한다

이관은 **우리가 알게 된 시각만 오늘이고 실제 일어난 시각은 과거다.** 자사몰을 쓰다 싱크를 나중에 붙이면 3년 전 구매가 오늘 들어온다.

**모든 전이형 트리거는 두 시각을 함께 본다. 발생 시각이 유예 기간보다 오래된 전이는 트리거로 인정하지 않는다.** 이 원칙 하나가 전이형 아홉 개(§3-1)를 전부 덮는다. 첫 구매는 구매일이 오래되면 트리거가 아니고, 휴면 복귀·등급 변동·구매 주기 이탈도 같다.

### §3-0-4 표의 "필요 데이터" 열 읽는 법

§3-1~§3-3 표의 "필요 데이터"는 **고객사에 요구하는 조건이 아니다.** 그 트리거를 판정하려면 무엇이 있어야 하는지를 적은 것이고, 실제 판정은 §2-3의 회사별 계산이 한다. 같은 트리거라도 회사에 따라 근거가 달라지고, 근거가 없으면 잠긴다.

### §3-1 상태 전이형 (양쪽 연동 공통 — `customers`·`purchases` 기반)

| # | 트리거 | 진입 | 종료 신호 | 재진입 | 필요 데이터 | 현재 |
|---|---|---|---|---|---|---|
| 1 | 신규 고객 | 처음 보는 식별자가 명단에 나타남 | 첫 구매 또는 스텝 소진 | 없음(1회) | 고객 명단 | 있음 |
| 2 | 첫 구매 | 구매 0건 → 1건 | **두 번째 구매** | 없음(1회) | 구매이력 | **없음** |
| 3 | 재구매 | 구매 N건 → N+1건 (N≥1) | 다음 구매 | 매 구매 | 구매이력 | 부분(구분 없음) |
| 4 | 휴면 진입 | 마지막 구매 후 N일 경과 | 구매 발생 | 쿨다운 | 구매이력 | 있음 |
| 5 | 휴면 복귀 | 휴면 상태에서 구매 | 스텝 소진 | 매 복귀 | 구매이력 | **없음** |
| 6 | 구매 주기 이탈 | 그 고객 평균 주기 × 계수 초과 | 구매 발생 | 쿨다운 | 구매 3건 이상 | **없음** |
| 7 | 등급 **상승** | 등급이 위로 바뀜(서열 확인된 회사만) | 스텝 소진 | 쿨다운 | 등급 필드 + **등급 서열** | 있음(방향 미구분 — 재작성 대상) |
| 8 | 생일 D-N | 생일 N일 전 | 스텝 소진 | 연 1회 | 생년월일 | 있음 |
| 9 | 포인트 소멸 D-N | 소멸 N일 전 + 임계 이상 | 포인트 사용 | 쿨다운 | 포인트 + 소멸 정책 | 있음 |

**#6 구매 주기 이탈은 Braze에 없는 축이다.** 고객마다 다른 리듬을 보는 트리거라 차별점이 된다. 표본 3건 미만이면 성립하지 않으므로 게이트에서 잠근다.

**#9 포인트 임계는 서버가 양수를 강제한다.** 지금 기본값 0이 사실상 전원이다(§9-J).

**#7은 상승만 발화한다 (2026-08-02 Harold 확정).** 하락 통지는 마케팅이 아니다 — 보내는 순간 고객사 브랜드가 손해를 본다.
그런데 현재 추출은 `l.state_value IS DISTINCT FROM c.grade`(`journey-target-extractor.ts:287`)라 **방향을 못 가린다** — 지금 켜면 등급이 떨어진 고객에게 축하 문안이 나간다(실사용 여정 0건이라 미발화).
상승을 판정하려면 **등급 서열**이 필요한데 우리는 그 표를 갖지 않는다 — `customers.grade`는 고객사 원문 보존이라 회사마다 값이 다르다(실버·골드·VIP / 우수·프리미엄 / A·B·C). 우리가 서열을 지어내면 정답표를 갖는 것이고, 구매액으로 추정하면 추정 금지를 어긴다.
**결정 3:**
- 회사 설정에서 그 회사 등급 목록을 뽑아 **AI가 서열 초안을 내고 사람이 한 번 확인**한다(입력이 아니라 순서 확인 1회 — 여정 만들 때마다 묻지 않는다).
- 확인 전에는 트리거를 잠근다. 사유는 화면에 그대로 — "등급 순서를 한 번 정해 주시면 열립니다."
- 하락 후 복귀가 반복되면 축하가 매번 나가므로 **최소 쿨다운을 함께 건다**(현재 #7에 없음).

⛔ 하락도 **원장 state 갱신은 계속한다**. 갱신을 건너뛰면 다음 상승 판정의 기준이 옛 등급으로 남는다. 발화만 막고 기억은 남긴다.

### §3-2 사건 발생형 (행동 데이터 필요 — 자사몰·SDK)

| # | 트리거 | 진입 | 종료 신호 | 재진입 | 필요 데이터 | 현재 |
|---|---|---|---|---|---|---|
| 10 | 장바구니 이탈 | 담고 N시간 결제 없음 | 결제 발생 | **쿨다운 필수** | 행동기록 | 있음(결함 §9-N1) |
| 11 | 배송 상태 | 발송·완료 통지 | 스텝 소진 | 매 건 | 주문 상태 연동 | 자사몰만 |
| 12 | 상품 조회 후 미구매 | 조회 후 N일 미구매 | 구매 발생 | 쿨다운 | 행동기록 | **없음** |

### §3-3 예약 3종 — **여정에서 제외 (2026-08-02 Harold 확정). 정보 알림 축으로 이관**

> **여정은 마케팅 자동화다.** 예약 리마인드는 그 자체가 목적이고 뒤에 마케팅 시퀀스가 붙지 않는다.
> 그 축은 이미 따로 있다 — 화면의 `마케팅 여정` / `정보 알림` 두 축과 `InfoAlertJourneyBuilder`(`specs/2026-06-22`·`2026-06-29`). 예약 3종은 그쪽 몫이다.
> **가르는 기준은 "트리거가 정보성이냐"가 아니라 "그 뒤에 마케팅 시퀀스가 붙느냐"다.** 그래서 #11 배송은 정보성 사건이지만 여정에 남는다(뒤에 후기 요청·재구매 유도가 붙는다).
> 아래 표와 §12는 **설계 자산으로 보존**한다 — 정보 알림 축에서 착수할 때 그대로 쓴다. 여정 트리거 집합에서는 빠진다.
> 코드는 이미 잠겨 있다(`RESERVATION_REASON` · 레지스트리 `implemented: false` 2종) — 지금 아무것도 안 해도 사고가 없다.

| # | 트리거 | 진입 | 종료 신호 | 재진입 | 필요 데이터 |
|---|---|---|---|---|---|
| 13 | 예약 접수 | 예약이 확정 상태로 들어옴 | 스텝 소진 | 매 예약 | 예약 원장 |
| 14 | 방문 D-N | **예약일 기준** N일·N시간 전 | 예약 취소·완료 | 예약 건마다 | 예약 원장 + 예약일시 |
| 15 | 방문 완료 후 N일 | 예약이 완료로 바뀐 뒤 N일 | 스텝 소진 | 매 방문 | 예약 원장 + 상태 |

노쇼(예약일 경과·완료 미표시)는 후보로 두되, 고객사가 노쇼를 실제로 찍는지 확인 후 연다(§10).

### §3-4 회사 지정형 (기존 유지)

- 지정일 D-N (`date_anchor`) — 회사가 정한 하나의 날짜.
- 1회 발송 (`one_shot`).

### §3-5 뺀 것

`custom_order_shipped`는 #11로 흡수한다. 자유 문자열 트리거(`custom`)는 §5-4 게이트 뒤로 넣는다.

---

## §4 시간축 3종

지금은 축이 하나뿐이다 — "사건 시점 + N시간". 그래서 예약이 깨진다.

### §4-1 사건 상대축 (기존)

진입 시점부터 N시간. 구매·가입처럼 **이미 일어난 일**에 쓴다.

### §4-2 회사 지정 절대축 (기존 `date_anchor`)

회사가 정한 날짜 기준 D-N. 전 고객 공통.

### §4-3 고객별 날짜축 (신설 — 이 설계의 핵심 추가)

**고객마다 다른 미래 날짜**를 기준으로 D-N. 예약일, 렌탈·구독 만료일, 보증기간 종료, 시술 후 관리 주기, 차량 정비 주기, 재방문 예정일이 전부 같은 모양이다. 병원·미용·정비·숙박·학원이 여기 걸린다.

**지금 예약 템플릿이 깨진 이유가 이 축이 없어서다.** `journey-builder.ts` 예약 템플릿은 1스텝 0시간("3일 후 예약 안내"), 2스텝 72시간("오늘 예약 시간"), 3스텝 96시간("어제 방문 감사")으로 **진입 시점부터** 센다. 예약일을 참조하지 않는다. 2주 뒤 예약자에게 3일 뒤 "오늘 예약이십니다"가 나간다. 이벤트 이름이 막혀 있어(§9-C1) 실사고는 없었다.

**배관은 절반 이상 있다.** `journey_anchor_dispatch`의 키가 (여정, 스텝, 고객, 발송일)이라 고객마다 발송일이 달라도 중복 방지가 이미 성립한다. 지금은 기준 날짜만 `journeys.anchor_date`(여정 단위)에 있어 전 고객이 같은 날일 뿐이다. **그 날짜를 고객 쪽에서 읽어오게 바꾸는 것이 변경의 본체다.**

---

## §5 진입·종료·재진입 규칙

### §5-1 종료를 트리거와 따로 정의한다

지금은 종료가 "구매하면 끝" 옵션 하나다. 앞으로는 **트리거마다 종료 신호를 갖는다**(§3 표의 종료 신호 열). 구매로 시작한 여정이 구매로 끝나는 모순이 사라진다.

- 첫 구매 여정은 **두 번째 구매**로 끝난다.
- 방문 D-N 여정은 **예약 취소 또는 완료**로 끝난다.
- 휴면 여정은 **구매 발생**으로 끝난다.

### §5-2 고객별 중단·재계산 (신설)

예약은 취소·변경이 흔하다. 지금 여정이 멈추는 길은 넷뿐이다 — 수신거부, 목표 달성, 조건 미충족, 관리자 정지. **"그 예약이 취소됐다"로 그 고객만 끊는 길이 없다.**

없으면 취소한 고객에게 "내일 뵙겠습니다"가 나간다. 날짜가 바뀌면 D-N을 다시 잡아야 한다.

**결정: "고객별 중단·재계산" 축을 신설한다.** 예약 때문에 만들지만 구독 해지·환불·등급 강등에 그대로 쓴다.

### §5-3 여정 간 관계

여정끼리 자동으로 밀어내지 않는다. 각 여정이 자기 종료 신호로 끝나면, 다음 여정은 자기 진입 조건으로 알아서 시작된다. 신규가입 여정이 첫 구매로 끝나고 첫 구매 여정이 시작되는 흐름이 §5-1만으로 성립한다.

동시 진행은 막지 않는다(신규가입 중 생일이 오면 둘 다 나간다). 과다 발송은 이미 있는 발송 피로도 게이트(`fatigue-guard`)가 담당한다.

### §5-4 트리거 화이트리스트

`trigger_event`를 §3의 15종으로 서버가 강제한다. 생성·활성화 양쪽에서 검증하고 DB CHECK를 건다. 지금은 검증이 없어 오타 하나로 영구 0건 여정이 만들어진다(§9-C2).

### §5-5 이관 구간 보류 게이트 (여정·자동마케팅 공용)

§3-0-2의 사람 단위 판정만으로는 부족하다. 기존 고객 5만 명이 한꺼번에 들어오면 신규가입만 터지는 게 아니다 — 휴면은 최근구매일이 오래된 사람 전원을, 생일은 오늘이 D-N인 사람 전원을, 포인트는 임계 이상 전원을 잡는다. **자동마케팅도 같은 고객 풀을 본다.**

트리거마다 조건을 다는 방식으로는 안 막힌다. 유입 자체를 봐야 한다.

- 짧은 시간에 고객 수가 급증하면 신규 유입이 아니라 **데이터 이관**이다.
- 연동이 처음 붙는 시점의 전량 적재는 **전부 이관**이다.

이 구간에서는 **진입을 자동 보류**하고 담당자에게 알린다. 지금의 임계값처럼 여정을 정지시키는 게 아니다 — 보류를 해제하면 그 시점부터 정상 진입하고, 밀린 분은 보내지 않는다. 연동 시작·대량 적재가 감지되면 신규 판정 기준선도 자동으로 다시 찍는다.

정지가 아니라 보류인 이유: 정지는 사람이 다시 켜야 하고, 그 사이 진짜 신규 고객도 함께 잃는다.

---

## §6 화면 흐름 재설계

### §6-1 진입 — 자연어 한 칸

원하는 타겟과 트리거를 자연어로 입력한다. AI가 읽고 **추천 여정을 모달로** 보여준다.

모달이 담는 것:
- 어떤 트리거를 왜 골랐는지 **근거**
- 이 여정의 **목적**
- 몇 스텝을 왜 그렇게 잡았는지
- 이 회사 데이터로 **가능한지 여부**(§2-3 게이트) — 불가면 무엇을 연동해야 하는지

### §6-2 스텝 1 — 한 화면에서 완결

모달의 "다음"이 스텝 1 화면으로 넘어간다. 한 화면에서 스텝 하나를 완전히 끝낸다.

- 트리거가 발생했을 때 보낼 **문안 추천**
- 변수 활용 + **AI 꾸미기 / AI 다듬기 / 스팸필터 테스트** 버튼 (오퍼레이터 화면과 같은 정렬·품질)
- 트리거 발생 후 **언제 보낼지** (즉시 / N시간 후 / N일 후 특정 시각)
- **야간 안내**: 광고성 메시지는 21시~08시 KST 발송 금지다(SEND_HOURS). 걸리는 설정이면 "오전 9시에 발송됩니다"를 화면에서 알린다.
  ⛔ **정정(2026-08-02 Codex 1R)** — 앞선 서술의 "8시"는 틀렸다. 금지 창의 끝(08시)과 밀려난 발송이 **도착하는 시각**은 다른 값이고, `send-time-util`의 ARRIVE_HOUR는 **9**다.

### §6-3 스텝 추가 — 화면 전환

우측 **[스텝 추가]** 버튼 → 화면이 스텝 2로 전환된다. 지금이 스텝 2임을 명확히 보여준다.

- **AI 문안생성** / **직접 입력** 선택
- 앞 스텝으로부터 얼마 뒤 보낼지 (일·시간 단위)

스텝을 추가할 때마다 화면을 전환한다.

### §6-4 AI 문안생성이 읽어야 하는 것

**지금 AI는 앞 스텝을 모른다.** 다듬기가 서버로 보내는 건 현재 본문·채널·광고 여부·의도 넷뿐이다(`routes/ai.ts` `journeys-refine-step`). 그리고 본문 10자 미만이면 버튼이 거부하므로 **빈 스텝에서는 못 누른다**(`JourneysPage.tsx` `handleRefineOpen`). 새 스텝은 자리표시만 들어가고 사람이 먼저 열 글자를 써야 AI를 부를 수 있다.

앞으로 문안생성이 받는 것:
- 이 여정의 **트리거와 목적**
- **앞 스텝들의 확정 문안 전부**
- 지금이 **몇 번째 스텝**이고 트리거로부터 **얼마 뒤**인지
- 채널·광고 여부·회사 브랜드보이스

**앞 스텝과 겹치지 않게**가 요구사항이다. 지금 구조로는 원리적으로 불가능하다.

> **설계 메모**: 화면을 스텝별로 전환하는 §6-3이 이 문제를 구조로 푼다. 앞 스텝이 이미 확정된 상태로 뒤에 있고 현재 스텝의 순번·지연이 화면에 있으므로, AI에 넘길 맥락이 그대로 갖춰진다. 프롬프트에 컨텍스트를 더 밀어 넣는 방식은 땜질이다.

### §6-5 저장 — 브리핑 모달

저장을 누르면 스텝 전체를 일목요연하게 보여주는 브리핑 모달이 뜬다. 스텝을 클릭하면 그 스텝의 상세 문안을 펼쳐 본다.

### §6-6 저장 후 편집 (필수 선행)

이 흐름은 스텝을 늘려 가며 쓰는 것이 전제인데 **지금은 저장 후 스텝을 못 늘린다.**

- `addStep`·`deleteStep`은 저장 전 패키지 상태만 고친다(`JourneysPage.tsx`). 최대 7개.
- 백엔드에 스텝을 새로 넣는 코드는 여정 생성 함수 한 곳뿐이고, **스텝을 지우는 코드는 아예 없다**(`DELETE FROM journey_steps` grep 0건).

**저장 후 스텝 추가·삭제 API가 이 재설계의 선행 조건이다.**

주의: 스텝 순번에 구멍이 생기면 여정이 통째로 죽는다. 실행기는 `current_step_order + 1`을 찾고, 진입은 `step_order = 1`을 찾는다. 삭제 후 반드시 재번호를 매긴다.

### §6-7 디자인 기준

신규 화면이므로 Journey Builder 동급 품질 의무(CLAUDE.md `design_quality_minimum_journey_level`). 착수 직전 `status/lessons/LESSONS_FRONTEND.md` "디자인 최소 기준" 절 정독. native dialog 금지, 다크 테마, 모바일 대응.

---

## §7 크레딧 (현행 확인 — 변경 없음)

| 시점 | 크레딧 | 비고 |
|---|---|---|
| 자연어로 여정 설계(추천 모달) | 3 | `journey-ai-generate`. 호출마다 |
| 저장·활성화 | 200 | `journey-activate`. draft→active **최초 1회**(멱등키=journeyId) |
| 일시정지 후 재개 | 0 | paused→active는 과금 없음 |
| 활성 여정 발송일 | 10 | `journey-operation`. 여정당 하루 1회 상한 |

Harold님이 말씀하신 "3크레딧" 구조가 이미 그 값이다.

**스텝을 늘려도 추가 과금은 없다**(200은 최초 활성화 1회). §6-6 설계와 잘 맞는다.

**트리거 종류는 바꾸는 API가 없다** — `trigger_event`를 UPDATE하는 경로가 코드 전체에 없다. 바꾸려면 새 여정이고 200크레딧이 다시 나간다. 트리거를 처음에 제대로 고르게 하는 것(§6-1 추천 모달)이 그래서 중요하다.

타이밍 값(휴면 며칠·생일 D-N·장바구니 몇 시간·포인트 기준)은 일시정지 후 변경 가능하고 추가 과금이 없다.

---

## §8 신규 데이터 — 예약 원장

구매 원장과 같은 모양으로 둔다. 싱크에이전트가 적재한다.

최소 필드 6:
- 고객 식별값(전화번호)
- 예약 일시
- 상태 (확정 / 취소 / 완료 / 노쇼)
- 예약번호
- 매장 또는 담당자
- 서비스명 (문안 변수로 사용)

**주의: 과거 예약 소급 적재 금지.** 옛 예약이 오늘 들어오면 전원에게 문자가 나간다. 적재 시작 시점 이후 신규분만 트리거 대상으로 본다(구매 이벤트 배선 §11-2에도 같은 조건).

---

## §9 아래층 — 기계 결함 13건 (보류)

2026-08-01 전수 점검 + Codex 적대검증에서 확인. **위층 결정에 따라 상당수가 자연 소멸하므로 개별 수정하지 않는다.** 재설계 착수 후 남는 것만 처리한다.

| 코드 | 내용 | 위층과의 관계 |
|---|---|---|
| N1 | 장바구니 여정 반복 발송·반복 과금 (정보 알림 빌더가 쿨다운 0 고정 → 추출 안티조인 소멸 + 24시간 창) | §3-2 #10 쿨다운 필수화로 해소. **다만 지금 열려 있어 SQL 확인 우선**(§10) |
| N2 | 신규가입 활성화가 fail-open (활성화 후 baseline 적재, 실패해도 활성 유지 → 추정 모드 영구) | §3-0 원장 일반화 때 활성화 원자성으로 해소 |
| C6 | 임계값 미설정 시 한 회차 10만 건 진입 (주석은 500 스로틀이라 적혀 있음) | 재설계 무관 — 단독 수정 필요 |
| N5 | 늦은 식별(stitching) 이벤트는 커서 뒤라 영원히 미진입 | **커서 축 전환으로도 안 닫힘(§11-B) — 소급이 `created_at`을 안 바꾼다. 착수 순서 5번(이벤트 단위 소비 원장)** |
| C1 | 예약 이벤트명이 적재 화이트리스트에 없어 발화 불능 | §3-3·§8로 해소 |
| C2 | `trigger_event` 서버 화이트리스트·DB CHECK 부재 → 오타 하나로 영구 0건 | §5-4로 해소 |
| C7 | 날짜축·1회발송이 트리거 전용 필터를 조용히 무시 | §5-4와 함께 |
| N3 | 커서 배치가 고객 단위 중복 제거 → 5분 내 2회 구매가 1건으로 뭉개짐 | §3-1 #3 재구매 정의 시 이벤트 단위로 |
| N4 | 같은 시각 이벤트가 chunk 경계에서 영구 누락(브라우저 배치는 배치 전체가 같은 시각) | 단독 수정 |
| C4 | 자동 재진입 워커가 start_kind·스케줄·홀드아웃 규약 이탈 | §5-1 종료 재정의 때 함께 |
| C3 | 진입 원장 기록 누락 고리(빈 전화번호·기존 execution) → 대량차단 오작동 | §3-0 원장 일반화로 해소 |
| C5 | 배송 커서 초기화 누락 (활성화~첫 tick 5분 유실) | §3-2 #11 재정의 때 |
| N6 | `points_min` 0을 서버가 안 막음 | §3-1 #9로 해소 |

원문·근거는 2026-08-01 세션 기록과 `memory/project_2026_0801_journey_redesign.md`.

---

## §10 미결 — Harold 확인 대기

1. ~~**싱크에이전트 구매 적재 주기.**~~ — **2026-08-01 종결.** 물어볼 값이 아니다 —
   **주기는 고객사가 자기 환경에서 설정하고 우리 권한이 아니다.** DB의 `sync_agents` 값이 현장과 같다는
   보장도 없다. 그래서 설계는 이 값을 **읽지 않고**, 매장 구매를 **하루 모아 다음 날 오전에 내보내는
   정책**으로 주기 의존을 없앴다(§11-C-2).
2. ~~**예약 데이터를 ERP·CRM에 가진 고객사가 어디인지, 취소·노쇼가 구분되는지.**~~ — **2026-08-02 종결. 물어볼 값이 아니었다.**
   묻는 것 자체가 **우리가 정답표를 갖겠다는 뜻**이다(불변 원칙 1). 연동처가 이새 하나이고 그마저 ERP를 교체 중이라 답을 가질 수 없다는 것이 곧 답이다 —
   무엇이 가능한지는 **그 회사가 준 데이터가 런타임에** 정한다. 코드도 이미 그 형태다(`resolveTriggerAvailability`가 facts로 판정, 예약만 회사 무관 하드 잠금이었다).
   그리고 예약 자체가 여정에서 빠졌다(§3-3) — 이 확인은 정보 알림 축에서도 되살리지 않는다.
3. ~~운영 DB 확인~~ — **2026-08-01 완료.** 아래 §10-3 결과.

### §10-3 운영 DB 실측 결과 (2026-08-01)

여정 전체 **19개** — active 1 · paused 3 · ended 2 · draft 13.

- **활성 여정은 `custom`(상시) 1개뿐.** 쿨다운 0이지만 `custom` 트리거는 추출에서 실행행이 하나라도 있으면 무조건 제외하므로 재진입이 구조적으로 안 된다 → **N1 반복 과금은 현재 열려 있지 않다.**
- **`cdp.cart_abandon` 여정 0건.** 예약·배송·포인트 여정도 0건. 쓰인 트리거는 다섯 종(구매 4 · custom 7 · 생일 3 · 휴면 2 · 신규가입 2)뿐.
- **`threshold_recipients_per_step`이 19개 전부 NULL** → §9-C6(회차당 10만)은 전 여정에 열려 있다.
- **`cdp.purchase` 여정 4개가 전부 비활성**(draft 3 · paused 1). 만들어는 보고 못 돌린 상태 — §2-2와 이어지는 그림.

⚠ **이 숫자는 채택률이 아니다.** 유료 요금제 업체가 아직 없어 표본 자체가 없다. "안 쓰인다"로 읽으면 오독이다.

### §10-4 `cdp_events` 스키마 실측 (2026-08-01, `\d cdp_events`)

- `created_at` timestamptz NOT NULL DEFAULT now() — **두 적재 경로 모두 안전하게 채워진다.** 커서 축으로 확정.
- `received_at`도 DEFAULT now()라 INSERT 문에 없어도 채워진다(앞선 "서버 경로는 안 채운다"는 서술은 오류였음).
- 기존 인덱스: `(company_id, event_name, occurred_at DESC)` · `(company_id, created_at DESC)` · `(company_id, occurred_at DESC)` 등.
- **커서를 `created_at`으로 바꾸면 기존 3축 인덱스를 못 탄다 → `(company_id, event_name, created_at)` 신설 필요(DDL).**
- `id`는 uuid PK — 동시각 타이브레이커로 사용.

---

## §11 착수 순서

**우선순위 기준 = "지금 새는 돈"이 아니라 "유료 고객이 들어왔을 때 무엇에 먼저 부딪히는가"다.** 유료 요금제 업체가 아직 없어 지금이 파괴적 변경을 할 수 있는 유일한 창이다(데이터 적음·마이그레이션 쌈·되돌릴 것 없음). 고객사가 붙는 순서는 연동 → 대량 적재 → 여정 생성 → 활성화이고, **대량 적재가 첫 사고 지점**이다.

1. ~~**이관 안전장치**~~ — **2026-08-01 완료** (§11-A).
2. ~~**데이터 게이트 노출**~~ — **2026-08-01 완료** (§11-A).
3. ~~**커서 축 전환**~~ — **2026-08-01 완료** (§11-B). N4는 닫혔고 **N5는 안 닫혔다**(5번으로). DDL은 배포 뒤 §11-B-2.
4. ~~**연동 배선 통일**~~ (§2-2) — **2026-08-01 코드 완료** (§11-C). 배포+DDL 대기.
   행동기록으로 **복사하지 않고** 원장을 커서로 직접 읽는다(적대검증이 뒤집은 판단 = §11-C-0).
   배송 축은 **안 열린다** — 싱크가 받는 것은 `purchases`뿐이고 배송 상태를 올리는 경로가 없다(`purchases` 스키마에 배송 칸 없음). 배송은 자사몰 전용으로 남는다.
5. **트리거 15종 재정의** (§3) — **2026-08-02 §11-D 1차 구현**(레지스트리·화이트리스트·C6·N1·N2·N5·N6 종결 + 신규 트리거 2종 개방). 잔여 = §11-D-4.
6. ~~**고객별 날짜축**(§4-3) + **예약 원장**(§8) + **고객별 중단·재계산**(§5-2)~~ — **2026-08-02 접음.** 예약이 여정에서 빠졌다(§3-3). 설계는 §12에 보존 — 정보 알림 축에서 착수한다.
7. **화면 흐름**(§6) — 저장 후 스텝 편집(§6-6)이 선행. **← 다음 착수**

1·2는 한 몸. 3은 4의 선행. 5는 4가 끝나야 의미가 있다. 7은 5가 확정된 뒤.
6을 접어 7이 바로 온다. **§5-2(고객별 중단·재계산)는 예약과 함께 보류**한다 — 만든 이유가 예약 취소였고, 구독 해지·환불·등급 강등은 아직 그 축을 요구하는 트리거가 없다.

> **순서 판단 기록** — 착수 직전 "지금 피해가 나는 순서"로 재배열했다가 되돌렸다. 유료 고객이 없는 지금은 현재 피해가 우선순위 근거가 못 된다. 활성 여정 1개·장바구니 여정 0건이라는 실측(§10-3)은 "안전하다"가 아니라 **"표본이 없다"**는 뜻이다. 0건은 안전의 증거가 아니다.

---

## §11-A 1·2 구현 결과 (2026-08-01, Codex 적대검증 5R)

**2026-08-01 배포완료.** backend tsc 0 / frontend tsc 0 / vitest 127파일 1,762건 / 프론트 atomic safe-build 성공.
DDL·신규 컬럼 0. 컬럼 타입은 운영 `information_schema` 실측으로 확정(§10-4).

**신규 CT 3** — `journey-identity-signals`(신규/기존 판정) · `journey-intake-grace`(이관 유예) · `journey-trigger-capability`(트리거 가능 여부). 전부 DB import 0.

**닫은 것**
- 사람 단위 식별 — 진입 원장 판정 키에서 매장코드 제거. 자사몰로 먼저 등록된 고객을 싱크가 매장코드와 함께 올리면 새 행이 생겨 10년 단골이 신규가 되던 경로(§3-0-2).
- 회사별 판정 근거 — `customers` EXISTS 전용 조회. **개인화 프로파일 캐시를 재사용하지 않는다**(5분 stale + 반올림된 충족률이 희소 근거를 0으로 떨어뜨려 더 보내는 방향으로 샜다).
- 게이트가 보는 신호 = 술어가 보는 신호. 어긋나면 그 자체가 fail-open이다.
- 같은 회차 중복 진입 — `DISTINCT ON (phone)`, 중복 제거를 LIMIT보다 먼저. 전화번호 없는 행은 후보에서 제외.
- 이관 유예 — 휴면·포인트·상시 3분기. 막는 것은 **이관 직후 즉시 발화 하나뿐**이다(아래 참조).
- 판정 불가 런타임 차단 — 신규가입 여정은 근거가 없으면 워커가 사유와 함께 정지.
- 데이터 게이트 화면 — 정보 알림 빌더 트리거 잠금(3번째 축) + 빠른 시작 칩 잠금. 사유를 그대로 노출.
- **활성화 게이트**(설계에 없던 추가) — 화면 잠금은 자연어 생성과 조회 실패로 우회된다. 활성화가 발송이 시작되는 유일한 길목이라 게이트를 거기 뒀다. 모르는 `trigger_event`도 여기서 거부(§5-4의 활성화 측을 앞당겨 닫음).
- 예약 트리거 — 화면 잠금 + 활성화 거부. 회사 데이터와 무관하게 막는다(§9-C1의 사용자 측).

**설계 정정 3 (적대검증이 뒤집은 것)**
- §5-5 유입 감지 보류 → **유예 + 상한 필수**로 대체. 감지 임계를 우리가 지어내면 정답표가 되고, 5만 고객사에 1만 명 조합처럼 비율로도 절대수로도 안 잡히는 구멍이 남는다.
- **유예는 대량 발화를 없애지 못한다.** `created_at <= NOW() - 유예`는 단조 조건이라 만료일에 이관 배치가 통째로 들어온다. 그것을 막는 것은 수신자 상한이고, 상한 기본값이 NULL이라 **커서 경로가 아닌 모든 트리거는 활성화 시 상한을 필수로 받는다.**
- **유예 대상과 상한 필수 대상은 다른 집합이다.** 생일은 유예가 필요 없지만(실제 날짜) 생년월일 대량 적재가 그날 코호트를 한꺼번에 만들어 상한은 필요하다. 상한 필수 = `classifyJourneyTrigger === 'state'`(모르는 값 포함 = fail-closed).

**동시성** — 상한 검사를 활성화 UPDATE의 WHERE에 넣어 원자화했고, 옵션 저장 PATCH에는 읽은 status를 쓰기 조건으로 걸었다(낙관적 동시성, 어긋나면 409). 검사만 하고 조건 없이 쓰면 그 사이 상한이 비워져 상한 NULL인 활성 여정이 남는다.

**회귀 방지** — 소스 스캔 불변식(주석 제거 후 검사 + 검출기 자체 fixture)과 행동 테스트 2종. 게이트 조건을 실제로 반전시켜 6/8 실패를 확인하고 원복했다. 토큰 스캔만으로는 반전을 못 잡는다는 것이 적대검증으로 드러났다.

**남은 것** — `points_min` 0 서버 미차단(§9-N6), 커서 경로의 배치 상한(§9-C6 잔여), 그 밖 §9 목록은 착수 순서 3~7에서 흡수.

---

## §11-B 3(커서 축 전환) 구현 결과 (2026-08-01, Codex 적대검증)

**2026-08-01 배포완료 + DDL 4단계 실행완료.** backend tsc 0 / vitest 127파일 1,773건.
**커서는 이제 도착 축(`created_at` + 이벤트 id)으로 돈다.** 전환 게이트(`last_event_cursor_id` 컬럼 존재)가 열렸다.
현재 커서 여정(구매·예약·배송)이 전부 비활성이라 전환으로 잃거나 겹친 이벤트는 없다.

**바꾼 것** — 커서 축을 `occurred_at`(발생) → `created_at`(도착) + 이벤트 `id`(동시각 타이브레이커).
커서 전진은 "실제로 본 마지막 행까지만"으로 바꿨다(옛 코드는 절단이 없으면 `windowEnd`로 건너뛰었다).

**닫힌 것**
- 배치로 늦게 도착한 데이터 — 싱크에이전트가 하루 뒤 올려도 도착 순서로는 항상 커서 앞이다. §11-4의 전제.
- 동시각 타이 — 브라우저 적재는 한 배치에 같은 시각을 찍는다. `(created_at, id)` 행 비교로 가른다.

**⛔ 안 닫힌 것 — 익명→회원 소급(§9-N5). 내가 닫힌다고 보고했다가 뒤집혔다.**
  소급 연결은 과거 행의 `customer_id`만 갱신하고 `created_at`은 그대로 둔다(`cdp-events` stitching·`cdp-identity`).
  그 행은 도착 축에서도 여전히 커서 뒤다. 축을 바꿔도 안 잡힌다.
  **제대로 고치려면 축을 하나 더 만들거나(삽입·소급 양쪽에서 갱신되는 시각) 이벤트 단위 소비 원장이 필요하다.**
  후자가 §3-0의 원장 일반화와 같은 구조라 **착수 순서 5번에서 함께 처리한다.** 여기서 반쪽으로 손대지 않는다.

**전환 안전장치 3 (적대검증 반영)**
- 축 전환은 `journeys.last_event_cursor_id` 컬럼이 있을 때만 켠다. 저장된 옛 커서는 발생 시각이라 그대로 도착 축으로 읽으면 처리분을 다시 잡는다(중복 발송·과금). 배포와 DDL 사이 창에서 동작 변화가 0이 된다.
- 커서 시각과 id를 **같은 조회에서** 읽는다. 따로 읽으면 그 사이 DDL 커밋 때 옛 축 시각과 새 축 id가 한 쌍이 된다.
- **42703(컬럼 없음)만** 폴백한다. 연결 끊김·타임아웃까지 삼키면 DDL 이후에도 옛 축으로 돌아가 축이 섞인다.

**창 끝 안전 지연** — `windowEnd`를 DB 시계에서 받고 1분 뒤로 물린다. `created_at DEFAULT now()`는 **트랜잭션 시작 시각**이라, 적재 트랜잭션이 먼저 시작되고 우리 SELECT가 먼저 실행되면 그 행은 안 보이는데 창을 지금까지 소모하면 나중에 커밋된 그 행이 영영 안 잡힌다. 커서보다 뒤로 물러나지 않게 `max`를 씌운다.
⚠ 이 지연은 **적재 트랜잭션이 1분 안에 끝난다는 가정** 위에 선다. 증명이 아니라 완화다.

### §11-B-2 배포 후 DDL — **2026-08-01 실행완료**

인덱스를 먼저 만들어야 컬럼이 생기는 순간부터 새 축 조회가 인덱스를 탄다.
인덱스에 `id`를 포함한다 — 없으면 같은 시각이 몰릴 때 정렬·seek를 못 해 매 chunk마다 같은 묶음을 다시 정렬한다(적대검증 지적).

1. 확인: `SELECT column_name FROM information_schema.columns WHERE table_name='journeys' AND column_name='last_event_cursor_id';`
2. `CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_cdp_events_company_event_created ON cdp_events (company_id, event_name, created_at, id);`
3. `ALTER TABLE journeys ADD COLUMN IF NOT EXISTS last_event_cursor_id uuid;`
4. 커서 재기준: `UPDATE journeys SET last_event_cursor = NOW() WHERE last_event_cursor IS NOT NULL AND trigger_event IN ('cdp.purchase','cdp.reservation_created','custom_order_shipped');`

4번을 빼면 축이 바뀐 뒤 옛 발생 시각을 도착 시각으로 읽어 중복이 난다. 현재 커서 여정이 전부 비활성이라 재기준으로 잃는 이벤트는 없다.

**실행 확인(2026-08-01)** — `pg_index.indisvalid = t`. `CREATE INDEX CONCURRENTLY`는 실패해도 무효 인덱스를 조용히 남길 수 있어 유효성까지 봤다. 4단계 전부 반영 완료.

---

## §11-C 4(연동 배선 통일) 구현 결과 (2026-08-01, Codex 적대검증 1R로 설계 전환)

**코드 완료 · 배포 전.** backend tsc 0 / vitest 128파일 1,799건(신규 26). DDL 2컬럼(배포 후).

**한 일** — 구매 여정 커서가 **`purchases` 원장을 직접 읽는다.** 싱크에이전트로 들어온 구매가 그 문이다.
자사몰(cdp_events)과 원장, 두 문 중 그 회사에 해당하는 하나를 읽는다(§2-2의 결정).

### §11-C-0 ⛔ 뒤집힌 판단 — 진실을 복사하지 마라

처음엔 싱크 적재 시점에 같은 구매를 `cdp_events`에 **미러**로 남기는 구조로 만들었다(멱등키 컬럼 신설,
`purchase_synced` 신규 이벤트명, 싱크 두 입구에서 호출). Codex 적대검증이 8개 가정 중 6개를 깼고,
결함 12건의 뿌리는 **하나**였다 — 사실을 복사하면 원본과 짝을 맞출 키가 필요하고(시각으로 대신했다가
교차 문 중복 발송으로 깨졌다), 무엇이 새 것인지 몰라 창 전체를 다시 읽어야 하고(요청 경로 부하),
복사가 실패하면 복구할 길이 없다(대조 워커 = 이중 진실). 여기에 얼굴 하나가 더 있었다 —
이름을 나눠 `event_name`을 보는 소비처는 막았지만 **테이블 자체를 세는 소비처**(`hasCdpData`·활성고객·
자가진단·성과 추정)는 못 막았다. 미러 행이 존재한다는 사실만으로 숫자가 바뀐다.

**원장을 그대로 읽으면 진실이 하나다.** 원장 행이 곧 사건이고, 커서가 (도착시각, 행 id)로 도니까
중복 진입은 구조가 막는다. 멱등키도, 대조 워커도, 새 이벤트 이름도, 미러 CT도 사라졌다.
코드가 늘지 않고 줄었다 — 구조를 고치면 덧댄 장치가 함께 사라져야 정상이다.

### §11-C-1 문은 회사마다 하나다

자사몰 주문(`cdp-orders`)은 `cdp_events`에만 쌓고 원장에는 쓰지 않는다(`INSERT INTO purchases`는
싱크 라우트 두 곳뿐 — grep 확인). 보통은 두 문이 겹치지 않는다.
문제는 **자사몰 주문을 ERP에도 동기화해 우리에게 두 번 주는 회사**다. 두 문을 다 읽으면 같은 구매가
두 번 진입한다 — 구매 템플릿은 재진입 허용에 쿨다운 0이라 그대로 중복 발송·중복 과금이다(Codex Critical).

**그래서 자사몰 문이 현역이면 원장 문을 열지 않는다.** 이 판정 구조는 새로 만든 게 아니다 —
`customer-purchase-aggregates`가 구매요약을 두 채널이 덮어쓰지 않게 하려고 이미 같은 방식을 쓴다.
"현역"은 존재가 아니라 **최근 30일**로 본다. 옛 테스트 이벤트 한 건이 원장 문을 영영 닫으면
"켜 뒀는데 0건"이 되고, 그게 이 재설계가 없애려는 바로 그 상태다(§2-3).

### §11-C-2 과거분 소급을 막는 것은 발생 시각 하나다

이관·전량 재적재는 3년 치 구매를 오늘 올린다. 커서는 도착 축이라(§11-B) 그 전량이 커서 앞에 선다.
**그래서 `purchase_date`(발생 시각)가 최근인 행만 후보가 된다.** 도착 시각으로 자르면 아무것도 막지 못한다.
날짜 없는 행·미래 날짜 행은 제외(언제 일어났는지 모르면 트리거가 아니다).

**⛔ 적재 주기에 기대지 않는다 — 정책으로 못 박는다 (2026-08-01 Harold 정정 — 뒤집힌 판단 둘째)**
처음엔 "그 회사 에이전트의 구매 적재 주기 × 3주기"로 창을 계산했다. 틀렸다 —
**동기화 주기는 고객사가 자기 환경에서 설정하는 값이고 우리 권한이 아니다.** 우리 DB의 `sync_agents`
값이 현장 설정과 같다는 보장이 없고, 고객사가 언제든 바꿔도 우리는 모른다. 모르는 값에 동작을 걸면
그 값이 어긋나는 순간 **구매가 조용히 안 잡힌다** — "켜 뒀는데 0건", 이 재설계가 없애려는 바로 그 상태다.

**해법은 주기를 알아내는 게 아니라 주기와 무관해지는 것이다(Harold).**
매장 구매는 **하루 모아 다음 날 오전(KST 09~12시)에 내보낸다.** 고객사가 10분마다 올리든 밤에 한 번
올리든, 그날 안에 도착한 것은 다음 아침에 함께 나간다. 주기가 바뀌어도 동작이 안 바뀐다.
덤으로 매장의 성질과도 맞는다 — 배치 적재라 "방금 구매 감사"는 애초에 불가능하고
"어제 다녀가셔서 감사합니다"가 정직한 문안이다. 야간 발송 차단(21~08시)과도 부딪히지 않는다.
⚠ **자사몰 문은 이 정책을 쓰지 않는다** — 주문이 즉시 들어오므로 실시간 그대로다.

실행 창을 한 시각이 아니라 09~12시 **구간**으로 둔 것은 워커가 5분 주기·회차당 1,000행이기 때문이다
(구간이 넓어야 하루치가 많은 회사도 그 아침에 소화한다 — 최대 36회차). 진입 시각이 그 안에서 흩어져도
**발송 시각은 첫 스텝 설정(`delay_mode`·`target_hour_kst`)이 통제**한다.

발생 시각 창도 이 정책에서 나온다(임의 상수가 아니다) — 최악 조합 = 고객사 하루 한 번 동기화(최대 1일 지연)
+ 우리 하루 한 번 실행(최대 1일 대기) + 여유 1일 = **3일**. 그보다 오래된 구매는 보내지 않는다.
이관 배치는 발생일이 몇 달·몇 년 전이라 이 조건에서 통째로 걸러진다.
코드에서 `sync_agents`를 읽지 않는다(회귀 테스트로 고정).

**★0802 Harold 확정 — 고객 공표 정책으로 못 박는다.**
"매장 구매 여정을 쓰려면 그날 구매가 **밤 11시(KST 23:00)까지 한 번은 동기화**되어 있어야 합니다.
그때까지 들어온 구매는 다음 날 아침에 발송됩니다."
- 우리는 고객사 주기를 읽지도 추측하지도 않는다 — **요구 조건을 공표**하고, 지켰으면 진입을 보장한다.
- 컷오프는 자정이라 23시 약속에 1시간 여유. 자정을 넘긴 동기화분은 하루 밀릴 뿐 3일 창 안이면 유실 0.
- 커서 트리거는 수신자 상한 면제(CAP_EXEMPT)라 조건이 맞으면 진입이 막힐 길이 없다.
- 화면 노출(§11-7 몫): 활성화 브리핑·구매 트리거 카드에 이 문구 + 마지막 구매 동기화 시각 표시
  (지키고 있는지 고객이 화면에서 스스로 확인 — §2-3 "판정 근거를 그대로 보여준다"와 같은 원칙).
- 싱크에이전트 설치 매뉴얼·마법사에도 같은 문구(여정 사용 시 권장 주기 안내).

⚠ **남는 노출**: 첫 연동 회사가 full sync를 돌리면 창 안(최근 24~72시간) 구매가 한꺼번에 후보가 된다.
그때 이미 활성인 구매 여정이 있으면 그 사람들에게 나간다(새로 만든 여정은 활성화 시 커서가 `NOW()`라
소급되지 않는다 — `journey-builder`). 실제로 산 사람들이고 커서가 1,000건/회차로 분산하지만,
**커서 경로의 수신자 상한(§9-C6)이 기본 NULL**이고 상한 검사는 회차마다라 총 backlog를 막지 못한다.
상한 필수화는 착수 5번 몫으로 남긴다.

### §11-C-3 시간대 — 두 컬럼의 전제가 서로 다르다 (Codex 2R 정정)

`purchases.purchase_date`·`created_at` 모두 `TIMESTAMP`(naive)지만 **기준이 다르다.**
- `created_at` = DB가 `CURRENT_TIMESTAMP`로 찍는다 → **세션 시간대 기준 naive**.
  운영은 `timezone=Etc/UTC`이고 드라이버도 1114 타입을 UTC로 파싱한다(`config/database.ts`).
  그래서 비교는 `current_setting('TimeZone')`으로 되돌린다 — 세션 설정이 무엇이든 컬럼과 같은 기준이 된다.
- `purchase_date` = 에이전트가 보낸 값을 그대로 넣는다 → **KST naive**(레포 규약).
  `dm.ts`·`inapp-funnel-stats.ts`·`campaign-response-attribution.ts`가 모두 `AT TIME ZONE 'Asia/Seoul'`로 비교한다.

"둘 다 KST"라고 뭉뚱그렸던 앞 서술은 부정확했다. 코드는 각 컬럼에 맞는 변환을 쓰고 있다.
- 커서·창 비교는 **우리 timestamptz를 세션 시간대 wall-clock으로 바꿔** 컬럼과 같은 기준에 놓는다.
  컬럼 쪽에 변환을 걸면 인덱스를 못 탄다.
- JS로 돌려줄 때만 timestamptz로 되돌린다 — 커서 컬럼이 timestamptz라 축이 섞이면 안 된다.
- 발생 시각은 `AT TIME ZONE 'Asia/Seoul'` 규약(dm.ts·inapp-funnel-stats.ts와 동일).

### §11-C-4 여정 안에서 원장을 함께 보는 곳

커서 진입 말고도 "구매했나"를 묻는 자리가 여정 안에 있다. 문이 둘이면 그 자리도 둘 다 봐야 한다.
- **장바구니 안티조인** — 매장에서 산 사람에게 리마인드를 보내지 않는다.
- **전환 판정**(`isGoalConvertedSinceEntry`) — 프로필 신호(`recent_purchase_date`)는 날짜 정밀이라
  진입 당일 구매를 못 가른다. 원장을 시각 정밀로 함께 본다.
- **홀드아웃 전환 통계** — 같은 이유로 원장 EXISTS 추가.
- **데이터 게이트**(`company-data-profile`) — 구매 트리거를 열 근거 = 자사몰 이벤트 **또는** 원장.
  게이트 사유 문구도 문 중립으로("자사몰 주문" → "구매 정보").

지표·인앱은 원장을 읽지 않는다. 매장 구매를 성과 귀속·매출에 넣을지는 별건 판단이다(이중계상 검토 필요).
회귀 테스트가 그 소비처 7개에 `FROM purchases`가 없음을 고정한다.

### §11-C-5 배포 후 DDL

1. 확인: `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='purchases' AND column_name IN ('created_at','purchase_date');`
   (기대 = 둘 다 `timestamp without time zone`. 다르면 §11-C-3 변환을 먼저 고친다)
2. 인덱스 확인: `SELECT indexname, indexdef FROM pg_indexes WHERE tablename='purchases';`
3. 2에 `(company_id, created_at, id)`가 없으면:
   `CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_purchases_company_created_id ON purchases (company_id, created_at, id);`
4. `ALTER TABLE journeys ADD COLUMN IF NOT EXISTS last_purchase_cursor timestamptz;`
5. `ALTER TABLE journeys ADD COLUMN IF NOT EXISTS last_purchase_cursor_id uuid;`
6. **커서 재기준(필수)**: `UPDATE journeys SET last_purchase_cursor = NOW() WHERE trigger_event = 'cdp.purchase' AND status = 'active' AND last_purchase_cursor IS NULL;`
7. 유효성: `SELECT indexrelid::regclass AS idx, indisvalid FROM pg_index WHERE indexrelid = 'idx_purchases_company_created_id'::regclass;`
   `CREATE INDEX CONCURRENTLY`는 실패해도 무효 인덱스를 조용히 남긴다.

**순서 엄수 — 인덱스가 커서 컬럼보다 먼저다.** 컬럼이 생기는 순간 원장 커서가 돌기 시작한다.
§11-B-2에서 배운 것과 같은 순서다.

**6번을 빼면 이미 켜져 있는 구매 여정의 원장 문이 영영 안 열린다.** 커서 NULL은 "기준 없음"이고
워커는 기준 없이 열지 않는다(열면 활성화 이전 구매까지 소급 발송된다). 커서를 심는 자리는 활성화뿐이라,
DDL 이전에 켜진 여정은 여기서 한 번 심어 줘야 한다. `NOW()`로 심으면 그 시각 이후 도착분부터 잡는다.

**활성화 시점별 커버 확인** — 6번과 활성화가 겹쳐서 빈틈이 없다.
- DDL 전 활성화: 커서 UPDATE가 42703으로 삼켜져 NULL → 6번이 심는다.
- DDL 중(컬럼 생성 후 6번 전) 활성화: 커서가 `approved_at`으로 이미 들어가 6번의 `IS NULL`에 안 걸린다(덮어쓰지 않는다).
- DDL 후 활성화: 평시 경로.
- DDL 시점에 `paused`인 여정: 6번이 건너뛰지만, 재개할 때 활성화가 심는다.
- 6번과 활성화가 동시에 실행돼도 둘 다 `IS NULL` 조건이라 한쪽만 성사된다(두 값 모두 그 시점 근처).

커서 컬럼(4·5) 전에는 원장 문이 열리지 않는다(42703만 폴백 — 컬럼 없이 열면 커서를 못 적어 매 회차
같은 구간을 다시 잡는다 = 중복 발송). 그 구간에 자사몰 여정은 정상이고 싱크 구매 진입만 0이며,
그 사실이 PM2 로그에 남는다(조용한 0건 금지).

### §11-C-6-A Codex 2라운드가 뒤집은 것 4 (2026-08-01)

1. **커서 초기화가 활성화와 원자적이지 않았다.** 활성화 UPDATE는 이미 커밋됐는데 커서는 여러 await 뒤에
   `NOW()`로 심었다 — 그 사이 적재된 구매가 `created_at > 커서`에서 영구히 빠진다.
   → 활성화가 찍은 `approved_at`을 그대로 심어 창을 0으로 만들었다.
2. **커서 NULL이면 열지 않는다(신설).** 전엔 NULL이면 자정 컷오프부터 읽어 **활성화 이전 구매가 소급 발송**됐다.
   커서를 심는 자리는 활성화 하나뿐이고, DDL 이전에 켜진 여정은 DDL 6번이 심는다.
3. **"자사몰 문 없음"을 캐시하지 않는다.** 캐시된 false는 그 5분 동안 원장 문을 열어 둔다 —
   그 사이 자사몰 첫 구매가 들어오면 두 문이 동시에 열려 중복 발송·과금이다. **기본값이 열림인 장치**였다.
   true(원장 잠금)는 안전한 방향이라 그대로 캐시한다.
4. **시각 판정을 DB 시계로 단일화.** 창 끝(자정)·구매 나이는 DB가 재는데 실행 시간대만 앱 시계로 재고 있었다.
   시계가 어긋나면 발송 시간대가 밀리고, 뒤로 가면 같은 구간을 다시 연다.

부수: 회차 절단과 DDL 미실행을 로그로 남긴다(조용한 0건 금지 — 상한 필수화를 판단할 유일한 관측점).
데이터 게이트의 원장 근거에 `customer_id IS NOT NULL`을 붙였다(연결 안 된 원장 행으로 여정을 열면 대상이 0이다).

**5(3라운드에서 내가 만든 결함을 되돌림)** — 커서 초기화 실패를 `throw`로 올렸더니
**여정은 켜졌는데 호출자는 실패를 받는** 경로가 생겼다. 활성화 UPDATE는 이미 커밋된 뒤라,
재시도해도 `status`가 `active`라서 활성화 WHERE(`draft`·`paused`)에 걸려 영원히 "켜지 못했습니다"만 돌아온다.
앞의 snapshot·baseline 단계와 같은 규약(삼키되 남긴다)으로 되돌리고, 42703이 아닌 오류는 `console.error`로
여정 id와 함께 남긴다 — 커서가 없으면 문이 닫힌 채(안전) 남고 DDL 재기준으로 되살릴 수 있다.

⚠ 남긴 것: 혼합 회사(자사몰+POS)는 자사몰 문이 현역인 동안 POS 구매를 못 쓴다. 중복 발송보다 이쪽을 택했다.

### §11-C-6-B 4라운드(Fable 적대검증)가 찾은 것 — 커서를 DB 밖으로 내보내지 마라 (2026-08-02)

**F1(치명, 수정 완료)** — DB 시각은 마이크로초인데 JS Date는 밀리초다. 커서 값이 드라이버(Date)를 거쳐
저장·재조회되면서 실제 마지막 행보다 최대 999µs 이르게 잘렸다. 행 비교 `(created_at, id) > (커서, id)`는
첫 항이 이기면 끝이라 **마지막 밀리초에 속한 행 전부가 매 회차 재선택**된다 — 싱크 벌크 INSERT는 한 문장에
500행이 같은 시각을 공유하므로 그 500행 통째다. 구매 여정은 재진입 허용·쿨다운 0·executions UNIQUE 없음이라
그대로 중복 발송·중복 과금. **같은 뿌리가 이벤트 문(§11-3 배포분)에 잠복** — 커서 여정 전부 비활성이라 미발화.

수정 = 커서를 DB 정밀도 밖으로 내보내지 않는다.
- 커서 SELECT 두 곳이 `::text` **원문**을 함께 싣고, 전진(planCdpCursorBatch)·비교 파라미터 모두 원문 사용.
- 커서 **읽기**도 `::text` — 쓸 때만 원문이면 읽기에서 다시 절사된다.
- 커서 쓰기에 **후진 금지 가드**(행 비교 `>=` WHERE) — 절사된 windowEnd가 마이크로초 커서를 뒤로 물리는 경로 차단.
- Date 경로는 창 계산(Math.max)에만 남는다 — 1ms 오차는 가드가 흡수.

**F2(수용된 한계)** — POS 시계 오류로 `purchase_date`가 미래인 행은 미래 필터에 걸리는 동안 커서가 지나가
영구 제외된다. 시계 오류 행은 보내지 않는 것이 정책이다(3일 창의 노화 제외와 같은 부류).

**F3(별건)** — 진입 변수 표면이 문마다 다르다(이벤트 `order_id`·`items` / 원장 `product_name`·`store_name`).
기존 변수 fallback이 처리한다. 문안 화면 안내는 화면 조각(§6)에서.

### §11-C-6 실측 (배포 + DDL 후)

1. 싱크 회사 1곳에 구매 여정을 활성화 → 최근 구매 1건이 증분으로 올라온 뒤 진입되는지
   (`SELECT id, customer_id, entered_at, entry_event_properties FROM journey_executions WHERE journey_id='...' ORDER BY entered_at DESC LIMIT 5;`)
2. 커서가 전진하는지 — `SELECT last_purchase_cursor, last_purchase_cursor_id FROM journeys WHERE id='...';`
3. 같은 구매가 다시 올라와도(에이전트 재전송) 진입이 늘지 않는지 — 커서 뒤라 안 잡히는 것이 정상.
4. 구매 여정 화면에서 구매 트리거 잠금이 풀리는지(게이트가 원장도 근거로 본다).
5. 매출 지표 불변 — 자동마케팅 ROI·캠페인 귀속 숫자가 그대로여야 한다.

---

## §11-D 5(트리거 재정의) 1차 구현 결과 (2026-08-02)

**코드 완료.** backend tsc 0 / frontend tsc 0 / vitest 128파일 1,822건. DDL 1건(아래 §11-D-3, 배포 후).

### §11-D-1 트리거 레지스트리 — 계약 단일 출처 (§5-4)

`journey-trigger-capability.TRIGGER_CONTRACTS`가 15종 + 상시의 계약(분류·구현 여부·종료 신호·최소 쿨다운)을 소유한다.
- **저장·활성화 화이트리스트**: 미등록·미구현 trigger_event는 저장 단계에서 거부(200크레딧 전에 안다) + 활성화가 최종 게이트. §9-C2 사용자 측 종결.
- **§9-C6 종결 — 수신자 상한 전면 필수.** 옛 면제 근거("커서는 대량 적재가 과거 이벤트를 안 만든다")는 §11-4 원장 문이 깼다(첫 full sync = 3일 창 구매 일괄). `CAP_EXEMPT_TRIGGERS = []`.
- **§9-N1 종결 — 장바구니 재진입 쿨다운 최소 1일**을 활성화가 강제(계약 `cooldownMinDays`).
- **§9-N6 종결 — 포인트 임계 ≥ 1** 활성화 강제.
- **§9-N2 종결 — baseline 적재 실패 = 여정 정지**(fail-open 금지). 활성화는 이미 커밋이라 정지+사유+실패 응답.
- **§9-N5 종결 — 익명→회원 소급 연결이 `created_at`(도착 축)도 갱신**(cdp-events·cdp-identity 두 곳). 익명이던 행은 커서가 소비한 적 없어(customer_id IS NULL 필터) 중복 진입 0. `occurred_at`은 불변이라 통계·귀속 무영향.

### §11-D-2 신규 트리거 5종 개방 (#2 첫구매 · #5 휴면복귀 · #6 주기이탈 · #7 등급변동 · #12 조회 후 미구매)

같은 구매 커서(이벤트+원장 양 문)를 읽고 **자격 필터**로 갈린다(`qualifyPurchaseTransition` — 커서는 자격과 무관하게 전진).
이력 판정 = `selectLastPriorPurchase`(양 문 합산, 자기 자신은 strict < 제외 — 한쪽 문만 보면 문을 옮긴 단골이 "첫 구매"가 된다).
- `purchase.first`: 이전 구매 0건만. 종료 계약 = 두 번째 구매(진입 자체가 첫 구매라 기존 구매 goal 판정과 일치).
- `customer.dormant_return`: 직전 구매가 `dormant_days`(기본 30) 이상 과거. 이전 구매가 없으면 복귀가 아니다.
카탈로그 카드 + AI 추천 화이트리스트 + parity 가드 갱신. #2·#5는 진입 이벤트 변수(상품명·결제금액·매장명) 동봉.
- `customer.cycle_lapsed`(#6): 평균 구매 간격 × 계수(기본 1.5) 초과 + 구매 3건 이상(양 문 합산, epoch 산술 = PG 버전 무관). 상태형 유예 대상 + 쿨다운 최소 1일.
- `cdp.browse_no_purchase`(#12): product_view 후 N일(기본 3) 무구매 — 장바구니와 같은 창 구조, 구매 부재는 양 문 확인. 개방 근거 = product_view 보유(facts.hasBrowseEvents).

### §11-D-3 배포 후 DDL — 화이트리스트 CHECK (§5-4 DB 측)

```sql
ALTER TABLE journeys ADD CONSTRAINT journeys_trigger_event_registered CHECK (trigger_event IN (
  'customer.created','purchase.first','cdp.purchase','customer.dormant','customer.dormant_return',
  'customer.cycle_lapsed','customer.grade_changed','customer.birthday_approaching','customer.points_expiring',
  'cdp.cart_abandon','custom_order_shipped','cdp.browse_no_purchase',
  'cdp.reservation_created','reservation.visit_dn','reservation.visit_done','custom'
)) NOT VALID;
ALTER TABLE journeys VALIDATE CONSTRAINT journeys_trigger_event_registered;
```
`NOT VALID` 후 `VALIDATE` — 기존 행(실측 5종, 전부 등록됨)을 잠그지 않고 검사한다. VALIDATE가 실패하면 미등록 값이 있는 것이니 결과를 보고 처리한다.

### §11-D-4 남은 것 (후속 조각)

- **#7 등급 변동** — 이전 등급 기억(원장 상태 일반화 DDL) 선행.
- **§5-1 런타임 완결** — `points_used`·`reservation_closed` 판정 + 종료 자동화 기본값(기존 여정 동작을 바꾸는 상품 결정이라 Harold 판단 필요).
- **§5-2 고객별 중단·재계산 + 예약 3종** — 착수 6번(예약 원장 §8)과 한 몸.
- §5-5 유입 보류 게이트는 §11-A에서 **유예+상한 필수**로 대체 확정(별도 구현 없음).

### §11-D-5 Codex 1R 판정 반영 (2026-08-02)

수용 5 — ①cycle_lapsed 양 문 중복 구매가 cnt를 부풀림 → 발생 시각 분 단위 dedupe(UNION DISTINCT). ②자격 필터의 진입 시각을 배치 내 **최소 발생 시각**으로(도착 역순 배치 오판 차단) + **신규 2종 커서 활성화 초기화 누락**(원장 문이 영영 안 열리던 실결함) → builder 초기화 목록에 purchase.first·dormant_return 추가. ③baseline을 활성화 **앞**으로 이동 — 실패 = 안 켜짐이라 N2 정지 장치 자체가 사라졌다(구조 수정). ④소급 갱신 부작용 — cdp-identity 소급에 30일 제한(ingest와 동일 계약), cdp-orders 중복 판정 정렬을 발생 축(occurred_at, id)으로(소급 created_at과 무관해짐). ⑤browse 카드 gated 누락.
부분 불수용 1 — capability가 구매 1건으로 cycle을 여는 것: 게이트는 회사 단위 근거고 3건은 **고객 단위** 성립 조건(§2-3 "판정을 할 만큼 있나"의 고객측) — 대상 수는 미리보기가 보여준다.
Codex의 vitest 미검증 표시는 샌드박스 EPERM — 로컬 재실행으로 1,826건 통과 확인.

### §11-D-6 2차 구현 — #7 등급 변동 + §5-1 런타임 완결 (2026-08-02)

**#7 등급 변동(`customer.grade_changed`) 개방 — 원장 상태 일반화(§3-0)의 본체.**
- `journey_entry_ledger`에 `kind='state'` 행(**사람 단위, store_code NULL 고정** — 매장별로 쪼개면 같은 사람의 다른 행이 옛 등급을 들고 계속 발화)으로 이전 등급을 기억한다.
- 활성화 **전에** 등급 기준선을 적재(`seedGradeStateForJourney` — baseline 선적재와 같은 규약, 실패 = 안 켜짐). `state_value` 미마이그레이션(42703)이면 활성화가 거부된다(fail-closed).
- 추출 = 원장 state와 현재 등급이 `IS DISTINCT FROM`인 사람(state 행이 없으면 "변동"이 아니라 최초 관측 — 대상 아님). 진입 트랜잭션이 state를 현재 등급으로 갱신(안 하면 매 회차 재발화).
- 상승·하락 방향 필터는 회사가 등급 서열을 준 경우에만 가능(정답표 금지) — 후속.

**§5-1 런타임 완결.**
- `goal_kind` 미지정 시 트리거 계약의 종료 신호에서 파생. purchase 계열은 기존 폴백과 동일(회귀 0), **`points_used` 신규** — 진입 시 포인트 스냅샷(`points_at_entry`, watcher가 entry_event_properties에 동봉)보다 현재 포인트가 줄면 종료. 스냅샷 없으면 판정 불가 = 계속.
- `steps_done`·`reservation_closed`는 기존 폴백 유지 — 기존 활성 여정의 조기 종료 동작을 바꾸지 않는다(자동 종료 기본화는 상품 결정).

**§11-D-3 DDL 추가(배포 후, CHECK보다 먼저):**
```sql
ALTER TABLE journey_entry_ledger ADD COLUMN IF NOT EXISTS state_value varchar(100);
```

**최종 잔여(§11-D-4 갱신)** — 예약 3종+§5-2(착수 6번: 예약 원장 §8 + Harold 확인 2건이 선행) · 화면 재설계(착수 7번) · 등급 방향 필터·자동 종료 기본화(상품 결정). **§11-5의 구현 가능 범위는 전부 종결.**

### §11-D-7 Codex 2R 판정 반영 (2026-08-02)

수용 2(둘 다 실결함) — ①**자기참조 구멍**: 기준선 이후 새로 나타난 고객은 state 행이 없어 대상이 아닌데 만드는 곳도 없어 이후 변동까지 영영 미포착 → 워커가 매 회차 **관측 적재**(`observeGradeStateForJourney` — state 없는 고객의 현재 등급 기록, 진입 없음. 첫 관측=기록만, 변동은 다음부터). ②**낡은 기준선 고착**: 거부된 활성화·정지 기간의 기준선이 다음 활성화 첫 회차에 변동을 몰아 발화 → **매 활성화마다 재기준**(등급=값 DO UPDATE, 신규가입=명단 보충 DO NOTHING — 둘 다 덜 보내는 방향. 정지 기간 변동은 보내지 않는다).
미완 인지 — state 경합·동시성 행동 테스트는 미추가(소스 정합·게이트 집합 검사만 있음). 2R의 vitest 미검증 표시는 동일한 샌드박스 EPERM — 로컬 1,826 통과 확인.

---

## §12 [접음 — 여정 밖] 예약 3종 + 고객별 날짜축 + 고객별 중단·재계산

> **2026-08-02 접음.** 예약이 여정에서 빠졌다(§3-3 — 여정은 마케팅 자동화, 예약 리마인드는 정보 알림 축).
> **설계는 지우지 않는다.** 정보 알림 축에서 예약을 착수할 때 이 절을 그대로 쓴다. 아래 내용은 그 시점 기준으로 읽는다.
> 착수 전 원래 두었던 "Harold 확인 2건" 블로커는 **폐기**했다(§10 미결 2) — 어느 고객사가 무엇을 가졌는지를 착수 조건으로 두는 것이 곧 정답표를 갖는 일이었다.
> 되살릴 때의 판정 축은 사람의 답이 아니라 **그 회사 원장의 런타임 관측**이다:
> 원장에 행이 있으면 접수(#13), **취소·노쇼 상태 행이 실제로 관측된 회사만** 방문 D-N(#14), 완료 상태가 관측된 회사만 방문 완료 후(#15).
> #14를 관측으로 가르는 이유 하나 — 리마인드를 멈출 수 있다는 유일한 증거가 "취소가 실제로 들어오는가"다. 매핑 선언은 증거가 아니다.
> 예약을 스냅샷으로 내보내 취소가 행 삭제로 사라지는 고객사는 영영 안 열리는데, 멈출 수 없으니 그것이 맞는 결과다.

### §12-1 예약 원장 `reservations` — DDL (배포 후 실행)

구매 원장과 같은 모양으로 둔다. 싱크에이전트가 적재한다(§8).

```sql
CREATE TABLE IF NOT EXISTS reservations (
  id             uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id     uuid NOT NULL REFERENCES companies(id),
  customer_id    uuid REFERENCES customers(id),         -- 고객보다 먼저 도착 가능(구매 원장과 같은 규약)
  customer_phone varchar(20) NOT NULL,                  -- 사람 단위 연결 키
  reservation_no varchar(80),                           -- 고객사 예약번호(멱등 축)
  reserved_at    timestamp NOT NULL,                    -- 예약 일시 = 고객별 날짜축 기준(KST naive — purchases 규약과 동일)
  status         varchar(20) NOT NULL,                  -- confirmed / cancelled / done / noshow / unknown
  status_raw     varchar(50),                           -- 고객사 원문 보존(정규화 실패 추적)
  store_code     varchar(50),
  store_name     varchar(100),
  staff_name     varchar(100),
  service_name   varchar(200),                          -- 문안 변수
  custom_fields  jsonb DEFAULT '{}',
  created_at     timestamp DEFAULT CURRENT_TIMESTAMP,   -- 도착 축(커서)
  updated_at     timestamp DEFAULT CURRENT_TIMESTAMP    -- 상태 변경 감지 축
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_reservations_company_no
  ON reservations (company_id, reservation_no) WHERE reservation_no IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_reservations_company_created_id
  ON reservations (company_id, created_at, id);
CREATE INDEX IF NOT EXISTS idx_reservations_company_reserved
  ON reservations (company_id, reserved_at);
CREATE INDEX IF NOT EXISTS idx_reservations_company_updated
  ON reservations (company_id, updated_at);
```

**⛔ 구매 원장과 다른 점 하나 — 예약은 UPDATE된다.** 구매는 사건이라 불변이지만 예약은 취소·변경·완료로 상태가 바뀐다.
그래서 `reservation_no` 유니크로 **upsert**하고 `updated_at`을 상태 변경 감지 축으로 쓴다.
예약번호를 안 주는 고객사는 (전화번호, 예약일시) 대체 키 — 그 회사는 예약일 변경을 신규 예약으로 본다(정직한 한계, 화면 표기).

**⛔ 과거 예약 소급 적재 금지(§8).** 적재 시작 이후 신규분만 트리거 대상. 구매와 같은 방식 — `reserved_at`이 과거인 행은 접수 트리거가 무시하고, 미래 예약만 방문 D-N 축에 오른다.

### §12-2 적재 — `POST /api/sync/reservations`

`POST /api/sync/purchases`와 같은 계약(배치 ≤5000 · mode · batchIndex · sync_logs 기록 · agentConfig 응답). 다른 점 둘:
- **upsert** — `ON CONFLICT (company_id, reservation_no) DO UPDATE SET status·reserved_at·updated_at = NOW()`.
- **상태 정규화 CT 신설** `utils/reservation-status.ts` — 고객사 원문(예약·확정·CONFIRM·취소·노쇼…)을 4값으로.
  ⛔ 매핑표를 지어내지 않는다 — 못 읽은 값은 `status_raw`에 남기고 `status='unknown'`으로 두어 **트리거 대상에서 제외**한다(§2-3: 판정 불가는 정직하게 불가). 회사별 매핑 추가는 화면에서.

### §12-3 고객별 날짜축 (§4-3) — 배관은 절반 이상 있다

`journey_anchor_dispatch`의 PK가 (journey_id, step_id, customer_id, send_date)라 **고객마다 발송일이 달라도 중복 방지가 이미 성립한다**(SCHEMA 실측). 지금은 기준 날짜만 `journeys.anchor_date`(여정 단위)에 있다.

**변경의 본체 = 기준 날짜를 고객 쪽에서 읽는 것.**
- `journeys.anchor_source` 신규(varchar20, default `'company'`) — `'company'`(현행 anchor_date) / `'customer_reservation'`(예약일).
- 앵커 스케줄러(`journey-anchor-scheduler.ts`)가 `anchor_source='customer_reservation'`이면 `reservations.reserved_at`을 고객별 기준일로 읽어 `step.anchor_offset_days`만큼 앞당겨 발송일을 잡는다.
- 나머지(멱등·발송·야간 시프트)는 기존 경로 그대로 — **새 발송 경로를 만들지 않는다.**

이 축이 서면 예약 말고도 렌탈·구독 만료일, 보증기간 종료, 시술 후 관리 주기, 차량 정비 주기가 같은 모양으로 붙는다.

### §12-4 트리거 3종 — 레지스트리 계약

| # | event | 진입 | 축 | 종료 | 개방 조건 |
|---|---|---|---|---|---|
| 13 | `cdp.reservation_created` | 예약이 confirmed로 **도착** | 도착 축 커서(`created_at`+id) — §11-C와 동일 규약 | `reservation_closed` | 예약 원장에 행이 있으면 |
| 14 | `reservation.visit_dn` | 예약일 D-N (고객별 날짜축) | 앵커 스케줄러(커서 아님) | 취소·완료 = 즉시 중단(§12-5) | **취소 구분되는 회사만** |
| 15 | `reservation.visit_done` | 상태가 done으로 바뀐 뒤 N일 | `updated_at` 변경 감지 커서 | `steps_done` | 완료 상태를 주는 회사만 |

⛔ #13은 지금 `cdp_events`의 `reservation_created`를 읽는다(커서 경로 기등록). 원장이 생기면 구매와 같은 **두 문 구조**가 된다 — §11-C의 `usesPurchaseLedger`와 같은 모양으로 `usesReservationLedger`를 두고, 문 판정도 같은 규약(자사몰 예약 이벤트가 현역이면 원장 문 잠금). **새 구조를 발명하지 않는다.**

### §12-5 고객별 중단·재계산 (§5-2) — 예약 때문에 만들지만 범용이다

지금 여정이 멈추는 길은 넷뿐이다(수신거부·목표 달성·조건 미충족·관리자 정지). **"그 고객만" 끊는 길이 없다.**

**신설: `journey_executions.status = 'cancelled'` + 사유.** 워커 하나가 소유한다.
- `utils/journey-execution-canceller.ts`(5분 주기 또는 예약 워커에 흡수): `reservations.updated_at`이 마지막 스캔 이후이고 `status IN ('cancelled','noshow')`인 행 → **그 예약을 기준으로 진입한 실행행**을 `cancelled`로, 사유 기록.
- **예약일 변경 = 재계산** — `reserved_at`이 바뀐 행은 `journey_anchor_dispatch`의 해당 (journey, step, customer) 행을 지우고 새 발송일로 다시 잡는다. **이미 발송된 스텝은 건드리지 않는다**(과거는 되돌리지 않는다).
- 매칭 키 = `journey_executions.entry_event_properties->>'reservation_no'`(진입 시 동봉). 사람이 여러 예약을 동시에 가질 수 있으므로 **고객 단위로 끊으면 안 된다.**

범용성 — 구독 해지·환불·등급 강등도 같은 축을 쓴다(진입 근거가 무효가 되면 그 실행행만 끊는다).

### §12-6 착수 순서 (6번 내부)

1. Harold 확인 2건 → 범위 확정(§12-0).
2. `reservations` DDL + 적재 엔드포인트 + 상태 정규화 CT (트리거는 아직 잠금).
3. capability 개방 — 원장에 행이 있으면 #13 열기(RESERVATION_REASON 해제 조건 신설).
4. #13 커서(두 문 구조) → 실측 1건.
5. 고객별 날짜축(`anchor_source`) + #14 — **취소 구분되는 회사만**.
6. §12-5 중단·재계산 워커 → #15.

---

## §13 착수 7번 완전 설계 — 화면 흐름 (§6 구현 수준)

> 근거 = §6-1~§6-7. **선행 조건이 하나 있다(§13-1) — 그것 없이는 나머지가 무의미하다.**
> 디자인 기준 = Journey Builder 동급 의무(CLAUDE.md `design_quality_minimum_journey_level`), 착수 직전 `LESSONS_FRONTEND` "디자인 최소 기준" 정독.

### §13-1 선행 — 저장 후 스텝 추가·삭제 API (§6-6)

이 흐름은 스텝을 늘려 가며 쓰는 것이 전제인데 **지금은 저장 후 스텝을 못 늘린다**(백엔드 INSERT는 생성 함수 한 곳뿐, DELETE는 아예 없음 — grep 0건). 화면을 아무리 고쳐도 이것 없이는 성립하지 않는다.

- `POST /api/journeys/:id/steps` — 추가(최대 7). 활성 여정도 허용하되 **진행 중 실행행에는 소급 안 됨**을 응답에 명시.
- `DELETE /api/journeys/:id/steps/:stepId` — 삭제 후 **반드시 재번호**.
- ⛔ **순번에 구멍이 생기면 여정이 통째로 죽는다** — 실행기는 `current_step_order + 1`을 찾고 진입은 `step_order = 1`을 찾는다. 재번호는 같은 트랜잭션에서 `journey_steps`·스텝 스냅샷·`journey_anchor_dispatch(step_id)` 3곳 정합을 함께 본다.
- 크레딧 0 — 200은 최초 활성화 1회다(§7). 스텝을 늘려도 추가 과금 없음이 이 설계의 전제.

### §13-2 화면 구조 — 스텝별 전환 (§6-2·§6-3)

한 화면 = 스텝 하나 완결. 우측 [스텝 추가] → 화면이 스텝 N+1로 전환(지금이 몇 번째인지 명확히).
- 스텝 화면 요소: 문안 추천 · 변수 · **AI 꾸미기 / AI 다듬기 / 스팸필터 테스트**(오퍼레이터 화면과 같은 정렬·품질) · 발송 시점(즉시 / N시간 후 / N일 후 특정 시각).
- **야간 안내** — 광고성은 21~08시 KST 금지. 걸리는 설정이면 화면이 **오전 9시**를 알린다(ARRIVE_HOUR=9. 금지 창의 끝 08시와 다른 값 — 2026-08-02 정정).

### §13-3 AI 문안생성이 받아야 하는 것 (§6-4)

지금은 현재 본문·채널·광고 여부·의도 넷뿐이고, 본문 10자 미만이면 버튼이 거부돼 **빈 스텝에서 AI를 못 부른다.**
앞으로 받는 것: 여정의 트리거·목적 / **앞 스텝들의 확정 문안 전부** / 지금이 몇 번째 스텝이고 트리거로부터 얼마 뒤인지 / 채널·광고 여부·브랜드보이스.

> **화면 구조가 이 문제를 푼다.** 스텝별 전환이면 앞 스텝이 확정된 채로 뒤에 있고 순번·지연이 화면에 있어 넘길 맥락이 그대로 갖춰진다. 프롬프트에 컨텍스트를 더 밀어 넣는 방식은 땜질이다(§6-4 설계 메모).

### §13-4 진입·저장 (§6-1·§6-5)

- 진입 = 자연어 한 칸 → AI가 추천 여정을 **모달**로. 담는 것: 트리거를 왜 골랐는지 근거 / 목적 / 스텝 수 이유 / **이 회사 데이터로 가능한지**(§2-3 게이트, 불가면 무엇을 연동해야 하는지).
- 저장 = 스텝 전체 브리핑 모달(스텝 클릭 시 상세 펼침).
- **1클릭 원칙**(CLAUDE.md `marketing_user_ux_priority`) — 추가 입력·중간 선택 단계 금지. 자유 입력은 별도 버튼으로 분리.

### §13-A 구현 결과 (2026-08-02) — §13 전량

| 조각 | 무엇을 했나 | 파일 |
|---|---|---|
| §13-1 | 저장 후 스텝 추가·삭제 API. step INSERT를 `insertJourneyStepRow` 단일 정의로 뽑아 **생성 경로도 같은 함수**를 쓴다 | `journey-builder.ts` · `routes/ai.ts` |
| §13-1 화면 | 저장된 여정의 **[이 단계 추가]**(AI 다음 단계 추천을 클릭 한 번으로 실제 스텝으로) · 스텝별 **[지우기]**. 옛 화면은 추천만 보여주고 넣을 길이 없었다 | `JourneysPage.tsx` |
| §13-2 | 스텝별 전환 화면 — 한 화면 = 스텝 하나 완결. 채널·문안·변수·AI 3버튼·발송 시점·야간 안내·[스텝 추가]·[이 스텝 지우기] | `JourneyStepStudio.tsx` |
| §13-3 | AI 문안이 여정 맥락을 받는다(트리거·목적·순번·트리거로부터 얼마 뒤·**앞 스텝 문안 전부**) + **빈 스텝 생성 모드** | `journey-ai-generator.ts` · `routes/ai.ts` |
| §13-4 | 진입 추천 모달(근거·목적·스텝 수 이유·이 회사 데이터로 가능한지) · 저장 브리핑 모달(스텝 클릭 시 상세 펼침) | `JourneyPlanModal.tsx` · `JourneyBriefingModal.tsx` |
| §13-5 | 매장 구매 정책 노출(문 판정 + 마지막 도착 시각) · 수신자 상한 입력 자리 · 트리거 카드 순서 | `journey-purchase-ledger.ts` · `JourneyActivationConfirmModal.tsx` · `journey-trigger-catalog.ts` |

**구조로 푼 것 셋** — 덧댄 장치가 함께 사라졌다.
1. **AI 문안 겹침**(§6-4) — 스텝별 전환이면 앞 스텝이 확정된 채로 뒤에 있어 넘길 맥락이 저절로 갖춰진다. 프롬프트에 컨텍스트를 더 밀어 넣는 길을 택하지 않았다.
2. **빈 스텝에서 AI를 못 부르던 것** — 10자 미만 거부를 없애고 맥락이 있으면 생성 모드로 간다. 맥락도 본문도 없을 때만 거절한다(지어낼 근거가 없다).
3. **삭제 후 실행행 보정** — 게이트(`current_step_order >= 삭제순번 − 1`인 active 실행행이 있으면 거부) 하나가 날짜축 단발까지 닫아서 보정 코드가 통째로 없어졌다.

**검증에 쓴 사실(재질의 금지)** — `status/SCHEMA.md` 0802 절이 소유. UNIQUE 즉시 검사 / 참조 3건 CASCADE / `journey_step_snapshots` FK 없음.

**저장 검증 단일화** — 저장을 막는 판정은 `collectStepIssues` 하나가 소유하고 저장 버튼과 브리핑 모달이 같이 쓴다. 따로 두면 브리핑은 통과시키는데 저장은 거부하는 어긋남이 생긴다.

⛔ **남은 위험 1** — 등급 변동은 아직 방향을 못 가린다(§3-1). 화면 라벨을 "등급 상승"으로 바꾸지 않은 것은 의도다. 코드가 그렇게 동작하지 않는데 라벨만 바꾸면 화면이 거짓말을 한다.

#### §13-A-1 Codex 1라운드 반영 (2026-08-02) — 7건 전량 수용

같은 부류가 2회씩 나와 개별로 때우지 않고 뿌리로 묶었다.

| 뿌리 | 지적 | 고친 것 |
|---|---|---|
| **입력을 믿었다** | 활성 여정 추가 · enum 미검증 | 활성이면 추가 거부 + `STEP_TYPES`·`STEP_CHANNELS`·`STEP_DELAY_MODES` 런타임 화이트리스트 |
| **프롬프트를 경계로 믿었다** | 생성 모드가 혜택을 지어낼 수 있다 | `copy-benefit-detector.stripUnauthorizedBenefits` 신설 — 근거에 없던 토큰만 placeholder로 |
| **화면이 대용값을 읽었다** | 목적이 텍스트 상자에서 옴 · 트리거 라벨이 캠페인 목적 | `genObjective`로 생성에 쓴 목적 확정 저장 · `triggerLabelOf`가 `trigger_event`에서 라벨을 끌어옴 |
| **화면이 실제 발송과 다른 것을 보여줬다** | 스팸 테스트가 순수 본문 · 야간 문구 8시 | `buildAdMessageFront`·`buildAdSubjectFront`로 합성 후 검사 · **오전 9시**로 정정 |

**활성 거부가 닫은 것** — `last_pretest_passed_at`은 활성화 엔드포인트만 읽고 실행기는 보지 않는다(전수 grep). 자동 재검사 스캐너는 `next_run_at > NOW()`만 고르므로 지연 0으로 붙인 스텝은 검사 창 자체가 없었다. 활성 여정을 막으니 그 구멍과 함께 **추가 경로의 snapshot 생성 코드도 사라졌다**(활성화가 전 스텝 snapshot을 새로 만든다 — **성공 경로 기준. 그 생성이 실패해도 활성 상태가 남는 것은 활성화 경로의 별건 결함이다**). 스텝 추가는 문안 수정이 아니라 구조 변경이라 기존 규칙(`updateJourneyStep`: 활성 중에는 문안만)과도 다시 일치한다.

#### §13-A-2 Codex 2라운드 반영 (2026-08-02) — 4건 수용 · 1건 별건

**뿌리 A — 확인과 반영이 갈라져 있었다.** 활성화 라우트가 사전검사 마커를 SELECT로 확인한 뒤 `activateJourney`까지 오는 사이에 스텝이 추가되면(추가는 마커를 NULL로 만든다) 검사받지 않은 문안이 켜진다. 반대로 검사가 스텝을 읽은 뒤 추가가 끝나고 마커가 늦게 기록되는 순서도 있다.
- 마커 검사를 **활성화 UPDATE의 WHERE 안으로** 옮겼다 — 상한 검사를 원자화한 §11 Codex 5R과 같은 수. 확인과 반영이 한 문장이면 틈이 없다.
- 사전검사는 시작 시점의 `updated_at::text`를 잡아 두고 **그 값이 그대로일 때만** 마커를 쓴다(CAS). 바뀌었으면 통과로 기록하지 않고 `staleRevision`으로 돌려 다시 검증하게 한다. ⛔ 시각은 `::text` 원문 — JS Date 왕복은 µs를 절사한다(§11 커서 교훈의 재적용).

**뿌리 B — 집합이 실제 동작과 어긋났다(3회).** 출처를 실제로 검사·발송하는 쪽에서 가져오는 것으로 통일했다.
- 채널 화이트리스트에서 `email` 제거(사전검사·실행기가 처리하지 못한다) + **message 스텝은 채널 필수**(비면 검증을 건너뛴다).
- 혜택 차단 집합에 **단독 '무료'** 추가 — 금지 규칙에는 있는데 키워드 목록에 없었다. ⛔ `detectBenefits`에는 넣지 않는다(그쪽은 강조 지시를 만드는 다른 일이라 기존 생성 경로의 프롬프트가 통째로 바뀐다). 차단 전용 패턴으로 분리하고 `무료수신거부`는 법정 문구라 제외.
- 혜택 비교를 **정규화 키**로 — `5,000원` = `5000원`, `1만원` = `10000원`, `무료 배송` = `무료배송`. 표기만 다른 정상 혜택이 지워져 미편집 placeholder로 활성화까지 막히던 것을 닫는다. 값이 다르면(10% ≠ 20%) 여전히 지어낸 것으로 본다.

#### §13-A-3 Codex 3라운드 반영 (2026-08-02) — 3건 전량 수용

**뿌리 A 이어서 — 검증을 무효로 만드는 변경이 한 규약을 안 따랐다.** 2R의 CAS는 `updated_at`을 축으로 삼았는데, 정작 스텝 편집은 마커만 지우고 판을 안 올렸고 옵션 변경은 판만 올리고 마커를 안 지웠다. **규약을 하나로 못 박았다 — 검증을 무효로 만드는 변경은 `last_pretest_passed_at = NULL`과 `updated_at = NOW()`를 함께 한다.**
- 스텝 편집(`updateJourneyStep`)·A/B 변이(`bandit-optimizer`) → `updated_at` 동반 갱신. 안 올리면 검사가 옛 본문을 읽은 뒤 편집이 끝나도 CAS가 성공해 **지워 둔 마커가 되살아난다.**
- 옵션 PATCH → 마커 무효화 추가(목표 자동 종료 토글 2종만 예외 — 운영 중에도 허용되는, 발송을 줄이는 방향의 값이라 문안 검증 결과를 바꾸지 않는다). 회신번호·상한을 검증 뒤에 바꿔도 그대로 켜지던 길을 닫는다.
- 화면 = 활성화 모달이 상한을 저장하면 **바뀐 구성으로 다시 검증**한 뒤에 켠다(저장한 값은 로컬로 기억 — 안 그러면 저장·재검증이 반복된다).

**뿌리 B — 토큰 경계를 안 봤다.** 두 지적이 같은 뿌리라 정규식을 각각 덧대지 않고 추출 자체를 경계 기준으로 바꿨다.
- 금액·비율은 **경계를 포함해 통째로** 잡는다. `\d+만원`은 `1.5만원`에서 부분 문자열 `5만원`을 집어냈고, 근거에 `5만원`이 있으면 전혀 다른 혜택이 통과했다. 정규화도 소수를 처리한다(`1.5만원` = `15,000원`, `1.5%` ≠ `5%`).
- 단독 `무료`는 형용사 활용(`무료한/무료함/무료해`)과 띄어 쓴 법정 문구(`무료 수신 거부`)를 제외하고, **조사를 떼어** 뽑는다. `무료 체험 행사`와 `무료 체험을`이 다른 키가 되어 허용된 혜택이 지워지던 것을 닫는다.

#### §13-A-4 Codex 4라운드 반영 (2026-08-02) — 6건 수용 · 1건 승인 대기

**뿌리 A 종결 — 규약을 흩어 적은 것이 문제였다.** 2R·3R에서 "검증 무효화는 마커+판을 함께"를 세 곳에 각각 적었더니 라운드마다 빠진 경로가 나왔고(스텝 편집은 판 누락, 옵션은 마커 누락, 회신번호는 둘 다 누락), 문장이 따로 나가는 **사이**에 활성화가 끼어들 수 있었다.
**공용 문 하나로 바꿨다 — `withJourneyValidationReset(companyId, journeyId, fn)`.** 부모 여정을 `FOR UPDATE`로 잠그고, 변경과 무효화를 **한 트랜잭션에서 함께 커밋**한다. `updateJourneyStep`·`updateJourneyCallback`·변이 생성·**변이 삭제**가 전부 이 문을 지난다.
- 변이 **삭제**도 무효화 대상이다 — 마지막 변이를 지우면 검증기·snapshot이 검사한 적 없는 기본 스텝 본문으로 되돌아가는데 옛 통과 마커가 남아 있었다.
- 회신번호는 사전검사 입력이다(스팸 테스트를 그 번호로 넣는다). 통과 뒤 번호만 바꿔 켜던 길을 닫았다.

**뿌리 B 종결 — 혜택 판정에서 비교를 없앴다.** 스크러버가 3라운드 연속 지적을 받았다. 원인은 판정이 열린 문제였다는 것 — "AI가 쓴 혜택이 근거의 그 혜택과 같은가"를 문자열로 맞추려 하면 표기·조사·복합 금액·교차 조합이 끝없이 나온다.
**필요한 불변식은 훨씬 좁다 — AI는 구체 혜택을 쓰지 않는다.** 근거를 **사람이 쓴 원본 본문 하나로** 좁히고 판정을 "그 자리가 원본에 그대로 있었나"로 바꿨다. 목적 문장·앞 스텝은 근거가 아니다(그건 AI에게 숫자를 렌더링할 면허가 아니다).
- 정규화 키·허용 집합 교차 비교가 **통째로 사라졌다**. 남은 것은 찾아서 바꾸기뿐이다.
- 토큰이 아니라 **자리(span)**를 다룬다 — 교체는 원문 그대로의 자리를 바꾼다(토큰을 재조립해 되찾으면 `무료 체험`으로 `무료체험을`을 못 잡는다).
- 인접한 자리는 하나로 묶는다 — 금액과 종류가 따로 놀면 원본의 `10% 할인`·`5% 적립`이 없던 `5% 할인`을 열어준다.
- 상한 입력은 정수로 정규화한다(1.5를 넣으면 저장값 1과 입력값 1.5가 영영 달라 저장·재검증만 반복했다).

#### §13-A-5 Codex 5라운드 반영 (2026-08-02) — 4건 수용, 라운드 종결(상한 5)

**뿌리 A 마지막 조각 — 상태 판정도 잠금 안으로.** 공용 문이 행을 잠그긴 했는데 **status를 안 읽었다.** 호출부가 잠금 전에 읽은 status는 그 사이 활성화가 성사되면 옛 값이라, 운영 중 여정을 비활성인 줄 알고 고칠 수 있었다. 잠금 SELECT가 status를 함께 읽어 콜백에 넘기고, `updateJourneyStep`·변이 생성·변이 삭제의 활성 게이트를 전부 잠금 안으로 옮겼다. ("확인과 반영을 같은 잠금 안에"의 세 번째 적용 — 상한(§11 5R) → 마커(§13-A-2) → 상태.)

**옵션 PATCH의 필터 덮어쓰기.** 트랜잭션 밖에서 읽은 `trigger_filters` JSON을 요청에 필터 키가 없어도 통째로 다시 썼다. 상한만 저장하는 요청과 다른 탭의 휴면일수·포인트 임계 편집이 겹치면 **나중 요청이 앞 요청의 필터를 되돌리고**, 그 뒤 모달이 되돌아간 필터로 재검증·활성화해 의도보다 넓은 고객군에 나간다. **DB 안에서 병합**(`trigger_filters || $patch::jsonb`)하고 필터 키가 온 요청에서만 건드리게 바꿨다 — 읽고-고쳐-쓰기가 사라졌다.

**혜택 대조는 자리끼리.** 원본을 `includes`로 보면 출력의 `5% 할인`이 원본 `15% 할인` 한가운데 걸려 다른 금액인데도 통과했다. 원본도 **같은 파서로** 자리를 뽑아 자리끼리 비교한다(비교 키는 공백만 정리).
**토크나이저 경계 둘** — `무료\n배송`이 어디에도 안 걸려 그대로 나가던 fail-open(`무료\s*(배송|증정)`을 한 자리로) · `제1원칙`의 `1원`을 금액으로 잡아 정상 문구를 지우던 오탐(앞의 `제`, 뒤의 `칙` 경계).
⚠ **남긴 한계(불수용 1)** — 원본이 "무료배송은 제공하지 않습니다"여도 그 자리는 근거로 인정된다. 부정 문맥 판정은 다시 열린 문제라 여기서 풀지 않는다(그 길로 갔다가 3라운드를 썼다). 다듬기는 사람이 후보를 골라 넣는 흐름이라 그 자리에서 걸러진다.

⛔ **승인 대기 1건(critical, 이번 변경과 무관한 기존 결함)** — A/B 변이 생성 엔드포인트(`routes/ai.ts` variants)가 URL의 `journeyId`만 회사로 검증하고 `stepId`가 그 여정 소속인지 보지 않는다. 자기 회사 draft 여정 id + **다른 회사 stepId** 조합으로 남의 변이를 만들거나 덮어쓸 수 있다. 고치는 방법은 `createJourneyStepVariant`에 `journeyId`를 함께 넘겨 `step.journey_id = $journeyId` 조건을 거는 것.

**별건(2R 불수용 1)** — `activateJourney`가 status를 먼저 커밋하고 snapshot 생성 실패를 삼킨다(`journey-builder.ts` 활성화 절). 실패하면 활성화 시점 본문 고정과 2시간 전 재검사 안전망이 함께 사라진다. **이 세션 변경이 만든 것도 악화시킨 것도 아니고**(추가 경로에서 snapshot을 만들었어도 활성화가 다시 만들기 때문에 같은 실패 모드다), 활성화 트랜잭션을 재구성하는 별도 공사라 착수 판단은 Harold님 몫으로 남긴다.

### §13-5 §11-5가 남긴 화면 몫

- **매장 구매 정책 노출**(§11-C-2) — 활성화 브리핑·구매 트리거 카드에 "밤 11시까지 동기화" 문구 + 마지막 구매 동기화 시각.
- **신규 트리거 5종 카드**는 이미 카탈로그에 있다 — 그룹·정렬만 정리.
- **수신자 상한 입력이 전 트리거 필수**가 됐다(§11-D-1) — 활성화 모달에서 받는 자리를 만든다(지금은 거부 사유만 뜬다).
