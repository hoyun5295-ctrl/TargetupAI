node_modules/
dist/
dist-old/
dist-new/
.vite/
*.log

# 비밀·자격증명 (★2026-08-09 보안 진단 — 개인키 패턴 누락으로 키 커밋 가능했음)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa*
id_ed25519*
*.ppk
credentials.json
serviceAccount*.json

# 데이터·산출물
*.sql
# ★2026-08-16 예외 — 배포용 seed·마이그레이션 스크립트는 레포가 소유한다(마케팅 진단 seed v3에서
#   *.sql 전역 무시 탓에 tp-push에 조용히 빠져 서버에 파일이 없던 사고. 예외는 이 폴더 하나만)
!scripts/sql/*.sql
backups/
targetup-app/
_archive/
# ★2026-08-09 정정 — 이 줄이 UTF-16으로 깨져 git에 안 먹고 있었다(업로드 47건 커밋됨).
packages/backend/uploads/

# Bito Agent — repo 밖 ~/bito-install 에서 운영 (git 추적 금지)
/bito-agent
/bito-agent.service
/Bito-Agent-*.tar.gz
/agent-config.example.yaml
/agent-config.yaml
/checksums.sha256
/install.sh
/uninstall.sh
/README.install-linux.md
/VERSION.txt
