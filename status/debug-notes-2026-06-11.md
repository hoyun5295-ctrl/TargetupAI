# 디버깅 배치 — 2026-06-11 (다음 세션 일괄 작업용)

> ★ 수정 설계서 = `status/DEBUG-FIX-DESIGN-2026-06-11.md` (작업 설계·우선순위·확정 SQL/로그·검증 시나리오 — 다음 세션은 두 문서 세트로 착수)
> 작성: 2026-06-11 비토. 인비토 담당자 디버깅 리포트를 건별로 정리.
> 규칙: 이 문서만 읽고 다음 세션이 바로 작업 가능해야 한다. 수정 착수 전 Harold님 승인 필수(6원칙). 코드 수정은 본 세션에서 하지 않았음.

---

## 건 1. 폐기된 자동발송의 '마일리지 테스트'가 매주 실행·발송됨 [긴급 — 다음 실행 06-16(월) 10:00 추정]

### 현상 (보고 그대로)
- 예전 자동발송 테스트 때 걸어둔 '마일리지' 문안 자동생성이 매주 발송되고 있음 (보고는 "화요일 오전 10시").
- 전부 결번이라 실패 처리되지만 담당자 테스트 문자 + 발송 이력이 계속 쌓임 → 삭제 요청.
- 기대 동작: 이전 자동발송 세팅 이력 초기화.

### 실측 (슈퍼관리자 발송 관리 캡처)
- 주식회사 인비토 / sgbaek — `[자동] 마일리지 테스트 #3/#6/#9/#12/#15`, 05.12·05.19·05.26·06.02·06.09 전부 **월요일 10:00** (보고의 "화요일"과 다름 — 요일은 실측이 우선), SMS 254건 성공 0 실패 254, 상태 완료, 유형 표기 '수동'.
- 담당자 폰(1800-8125 발신): `[AI문안생성]`(생성 단계 알림), `[스팸테스트]`(발송 당일 08:00 결과 통과) 문자 수신 중.

### 원인 (코드 확정)
1. `utils/auto-campaign-worker.ts` — app.ts 안 매 1분 setInterval. `status='active' AND next_run_at <= NOW()` 캠페인을 실행하고 실행 후 `next_run_at`을 다음 주기로 갱신(L1135-1136) → **weekly는 영원히 반복, 자연 소멸 없음**.
2. D188 Phase 2-B-4(5-21) 자동발송 영구 폐기 = **POST 신규 생성만 410 Gone**. `routes/auto-campaigns.ts:295` 주석 "운영 데이터 보존 + worker 유지 (기존 활성 자연 소멸)" — weekly에는 자연 소멸이 성립하지 않는 설계 구멍. 헤더 메뉴도 제거되어 **고객사가 스스로 중지할 화면이 없음**.
3. 워커 4단계 라이프사이클(파일 머리주석): D-2 23:00 AI 문안 생성+담당자 알림 → D-1 사전알림 → D-day 2시간 전 스팸테스트+알림 → D-day 발송. 담당자 문자가 매주 여러 통 나가는 이유.
4. PUT `/:id`(L560)·DELETE `/:id`(L876)는 라우트에 살아 있음(410은 POST만) — API로는 중지 가능하나 화면이 없음.

### 미확정 (다음 세션 확인)
- 캠페인명 #N이 3씩 증가하는 산식 — `auto-campaign-worker.ts:804` `[자동] ${name} #${runNumber}`의 runNumber 출처 정독.
- 목록 '유형: 수동' 표기 경위 (자동 캠페인인데 수동 표기).

### 영향 범위 (전수 확인 SQL — 인비토만의 문제인지)
```sql
SELECT id, company_id, campaign_name, schedule_type, schedule_day, schedule_time,
       next_run_at, last_run_at, total_runs, status
  FROM auto_campaigns
 WHERE status = 'active'
 ORDER BY next_run_at;
```

### 조치안 (승인 후 다음 세션)
1. [긴급] 인비토 '마일리지 테스트' 캠페인 종료 UPDATE — 06-16(월) 10:00 전 처리 못 하면 또 발송됨. (이른 처리가 필요하면 위 SQL로 id 확인 후 `UPDATE auto_campaigns SET status='paused', updated_at=NOW() WHERE id='<id>';` 한 줄로 즉시 중지 가능 — Harold님 판단 시점에 실행)
2. [근본] 폐기 기능인데 active가 계속 도는 구조 정리 — 전 고객사 active 일괄 종료 + worker에 폐기 가드(또는 워커 등록 해제). "운영 데이터 보존"은 유지(레코드 삭제 X, status만 변경).
3. [Harold 결정 필요] "이력 삭제" 요청 범위 — 슈퍼관리자 발송 관리 목록의 과거 [자동] 캠페인 이력은 정산·감사 근거(0610~0611 교훈)라 물리 삭제는 신중. 보존+캠페인만 종료를 권장안으로 보고 예정.

---

## 건 2. 알림톡 템플릿 검수 알림 — 한 달 지연 후 6/11 11:07 일괄 수신 + 카카오 반려 건 미알림

### 현상 (보고 그대로)
1. 5/11~15 등록·검수요청 템플릿들의 검수 현황 알림이 안 오다가 2026-06-11 오전 11:07 한꺼번에 문자 수신 (빨간 박스 — 승인 5건).
2. '인비토' 프로필 반려 템플릿(문의 접수 요청)은 알림이 오지 않음 (파란 박스).
- 기대 동작: 승인 시 즉시 알림 + 반려 시에도 알림.

### 실측 (캡처)
- 승인 5건(B_IV_013_02_79738/79773/79703/79955/79704) 업데이트 시각 전부 **2026-06-11 11:07:25** 동일.
- 반려 1건(문의 접수 요청, Tmpm1qj80y5hdvj54iq, 카카오 반려)과 '버튼 3개'(팝폰)는 **06-10 16:21** 갱신.
- 담당자 폰에 6/11(목) [알림톡 템플릿 승인] 5통 수신.

### 원인 ① 지연 일괄 수신 — 코드로 확정 (신규 버그 아님, 기수정 사고의 밀린 알림 방출)
- `utils/alimtalk-jobs.ts:260` "★ 2026-06-10 정정" 주석이 본 건 그 자체: D217(5-26)이 template_code를 진짜 카카오 코드(B_IV_...)로 바꾼 뒤 IMC GET을 code로 호출 → 4013(찾을 수 없음) **조용 실패** → 5분 주기 `syncPendingTemplatesJob` 동기화가 죽어 있었음. 06-10에 template_key 우선으로 수정.
- 수정 배포 후 동기화 재가동 → 밀려 있던 5/11~15 승인 건들이 06-10 16:21 / 06-11 11:07에 일괄 갱신 + `alarm_notified_status IS NULL`이라 일괄 알림 발송. **승인 자체는 카카오에서 이미 옛날에 난 것이고, 알림만 묶여 있다가 풀린 것.**
- 재발 조건: 해소(코드 확인). 두 타이밍(16:21/11:07)으로 나뉜 이유는 배포 시각·`last_synced_at` 5분 가드·LIMIT 100 순차 처리 조합 — PM2 로그 `pendingTemplateSync`로 대조 가능(필수는 아님).

### 원인 ② 카카오 반려(KREJ) 미알림 — 미확정 (후보 2개, 확인 1회로 확정 가능)
- 코드상 반려 알림은 **설계되어 있음**: `toTerminalStatus`가 KREJ→'REJECTED'(알림 대상, L372), 폴링 SELECT에 `KREJ AND alarm_notified_status IS NULL` 포함(L243), 발송 실패 시 5분마다 재시도 설계(D188 fix).
- 모순: 같은 회사(인비토)의 승인 알림 5통은 정상 수신 → 수신자(kakao_alarm_users)·콜백 부재 가설과 충돌.
- 후보: (a) `notifyTemplateInspectionResult` 예외 또는 count=0이 반복 중 (로그에 `alarmNotify skip`/`logErr` 5분마다 반복돼야 함) (b) `alarm_notified_status='REJECTED'` 선기록 잔존(그 경우 폴링 SELECT에서 제외돼 영구 미알림).
- 확인 방법 (다음 세션 첫 단계):
```sql
SELECT template_name, status, alarm_notified_status, reject_reason, last_synced_at, updated_at
  FROM kakao_templates
 WHERE template_code = 'Tmpm1qj80y5hdvj54iq';
```
```bash
pm2 logs --lines 2000 --nostream | grep -E 'alarmNotify|pendingTemplateSync' | tail -40
```
- 영향 범위 전수 (공통 결함인지 데이터 1건 문제인지):
```sql
SELECT count(*) FROM kakao_templates
 WHERE status IN ('KREJ','REJECTED') AND alarm_notified_status IS NULL;
```

### 보고자 회신용 정리
- 지연 건: 검수 상태 동기화가 5/26~6/10 멈춰 있던 결함이 원인이고 6/10에 수정 완료 — 이후 등록 건은 5분 내 반영·즉시 알림.
- 반려 알림: 기능은 존재하며 이번 미수신 건은 원인 확정 후 회신.

---

## 건 3. [에이치피오 87,014건 사고 후속] 취소 표시로 굳은 캠페인의 발송 실적 정정 — 청구(돈) 직결

### 현상 (보고 그대로)
- 6/11 오전 10시 예약 건을 취소했으나 실제 발송됨(= 오늘 87,014건 사고 그 건). 발송이 진행됐으므로 그 데이터로 업체에 청구해야 하는데, 사용자·슈퍼관리자 모두 성공 건수가 0으로 잡힘 → 발송된 내용으로 정정 요청.
- 발송결과 상세에서 통신사 이력(성공)은 간략히 보이나, 엑셀 다운로드는 "취소된 이력"이라 데이터가 출력되지 않음.
- 캠페인정보의 발송일시가 등록일시와 동일하게 표시 → 실제 발송일시로 수정 필요.
- 슈퍼관리자도 성공 0 → 마감 시 금액 차이 발생 우려, 사전 정정 요청.
- 기대: 발송 완료 건으로 정정해 정상 청구. 업체와는 "성공 건 과금 후 100만원 할인(차감)"으로 합의 완료.

### 실측 (캡처)
- `수동(취소)` hpio "(광고) [덴프스] 선착순! 트루바이타민…" 등록 06.10 16:56, ~~06.11 오전 10:00~~ (예약취소) 표시, LMS 87,014건, 성공 0 / 실패 0 / 대기 87,014, 0%.
- 상세 모달: 전송건수 87,014 / 성공·실패 0/0 / 등록 06.10 16:56 / **발송일시 06.10 16:56(등록과 동일 — 문제)** / 통신사별 분포 SKT 39,162·KT 22,646·LG U+ 21,999 (MySQL 통신사 결과는 존재).
- 발송 내역 모달: "취소된 캠페인입니다. 대기중이던 메시지는 취소 시 삭제되었습니다." 안내 + 1/8702 페이지.

### 원인 (맥락 확정 — 오늘 사고의 데이터 후유증)
- 0611 사고: 취소 DELETE가 사용자 라인 테이블을 못 봐서 0건 삭제 + 검증 없이 cancelled 표시 → 실발송. 근본 fix 5겹은 코드 완료(미배포 시점에 발생).
- 캠페인 상태가 'cancelled'로 굳어 있어: ① 성공 집계 워커·통계가 취소 캠페인을 집계 대상에서 제외 ② 발송내역/엑셀 API의 취소 가드("취소된 캠페인" 분기)가 데이터 차단 ③ 정산 마감 집계에서도 빠짐.
- 발송일시=등록일시 표시는 0609 세션에서 확정한 "예약 건 sent_at이 생성 시점에 기록되는" 문제와 동일 계열.

### 처리안 (다음 세션 — 데이터 정정 + 표시 정정, Harold 승인 후)
1. 캠페인 식별: `SELECT id, status, total_count, sent_at, scheduled_at, send_config FROM campaigns WHERE company_id = (에이치피오) AND created_at::date = '2026-06-10' AND total_count = 87014;` (0611 사고 메모리의 재예약 건 bfee8e09 대조)
2. MySQL 실측 재집계: 에이치피오 전 라인 합집합 `_202606` 월별 이력에서 본 건 성공/실패 카운트 확정 (실측이 청구 근거).
3. 캠페인 status를 발송 완료로 정정 + sent_at = 실제 발송시각(06-11 10:00) + 성공/실패 counts 실측값 UPDATE → 통계·엑셀·정산 가드 자연 해제 확인.
4. 후속 점검: 취소 가드("취소된 캠페인" 안내)가 status 기준이므로 정정 후 상세/엑셀 출력 재확인. 슈퍼관리자 마감 금액 재확인.
5. [주의] 사고 감사 기록과의 일관성 — 정정 사유를 audit-log(ceo 전용)에 남기는 것 포함.

---

## 건 4. [폴라초이스] 예약 차감 15,697건 vs 실제 전송 15,470건 — 227건 차감만 되고 미환불 (돈)

### 현상 (보고 그대로)
- 충전 후 잔액이 이상하다는 폴라초이스 문의 → 차감 후 미환불 문제 확인.
- 6/9 발송: 예약 시 "중복제외 후 15,697건 예약" 안내 → 실제 발송 결과 총 15,470건. 차이 227건의 사유를 알 수 없음(업체 설명용 필요).
- 차감 내역: 예약 시점에 15,697건 전액 차감(-949,668.5원, 건당 60.5원) → 실제 전송 15,470건 → **227건은 차감만 되고 환불 없음**.
- 기대: 대상 건수와 전송 건수가 다른 사유 설명 (업체 안내용).

### 실측 (캡처)
- 잔액 변동: 06.08 14:45 "MMS 15697건 발송 차감" -949,668.5원 / 06.09 오전 "발송 실패 환불 (sweep)" 다수(+1,391.5 = 23건 등) — **실패분 환불 sweep은 동작 중**.
- 슈퍼관리자 엑셀: 2026-06-09 폴라초이스 MMS 직접 — 대상 15,697 / 전송 15,470 / 성공 15,122 / 실패 348 / 대기 0.
- (참고 캡처: 6/12 09:00 예약 폼 — 총 15,713 = 발송 15,697 + 중복제외 16. 차감 기준은 중복제외 후 수치.)

### 원인 구조 (다음 세션 코드 확정 필요 — 후보)
- 차감은 예약 등록 시점에 "중복제외 후 전체(15,697)" 기준. 실제 큐 적재는 발송 시점 워커가 수행하며 추가 제외(수신거부·결번/형식 오류·080 거부 등)로 15,470건만 적재된 것으로 추정 → **적재 단계 제외 227건은 MySQL에 행이 없어 "발송 실패 환불 sweep"(실패 행 기준)에 영원히 안 잡힘 = 미환불 구멍**.
- 확인 지점: 직접발송 예약의 prepaidDeduct 시점(등록 시) / direct-send-worker 적재 시 제외 사유별 카운트 로그 / 적재 제외분 환불 로직 존재 여부(추정: 없음).
- 업체 설명용 사유 분해 SQL(다음 세션): 15,697 - 15,470 = 227건의 구성(수신거부 N + 형식오류 N + 기타 N)을 캠페인 적재 로그/수신거부 테이블 대조로 산출.

### 처리안 (다음 세션)
1. [환불] 폴라초이스 227건 × 60.5원 = 13,733.5원 환불 (실측 확정 후 정확액).
2. [근본] 적재 시점 "차감 건수 - 실제 적재 건수" 차액 자동 환불(또는 차감 자체를 적재 확정 시점으로 이동) — 6원칙 ③ 이중 진실 안전망 사상으로 차감↔적재 자동 대조 sweep 추가.
3. [전수] 같은 구조의 과거 미환불 전수 SQL — 캠페인별 (차감 건수 vs MySQL 적재 건수) 차이 > 0 목록.

---

## 건 5. [시세이도] 대상 2,885 vs 전송 2,895 + 화면별 수량 불일치 (985↔978 / 1,906↔1,903) — 추가 이미지 수신 대기 중

### 현상 (보고 그대로)
- 6/9 시세이도 발송: 슈퍼관리자 발송통계에서 캠페인 3건 대상 2,885건 / 전송 2,895건 — **전송이 대상보다 많음**.
- 슈퍼관리자 캠페인관리: 캠페인정보 총 건이 985·1,906으로 보이나 상세 조회 데이터는 978·1,903.
- 사용자 발송결과: 상단 전송 건수(978/1,903)와 캠페인 정보 전송건수(985/1,906)가 상이. 엑셀은 상세(978/1,903)와 동일.
- 기대: 실제 전송 건 기준으로 내역 정리.

### 실측 (캡처)
- 6.8 15:51 등록 MMS 캠페인: 상단 전송/성공 1,903/1,866 ↔ 캠페인정보 전송건수 1,906 (차이 3). 발송일시 표기는 정상(예약 06.09 10:01~02 발송).
- 6.8 16:41 등록 MMS 캠페인: 상단 전송 978 ↔ 캠페인정보 전송건수 985 (차이 7). 발송 상세 내역 총 978건.
- 슈퍼관리자 엑셀: 2026-06-09 시세이도 MMS 직접 3캠페인 — 대상 2,885 / 전송 2,895 / 성공 2,818 / 실패 77.

### 원인 구조 (건 4와 같은 뿌리 — 출처 불일치)
- '캠페인정보 전송건수'(985/1,906) = campaigns 테이블 기록값(등록/차감 시점 수), '상단·상세·엑셀'(978/1,903) = MySQL 실측 행 수 — **적재 시 제외분(7건/3건)이 캠페인 기록값에 보정되지 않아 화면마다 다른 출처를 읽음**.
- 추가 캡처(슈퍼관리자 캠페인 목록, 6/9 시세이도 4건)로 합산 조합이 계산으로 확정됨:
  - 캠페인 목록: 985(성공 948+실패 37) / 1,906(성공 1,866+실패 40) / 4 / 7 — 캠페인 행은 성공+실패=총건수로 떨어지게 굳혀져 있음(실패가 total-성공으로 계산된 값. MySQL 실제 실패 행은 30/37 — 즉 **적재 누락 7건/3건이 '실패'로 계산돼 캠페인에 기록**되고 MySQL엔 행 자체가 없음).
  - 발송통계 엑셀: 대상 2,885 = **978+1,903+4 (MySQL 실측 합)** / 전송 2,895 = **985+1,906+4 (캠페인 기록 합)** — 두 컬럼이 서로 다른 출처를 합산. 폴라초이스 행(대상 15,697=기록 / 전송 15,470=실측)과 출처 방향이 반대라 **컬럼-출처 매핑 자체가 뒤섞여 있음** → 통계 쿼리의 컬럼별 출처 정의 확정 필요.
- 시세이도는 0610 failed 오판·재대조(reconcile) 작업 대상 회사였음 — 그 배포 상태와 본 건 수치의 관계도 다음 세션에서 대조.

### 처리안 방향 (건 4와 통합)
- 수량의 단일 진실 원칙 확정: "대상(차감 기준) / 적재(실제 큐) / 전송(통신사 결과)" 3층을 분리 정의하고, 모든 화면·엑셀·통계·정산이 같은 층을 같은 출처에서 읽도록 통일 + 적재 제외분은 차감 환불로 정산 일치. (Harold 명시 — "선불차감이랑 수량 다른거 한 번에 끝내자")

---

## 전체를 관통하는 근원 진단 (다음 세션 작업 설계의 출발점)

5건 중 1·3·4·5는 한 가지 구조 결함의 변주다: **기록 시점 값(등록·차감·취소·폐기)을 진실로 쓰고, 실측(MySQL 행·통신사 결과)과 자동 대조하는 장치가 없다.**
- 건 1: "폐기했다"(기록)지만 워커는 active를 계속 실행(실측) — 종료 효과 검증 부재.
- 건 3: "취소했다"(기록)지만 실제 발송됨(실측) — 취소 효과 검증 부재(0611 사고, fix 코드 완료) + 굳은 기록의 사후 정정 절차 부재.
- 건 4: "차감했다"(15,697 기록)지만 적재는 15,470(실측) — 차액 자동 환불 부재.
- 건 5: 화면·통계마다 기록값/실측값을 제각각 읽음 — 출처 단일화 부재.
CLAUDE.md 6원칙 ②(효과 검증 후 성공 표시)·③(이중 진실 = 안전망 워커)이 정확히 이 답이다.

### 근원 수정 설계 (다음 세션 — Harold 승인 후 착수)
1. **수량 3층 정의 확정**: ① 차감 기준 수(등록·중복제외 후) ② 적재 실측 수(MySQL INSERT 행) ③ 통신사 결과 수(성공/실패). 모든 화면·엑셀·통계·정산이 "어느 층을 보여주는지"를 명시하고 같은 층은 같은 출처에서만 읽는다. (통계 쿼리 컬럼-출처 매핑 전수표 작성 — 건 5)
2. **차감↔적재 자동 대조 sweep**: 발송 시작 후 캠페인별 (차감 건수 - 적재 건수) > 0이면 차액 자동 환불 + 캠페인 카운트 보정 + 사유 분해(수신거부/형식오류/기타) 기록. 과거분 전수 SQL로 일괄 정산. (건 4 근본 + 건 5의 7건/3건 자동 해소)
3. **상태 변경의 효과 검증 일반화**: 폐기·취소·종료류는 "실측 잔존 0 재확인" 후에만 종결 표시(0611 fix 사상을 자동발송 폐기에도 적용 — active 일괄 종료 + 워커 가드). (건 1)
4. **굳은 기록 정정 절차**: 사고/지연으로 굳은 캠페인(취소·failed)을 실측 기준으로 재집계해 정정하는 관리자 절차(+audit 기록). (건 3 즉시 적용, 0610 reconcile과 통합)

## 다음 세션 작업 순서 제안
1. [긴급·데이터] 건 1 인비토 자동발송 종료(6/16 월 10:00 전) + 건 3 에이치피오 정정(청구 시한) + 건 4 폴라초이스 227건 환불.
2. [근원·코드] 위 근원 설계 1·2·3 구현 (건 4+5 통합이 본체).
3. [확인] 건 2-② 반려 미알림 — SQL 1 + 로그 grep 1로 확정 후 fix.
4. [전수] 과거 차감-적재 차이 미환불 전수 + 통계 출처 불일치 화면 전수.

## 공통 메모
- 건 1·2 = 인비토(첫 연동 고객), 건 3 = 에이치피오(오늘 사고 후속·청구 직결), 건 4 = 폴라초이스(돈·미환불), 건 5 = 시세이도(표기 신뢰).
- 건 1은 매주 반복 발송이라 시한 있음(06-16 월 10:00). 건 2-①은 종결(기수정), 2-②는 확인 2개로 확정 가능.
- 추가 디버깅 발생 시 "건 6..."으로 이어서 기록.

---

## 종결 기록 (2026-06-11 세션 — P0 데이터 + P1 코드)

- **건 1 종결**: active 4건 전부 인비토(다른 회사 0) — monthly 3건(VIP·테스트·테테스트, 6/13~6/14)도 잔존이라 첫 시한은 6/13(금) 10:00이었음. 일괄 paused + 잔존 0 검증. 워커는 P1에서 영구 가드(AUTO_CAMPAIGN_RETIRED) 봉인. 이력 보존.
- **건 3 종결**: 사고 캠페인 5205af86 실측 = 이력 84,886(성공 84,259/실패 627) + 큐 잔존 2,128 삭제 = 87,014 오차 0. 잔존은 죽은 행이 아니라 에이전트가 계속 내보내던 살아 있는 행(확인 중 1건 추가 발송 실측). 캠페인 completed/성공 84,259/sent_at 6/11 10:00 정정 + audit 2행. 청구 = 성공 84,259건(합의: 과금 후 100만원 차감). bfee8e09(18:00 재예약)는 이력 0+큐 0 = 이중 발송 없음(삭제 주체 기록 없음 — audit 0행).
- **건 4 종결**: 환불 불필요. MySQL 실측 15,697행 전량 존재(적재 누락 0) = 성공 15,135 + 실패 562. sweep이 6/9~6/11 562건 전액 자동 환불 완료 — 잔액 차감 = 성공 15,135 × 60.5 정확 일치. "전송 15,470/대기 0"은 sync가 sent_count를 결과 도착 합으로 덮은 표시 오염(건5와 같은 뿌리, P1에서 제거).
- **건 5 종결(실측 확정)**: 시세이도 실측 — target(차감) 978/1,903이 적재 실측과 일치(미적재 0·돈 차이 0). 985/1,906은 sync가 sent_count를 success+계산fail(37/40)로 덮은 오염값(948+37=985 ✓ 1,866+40=1,906 ✓). 발송통계 산식 자체는 원칙과 일치(대상=target/전송=sent_count/대기=차) — 데이터 오염이 원인이었고 P1의 덮어쓰기 제거로 재발 0. 굳은 2건은 실측(978=948+30 / 1,903=1,866+37)으로 수동 정정. 직전 보고의 "시세이도 7건분 과소 환불" 추정은 실측으로 기각(환불 정확 — 미적재 시나리오는 D231 톤28형 잠재로 산식 가치 유지).
- **건 2-② 종결**: 'REJECTED' 선기록은 5월 구코드(D188 이전 무조건 마킹) 잔존으로 확정 — 6월 인비토 알림 6행 전부 승인(반려 알림 발송 이력 0, 6/10 16:21 155바이트 행은 '버튼 3개' 승인). 현행 코드는 발송 성공 시에만 마킹이라 재발 경로 차단 상태. alarm_notified_status NULL 정정 → 5분 폴링 자동 재발송(반려 사유 포함). 전수: 반려 상태 템플릿이 시스템 전체 본 건 1건뿐. 참고: 템플릿 소속 회사 = 테스트계정2(수신자 박성용 010****5868).
- **건 6 (P3-1에서 신규 발견·종결)**: sent_count > target 시그니처 16건 — toun28 6/11 캠페인(차감 7,171 vs PG 성공 7,514)을 실측한 결과 **큐 92 + 이력 7,079 = 정확히 7,171, 전원 고유** = 발송 정상. PG +349는 **큐→이력 이동 중 이중 집계**(에이전트의 이력 복사~큐 삭제 사이에 집계가 양쪽에서 같은 행을 잡음)가 기록된 값. 부풀린 success+fail ≥ target이 되면 sync 재집계(target > success+fail)에서 영구 제외돼 굳음 — 0609 굳히기 계열의 새 변형. 자가 치유: 0610 reconcile 워커가 completed+result_final + 발송+24h에 실측 재집계로 교정(toun28은 6/12 19:46 이후 자동). 과거 굳은 13건(테스트계정2 11·메트로시티 1·시세이도 5/29 1)은 result_synced_at NULL + result_final=true 리셋으로 reconcile 1시간 내 자동 교정. 시세이도 6/9 2건은 실측 수동 정정(978=948+30 / 1,903=1,866+37). **근본 구조 fix(같은 세션 — Harold "정합성 100%" 지시 직후 구현, 미배포)**: 이중 집계가 기록되는 것 자체를 차단 — 결과(성공/실패)는 월별 이력에서만, 대기는 라이브 큐의 대기 코드에서만 세는 단일 산식 `smsCampaignCountsSafe`(sms-queue) + 순수 분리 `sms-table-split.ts` 신설. 이력은 append-only라 과대 집계가 구조적으로 불가능하고, 이동 순간의 일시 과소는 다음 사이클에 자연 수렴(굳히기 조건은 과대에서만 발동하므로 영구 굳음 소멸). 소비처 6곳 전수 교체(sync AI·직접 / cleanup / reconcile / refund-sweeper / aggregateSmsCountsByCampaign=화면·통계 공유) + 채널 분리 집계(aggregateSmsChannelSplitByCampaign)도 라이브=대기 한정. smsBatchAggByGroup 외부 직접 호출 잔존 0(grep 증거). tsc 0 · sms-table-split 8/8 · refund-calc 8/8. 배포 후 = 16건 일괄 리셋(result_synced_at NULL+final=true)이면 reconcile이 정확 산식으로 1시간 내 자동 교정(수동 counts UPDATE 불요), toun28은 발송+24h 도달 시 자동.
- **P1 코드(미배포)**: ①환불 단일 산식 calcRefundDue CT(차감 실측−성공−대기, 8/8 테스트) + sweeper 적용(send_phase 가드) ②전 bulk 라인 합집합 getAllBulkSmsTables — 큐 6곳·집계 10곳 자동 보강 ③billing 전 라인 + 신 직접발송(campaign_runs 미생성) UNION 포함(정산 누락 2축 fix) ④cancelled-queue-sweeper 픽업 행 9999+재카운트 검증 ⑤취소 시 counts 덮어쓰기 제거(실측 보존) ⑥worker 제외 사유 send_config.exclusions 기록 ⑦자동발송 워커 영구 가드 ⑧코드 92 라벨. tsc 0 / refund-calc 8/8·sms-channel-split 13·direct-send-spec 12 통과(line-merge는 DB env 필요 스크립트 — 기존과 동일).
