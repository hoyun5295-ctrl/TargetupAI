# 완료·회전 작업 원문 — 2026-09

> STATUS.md §2 에서 회전해 내린 세션 카드 원문. 인덱스 = [INDEX.md](INDEX.md).

### ⏱ 이전 세션 — 2026-09-16 (모바일 DM 블록 조립 · **정산 접수 2건** · **단축 URL 한글 주소**)

> 다음 세션이 3줄로 이어받는 자리. **직전 세션 것만 둔다**. 경위·상세는 BUGS·기능 문서 소유. **설계·단계 = [블록 조립 설계서](../docs/2026-09-16-dm-block-assembly-design.md)** · 쪽 템플릿·효과·칩 상설 = [FEATURE-CATALOG-DM §7-2](../docs/FEATURE-CATALOG-DM.md).

| 항목 | 상태 |
|---|---|
| 한 일(1세션 · 모바일 DM) | 블록 조립 신설 · 쪽 템플릿 4종 + 합성 · 넘김 효과 3종 · 목록 리스트형 · 시작 화면 입구 통합 = **[설계서 0916](../docs/2026-09-16-dm-block-assembly-design.md)이 전부 소유** · 접수 [B-0916-1·2·3](BUGS.md) |
| 한 일(2세션 · 정산·단축URL) | 계정별 장에 테스트·스팸 합류(0726 결정 개정) · 수기 부가서비스 수량 컬럼화(ALTER 3 실행완료) = **[FEATURE-BILLING §2-21·§2-22](../docs/FEATURE-BILLING.md)** · 단축 URL 한글 주소 + 100→20크레딧 · 접수 [B-0916-4·5·6](BUGS.md) |
| 한 일(3세션 · 이메일 캠페인 8건) | 편집기 속성 6종 발송 HTML 미반영 정정(헤더 형태·브랜드명 / 히어로 오버레이 / 매장 지도 / SNS 표시 방식·아웃룩 면 / 푸터 수신거부 = 법이 소유) + 헤드라인 강조 중복 + **구조 = 공용 편집기 속성 자동 수집 전수 대조 게이트**(같은 부류 네 번째 재발 차단) · 경위 = [B-0916-7·8](BUGS.md) · 교훈 = [LESSONS_FRONTEND](lessons/LESSONS_FRONTEND.md) · DDL 0 |
| 지금 상태 | backend 322파일 4,973건 · tsc 0(백·프) · **배포완료**(DM `d8e8c7d9` / 정산 `572e133a` + ALTER 3 실행완료 / 단축URL `5b6ef1cd`) · **이메일 8건 = 배포 대기** · Codex = 정산 2R·단축 1R·이메일 법정 footer 2R 전부 `approve` |
| 다음 한 수 | **실측만 남았다.** 정산 = 시세이도 9월 장 삭제·재발행(계정 장에 테스트·스팸 · 수기 항목 재입력 1줄) · 단축URL = 한글 주소 1건 발급(20크레딧 차감 · 재발급 409 + 미차감 · 접속 이동) · DM = 시작 화면·블록 창·목록 육안 + 쪽 합성 1건 + 효과 폰 확인 → [0915 인계 §5-2](../docs/2026-09-15-session-handoff.md) 잔여 |
| 미결 | **AI 자동제작에 쪽 템플릿 배정**(설계서 §7 6단계 · 돈 경로라 Codex 적대검토 + 실측 1건 뒤 착수) · PC 책 펼침의 상품 칩(모바일 무대만 구현) · 빠른 시작 생성 API 폐기 범위(사용량 SQL) · 목록 상태 라벨이 검수 축을 본다(발행 DM 도 "임시저장") |
| 배운 것 | **입구가 겹치면 먼저 짚고 한 번에 정리한다**(라이브러리·직접 제작·시나리오를 세 번에 나눠 고쳐 Harold 가 세 번 지적) · **창과 화면은 표면 색이 다르다**(흰 모달에 다크 전제 색 = 글씨 소멸) · **품질 문제가 프롬프트가 아니라 재료 부재일 수 있다**(빠른 시작 = 사진 0장) · **수량을 행 수로 표현하면 화면마다 줄이 늘어선다**(+같은 판정이 두 집합으로 나뉘면 엇갈린다 · 부품이 다 있어도 입구가 없으면 죽은 기능이다 · 주석에 복사한 값이 먼저 낡는다 — 넷 다 [[feedback_never_express_a_quantity_as_row_count]]·[FEATURE-BILLING §2-21·§2-22](../docs/FEATURE-BILLING.md) 소유) |

### ⏱ 이전 세션 — 2026-09-04(2) (비토 게이트웨이 접수 4건 + 상용 관리 2메뉴 통합)

> 다음 세션이 3줄로 이어받는 자리. **직전 세션 것만 둔다**. 경위·상세는 SoT 문서·memory 소유.

| 항목 | 상태 |
|---|---|
| 한 일 | **서수란 접수 4건 + 통합 A안 3단계 전량 구현**(SoT = [docs/FEATURE-BITO-GATEWAY.md](../docs/FEATURE-BITO-GATEWAY.md) §12). ①식별코드 "연장" 제거 ②계정 발급 고객사 검색형 선택(신규 공용 `SearchSelect`) ③**회선별 발송 통계** `GET /admin/stats/lines` + 패널 + 엑셀 7시트 ④**접속·동작 이력 원장** `account_access_event`(056) + writer 2곳(Go gRPC·Node client-auth) + 조회 API 2종 + 계정 줄 "마지막 접속"·이력 모달 ⑤**"고객사"·"사업자·계정" 2메뉴 통합**(같은 `reseller`를 두 이름으로 부르던 구조. `AccountsPage.jsx` 삭제) |
| 지금 상태 | **전량 배포 완료**(커밋 `f9f4f0d` · 2026-09-04 23:05~23:15). 056 → api 7파일 → Gateway 바이너리 → dist(45 files, manifest `9a3171093d48`) 각 `GW_DEPLOY_OK`. **접속 이력 실측 통과** = `account_access_event` 31행, Agent 4대 connect/disconnect 짝 일치, REST 요청 3건, `remote_ip` 포트 없음. 신규 endpoint 2종 401(마운트 확인) |
| 다음 한 수 | 화면 육안 5가지 = 사용량 통계 "회선별 전송" 패널 · 고객사 화면 "마지막 접속" 열과 이력 모달 · Agent 행 작업 5종(이력·수정·결과코드·토글·삭제) · 고객사 행 작업 4종(상세·수정·충전·삭제) · 좌측 메뉴에 "사업자·계정"이 사라지고 "고객사" 하나만. 그 뒤 회선별 표와 상위단 청구 수량 대조 1건 |
| 미결 | Go `internal/agent/bootstrap` 테스트 2건은 Windows 권한 모델 실패(내 변경과 무관 · WSL 통과 기왕 확인) · 접속 이력 보존 기간 정책 미정(현재 무한 보존) |
| 배운 것 | **정적 렌더 테스트는 폭을 못 본다** — 계약 25건이 다 통과했는데 운영 화면에서 "삭제"를 못 눌렀다. grid 항목이 폭에 묶이지 않으면(`min-width:auto`) 표가 페이지를 밀어내 안쪽 `overflow-x` 가 발동하지 않는다([[feedback_inner_overflow_needs_a_bounded_container]]) · **실측 SQL 두 줄이 설계 두 곳을 확정했다** — `route_id` 0건이 회선 축을 정했고, 등록부 `source` 1행이 T4 제거 범위를 정했다 · **"활성"과 "접속"은 다른 말이다** — 화면이 오래 보여 준 것은 `is_active` 플래그였고 접수자가 물은 것은 실제 접속이었다 · **이력 적재가 발송을 막으면 안 된다** — 두 writer 모두 실패를 삼키고 테이블 부재도 조용히 지나간다 · **메뉴 통합은 화면이 아니라 코드를 옮기는 일** — 원본을 줄 단위로 복사하고 옮긴 화면을 전부 렌더해 봤다(import 성공은 증거가 아니다) · **옛 화면을 지울 때 그 화면이 지키던 계약도 같이 지우면 안 된다** — Token 직접 입력 금지 등 2건을 남은 테스트로 이관 |

---

> 직전 세션(0904 직원 접수 3건 · 이메일 2 + 모바일 DM 1) 카드는 [archive/TASKS_2026-09.md](archive/TASKS_2026-09.md)로 회전. 배포 대기 상태는 그대로다.

### ⏱ 이전 세션 — 2026-09-04 (직원 접수 3건 · 이메일 2 + 모바일 DM 1)

> 다음 세션이 3줄로 이어받는 자리. **직전 세션 것만 둔다**(다음 세션이 자기 것으로 덮어쓴다). 경위·상세는 SoT 문서·memory 소유.

| 항목 | 상태 |
|---|---|
| 한 일 | **직원 접수 3건 수정 완료**([B-0904-1](BUGS.md)·[B-0904-2](BUGS.md)·[B-0904-3](BUGS.md)). ①이메일 히어로 기본 구도가 사진을 CSS 배경으로 내보내 수신함에서 통째로 사라지던 것을 `<img>` + 아래 텍스트 밴드로 재구성 ②텍스트 카드 자동 번호(아트디렉션 모티프)를 끌 입구 신설 = 테마 모달 **포인트 장식** 5종(전역) + 블록 스타일 **자동/숨김**(블록별 `section.motif`) ③쿠폰 **할인 타입** 제거(캔버스·DM SSR·이메일 어디도 안 읽던 고아 속성). 계약 3종 신설 = `EMAIL_PHOTO_AS_IMG_TAG`·`EMAIL_SECTION_MOTIF_OFF`·`DM_REMOVED_DEAD_CONTROLS` |
| 지금 상태 | **코드 완료 · 미커밋 · 배포 대기**. BE tsc 0 · FE tsc 0 · **backend 전체 226파일 3,584건 통과**(골든 스냅샷 회귀 0) · 파리티 신규 10건. DDL 0 · ENV 0. Codex 대상 아님(쓰기 경로·DDL 무접촉 = `codex_review_after_code_change` 범위 밖) |
| 다음 한 수 | ①Harold 커밋·배포(FE 빌드 포함 = 편집기·테마 모달 변경) ②실측 3건 = 하이웍스로 기본 구도 히어로 테스트 발송 1건 / 포인트 장식 '없음' + 블록 '숨김' 각 1회 / 쿠폰 편집 패널 육안 |
| 미결 | 히어로 편집기의 **오버레이 프리셋**(`overlay`)은 이번 변경 이전부터 이메일이 안 읽는 죽은 컨트롤(B-0904-1 추가 과제 · 기록만) · 0903 이전 배포 대기분 전량 이월 |
| 배운 것 | **접수 원문이 판별식일 때가 있다** — "분할·타이포는 정상"이 곧 "같은 URL인데 태그가 갈랐다"였고, 수신 클라이언트를 확정하지 않고도 원인이 닫혔다 · **VML은 처방의 절반뿐** — MSO 조건부 주석은 워드 엔진만 해석하니 웹메일이 CSS 배경을 지우는 경우는 못 막는다. 사진은 배경이 아니라 `<img>`로 · **조판을 바꾸면 그 조판이 소비하던 컨트롤의 의미도 함께 옮긴다**(오버레이 토글 → 텍스트 밴드 명암 = 색 미지정 캠페인의 흰 글씨가 그대로 산다) · **블록별 override는 브랜드 재해석 뒤에 덮어야 한다**(다크 배경면·강조색이 design을 다시 읽어 되살린다) · **죽은 컨트롤은 제거 자체를 계약으로 남긴다**(안 그러면 다음 사람이 같은 칸을 또 만든다)

> **카드 스키마(고정 4줄):** 제목 / `SoT·기억·다음` / `⛔`(있을 때만) / `잔여` = 착수 원장 포인터만(실측 대기 열거 금지 — ★0814). 경위·수치·함정은 SoT 문서·memory 소유(`doc_ownership`).
> **회전 룰(★0814 개정):** 잔여가 실측·운영확인뿐이면 카드를 지운다 — 원문은 archive/TASKS_YYYY-MM.md, 살아있는 포인터는 "다음 세션" 한 줄. 지우기 전 ⛔축·잔여의 소유 문서 실존을 grep 확인, 없으면 먼저 옮긴다.

### 회전 — 2026-09-12 STATUS §2 완료 카드 원문 2건(30KB 초과 회전 · 잔여는 각 SoT 문서가 소유)

### 비토 링크가드 — .65 관제센터 배포 완료 · 게이트웨이 연결 확인 · 토글 off (0904)
> 호출어 **"링크가드"** → **`projects/linkguard/docs/FEATURE-LINKGUARD.md` 가 전부 소유**(§3 불변식 9 필독 → §1 구조 → §5 계약 · §7 에이전트 · §8 게이트웨이 훅 · §13 배포). 한줄로 쪽 = [포인터](../docs/bito-gateway/FEATURE-GW-LINKGUARD.md). 기억 [[project_2026_0904_bito_linkguard]]. KISA 보완 회신 = `projects/linkguard/docs/KISA-보안조치-증빙-패키지-2026-09.md`
> ⛔ **큐당 작업 세션 1(인스턴스 락 = 작업 세션 · 상실 = 종료)** / **held → ready 는 잠금 안 재평가로만** / **KISA URL 평문 0(해시 + hash_key_id)** / 전환 순서 = 전송기 정지 → pickup → 확인 · 되돌리기 = 에이전트 stop → held 0 → pickup — 상세 §3·README
> 배포 = 관제 `linkguard.hanjulgw.com` + 게이트웨이 9be2ade(토글 on · 차단 실측 1건). 잔여 = **KISA 회신 docx 에 그림 2장 넣어 PDF 제출(`linkguard/docs/kisa-submission/`) → API 발급 → 목록 적재 → .57/.58 에이전트.** 백신·캡처는 0904 완료. 상세 = SoT §14 (9) · 2차 = §15

### 자동마케팅 — 재설계 배포완료(0804) · **0805 §12-1·5·6도 배포완료**(커밋 `08605ab9` · 남은 DDL 0)
> 호출어 **"자동마케팅"** → **[FEATURE-AUTOMARKETING.md](../docs/FEATURE-AUTOMARKETING.md)가 전부 소유** — 착수 전 필독 §1(정체성 §0 → 불변 원칙 §2 → 계약 §4). 실측 사실 §10 · 범위 밖 별건 §11 · 기억 [[project_2026_0803_automarketing_targeting_redesign]]
> ⛔ 새 축 판정 기준은 §0이 소유(사건 = 여정 / 상태·변화 = 자동마케팅) — 여기 재서술 금지. **DDL 전량 실행완료(0804 2건 + 0805 1건) · 남은 DDL 0**
> 잔여 = **남은 개발 4건 = [문서 §12](../docs/FEATURE-AUTOMARKETING.md)가 착수 원장**(조건부 대기 = 상품 데이터 · 나머지는 다른 트랙과 함께) + 화면 실측 9건(§12 하단 7 + 0805분 2). 운영을 막는 건 0

### 회전 - 2026-09-16 STATUS 다음 세션 목록 4건(30KB 초과 회전 · 잔여는 각 SoT 문서가 소유)

> ⑪ ~~상시 로드 감축~~ **종결(0814(3) Harold "원래대로 둔다")** — 1차 50.0→40.0KB로 끝. **룰 병합은 폐기·재개 금지**(그룹1 −62B·파급 −180B 실측 = 세션 1회 표류 +191B보다 작다. 룰은 서로 다른 축이라 실중복이 없었다). 경위·4안 실측표 = [[feedback_claude_md_rule_merge_plan]]
> ⑯ **게이트웨이 PAY 통계 적재 — ★0820 1호 개통·실측 완주** — [허브 §10](../docs/FEATURE-BITO-GATEWAY.md)이 소유(접두 `V` 확정 · `V0001` = api-rabd-api-01 · 통계+발급명 양축 적재 실측 · 방화벽 `.65` 허용·영속화 · 걷어낸 막힘 2종). 다음 = 한줄로 매핑+단가(서수란) · 통계 UPSERT 실측 1건 · `sales@.65` 비번 로테이션 · 자동 채번·토글 화면(상용 전)
> ⑰ **게이트웨이 2차 전수점검 — 0815(2) 결함 5종 수정·배포 완료** — 호출어 **비토 게이트웨이** → [허브 §7-2](../docs/FEATURE-BITO-GATEWAY.md)가 전부 소유. Codex 2R `approve` · 3단위 `GW_DEPLOY_OK`. 다음 = 운영 실측(발송 1건 · finalize 버튼 · PAY 첫 적재)
> ⑱ **게이트웨이 대시보드 — 0816(2) 결함 3종 전량 + 구성 패턴·발송 이력 완료** — 호출어 **비토 게이트웨이** → [허브 §7-4](../docs/FEATURE-BITO-GATEWAY.md)가 전부 소유. 잔여 = 패턴 이식 나머지 페이지([지시서](../docs/2026-08-16-gw-dashboard-redesign-design.md) §2-6 ③④⑤) + 실측 1건
