# 서버 목록·사양 (자체 인프라 원장)

> **이 문서가 서버 사양·용도·리스크를 소유한다.** 접속 절차·운영 명령은 [OPS.md](OPS.md)가 소유한다(여기 복사 금지).
> 실측 = 2026-08-15. 갱신 시 **측정일을 함께 적는다** — 가동일수·부하는 시점 값이라 날짜 없으면 쓸모가 없다.
> 수집 명령 = §5 (서버마다 그대로 붙여넣으면 같은 형식으로 나온다).

## 1. 한눈에 보기

| | **.62** | **.65** | **.66** | **.58** | **.57** | **.54** |
|---|---|---|---|---|---|---|
| 이름 | invito | invito | invito | invito58 | mmsr3-57-svc-dk | mmsr-qtmsg |
| 용도 | **한줄로 운영** | **비토 게이트웨이** | 사내(그룹웨어 등) | 운영 상시 접속 | 중계 | **중계(현역 트래픽)** |
| 소유 | 우리 | 우리 | 우리 | 우리 | 중계사 | 중계사 |
| OS | Ubuntu 24.04.3 | 24.04.4 | 24.04 | 24.04.3 | — | **20.04.3** |
| 커널 | 6.8.0-124 | 6.8.0-136 | 6.8.0-31 | 6.8.0-88 | 5.15.0-71 | **5.4.0-91** |
| CPU | Xeon 6325P 4C/8T | **i7-12700 12C/20T** | i7-10700 8C/16T | Xeon 6325P 4C/8T | i7-10700 8C/16T | i7-10700 8C/16T |
| 메모리(가용) | **62Gi** (46) | 31Gi (29) | 31Gi (29) | 31Gi (24) | 62Gi (56) | 62Gi (35) |
| 스왑 사용 | 1.5Mi | 0B | 0B | 758Mi | 24Mi | **3.8Gi** |
| 디스크 | 분할(§2) 5·2·26% | 899G (3%) | 899G (1%) | 1.9T (35%) | 3.6T (17%) | 937G (55%) |
| 부하(16~20T 기준) | 0.38 | 1.16 | 0.00 | 0.40 | 0.38 | **5.72** |
| 가동일수 | 57일 | 14일 | 15일 | 252일 | **1204일** | **1696일** |
| 환경 | host/vm | host/vm | host/vm | 물리 | **컨테이너** | host/vm |

**자원은 여섯 대 모두 여유롭다.** 부하 최대가 `.54`의 36%, 디스크 최대가 `.54`의 55%다. 성능이 병목인 지점은 현재 없다.

## 2. 서버별 상세

### .62 — 한줄로 운영 (58.227.193.62 / `administrator`)
운영 전체가 도는 서버. 디스크가 용도별로 갈려 있다 — `/` 98G(5%) · `/boot` 974M · `/home` 1.5T(2%) · `/var` 295G(26%).
- **도커 4개**: `targetup-mysql`·`targetup-postgres`·`targetup-redis`·`pay-ingest-db`
- **메모리 상위**: mysqld 4.5G · postgres 1.2G · java ×4(각 1G, QTmsg 에이전트) · node 762M
- **포트**: 외부 22·80·443·23388 / 로컬 전용 3306·5432·6379·8555
- ✅ **DB 바인딩 원칙 준수 실측** — MySQL·PG·Redis 전부 `127.0.0.1`. 랜섬웨어 이후 원칙이 실제로 지켜지고 있다.
- ⚠ **23388 = `pay-ingest-db`(MariaDB) 외부 개방** — 상세 §3.
- ⚠ **재부팅 시 자동 복구 공백(2026-08-16 실측)** — 도커 4종(DB)은 restart 정책으로 자동 복구되지만, **pm2 3종(systemd 유닛 없음 — dump는 저장돼 있어 `pm2 resurrect` 한 줄로 복원)과 QTmsg 발송 에이전트 11프로세스(0731 21:30 수동 기동 — 자동 기동 장치 전무)는 재부팅 후 수동 기동 필요.** Tomcat은 현재 미가동. 재부팅은 Harold 접속 가능 시간대로만 + 직후 pm2 resurrect·에이전트 기동. 근본 개선(자동 기동 등록)은 별도 과제.
- ⚠ **frontend 빌드 간헐 네이티브 크래시(2026-08-16 저녁 실측)** — 같은 코드·같은 node(1/13 설치 v20.20.0)에서 build:safe가 18:26 Segfault → 재시도 성공 → 18:53 V8 Sweeper Check failed → 18:57 Segfault. 자원 정상(가용 46G·디스크 5%·load 0.4)·pm2 3종 온라인·kern.log에는 우리 세그폴트조차 미기록(판정 채널 침묵). 당시 uptime 59일. **가설 = 메모리 계통 누적(커널/RAM) — 확정 수단은 재부팅 후 관찰, 재발 시 memtest.** atomic 안전망이 매번 dist를 보존해 서비스 영향 0. 운영 프로세스가 아닌 빌드류 대용량 작업에서만 관측됨.

### .65 — 비토 게이트웨이 (58.227.193.65 / `invito`)
가장 좋은 CPU에 부하가 낮다. 실사용 트래픽 이관 전이라 여력이 크다.
- **도커 3개**: `bito-bench-postgres`·`bito-agent-bench-mysql`·`bito-bench-redis`
- **포트**: 외부 22·80·443 · **9090(gRPC, Agent 접속)** · 4404(webhook) / 로컬 33306·4000(Admin API)·5432·6379·9081
- `fail2ban` 가동 중
- 환경변수 = `/etc/default/bito-gateway`(EnvironmentFile) + 유닛 드롭인 3종
- ⚠ **비밀번호 평문 노출** — 상세 §3.

### .66 — 사내 (58.227.193.66)
그룹웨어 등 사내 용도. **부하 0.00**으로 사실상 유휴 — 사양 대비 여력이 가장 크다.
- node·next-server·postgres 구동, 포트 외부 80·443·8080·4000
- ⚠ 커널 `6.8.0-31`로 같은 24.04 계열 중 가장 오래됐다(`.65`는 136). 패치 주기에서 빠져 있다.

### .58 — invito58 (58.227.193.58)
유일한 **물리 서버**(efivarfs·/dev/sda). 접속자 22명 = 사람이 상시 쓰는 서버.
- ⚠ **스왑 758Mi 사용** — 지금 24Gi가 남는데도 스왑이 나간 건 과거 메모리 압박 흔적. 피크 때 무엇이 먹었는지 확인 가치 있음.
- 브랜드메시지 **미지원 중계**(0814 확인 — 접수 후 5초 뒤 `7421` 거절).

### .57 — mmsr3 (중계, 컨테이너)
QtMsg 3.0(`test11/12`)·4.0(`insvc11/12`)·KAW(웹)·ngen(GemTek 문자) 구동.
- **카카오/브랜드 트래픽 0** — 0815 실측(로그 PING/PONG만, 키워드 0건). 브랜드는 여기서 나가지 않는다.
- ⚠ **1204일 무재부팅 · 커널 2023년** — 상세 §3.

### .54 — mmsr-qtmsg (중계, **현역**)
**실제 트래픽을 지고 있는 서버.** 고객사별 로그가 `~/ngen/cms_mg2/logs/YYYY-MMDD/`에 `_m-deliver`(발송)·`_r-report`(결과)로 쌓인다(27개 파일 = 고객사 단위). `isaekko`·`macaw` 등 우리 아는 이름이 여기 있다.
- java 인스턴스 다수(34xxx·35xxx 포트 대역) + mysqld 1.7G + `portfwd_mysql`(23316)
- **이관 대상** — 여기 붙은 업체들을 우리 게이트웨이(.65)로 옮기는 것이 목표(2026-08-15 Harold).
- ⚠ **가장 위험** — 상세 §3.

## 3. 리스크 원장

| # | 대상 | 내용 | 판단 |
|---|---|---|---|
| 1 | **.54** | **1696일(4.6년) 무재부팅 · 커널 5.4.0-91(2021) · Ubuntu 20.04(지원 종료 구간) · 부하 36%(최고)** | 가장 오래되고·가장 안 껐고·가장 안 고쳐진 서버인데 **실트래픽을 혼자 진다**. 재부팅이 4.6년간 검증된 적 없어, 정전·강제 재시작이 나면 그때 처음 확인하게 된다. 중계사 관리 영역 |
| 2 | **.57** | 1204일 무재부팅 · 커널 2023년 · 컨테이너라 systemd 미응답 | 재부팅 시 자동 기동 여부 미검증. 중계사 관리 영역 |
| 3 | **.62** | ~~`pay-ingest-db` 23388 DOCKER-USER 미적용~~ → **★0820 해소 실측**: DOCKER-USER에 출처 제한 적용돼 있음 — 허용 5(139.150.81.213·.54·.57·.58·**.65 0820 추가**) + 그 외 DROP. `.65` 추가 = PAY 통계 적재 개통(비토 게이트웨이 허브 §10) | **영속화 완료(0820)** — 네 곳(iptables-persistent·crontab·rc.local·after.rules) 전부 비어 있던 것을 실측으로 확인(기존 4규칙도 휘발 상태였다) → `/etc/ufw/after.rules`에 전 규칙 블록 등재 + `ufw reload` 적용 실측. 재부팅 생존 |
| 4 | **.65** | 유닛 드롭인에 **IMC 비밀번호 평문**(`systemctl show`로 일반 사용자도 읽힘), `/etc/default`에 PG 비밀번호 평문 | `/etc/default/bito-gateway`는 0815에 `chmod 600` 적용. 드롭인(`zz-humuson-invito11.conf`)은 미조치 — EnvironmentFile로 이관 필요(재시작 1회) |
| 5 | **.66** | 커널 6.8.0-31(계열 내 최고령) | 사내용이라 급하지 않으나 패치 주기 이탈 신호 |
| 6 | **.58** | 스왑 758Mi 사용 이력 | 과거 메모리 압박. 원인 미확인 |

**정리된 것(0815)** — `sales@139.150.81.213`(옛 게이트웨이 잔여 계정) 삭제 완료 / `sales@58.227.193.65` 신설(PAY 적재용) / `/etc/default/bito-gateway` 권한 600.

## 4. 계정·경로 메모

- **PAY 수집 DB** = `.62`의 `pay-ingest-db`(MariaDB, 포트 23388, DB `sales`). 쓰기 계정 `sales`는 **IP 제한**: `.54`·`.57`·`.58`(중계) + `.65`(비토 게이트웨이, 0815 신설). 읽기는 `hanjul_ro`·`paystats`(도커 내부 `172.%`).
- **게이트웨이 → PAY 적재** = `.65`가 `.62:23388`로 밀어 넣는다(`GW_PAY_STATS_DSN`). SysId `65`. 상세 = [FEATURE-BITO-GATEWAY.md](../docs/FEATURE-BITO-GATEWAY.md).
- **Agent → GW** = `.62`(에이전트 3대) → `.65:9090` gRPC.

## 5. 수집 명령 (서버마다 그대로)

```bash
echo "========== $(hostname) / $(hostname -I 2>/dev/null | awk '{print $1}') =========="; grep PRETTY /etc/os-release 2>/dev/null | cut -d'"' -f2; echo "kernel : $(uname -r)"; { [ -f /.dockerenv ] && echo "env    : container"; } || echo "env    : host/vm"; echo "--- CPU ---"; lscpu | grep -E "^(Model name|CPU\(s\)|Core\(s\) per socket|Socket\(s\)|Thread\(s\) per core)"; echo "--- MEM/SWAP ---"; free -h | head -3; echo "--- DISK ---"; df -hT -x tmpfs -x devtmpfs -x efivarfs 2>/dev/null | head -8; echo "--- LOAD / UPTIME / USERS ---"; uptime; echo "--- TOP MEM (RSS KB) ---"; ps -eo comm,rss --sort=-rss | head -8; echo "--- LISTEN PORTS ---"; { ss -tlnp 2>/dev/null || netstat -tlnp 2>/dev/null; } | awk 'NR>1{print $4, $NF}' | sort -u | head -15; echo "--- DOCKER ---"; docker ps --format '{{.Names}}\t{{.Status}}' 2>/dev/null | head -8 || echo "(none/no-perm)"
```

사양만이 아니라 **메모리 상위 프로세스·리스닝 포트·도커 목록**을 함께 받는다 — "이 서버가 무엇을 하는가"가 있어야 장애 때 판단이 된다. `ss`·`docker`가 비면 `sudo`로 재실행한다.
