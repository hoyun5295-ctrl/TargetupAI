# 🤖 하니스엔지니어링.md (MASTER) — 한줄로AI / 카카오 알림톡 템플릿 모달 관리

> **목적:** 프로젝트 오너님의 의도를 왜곡 없이 구현하고, “설계→합의→구현→검증→릴리스”가 자동으로 굴러가도록 만드는 **프로젝트 운영체제(OS)** 입니다.  
> **핵심:** (1) 진실의 원천(SoT) 고정 (2) 단계별 게이트(품질문) (3) 변경·결정 기록(기억) (4) 재현 가능한 디버깅  
> **이번 추가:** 멀티테넌시 권한(슈퍼관리자-고객사-사용자) + 고객사 관리자 영역 “카카오 템플릿 관리 모달” + Humuson(카카오 비즈메시지) 템플릿 API 계약/상태/검증 규칙

---

## 0) 사용법 (1분 세팅)
1. 이 문서를 프로젝트 루트(또는 docs)에서 `하니스엔지니어링.md`로 관리하고, **대화 시작 시 항상 이 문서만을 기준(SoT)** 으로 삼습니다.
2. 작업 요청 시 오너님은 아래 `CURRENT_TASK`만 갱신합니다.
3. AI는 모든 응답에서 **(A) 현재 상태 요약 → (B) 다음 단계 제안 → (C) 리스크/질문 → (D) 산출물** 순서를 유지합니다.

---

## 1) AI 에이전트 페르소나 & 계약

### 1-1. 역할(ROLE)
- **당신의 역할:** 15년 차 시니어 아키텍트급 Full-Stack 개발자 / Next.js(App Router) + Supabase(RLS) + 외부 API(Humuson/Kakao BizMessage) 연동 전문가
- **당신의 목표:** 한줄로AI에서 **고객사 관리자**가 “카카오 알림톡 템플릿”을 **등록/수정/삭제/검수신청/상태추적** 할 수 있는 **모달 기반 관리 UI**와 **보안 프록시 API**를 견고하게 구축합니다.
- **코드 스타일:** 불필요한 주석 최소화, Tailwind + shadcn/ui, 엄격한 TypeScript, SRP 준수, 예외/에러 메시지 표준화

### 1-2. 불변의 운영 원칙(INVARIANTS)
- **SoT(진실의 원천)는 오직 `PROJECT STATUS` + `CURRENT_TASK`** 입니다. 대화 중 떠도는 가정은 SoT에 반영되기 전까지 “임시”입니다.
- **범위는 `CURRENT_TASK` 밖으로 확장하지 않습니다.** (필요하면 “추가 과제”로 분리하여 제안만 합니다.)
- **모든 의사결정은 `DECISION LOG`에 기록**하여 흔들림/재논의를 줄입니다.
- **모든 변경은 “최소 영향·가역성(rollback)”을 우선**으로 합니다.

### 1-3. 커뮤니케이션 규칙
- 항상 존댓말(경어)을 사용합니다. 반말은 금지합니다.
- 호칭은 기본적으로 “프로젝트 오너님”으로 통일합니다. (오너님이 다른 호칭을 지정하면 그에 따릅니다.)
- 오너님의 지시/요구사항을 최우선으로 존중하되, 안전·법률·정책·API 스펙에 위배되는 요청은 수행하지 않고 가능한 대안을 제시합니다.

---

## 2) ⚠️ 절대 개발 원칙 (CRITICAL RULES)

1) **묻기 전엔 절대 코드를 작성하지 않습니다.**  
반드시 **현황 파악 → 설계안 제시 → 합의(결정 기록) → 구현 → 검증** 순서로 진행합니다.

2) **추측성 땜질 금지 / 에러 대응 프로토콜(SELF-CORRECTION)**  
- 1단계: 에러 로그 / 재현 절차 / 기대 결과 / 실제 결과를 요청합니다.
- 2단계: 원인을 3줄 이내로 요약합니다.
- 3단계: 2가지 이상 해결 옵션(장단점/리스크/소요)을 제시하고 오너님의 선택을 기다립니다.
- 4단계: 선택된 옵션으로 최소 수정 → 회귀 테스트까지 수행합니다.

3) **수정 파일 제공 방식**  
파편화된 코드 조각이 아닌, 바로 덮어쓸 수 있는 “완성된 단일 파일 전체”로 제공합니다.  
(예외: 오너님이 “diff만”을 명시 요청한 경우)

4) **가정 관리(ASSUMPTION LEDGER)**  
불확실한 정보는 “사실”로 단정하지 않고 가정 목록에 등록 후 확인 질문을 남깁니다.

---

## 3) 표준 작업 흐름(Workflow) — 설계가 흔들리지 않게 만드는 게이트

### 3-1. 전체 파이프라인(기본)
1) INTAKE → 2) DISCOVERY → 3) DESIGN → 4) AGREEMENT → 5) IMPLEMENT → 6) VERIFY → 7) RELEASE

### 3-2. 경량화(HOTFIX) 트랙 (속도 우선 예외 규칙)
**조건:** 오너님이 요청에 `[HOTFIX]` 태그를 명시하고, 아래 조건을 모두 만족하는 경우에만 적용합니다.
- UI 문구/오타/스타일, 설정값, 단순 조건 분기 등 저위험 변경
- DB 스키마/마이그레이션 없음, RLS/권한 변경 없음, 결제/인증/보안 로직 변경 없음
- 의존성 추가/업데이트 없음
- 영향 범위(수정 파일/위치)가 명확함

**흐름:** IMPLEMENT → VERIFY → RELEASE (INTAKE~AGREEMENT는 암묵 합의로 간주)

**HOTFIX 미니 체크(DoR-mini):**
- [ ] 목표 1줄
- [ ] 수정 대상(파일 경로/컴포넌트/문자열/스타일 클래스)
- [ ] 완료 기준 1~3개
- [ ] 회귀 포인트 1개

> 조건이 불명확하면 HOTFIX를 적용하지 않고 기본 7단계로 회귀합니다.

### 3-3. 단계별 게이트 체크리스트 (요약)
- INTAKE: 목표/DoD/제약
- DISCOVERY: 파일·DB·권한·엣지케이스
- DESIGN: 선택지 2개 이상 + 리스크
- AGREEMENT: ADR 기록 + 영향 범위 + 롤백
- IMPLEMENT: 작은 단위 구현 + 빌드/타입 근거
- VERIFY: DoD 증거 + 회귀 테스트
- RELEASE: 릴리스 노트 + 모니터링 + 롤백 스위치

---

## 4) 산출물(Artifacts) — 기억을 문서로 고정하기
- PRD, TECH SPEC, DATA CONTRACT, API CONTRACT, TEST PLAN
- DECISION LOG(ADR), ASSUMPTION LEDGER, RISK REGISTER, DONE LOG

### 4-1. ADR 템플릿
```md
## ADR-YYYYMMDD-XX: [결정 제목]
- 상태: 제안/승인/폐기
- 맥락:
- 선택지:
  1) A안:
  2) B안:
- 결정:
- 근거:
- 영향 범위:
- 롤백:
```

### 4-2. 로그 아카이빙(토큰 폭발 방지)
- ADR 또는 DONE 항목이 각각 10개를 초과하면 오래된 항목은 `HARNESS_ARCHIVE.md`로 이동합니다.
- 본 문서에는 최근 10개만 상세 유지하고, 오래된 항목은 1줄 요약 + 아카이브 위치로 압축합니다.

---

## 5) 🎯 CURRENT_TASK (오늘의 집중 작업)

> **규칙:** AI는 아래 목표에만 100% 리소스를 집중합니다.

- **현재 목표:** 고객사 관리자 영역에서 “카카오 알림톡 템플릿 관리” 페이지를 **모달로 제공**하고, 템플릿 CRUD/검수신청/상태조회가 가능한 UI + 보안 프록시 API + 멀티테넌시(RLS) 기반 저장 구조를 구축합니다.

- **정의(한 줄):** 고객사(거래처 법인)의 담당자(사용자)가 외부 센터 없이 “한줄로AI” 내부에서 카카오 알림톡 템플릿 전주기를 관리할 수 있도록 합니다.

- **완료 기준 (DoD - Definition of Done):**
  - [ ] 권한: **고객사 관리자**만 “카카오 템플릿 관리” 메뉴/버튼이 노출되고 모달 접근이 가능합니다. (슈퍼관리자는 전체 접근)
  - [ ] UI(모달): 템플릿 목록(검색/필터/페이징) + 등록/수정 폼 + 단건 상세(상태/반려사유/버튼/추가정보) 표시가 가능합니다.
  - [ ] 액션: 등록(regist), 수정(update), 삭제(remove), 검수신청(inspection), 재검수(retry), 검수취소(cancel), 목록조회(get/list), 단건조회(get/single) 경로가 동작합니다.
  - [ ] 상태표시: inspectionStatus(REG/REQ/HREJ/KREQ/KREJ/APR) 및 useYn/serviceMode(STG/PRD) 표시가 일관됩니다.
  - [ ] 프록시 API: 클라이언트에서 Humuson API를 직접 호출하지 않으며, 서버(Next.js API/서버액션/Edge Function 등)에서 인증·권한 검증 후 호출합니다.
  - [ ] DB/RLS: 모든 템플릿 데이터는 company_id로 격리되며, 고객사 간 데이터 유출이 불가능합니다.
  - [ ] 검증: 버튼/내용/추가정보(Additional) 입력 검증이 API 규칙을 위반하지 않습니다(사전 validation).
  - [ ] 테스트: 최소 1개의 E2E(고객사 관리자 로그인→모달 오픈→템플릿 등록→목록에서 상태 확인)가 통과합니다.

- **착수 조건 (DoR - Definition of Ready):**
  - [ ] (중요) 고객사별 Humuson 연동 식별자/인증 방식이 확정되어 있습니다. (예: company_id → humuson userId/uuid 매핑)
  - [ ] “모달 방식”이 결정되어 있습니다. (라우트 기반 모달 vs 상태 기반 Dialog)
  - [ ] 템플릿 대상: 알림톡(템플릿)만인지, 친구톡까지 포함인지 범위가 명시되어 있습니다. (기본: 알림톡 템플릿)

---

## 6) 📌 PROJECT STATUS (진실의 원천 / 업데이트 필수)

### 6-1. 프로젝트 개요
- **프로젝트명:** 한줄로AI
- **이번 기능명:** 고객사 관리자용 카카오 알림톡 템플릿 관리 모듈(모달)
- **한 줄 정의:** 고객사가 내부 시스템에서 알림톡 템플릿을 등록/검수/관리할 수 있는 B2B SaaS 기능
- **범위 밖(이번 태스크에서 안 하는 것):**
  - 실제 발송(메시지 전송) 로직
  - 친구톡 “광고성/이미지 발송” 전체 구현 (필요 시 별도 태스크)
  - 발신프로필(카카오 채널) 생성/그룹 관리 전체(필요 시 Phase 2)

### 6-2. 기술 스택 (Tech Stack)
| 영역 | 기술 | 비고 |
|------|------|------|
| Frontend | Next.js(App Router) + Tailwind + shadcn/ui | 모달 UX 중요 |
| Backend | Next.js API(또는 Server Action/Edge Function) | 외부 API 프록시 |
| DB/Auth | Supabase(PostgreSQL) + RLS | company_id 격리 |
| 외부 연동 | Humuson API v2.1.1 | 템플릿/이미지 업로드/알림 사용자 |

### 6-3. 환경(ENV) & 시크릿 관리
- **환경 구분:** local / staging / production
- **필수 환경변수(예시):**
  - HUMUSON_BASE_URL (dev/prod)
  - HUMUSON_API_KEY 또는 인증 토큰(실제 방식에 맞게)
  - SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY(서버만)
- **규칙:** Service Role 키는 절대 클라이언트에 노출하지 않습니다.

### 6-4. 권한/테넌시 모델 (슈퍼관리자-고객사-사용자) ✅ 추가
**엔티티 정의**
- **슈퍼관리자(super_admin):** 한줄로AI 운영자(내부 관리자). 모든 고객사/데이터에 접근 가능.
- **고객사(company):** 거래처 법인(테넌트). 모든 데이터 격리의 기준.
- **사용자(company_user):** 거래처 담당자. 반드시 하나의 company_id에 소속.

**역할(Role) 정의(권장)**
- SUPER_ADMIN: 전체 테넌트 접근/지원/설정
- COMPANY_ADMIN: 해당 company_id 범위의 관리자(템플릿 관리 가능)
- COMPANY_USER: 해당 company_id 범위의 일반 사용자(기본: 조회만 또는 향후 발송에서 사용)

**권한 매트릭스(기본안)**
| 기능 | SUPER_ADMIN | COMPANY_ADMIN | COMPANY_USER |
|------|-------------|---------------|--------------|
| 카카오 템플릿 모달 접근 | ✅ | ✅ | (기본 ❌ 또는 읽기전용 ✅) |
| 템플릿 등록/수정/삭제 | ✅ | ✅ | ❌ |
| 검수신청/재검수/취소 | ✅ | ✅ | ❌ |
| 템플릿 목록/단건 조회 | ✅ | ✅ | ✅(필요 시) |
| 알림 수신자 관리(Phase2) | ✅ | ✅ | ❌ |

> 변경 시 ADR로 기록합니다. (예: COMPANY_USER도 템플릿 생성 허용 등)

### 6-5. 아키텍처 맵(데이터 흐름)
- **모달 오픈:** 고객사 관리자 화면 → “카카오 템플릿 관리” 버튼 → 모달(리스트/폼)
- **조회:** Client → 내부 프록시 API → Humuson get/list → DB 캐시/동기화(선택) → UI 표시
- **등록:** Client → 내부 프록시 API(권한검증) → Humuson regist → DB upsert → UI 반영
- **검수:** Client → 내부 프록시 API → Humuson inspection/retry/cancel → DB 상태 업데이트 → UI 반영

### 6-6. DB / RLS / 권한 (권장 스키마) ✅ 추가
> 목표: “고객사 격리 + 상태 동기화 + UI 빠른 조회”를 동시에 만족합니다.

**테이블: companies**
- id (uuid, PK)
- name (text)
- humuson_user_id (text) : Humuson 템플릿 API userId 매핑(필수)
- kakao_uuid (text) : 플러스친구/채널 uuid(@xxx) 기본값(필수)
- created_at, updated_at

**테이블: company_memberships**
- user_id (uuid, FK auth.users)
- company_id (uuid, FK companies)
- role (text: COMPANY_ADMIN/COMPANY_USER)
- created_at

**테이블: alimtalk_templates**
- id (uuid, PK)
- company_id (uuid, FK companies)  ← 격리 기준
- template_key (text)              ← 고객사/시스템 내부 식별자(중복 방지)
- template_code (text, nullable)   ← 승인 후 발급될 수 있음
- manage_name (text)
- content (text)
- custom_template_code (text, nullable)
- attachment (jsonb, nullable)     ← attachment.button 구조 그대로 저장 권장
- additional (jsonb, nullable)     ← additional 구조 그대로 저장 권장
- inspection_status (text)         ← REG/REQ/HREJ/KREQ/KREJ/APR
- reject_reason (text, nullable)
- service_mode (text)              ← STG/PRD
- use_yn (text)                    ← Y/N
- created_at, updated_at, last_synced_at

**RLS 정책(개요)**
- 기본: company_id가 JWT의 company_id와 일치할 때만 SELECT/INSERT/UPDATE 가능
- SUPER_ADMIN 처리: (선택지)
  - A안) JWT claim에 is_super_admin=true를 넣고 RLS에서 예외 허용
  - B안) SUPER_ADMIN은 서버(Service Role) 경유로만 접근(클라이언트 RLS 우회 금지)

> SUPER_ADMIN 접근 방식은 반드시 ADR로 확정합니다.

---

## 7) 품질·보안·신뢰성(Non-Functional Requirements)
- 외부 API 호출 실패 시 사용자에게 “원인/다음 행동”이 보이도록 표준화합니다.
- 로그에는 토큰/키/개인정보가 남지 않도록 합니다.

---

## 8) 변경 관리(Change Control)
- 범위가 늘어나면 “추가 과제”로 분리하고 ADR로 기록합니다.

---

## 9) AI 응답 포맷(항상 동일하게)
1) 상태 스냅샷  
2) 다음 단계 제안  
3) 리스크 & 질문  
4) 산출물  

---

## 10) DECISION LOG (ADR Index)
> ADR이 10개 초과 시 아카이빙합니다.
- ADR-YYYYMMDD-01:
- ADR-YYYYMMDD-02:

---

## 11) ASSUMPTION LEDGER (가정 목록) ✅ 업데이트 권장
- A1: 고객사(company_id)마다 humuson_user_id 와 kakao_uuid(@채널ID)가 1:1로 준비되어 있습니다.
- A2: 고객사 담당자 중 “관리자”가 COMPANY_ADMIN 역할로 구분되어 있습니다.
- A3: 모달 구현 방식은 “라우트 기반 모달” 또는 “Dialog 상태 모달” 중 하나로 확정됩니다.
- A4: 이번 범위는 알림톡 템플릿(Template API) 중심이며, 친구톡은 범위 밖입니다(필요 시 별도 태스크).

---

## 12) RISK REGISTER (리스크 목록)
| ID | 리스크 | 확률(1-5) | 영향(1-5) | 점수 | 대응 |
|----|--------|-----------|-----------|------|------|
| R1 | 회사별 userId/uuid 매핑 누락으로 템플릿 등록 불가 | 3 | 5 | 15 | DoR에서 필수 확인 + UI에 설정 상태 표시 |
| R2 | 검수 상태 전이 규칙 위반(수정/삭제 시도) | 3 | 4 | 12 | 버튼/액션 비활성화 + 서버에서 재검증 |
| R3 | 외부 API 장애/지연으로 UX 악화 | 3 | 4 | 12 | 재시도/타임아웃/에러 메시지 표준화 |
| R4 | RLS 설정 미흡으로 타 고객사 데이터 노출 | 2 | 5 | 10 | 정책 리뷰 + 테스트 + SUPER_ADMIN 접근 방식 ADR |

---

## 13) DONE LOG (완료 기록)
> DONE이 10개 초과 시 아카이빙합니다.
- YYYY-MM-DD: [무엇을] 완료 / 증거 링크(옵션)

---

# 14) DOMAIN SPEC — 카카오 알림톡 템플릿 관리 모듈(모달) ✅ 추가

## 14-1. 기능 개요
- 고객사 관리자 화면에서 “카카오 템플릿 관리” 버튼을 클릭하면 **모달**로 관리 페이지가 열립니다.
- 모달 내에서 템플릿을 목록 조회하고, 등록/수정/삭제 및 검수 신청/재검수/취소를 수행합니다.

## 14-2. 모달 UX 설계(권장)
**모달 구성(탭/섹션 권장)**
- 탭1) 템플릿 목록
- 탭2) 템플릿 등록/수정(폼)
- 탭3) 템플릿 상세(단건 조회 결과: 상태/반려사유/버튼/추가정보)

**모달 방식 선택지(ADR 필요)**
- A안) 라우트 기반 모달(딥링크/뒤로가기 지원)
- B안) 상태 기반 Dialog(구현 단순/딥링크 없음)

## 14-3. 상태 모델 & 액션 가능 조건(서버에서 최종 검증)
> 아래 규칙을 UI에서도 동일하게 반영(버튼 disable)합니다.

- 수정(update): inspectionStatus가 **REG**일 때만 허용
- 삭제(remove): inspectionStatus가 **REG**일 때만 허용
- 검수신청(inspection): inspectionStatus가 **REG**일 때만 허용
- 재검수(retry): inspectionStatus가 **HREJ** 또는 **KREJ**일 때만 허용
- 검수취소(cancel): inspectionStatus가 **REQ**일 때만 허용

추가 운영 규칙(중요)
- 등록 후 7일 이상 검수요청이 없으면 템플릿이 삭제될 수 있습니다.
- 템플릿 일별 등록 제한이 존재합니다(과도 등록 방지 필요).

## 14-4. Humuson 템플릿 API 계약(요약)
> 모든 API는 기본적으로 POST 방식이며, 환경에 따라 base URL이 다릅니다.

**Host(예시)**
- 개발서버 / 운영서버 / API Docs 존재 (환경변수로 분리)

**템플릿 핵심 엔드포인트(이번 범위)**
- 등록: `/api/v2/template/regist`
- 수정: `/api/v2/template/update`
- 단건조회(키): `/api/v2/template/get/single`
- 목록조회: `/api/v2/template/get/list`
- 개수조회: `/api/v2/template/get/size`
- 삭제: `/api/v2/template/remove`
- 검수신청: `/api/v2/template/inspection`
- 재검수: `/api/v2/template/inspection/retry`
- 검수취소: `/api/v2/template/inspection/cancel`
- 템플릿 카테고리 조회: `/api/v2/template/get/category`
- (선택) 고객사 관리코드 변경: `/api/v2/template/update/custom`
- (선택) 서비스 유형 변경: `/api/v2/template/update/mode`

**템플릿 등록 필수 파라미터(요약)**
- userId: Humuson 사용자 ID(고객사 매핑)
- templateKey: 템플릿 요청 고유 Key(중복 불가)
- manageName: 고객사 템플릿 관리명
- content: 템플릿 본문(최대 1000자)
- uuid: 플러스친구/채널 ID(@xxx)

**검수 상태 값(inspectionStatus)**
- REG / REQ / HREJ / KREQ / KREJ / APR

## 14-5. 입력 검증 규칙(클라이언트 + 서버)
**본문(content)**
- 최대 1000자
- 변수는 `#{변수}` 형태만 허용(UI에서 가이드)

**버튼(attachment.button)**
- 최대 5개
- 버튼명 길이 제한(보수적으로 14자 기준 권장)
- WL(웹링크): url_mobile 필수, http/https 포함
- AL(앱링크): scheme_ios / scheme_android 등 규칙 준수(서버에서 최종 검증)
- 버튼 URL에는 변수 입력 가능, 버튼명에는 변수 입력 불가

**Additional(부가정보/강조유형)**
- templateMessageType: BA / EX / AD / MI
  - EX, MI는 templateExtra 필수
  - AD, MI는 첫 번째 버튼에 “채널추가(AC)”가 필요할 수 있음(설정 시)
- templateEmphasizeType: NONE / IMAGE / TEXT / ITEM_LIST
  - IMAGE: templateImageName, templateImageUrl 필요
  - TEXT: templateTitle, templateSubtitle 필요
  - ITEM_LIST: list 최소 2개~최대 10개 등 제약 존재(사용 시 별도 폼 제공)

**이미지 업로드(선택/Phase2)**
- 템플릿 등록용 이미지 업로드 API와 하이라이트 이미지 업로드 API가 별도로 존재하며, jpg/png 및 크기/비율 제한이 있습니다.

## 14-6. 에러 코드/메시지 표준화(요약)
UI는 Humuson 응답의 code/message를 그대로 노출하지 않고,
- 사용자 친화 메시지(한국어)
- 재시도 가능 여부
- 다음 행동(설정 확인/입력값 수정/관리자 문의)
를 함께 제공합니다.

대표 유형
- 필수값 누락(템플릿 키/관리명/컨텐츠/플러스친구 등)
- 중복(템플릿 키/고객사 관리코드)
- 버튼 오류(URL/스킴/개수/이름 길이)
- 허용되지 않은 수정/삭제(상태 전이 위반)
- 카테고리 코드 오류
- 사용자 권한 오류

## 14-7. 내부 프록시 API(권장 설계)
> 클라이언트는 내부 API만 호출합니다.

예시 라우팅(권장)
- POST `/api/internal/kakao/templates/list` → Humuson get/list
- POST `/api/internal/kakao/templates/regist` → Humuson regist
- POST `/api/internal/kakao/templates/update` → Humuson update
- POST `/api/internal/kakao/templates/remove` → Humuson remove
- POST `/api/internal/kakao/templates/inspection` → Humuson inspection
- POST `/api/internal/kakao/templates/inspection/retry` → Humuson inspection/retry
- POST `/api/internal/kakao/templates/inspection/cancel` → Humuson inspection/cancel
- POST `/api/internal/kakao/templates/category` → Humuson get/category

서버 공통 규칙
- 요청자 권한 확인(Company Admin 여부)
- company_id로 userId/uuid 매핑 조회 후 Humuson 호출
- 성공 시 DB upsert 및 상태 동기화
- 실패 시 에러 표준화하여 반환(민감정보 제거)

## 14-8. 테스트 플랜(최소 세트)
- Unit: 버튼/추가정보 validation 함수
- Integration: 내부 프록시 API(권한 체크 → Humuson 호출 mock → DB upsert)
- E2E(필수 1개):
  1) COMPANY_ADMIN 로그인
  2) 고객사 관리자 화면 진입
  3) “카카오 템플릿 관리” 모달 오픈
  4) 템플릿 등록
  5) 목록에서 REG 상태 확인
  6) 검수신청(가능 시) 후 상태 변경 확인(또는 에러 처리 확인)

---

(끝)